# BHSM integrated validation report

The integrated current authority validates, and all fetched Git lineages are
ancestors of `main`. Repository-wide collection finds 9,180 tests after
recovering the one missing working-BHSM module required by the retained Gate 7
collocation chain.

Validated surfaces include the physical-identification bridge, Git lineage
ledger, museum and animation contracts, Sites production build, forbidden-claim
audit, current-status audit, frozen-prediction integrity, the updated AE2
photon/neutrino reconvergence audit, and deterministic join-skin spectrum.

Three integration defects were repaired without changing scientific thresholds:

1. The retained 100,000-event coordinate benchmark and ROOT integration job
   were restored to CI.
2. The AE2 photon/neutrino reconvergence audit now consumes the current
   completion-DAG owner `G7_08_FORCE`; `G7_07_ANGULAR_TAIL` is recorded as its
   superseded historical owner.
3. The sparse join-skin eigensolver now receives a fixed starting vector, making
   repeated serialization deterministic without reducing precision.

## Historical N=3 replay boundary

A bounded full-Aether run exposed historical N=3 exploratory tests whose
materializers reuse solver functions that evolved after their artifacts were
frozen. On both Python 3.12 and 3.14, some current recomputations therefore do
not byte-match those frozen snapshots, and some old acceptance predicates no
longer hold against the later shared solver state.

These files remain integrated BHSM assets. They are classified as historical or
reconnaissance records at their original claim strength, not deleted, retuned,
or silently promoted. Their replay divergence does not supply or refute the
missing physical-enclosure identification bridge. The machine-readable report
at `artifacts/flagship_integration/BHSM_INTEGRATED_VALIDATION_REPORT.json`
records representative failures and the validated current surfaces.

`FULL_BHSM_COMPLETE` remains false.
