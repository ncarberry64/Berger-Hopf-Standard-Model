# Weak Gauge Action Source Audit

The v3.0 audit separates structural action evidence from physical coupling-value provenance. The candidate weak algebra is conditional, the normalized gauge action is a skeleton with an unfixed overall coefficient, and the trace calculation fixes relative conventions only.

`g2_BH` remains an artifact-backed runtime input. `alpha2_BH` remains an artifact-backed registered coupling. Neither is derived by the located action sources.

The canonical machine-readable report is `artifacts/BHSM_weak_gauge_action_source_report_v3_0.json`.
