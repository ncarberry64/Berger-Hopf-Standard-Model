# BHSM v15.61 — actual-child zeta and RG completion test

The actual symmetric boundary operator is

\[
 D_{\rm actual}=i\gamma^\mu(\nabla_\mu+A_\mu)
\]

on the rank-48 Weyl bundle. Its finite left--right bifundamental block is
zero. A heat trace can calculate functions of entries already present in its
operator; it cannot generate a missing zero-order Yukawa block or a missing
((1,2)_{1/2}) scalar representation.

The child fixes a matching scale,

\[
 \mu_*=R_4^{-1}=2R_F^{-1},
\]

and (a_4(D^2)) fixes the gauge coefficient ray. It does not fix the common
amplitude. For the induced determinant,

\[
 \Gamma_\zeta=\frac12\operatorname{STr}\log\det_\zeta(P/\mu^2),
\]

the operator fixes the nonlocal part and logarithmic anomaly, while finite
local (F^2), Higgs, and curvature counterterms remain renormalization data.

At one loop,

\[
 \frac{d}{d\log\mu}\frac1{g_i^2}=-\frac{b_i}{8\pi^2},
 \qquad
 (b_Y,b_2,b_3)=\left(\frac{41}{6},-\frac{19}{6},-7\right).
\]

This vector is not tangent to

\[
 (K_Y,K_2,K_3)=Z_{\rm gauge}\left(\frac53,1,1\right).
\]

Thus the carrier-trace relation is a matching condition, not an RG-invariant
ray, and RG self-similarity does not determine (Z_{\rm gauge}). The only
common perturbative fixed point is Gaussian.

Consequently neither a pure zeta determinant, local (a_4), a generic cutoff
spectral action, nor a perturbative RG fixed-point condition produces the
missing interacting Standard Model data from the actual operator. The next
construction must specify one global operator-measure principle that derives
its finite Dirac block and boundary renormalization conditions from the child
event data.
