# BHSM v11.1 Haar Scale Normalization

Five parameter-free routes were exhausted: coordinate convention, primitive
character selection, canonical kinetic matching, topology, and global
normalization. Coordinate rescaling changes the canonical kinetic
normalization; the action selects no primitive character; no common normalized
reduction exists; topology does not normalize the continuous support group;
and the global geometry remains downstream.

Thus `lambda_D` is physical but unset. Verdict:
`BHSM_HAAR_SCALE_REMAINS_PHYSICAL_AND_UNFIXED_WITHOUT_AN_ACTION_SELECTED_SUPPORT_FUNCTOR`.
No numerical value is introduced.
