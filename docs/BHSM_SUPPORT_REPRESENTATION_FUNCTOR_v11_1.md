# BHSM v11.1 Support Representation Functor Audit

The v11.1 exact object is not derivable from the frozen parent action. The
action defines the underlying stratified objects and maps, but no natural
`G_D=(R_{>0}, multiplication)` action on primitive fields, measures,
embeddings, bundles, or core boundary data.

For a continuous character, `chi_w(upsilon)=upsilon^w`. Two lifts exhibit the
obstruction. `F_A` assigns core/wall/fiber-measure weights `(1,1,0)`; `F_B`
assigns `(1,2,1)`. Both preserve identity, composition, tensor multiplication,
and homogeneous fiber integration, and both reduce to the same frozen action
at `upsilon=1`. They are inequivalent and neither is action-selected.

These are distinct fixed-character representations, but their physical
equivalence quotient is not decided by the frozen action. Verdict:
`BHSM_SUPPORT_FUNCTOR_PHYSICAL_EQUIVALENCE_QUOTIENT_BLOCKED_BY_ABSENT_COMPLETE_LOCAL_BOUNDARY_AND_CORE_ACTION_DATA`.

The smallest blocking object is the primitive regular-bulk metric/measure pair
and its induced measures. The smallest blocking morphism is fiber integration.
The next exact object is
`COMPLETE_LOCAL_SUPPORTED_ACTION_WITH_SUPPORT_DERIVATIVE_COUPLINGS_AND_BOUNDARY_CORE_CANONICAL_DOMAIN`.
