# BHSM v10.0 Dynamic Particle Completion Program

## 1. Extended structural action

The v9.1 metric-plus-scalars no-go is retained. BHSM v10 adopts the following
extension as a `STRUCTURAL_POSTULATE`:

\[
\begin{aligned}
S_8^{\rm env}=\int_{M_8}\sqrt{-G}\Big[&
\frac{\kappa_1}{2}R_8-\frac{\kappa_0}{2}
-\frac{Z_\chi}{2}(1+g\sigma^2)|d\chi|^2
-\frac{Z_\sigma}{2}|d\sigma|^2-U(\sigma)\\
&-(1+g\sigma^2)\left(\frac{\kappa_1}{2}\mathcal X_\eta
+\frac18\mathcal X_\eta^4\right)
+\frac{\Lambda_\eta}{2}(\langle\eta,\eta\rangle-1)\Big],
\end{aligned}
\]

where (\mathcal X_\eta=\langle D_A\eta,D^A\eta\rangle). The field (\eta)
is a constrained bosonic unit triality-spinor order parameter. It is neither
an anticommuting fermion nor a new mediator. The (1/2) and (1/8)
coefficients are canonical (p)-energy normalizations, not a proved unique
choice. (\Lambda_\eta) is an auxiliary field, not (\lambda(R_8)).

The stratified action remains

\[
S_{\rm BHSM}^{\rm env}=S_8^{\rm env}
+\sum_{\epsilon=\pm}(S_{5,\epsilon}+S_{{\rm GHY},\epsilon})
+S_{4,{\rm intrinsic}}+S_{\rm compatibility}+S_{\rm current}.
\]

Intrinsic (M_4) Standard Model fields are not claimed to descend from the
metric.

## 2. Variation

Variation of (\eta), followed by integration by parts, gives

\[
D_A\!\left[(1+g\sigma^2)(\kappa_1+\mathcal X_\eta^3)D^A\eta\right]
+\Lambda_\eta\eta=0,
\]

with constraint (\langle\eta,\eta\rangle=1) and boundary form

\[
n_A(1+g\sigma^2)(\kappa_1+\mathcal X_\eta^3)
\operatorname{Re}\langle\delta\eta,D^A\eta\rangle.
\]

The eta contribution to the sigma equation is

\[
-g\sigma\left(\kappa_1\mathcal X_\eta+rac14\mathcal X_\eta^4\right).
\]

The eta stress contains

\[
(1+g\sigma^2)(\kappa_1+\mathcal X_\eta^3)
\operatorname{Re}\langle D_A\eta,D_B\eta\rangle+G_{AB}\mathcal L_\eta.
\]

These formulas are `DERIVED_CONDITIONAL` on the declared representation,
spin structure, and boundary domain.

## 3. Topology and current

For the based degree-(N) mapping component,

\[
\mathcal C_\eta^{(N)}=\operatorname{Map}_*^N(S^7,S^7),
\qquad
\pi_1(\mathcal C_\eta^{(N)})\cong\pi_8(S^7)\cong\mathbb Z_2.
\]

This is an exact mapping-space result. The physical (2\pi)-rotation loop
and two-texture exchange loop remain unconstructed. No quotient by the full
localized gauge/diffeomorphism action has been silently inserted.

Independent connection variation gives the candidate spin current

\[
\mathcal J_A{}^{IJ}=(1+g\sigma^2)(\kappa_1+\mathcal X_\eta^3)
\operatorname{Re}\langle D_A\eta,\Sigma^{IJ}\eta\rangle.
\]

It is real, antisymmetric in (I,J), and gauge covariant. Its divergence is
the covariant Noether identity generated by the eta field equation, modulo
boundary flux and the connection equation. If the spin connection is induced
by the metric, the same expression is a metric-variation contribution rather
than an independent gauge equation.

The weight-(+3) channel

\[
\mathcal T_\Omega(\mathcal J_A)
=\frac12\Omega_{cab}\mathcal J_A{}^{ab}\Gamma^c
\]

is retained as a `CANDIDATE`. Hermitian action ownership requires its adjoint
pair. Neutral-current centrality, charged-current compatibility, and the rank
of the physical pullback remain unevaluated.

## 4. Seven-dimensional collective scaling

For (x=Ry), a spatial (p)-energy scales as (R^{7-p}). Therefore

\[
\begin{aligned}
V(R,s)={}&\kappa_1A_2R^5+\frac{A_8}{R}
+Z_\sigma B_2s^2R^5+A_0B_0s^2R^7+G_0B_4s^4R^7\\
&+gs^2\left(\kappa_1C_2R^5+\frac{C_8}{R}\right).
\end{aligned}
\]

Every coefficient is an explicit profile integral. For the deterministic
degree-one audit, the compactified-(\mathbb R^7) profile is

\[
\eta_0=(\cos f,\sin f\,\widehat\Omega),\qquad
f(r)=2\arctan(r^4),
\]

so

\[
\mathcal X_0=\frac{88r^6}{(1+r^8)^2}.
\]

The scalar profile is (q(r)=Ne^{-r^2}), normalized by
(\int_{\mathbb R^7}q^2=1). The definitions are

\[
A_2=\frac12\int\mathcal X_0,
\quad A_8=\frac18\int\mathcal X_0^4,
\quad B_2=\frac12\int|\nabla q|^2,
\quad B_0=\frac12\int q^2,
\quad B_4=\frac14\int q^4,
\]

\[
C_2=\frac12\int q^2\mathcal X_0,
\qquad C_8=\frac18\int q^2\mathcal X_0^4.
\]

The deterministic values are:

| Coefficient | Value |
|---|---:|
| (A_2) | 386.594414700179 |
| (A_8) | 222345.015325136 |
| (B_2) | 3.5 |
| (B_0) | 0.5 |
| (B_4) | 0.00454899494516074 |
| (C_2) | 4.32984572555144 |
| (C_8) | 6626.22531507428 |

They are computed by 70-digit tanh--sinh quadrature and independent adaptive
Gauss--Kronrod quadrature. The certified relative method bound is
(2\times10^{-9}). Raw last-bit differences are not serialized. The domain
is (r\in[0,\infty)), with (f(0)=0), (f(\infty)=\pi), regular origin,
constant target value at infinity, and Gaussian scalar decay. These are
profile-integral diagnostics, not physical particle solutions.

## 5. Equilibrium and quadratic-only no-go

At (s=0),

\[
R_0=\left(\frac{A_8}{5\kappa_1A_2}\right)^{1/6},
\qquad
V''(R_0)=30\kappa_1A_2R_0^3>0,
\]

and

\[
E_0=\frac6{5^{5/6}}\kappa_1^{1/6}A_2^{1/6}A_8^{5/6}.
\]

All identities are verified symbolically. If (A_8=0), the energy is
(\kappa_1A_2R^5) and has no positive stationary radius. Thus the
quadratic-only eta texture is `INVALIDATED` as this collective stabilization
mechanism.

The natural dimensional scale is

\[
\mu_{\rm env}=\kappa_1^{1/6}.
\]

No eV or GeV conversion is made.

## 6. Collective kinetic metric and breathing

Define

\[
D_2=\int|y\cdot\nabla\eta_0|^2,
\quad D_8=\int\mathcal X_0^3|y\cdot\nabla\eta_0|^2,
\]

with (E_2,E_8) obtained by inserting (q^2), and
(S_0=\int q^2), (S_1=\int q(y\cdot\nabla q)),
(S_2=\int(y\cdot\nabla q)^2). The quadratic-velocity metric is

\[
M_{RR}=\kappa_1D_2R^5+\frac{D_8}{R}
+gs^2\left(\kappa_1E_2R^5+\frac{E_8}{R}\right)
+Z_\sigma s^2S_2R^5,
\]

\[
M_{Rs}=-Z_\sigma sS_1R^6,
\qquad M_{ss}=Z_\sigma S_0R^7.
\]

The (p=8) term also produces higher-than-quadratic velocity terms; they are
not discarded from the full theory. At (s=0), the small-breathing frequency
is

\[
\omega_R^2=
\frac{30\kappa_1A_2R_0^3}
{\kappa_1D_2R_0^5+D_8/R_0}.
\]

For the nonphysical normalization (\kappa_1=Z_\sigma=1,g=0), the prototype
gives (R_0=2.20529640583177) and
(\omega_R=0.425901033279284). These are `PROXY_ONLY`.

## 7. Sigma formation branch

\[
C_\sigma(R)=Z_\sigma B_2R^5+A_0B_0R^7
+g\left(\kappa_1C_2R^5+\frac{C_8}{R}\right).
\]

Since (\partial_s^2V(R,0)=2C_\sigma(R)), a fixed-(R) branch exists for
(C_\sigma(R)<0) and (G_0B_4>0):

\[
s_*^2=-\frac{C_\sigma(R)}{2G_0B_4R^7}.
\]

This is not yet a complete envelopment solution. The radial equation,
constraints, metric response, boundary data, and stability problem must also
close.

## 8. Physical-orbit boundary

No least-restrictive gauge-fixed eta--sigma--metric--connection ansatz and
boundary domain is selected by the present action data. Consequently:

- no charged electron-like orbit is derived;
- no physical period, energy, radius, or Floquet operator is available;
- the collective radius proxy is not a particle;
- (A_f,G_f,Q_f,K_{ud}) are not evaluable on a physical orbit;
- CKM and PMNS remain `null`.

The exact next object is
`ACTION_SELECTED_GAUGE_DRESSED_CHARGED_SELF_ENVELOPMENT_RELATIVE_PERIODIC_ORBIT_WITH_LOCAL_CHIRAL_TRANSGRESSION`.

## 9. Cosmic scale

The current dimensionful coupling family admits the covariance

\[
G\mapsto c^2G,
\quad \kappa_1\mapsto c^{-6}\kappa_1,
\quad (\kappa_0,A_0,G_0,\Lambda_\eta)\mapsto c^{-8}(\cdots),
\]

with (Z_\chi,Z_\sigma\mapsto c^{-6}(\cdots)), (g\) invariant, and the
(\mathcal X_\eta^4) term invariant. This is not a symmetry of one theory with
all couplings held fixed: it shows that the current construction does not
select the numerical value of the dimensionful input (\kappa_1), hence it
does not select the physical unit. No closed cosmic boundary-value solution
or total topological-energy normalization removes that freedom. Therefore

`BHSM_GLOBAL_SCALE_REMAINS_UNDERDETERMINED_BY_CURRENT_ACTION`.

## 10. Downstream routes

- Quarks require a stable color-neutral parent, color-open subsectors,
  separation coordinate, energy curve, and hadronization bifurcation.
- Neutrinos require a near-null orbit with three monodromy sectors, a weak
  current pullback, and the adiabatic (L/E) limit.
- Measurement requires a normalized coupled-basin transition amplitude.
- Four-dimensional completion waits for physical orbit, scale, field
  dictionary, normalization, representation, scheme, and runtime closure.

No corresponding matrix or physical number is printed.
