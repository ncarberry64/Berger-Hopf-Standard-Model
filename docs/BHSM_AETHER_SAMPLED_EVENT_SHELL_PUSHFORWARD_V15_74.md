# BHSM v15.74 sampled event-shell pushforward

The constraint-solved Lorentzian child was reconstructed at
\(t=0.08,0.10,0.103\), and at the refined last regular slice
\(t=0.10602\).  On every slice the full radial functions
\(A,B,C,N,\beta,\sigma,X_\eta\) were used; no replacement by
\(\min L_\eta\) was made.

After diagonal \(Sp(1)\) reduction,

\[
 r=\frac{AB}{\sqrt{A^2+B^2}},\qquad
 K_F(\chi)=\pi^2(A^2+B^2)^{5/2},
\]

and

\[
 W_t=(1-4\sigma_t^2)(1+X_{\eta,t}^3).
\]

The transverse radial form is

\[
 Q_T[u]=\frac12\int d\chi\left[
 K_FW_t\frac rC(u')^2+K_FW_t\frac C r m^2u^2\right],
\]

while the electric form is

\[
 Q_E[v]=\frac12\int d\chi\left[
 K_FW_t\frac {r^3}C(v')^2+K_FW_tCr\ell(\ell+2)v^2\right].
\]

The same inverse stiffnesses enter the conservative up-channel LR bound.
Across the controlled interval the lowest transverse stiffness decreases from
approximately \(3.09\times10^3\) and the electric stiffness from
\(2.30\times10^3\).  Consequently the LR norm bound increases, as required
by the Dirichlet monotonicity theorem, but remains of order \(10^{-5}\), far
below one.

The next integration stage makes the eta Legendre form singular before a
regular near-zero-weight slice is obtained.  Hence this free-conformal branch
does not establish a composite crossing.  The required next enlargement is
not a separate Yukawa term: it is the common gauge--fermion supertrace-log
backreaction in the same constrained child action, followed by recomputation
of both weighted kernels.
