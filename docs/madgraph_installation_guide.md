# MadGraph Installation Guide

Machine-readable artifact:

```text
artifacts/BHSM_madgraph_installation_status_v1_7.json
```

Supported environment variables:

```text
MADGRAPH_PATH
MG5AMC_PATH
```

MadGraph should be obtained only from an official/legal source. The repository
does not run MadGraph smoke tests unless UFO export and loadability pass first.

Current status:

```text
madgraph_detected = false
import_test_attempted = false
import_test_passed = false
ready_for_smoke_test = false
```

