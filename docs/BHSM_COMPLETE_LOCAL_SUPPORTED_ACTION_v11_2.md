# BHSM v11.2 Complete Local Supported-Action Audit

The known regular skeleton is the frozen stratified parent action plus
`-1/2 |dq_D|^2`, with zero bare support potential and
`A_D=-(dq_D/lambda_D)`. Supporting a derivative term requires replacing each
parent derivative by `D^(w)`. Its expansion necessarily contains a linear
support-current coupling and the associated quadratic connection term:

```text
-A_D,A J_D^A + A_D,A A_D^A K_D.
```

Their coefficients are the primitive support characters. The parent action
does not assign them for its coframe, measures, normals, gauge, fermion,
scalar, current, compatibility, or core data. Writing a candidate formula
would therefore choose new physics rather than derive it.

Exact next object:
`ACTION_DERIVED_PRIMITIVE_SUPPORT_CHARACTER_AND_CURRENT_COUPLING_LEDGER`.

