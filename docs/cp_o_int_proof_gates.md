# CP O_int proof gates

The focused theorem requires 21 gates covering statement, domain, codomain,
field representation, Lorentz structure, gauge admissibility, action,
attachment, coupling, callable, schema, artifact, provenance, input hygiene,
tests, registry and formula proposals, claim policy, and blocker recording.

The default audit passes 13 gates and fails 8. It cannot be promoted while any
required gate fails.

A standalone CP O_int theorem requires field, Lorentz, gauge, coupling, and action/operator definitions.

Runtime-disabled software gates remain disabled until live external validation passes.
