# Wolfram Runtime Mapping Guide

Machine-readable artifact:

```text
artifacts/BHSM_wolfram_runtime_mapping_status_v1_7.json
```

Supported environment variables:

```text
WOLFRAMSCRIPT_PATH
WOLFRAM_KERNEL_PATH
MATHEMATICA_PATH
```

Current status:

```text
wolframscript_detected = false
wolfram_kernel_detected = false
mathematica_detected = false
ready_for_feynrules = false
```

No Wolfram license bypass, unauthorized key, or improper installer is used.
An external reviewer should provide a licensed runtime path and rerun the
Phase Three-N/O preflight.

