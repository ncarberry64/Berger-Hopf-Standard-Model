# BHSM Full-Completion Priority Map

Targets are scored before their closure result is evaluated:

```text
score = necessity
      + artifact locality
      + no-empirical path
      + cross-sector leverage
      + feasibility now
      - external-runtime penalty
```

The highest score is `24` for `boundary_measure_collar_transport`. It links seven completion categories and has repository-backed geometric and transport subresults that can be separated cleanly from missing physical normalization.

No observed masses, mixing values, residuals, PDG values, W calibration, neutrino limits, or legacy particle threshold tables enter the score.

Run:

```bash
python -m bhsm.interface full-completion-priority-map --format json
```

After the v1.7 charged cluster audit, the next upstream target is complete charged action normalization because it jointly controls `rho_ch`, physical stiffness, eta_l normalization, and higher-channel mixing provenance.
