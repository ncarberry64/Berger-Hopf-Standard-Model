# BHSM Renormalization Scheme Status v0.3

Machine-readable artifact:

```text
artifacts/BHSM_renormalization_scheme_status_v0_3.json
```

The repository currently has symbolic/common-scale transport artifacts, but it
does not provide a production renormalization prescription for FeynRules/UFO
export.

Current status:

```text
renormalization_scheme_complete = false
feynrules_ready = false
ufo_ready = false
```

Missing items include counterterm conventions, threshold matching scheme,
running scheme, and a map from BHSM boundary parameters to 4D gauge/Yukawa
couplings in a production scheme.
