# Notebook pack

The three notebooks demonstrate gallery, CLI/report, and theorem-blocker
inspection. Tests parse notebook JSON without executing cells or requiring
Jupyter, internet, PDG, Wolfram, FeynRules, or MadGraph.

```powershell
python -m bhsm.interface notebook-pack --manifest
python -m bhsm.interface notebook-pack --check
```
