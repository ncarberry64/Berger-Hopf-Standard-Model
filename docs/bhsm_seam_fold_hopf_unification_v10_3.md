# BHSM v10.3 Historical One-Mode Unification Audit

Status: `INVALIDATED_BY_AUTHOR_ONTOLOGY` as the active physical architecture.
The mathematical comparison below remains useful provenance. The corrected
architecture has distinct core, wall, and depth slots; the seam is a coordinate
readout and is not one of them.

The common state is

\[
\mathbf q=(\psi,\zeta,\varphi_F)^T,
\]

where `psi=S_Sigma` is the invariant endpoint trace, `zeta=q_fold` is the
normalized scalar-wall Jacobi amplitude, and
`varphi_F=delta ln(a_F/a_F0)` is the M8 Hopf breathing perturbation.

The first two components are already related by the v6.27 constraint solve:

\[
\psi=-\frac{\tau\pi\chi_1}{16}\zeta.
\]

This is a boundary projection of the fold response, not a new gauge choice and
not a duplicate physical degree. For the imported invariant representatives,
the gauge matrix has rank zero; the seam--fold constraint has rank one.

The conditional kinetic entries are `K_zeta,zeta=6.935084858283065` and
`K_F,F=6 kappa5`. The mixed `K_zeta,F` and `H_zeta,F` entries are
`UNDEFINED_CROSS_DOMAIN`: the repository has no action varied simultaneously
on the M5 fold domain and the M8 breathing domain. They are not set to zero.
Consequently the reduced two-component kinetic matrix may have rank one or
two, and no coupled eigenvector may yet be computed.

The current evidence therefore neither proves one unified mode nor proves
physical inequivalence. The exact verdict is

`BHSM_COMMON_ENVELOPMENT_MODE_EQUIVALENCE_BLOCKED_BY_UNDERIVED_CROSS_DOMAIN_HESSIAN`.
