# CP Holonomy Attachment Resolution Attempt

Machine-readable artifact:

```text
artifacts/BHSM_cp_holonomy_attachment_resolution_attempt_v1_0.json
```

## Result

`delta_BH = pi/3` is already sourced in CKM/PMNS mixing artifacts, so CP
holonomy attachment is partially resolved for CKM/PMNS mixing vertices.

The generic standalone vertex

```text
G_raw * exp(i*delta_BH) * O_int + h.c.
```

remains blocked by the missing interaction operator `O_int`.

