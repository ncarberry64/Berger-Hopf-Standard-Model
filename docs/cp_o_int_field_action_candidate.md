# CP O_int field/action candidate

The symbolic candidate is

```text
G_raw * exp(i delta_BH) * (Psi_CP_interface_bar O_int Psi_CP_interface) * H_CP + h.c.
```

Only the phase and the pre-existing placeholder symbols are artifact-backed.
The physical field representation, contractions, projectors, normalization,
measure, and variation are not derived.

A symbolic field/action candidate is not action-level closure.
