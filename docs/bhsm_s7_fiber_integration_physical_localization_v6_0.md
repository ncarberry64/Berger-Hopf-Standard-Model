# BHSM v6.0 S7 Fiber Integration and Physical Localization

## Result

`BHSM_S7_ARCHITECTURE_AMBIGUOUS`

The exact nested Hopf–twistor geometry can be constructed, and its fiber
pushforwards can be stated with all orientation, measure, degree, covariance,
and boundary hypotheses visible. The current BHSM repository does not contain
an action-selected `S7` total space, `B8` bulk action, physical `S7` metric,
signature, orientation, fiber scale, or collar matching map. Consequently this
sprint does not promote the mathematical diagram to the physical BHSM action
domain.

## Exact mathematical diagram

The two subgroup quotients generated by `U(1) subset Sp(1)` give

```text
S1 = U(1)  ->  S7  --p_C-->  CP3
                         \
                          p_H
                           \
                            S4
                         /
                    tau /
S2 = Sp(1)/U(1) -> CP3
```

with `p_H = tau o p_C`. Thus:

- `S3 = Sp(1) -> S7 -> HP1 = S4` is the quaternionic Hopf principal bundle;
- `S1 = U(1) -> S7 -> CP3` is the complex Hopf principal bundle;
- `S2 = CP1 -> CP3 -> S4` is the associated twistor bundle, not a principal
  `S2` bundle.

This closes the topology and local dimensions as mathematics. It does not
choose which quotient, if any, is the physical BHSM reduction.

## Metric, orientation, and measure

For the complex Hopf description one may write, after fixing normalization,
`g_S7 = p_C^* g_FS + eta^2`. The quaternionic description has a 3-Sasakian
horizontal/vertical split with three `Sp(1)` vertical one-forms. A uniform
canonical variation of the three-dimensional vertical metric and a Berger
anisotropy that singles out a `U(1)` inside `Sp(1)` are different choices.

The repository's explicit Berger source is three-dimensional:
`g = r_base^2(sigma_1^2+sigma_2^2)+r_fiber^2 sigma_3^2`. It is an `S3`-type
Hopf geometry and is not, by itself, an `S7` metric or a rule for embedding a
Berger `S3` fiber into `S7`.

The convention used here orients the total space by base first and fiber
second. Fiber integration uses the actual action or metric measure. Dividing
by fiber volume would change an action integral into an average and is not
allowed unless a normalized measure is independently selected by the action.

## Fiber pushforward theorem

Let `pi:E->B` be a proper smooth submersion with compact oriented fibers of
dimension `r`. For an integrable `k`-form,

```text
pi_* : Omega^k(E) -> Omega^(k-r)(B)
```

integrates the vertical top-degree component over each fiber. In the declared
product convention,

```text
pi_*(alpha wedge beta_top) = alpha integral_F beta_top.
```

For closed fibers, `d pi_* omega = pi_* d omega`. If the fiber has a boundary,

```text
d pi_* omega = pi_* d omega
             + (-1)^(k-r) (pi_boundary)_* i^* omega.
```

Reversing fiber orientation reverses the pushforward sign. The operation on
forms is metric independent, while a metric-derived integrand or volume form
retains all radius and squashing dependence. For bundle-valued coefficients,
the reduction is canonical only for pullback/basic data or after a connection,
parallel identification, and invariant trace have been supplied.

Compatible orientations and Fubini hypotheses yield
`(p_H)_* = tau_* o (p_C)_*`. This is a conditional action-reduction theorem,
not evidence that the current v5 action was obtained from such a parent action.

## Physical-domain fork

The following remain distinct and unselected: a Euclidean `B8` with boundary
`S7`, a Lorentzian eight-dimensional bulk, a seven-dimensional canonical
spatial slice, a timelike or spacelike boundary/collar, and a static energy or
action-per-time reduction. The Riemannian `S4` base of the quaternionic Hopf
map is not automatically observed Lorentzian spacetime.

The v5 collar has a normalized coordinate and conditional Jacobian measure.
`rho_star=1` is not a physical length. Without a parent action and physical
fiber radius, pushing a top form forward cannot produce a physical boundary
tension, absolute unit, mass, or gauge coupling.

## Claim boundary and next gate

Derived: the nested bundle diagram, its dimension ledger, and the conditional
fiber-integration theorem. Conditional: metric/action pushforward after the
missing physical choices are supplied. Not derived: an `S7` BHSM action domain,
absolute scale, physical validation, particle masses, gauge couplings, CKM
completion, rare-B predictions, or full BHSM completion.

The exact next gate is
`bhsm-b8-s7-physical-domain-action-source-closure-v6-0-1`: locate or derive the
parent action and signature that select an `S7` boundary, then fix its metric,
orientation, physical fiber scale, and collar matching before beginning the
scalar localization planned for v6.1.
