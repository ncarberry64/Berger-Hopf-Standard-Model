# CP Holonomy O_int Attachment Theorem

Phase Three-I audits whether the BHSM CP holonomy source can be promoted to a
standalone production interaction vertex.

## Finding

The BHSM phase

```text
delta_BH = pi/3
```

remains partially attached through CKM/PMNS mixing sources. A standalone CP
holonomy vertex requires an interaction operator:

```text
O_int
```

The repo does not yet define `O_int` with field content, Lorentz structure,
gauge structure, and coupling placement.

## Status

```text
theorem_status = OPEN_EXACT_MISSING_THEOREM
attached_to_CKM_PMNS_mixing = true
standalone_attachment_defined = false
promotes_standalone_cp_vertex = false
feynrules_ready = false
ufo_ready = false
```

The existence of `delta_BH = pi/3` alone does not define a general
CP-violating Lagrangian.

