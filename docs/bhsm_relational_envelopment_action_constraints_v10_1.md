# BHSM v10.1 Relational Envelopment Action Constraints

## Geometry reconciliation

The global parent remains `M8=I_t x S7`. The quaternionic Hopf `S3=Sp(1)` is
the vertical fiber of `S3 -> S7 -> S4`; `M5=I_t x S4` is the pushforward/cap
stratum; and `M4=I_t x S3` is the intrinsic equatorial seam. The author
expression `S3 x M4` is seven-dimensional and is not M8. It is realized as
the Hopf-fiber lift of the M4 seam; locally its collar completion is
`S3_fiber x M4 x I_rho`. Globally the Hopf bundle is not replaced by a
product.

Verdict:
`BHSM_S3_M4_IS_A_LOCAL_OR_REDUCED_DESCRIPTION_NOT_THE_FULL_PARENT`.

## Action exhaustion

V10.1 adds no action term or continuous parameter. It audits the strongest
current action:

\[
S_{\rm BHSM}^{\rm env}=S_8^{\rm env}+S_5+S_{\rm GHY}
+S_{4,\rm intrinsic}+S_{\rm compatibility}+S_{\rm current}.
\]

The eta equation, stress, connection current, unit constraint, and dimensions
remain those of v10.0. M5 cap ADM/GHY/matcher and intrinsic M4 ownership remain
separate. Intrinsic Standard Model fields are not claimed to descend from the
M8 metric.

The current action supports local covariant conservation conditionally,
smooth eta-degree conservation, a stable collective proxy radius, and an
eta-sector conjugation involution. It does not supply the full buoyancy
functional, global scalar cosmic energy, entropy coarse graining, full
matter--antimatter equivalence, neutrino vertex observables, or normalized
closed-system transition amplitudes.

Primary verdict:
`BHSM_RELATIONAL_ENVELOPMENT_PARENT_ACTION_CONSTRAINTS_CONSTRUCTED_CONDITIONALLY`.

Exact next object:
`COVARIANT_ACTION_DERIVED_NORMAL_RADION_BUOYANCY_FUNCTIONAL_WITH_GLOBAL_CONSTRAINT_AND_LOCAL_ENVELOPMENT_BACKREACTION`.
