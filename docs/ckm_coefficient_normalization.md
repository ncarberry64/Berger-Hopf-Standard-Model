# CKM Coefficient Normalization

Status: `OPEN_MISSING_CKM_COEFFICIENT_NORMALIZATION`.

The bounded target contains `g2_BH_runtime/sqrt(2)`, but the repository explicitly classifies `g2_BH_runtime` as a runtime/scheme input. No fixed geometric `C_CKM` or action normalization rule is present.

A coefficient normalization alone does not derive the CKM exponent.

The v2.9 audit closes only the interface form, not the value source.
