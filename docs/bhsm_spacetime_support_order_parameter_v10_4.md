# BHSM v10.4 stratified-core spacetime-support order parameter

## Author-selected extension

The author selects a dimensionless diffeomorphism scalar
`upsilon in [0,1]`. On the regular inverse-metric action domain,
`0 < upsilon <= 1` and `det(G) != 0`. The normalized background is
`upsilon=1`; `0<upsilon<1` denotes depleted ordinary spacetime support; and
`upsilon=0` is boundary data on `Sigma_core` or data of `M_core`, not an
interior degenerate metric in the existing Einstein-type equations.

This field is geometric. It is not a matter particle, gravity mediator,
coordinate radius, seam displacement, metric determinant, Hopf radion,
scalar-wall fold, generation label, or imposed density profile.

The prior result is unchanged:

`PROPER_VOLUME_DEFICIT_AS_Q_D: INVALIDATED`.

The common-volume pair is removed by the lapse/Hamiltonian constraint chain.
The author-selected field is a new configuration-space extension, not a
renaming of that removed direction.

## Depth coordinate

`d_log=-log(upsilon)` is monotone, vanishes at the regular background, and
diverges at the core boundary. It is only an ontology coordinate until the
kinetic function is selected. The canonical coordinate is

`q_D(upsilon)=integral_upsilon^1 sqrt(Z_reduced(s)) ds`.

Thus `q_D` is proportional to `-log(upsilon)` only when
`Z_reduced` is proportional to `upsilon^-2`.

Status:
`STRATIFIED_CORE_SPACETIME_SUPPORT_ORDER_PARAMETER_AUTHOR_SELECTED_GEOMETRIC_EXTENSION_CLASS`.
