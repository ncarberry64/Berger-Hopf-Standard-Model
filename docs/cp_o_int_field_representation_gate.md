# CP O_int field representation gate

The existing effective-Lagrangian candidate names a generic `psi` field. It
does not define chirality, families, conjugation, or a physical gauge
representation. Sprint C therefore records an `AVAILABLE_SYMBOLIC_CANDIDATE`
factor with an explicit missing physical representation.

A standalone CP O_int theorem requires field, Lorentz, gauge, coupling, and action/operator definitions.
