# eta_l Source Audit

Status: `CONDITIONAL_ETA_L_SOURCE_CANDIDATE`.

The trace-normalized suppression kernel conditionally supplies the lepton projection fraction, while the stochastic chain supports `alpha/pi` and the repository convention supports

```text
eta_l=8*alpha/(9*pi).
```

The complete charged stochastic/action generator, cycle/time normalization, action-level lepton coupling, and transport normalization remain open. A historical fitted eta exists, but it is not used as a theorem input and no official value is changed.
