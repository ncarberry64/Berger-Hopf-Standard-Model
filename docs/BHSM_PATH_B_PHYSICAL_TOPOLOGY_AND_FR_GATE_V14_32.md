# BHSM v14.32 — Path B physical topology and FR gate

## Verdict

`BHSM_PATH_B_M4_S6_ACTION_CLOSES_COLOR_ETA_OWNERSHIP_BUT_CANNOT_CARRY_THE_DEGREE_ONE_ETA_KNOT_OR_FR_FERMION_SECTOR_BECAUSE_PI3_S6_AND_PI4_S6_VANISH`

Path B remains a coherent foundational **bosonic color–eta action**. It does not, by itself, retain the topology used by the earlier `M8` eta-knot and Finkelstein–Rubinstein construction.

## 1. Global physical target

The physical Path-B field may be represented globally as

\[
\eta=(s,z)\in \mathbb R\oplus\mathbb C^3,
\qquad
s^2+2z^\dagger z=1.
\]

The round metric in the retained local normalization is

\[
 ds^2+2\operatorname{Re}(dz^\dagger dz).
\]

`SU(3)` leaves `s` invariant and acts on `z` in the fundamental. At the selector `eta0=(1,0)`, the tangent space is the real six-dimensional `3+bar3` representation used in v14.29–v14.31.

## 2. Static particle topology

Finite energy on physical three-space requires the field to approach one vacuum at spatial infinity. Hence

\[
\mathbb R^3\cup\{\infty\}\simeq S^3,
\]

and static eta configurations are maps

\[
S^3\longrightarrow S^6.
\]

Because a sphere is `(n-1)`-connected,

\[
\pi_3(S^6)=0.
\]

There is therefore no integer degree-one sector for the physical `M4` eta field. Adding one compactified collar-normal coordinate gives `S4 -> S6`, but

\[
\pi_4(S^6)=0
\]

as well.

This does not contradict the earlier eta-knot calculation. That calculation used a seven-dimensional compactified spatial domain and an `S7` target:

\[
S^7\longrightarrow S^7,
\qquad
\pi_7(S^7)=\mathbb Z.
\]

The two topological problems are not interchangeable.

## 3. FR obstruction

For the based physical mapping space

\[
\mathcal C_\eta=\operatorname{Map}_*(S^3,S^6),
\]

loop–suspension adjunction gives

\[
\pi_1(\mathcal C_\eta)\cong\pi_4(S^6)=0.
\]

Thus the physical `M4` `S6` sigma field alone has no nontrivial mapping-space loop that can support the earlier FR sign choice. The old FR-odd result remains valid only inside its original ultraviolet/topological sector until an action-owned transgression to an `M4` spinor is proved.

Large `SU(3)` gauge transformations and instanton/Chern–Simons sectors are separate gauge topology. They do not automatically turn the eta field into a fermion.

## 4. What remains valid

The Path-B action ownership result remains valid:

\[
S_{\rm color-eta}=S_{\rm YM}[A]+S_\eta[A,\eta].
\]

Its physical color current remains an Euler–Lagrange source, and the composite `theta` still adds no independent vector field.

The p2+p8 action can support a non-topological stationary scale. In three spatial dimensions,

\[
E(\lambda)=\lambda E_2+\lambda^{-5}E_8+\lambda^{-1}E_{\rm YM},
\]

so a stationary source-free branch obeys

\[
E_2-5E_8-E_{\rm YM}=0.
\]

This permits a saddle or source-bound response. It does not supply topological protection or FR quantization.

## 5. Wilson BVP status

The gauge-fixed Wilson-sourced eta–SU(3) BVP remains action-eligible as an external-response and confinement calculation. A stable solution would describe a gauge/eta response to imposed color-singlet sources. It must not be labeled a derived quark or an eta-knot particle.

The parallel confinement object remains:

`GAUGE_FIXED_WILSON_SOURCED_ETA_SU3_NONABELIAN_STATIONARY_BVP_WITH_SELF_ADJOINT_DOMAIN_PARENT_RELATIVE_SUBTRACTION_NONRADIAL_HESSIAN_AND_RELATIVE_DETERMINANT`

## 6. Matter-completion fork

BHSM now has four honest possibilities:

1. **M8 collective transgression:** derive the `S7` degree-one sector as a normalized `M4` Dirac field with color, chirality, current matching, self-adjoint domain and no double counting.
2. **Foundational Dirac matter:** explicitly retain quark and lepton Dirac bundles as foundational low-energy fields. This is a new postulate, not an eta derivation.
3. **Change the physical target or domain:** choose topology with a nontrivial physical particle homotopy group. This changes Path B and requires a full reaudit.
4. **Gauge-topology index mechanism:** derive a genuine index or spectral-flow theorem converting gauge topology into the required fermionic matter sector.

The controlling next object is:

`ACTION_OWNED_M8_DEGREE_ONE_TO_M4_FERMIONIC_COLLECTIVE_TRANSGRESSION_OR_EXPLICIT_FOUNDATIONAL_DIRAC_MATTER_POSTULATE_WITH_NO_DOUBLE_COUNTING`

## Classification ledger

### Validated

- Path B gives one global physical `S6` eta target tied to the physical color bundle.
- `pi3(S6)=0` for static physical configurations.
- `pi4(S6)=0`, so the physical eta mapping space has no FR loop sector.
- p2+p8 and Yang–Mills scaling can balance a non-topological source-bound response.
- The external Wilson BVP remains eligible.

### Invalidated

- A degree-one eta knot formed solely by the physical `M4` `S6` field.
- Direct reuse of the historical `M8` FR-odd line as the Path-B physical quark.
- Calling a Wilson-supported eta response a topologically protected matter particle.

### Reclassified

- `M8` matching is not a blocker on bosonic action ownership, but it is again a blocker on **derived fermionic matter** under the original eta-knot route.
- Path B is a bosonic physical-action completion layer, not yet a complete particle ontology.

Frozen predictions are unchanged. No string tension, mass, CKM, PMNS, or neutrino output is emitted.
