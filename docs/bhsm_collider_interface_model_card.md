# BHSM Collider-Interface Model Card

Machine-readable artifact:

```text
artifacts/BHSM_collider_interface_model_card_v1_7.json
```

Model:

```text
BHSM_Minimal_Collider_Interface
```

Scope:

```text
bounded minimal CKM/PMNS collider-interface subset
```

Appropriate uses:

- reviewing BHSM collider-interface structure;
- checking reproducibility of the minimal CKM/PMNS bounded subset;
- attempting FeynRules/UFO/MadGraph validation in a licensed HEP software
  environment.

Inappropriate uses:

- claiming a complete BHSM collider model;
- claiming experiment-approved integration;
- claiming empirical validation;
- using runtime PDG values as BHSM derivation inputs;
- using excluded vertices as validated production vertices.

