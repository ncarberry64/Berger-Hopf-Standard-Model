# BHSM N=3 projected event Calderon flux (v17.92)

The terminal GHY/eta flux has been projected onto the same
constraint-preserving attachment tangent `c=(q_W,x_D)` used for the v17.91
momentum and force. The projection uses

\[
\Gamma_{1,q}^{\rm event}
=\Pi_{\log A}\,d_q\log A+\Pi_{\log B}\,d_q\log B.
\]

The eta flux is retained but has zero attachment variation in the `f=chi`
quotient; lapse is a multiplier rather than either attachment configuration
coordinate.

This is only the event metric/eta scalar half. The reconstructed-child flux,
pregeometric event-core flux, and complete gauge/spinor/ghost projection are
still open and are not set by reflection or by a static-balance assumption.
The finite one-sided flux is part of the dynamic Wentzell equation, not a
particle defect.
