# BHSM v15.54 — hybrid flavor spectrum and mixing theorem

For a scalar Berger mode \((k,j)\), put \(q=k-2j\). Its exact eigenvalue is

\[
 \lambda_{k,j}
 =\frac{k(k+2)-q^2}{L_2^2}+\frac{q^2}{L_1^2}.
\]

The actualization reconstruction has a round diagonal fiber,
\(L_1=L_2=R_F\), so

\[
 R_F^2\lambda_k=k(k+2).
\]

This evaluates every stored three-family mode ledger without a measured
input. These are action-normalized scalar Berger excitation seeds. They are
not yet physical fermion masses: that identification requires the spinor lift
and the Dirac--Yukawa matrix element.

Let \(P_0,P_1,P_2\) be the exact (C_3) triality projectors. The family
operator presently supplied by the action has the form

\[
 M_f=\sum_{a=0}^2m_{f,a}P_a
\]

for every sector. Therefore all sector operators commute and share the same
triality Fourier eigenbasis. Consequently

\[
 \boxed{V_{CKM}=I_3,\qquad U_{PMNS}=I_3,\qquad J_{CP}=0}
\]

up to unphysical diagonal phases. This is the actual prediction of the
current operator content. Nontrivial mixing requires an action-owned family
operator that fails to commute with at least one (P_a); no such operator is
present, so a historical numerical mixing screen is not promoted.

The reset spectral unit is (R_F^{-1}), with
\(R_F=(343/5)^{1/6}\) in the \(\kappa_1=1\) convention. Conversion to an
external unit is controlled by the dimensionful parent Einstein coefficient,
not by calibration to a measured particle mass.
