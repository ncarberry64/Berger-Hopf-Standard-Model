# BHSM v16.36: physical inverse closure

## Result

Known particle physics is now encoded as a structural engineering
specification, not as a parameter table. The machine-readable matrix records
the representation, rank, nullity, degeneracy, relative orientation,
persistence, and symmetry properties that a completed BHSM child must
produce. No measured mass, angle, coupling, splitting, or lifetime is used to
select an action coefficient, branch, geometry, normalization, or KKT state.

## First electron-like child

The existing rank-16 bundle owns electron chirality and representation data;
the geometric hypercharge operator and eventual unbroken electromagnetic
generator own its charge; the M4 spin domain plus odd FR line own the
spin/statistics structure. None of those facts supplies rest mass.

Electron mass must be the nonzero electron projection of the returned broken
LR/HS operator generated by the same common M5 -> M4 pushforward that supplies
the gauge response. A transient LR/HS pulse is insufficient. The return
certificate requires a positive mass singular value preserved by the physical
return intertwiner, exact charge-projector intertwining, projective
persistence, and no action-allowed decay channel.

## Generations, flavor, and gauge structure

The returned charged-lepton operator must not remain `c I3`. The quark
Hermitians must have a nonzero commutator before CKM can be physical, and a
CP-odd invariant must be nonzero before CP violation can be claimed. The
neutral propagation operator must acquire at least two nonzero eigenvalue
differences only in reconstructed-spacetime propagation.

On the twelve gauge generators, the returned broken gauge Hessian must have a
nine-dimensional kernel—eight color plus electromagnetism—and a
three-dimensional positive electroweak image. This gauge Hessian and the LR
mass operator are derivatives of one common pushforward.

## Current KKT diagnostic

The current N=3 variables are the parent scale, the `u/w/v` geometry modes,
lapse/shift constraints, period, and event multiplier. They own the unbroken
physical event background and the common determinant response. They do not
contain an explicit broken HS/LR order parameter or family-orientation
coordinate. That is not a defect in the current saddle: the explicit mass
order parameter first belongs to the nonlinear broken branch after event
closure and higher-N convergence.

The artifact reports which current coordinate groups dominate stationarity
and the downstream physical role of each group. This diagnostic does not add
coordinates or alter the active solve.

## Collapsed completion graph

Real-particle requirements reduce to nine independent objects: simultaneous
N=3 event closure; the shared event pushforward; N=4+ Sobolev convergence; the
nonlinear broken child; reconstruction and persistent return; returned
fermion/gauge/neutral operators; color-singlet and decay-classifier domains;
the absolute scale from the same pushforward; and the frozen held-out kill
screen plus Unique Actualization audit.

Status: **RECLASSIFIED**. The physical inverse audit validates continuation of
the unchanged N=3 solve and invalidates any attempt to insert particle data or
an independent gauge/Yukawa normalization. `FULL_BHSM_COMPLETE = FALSE`.
