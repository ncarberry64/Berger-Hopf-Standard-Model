# BHSM N=3 complete-child persistence (v17.99)

Starting from the v17.96 flux-balanced child, with the v17.97 zero-background
Standard Model sector and v17.98 discrete firewall match, the full N=3
Euler--Dirac evolution is advanced for ten constraint-projected steps over a
positive proper-time interval of `1e-4`.

Throughout the interval, all seven local constraints remain below `1e-8` and
the refined eta-Legendre minimum remains positive. The configuration and
velocity both change, so the witness is genuinely time dependent. Nonzero
motion, momentum, force, and acceleration are retained as parts of the
constraint-consistent relative evolution.

Persistence means remaining inside this reconstructed child domain for a
positive duration. Decay is the first exit caused by constraint loss,
eta-Legendre loss, or a nonfinite reconstructed state. No such exit occurs on
the witness interval; no claim of eternal stability is made.

The event-to-complete-child dependency is now closed sufficiently to resume
the nonlinear N=3 closure with the child solvability relation enforced as an
acceptance condition.
