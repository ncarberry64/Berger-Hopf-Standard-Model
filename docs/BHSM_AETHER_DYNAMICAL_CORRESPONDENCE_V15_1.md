# BHSM v15.1 Aether dynamical correspondence

Primary verdict:

`BHSM_V15_1_THE_EXISTING_ARCHIVE_FIXES_THE_UNIVERSAL_RELATIONAL_SCHRODINGER_ACTION_FORM_AND_ADMITS_EXACT_SELF_ADJOINT_INVARIANT_PRESERVING_EVENT_DOMAINS_WITH_AN_IDENTITY_LIMIT_RECOVERING_REGULAR_BHSM_BUT_DOES_NOT_ACTION_SELECT_THE_PREGEOMETRIC_GENERATOR_CORE_BOUNDARY_HILBERT_REPRESENTATION_OR_REFERENCE_CLOCK_CYCLE;_TWO_INEQUIVALENT_FIXED_INTEGER_SPECTRUM_GENERATORS_SATISFY_ALL_CLOSED_GATES_SO_THE_REQUESTED_PHYSICAL_EVENT_LAW_REMAINS_UNDERDETERMINED`

This campaign closes the structural dynamical theorem class but not the
physical action-ownership gate. No arbitrary coefficient, primitive core
field, fitted datum, background clock, or preferred frame is introduced.

## 1. Variational functional and transition kernel

Let `chi` be the dimensionless additive process cocycle derived in v15.0 and
let `H_A` be a candidate attachment Hilbert module. Strongly continuous
unitary composition fixes the universal first-order form

\[
S_A[\psi]
=\int_\gamma d\chi\,
\left(
-\operatorname{Im}\langle\psi,D_\chi\psi\rangle
-\langle\psi,K_A\psi\rangle
\right),
\]

with

\[
K_A=K_A^*,
\qquad
U_A(\chi)=e^{-i\chi K_A},
\qquad
W[E]=e^{iS_A[E]}.
\]

This is a precise dynamic weight and composition law once `K_A` and the
Hilbert pairing are supplied. The form is parameter-free. It is not yet an
action-owned physical law because the retained BHSM archive does not derive
`H_A` or `K_A` for the non-geometric core.

## 2. Exact self-adjoint relative-boundary theorem class

The attachment boundary data are abstract Hilbert traces, not spacetime
coordinates on the core:

\[
\Gamma_0=(u_G,u_C),
\qquad
\Gamma_1=(p_G,p_C).
\]

For a Hermitian Wentzell response `W`, impose

\[
u_G-u_C=0,
\qquad
p_G+p_C+Wu=0.
\]

Writing these conditions as

\[
A\Gamma_0+B\Gamma_1=0
\]

gives

\[
A=
\begin{pmatrix}
I&-I\\ W/2&W/2
\end{pmatrix},
\qquad
B=
\begin{pmatrix}
0&0\\ I&I
\end{pmatrix}.
\]

The executable theorem verifies

\[
\operatorname{rank}(A\ B)=\dim\partial,
\qquad
AB^*=BA^*,
\]

and the boundary Green form vanishes. Thus Hermitian Wentzell data define an
exact self-adjoint `G_A`--`C_A` correspondence domain and the induced unitary
kernel conserves norm.

This proves theorem-class existence. It does not derive the physical
Wentzell/Calderon block. That distinction is required by v14.66, where the
physical action-normalized Gram-Hessian and tangential blocks remained open.
It also preserves v14.43: self-adjointness selects a matcher class, not a
unique physical relative holonomy.

## 3. Parent invariants and clock calibration

For every declared closed-system parent invariant `I_alpha`, dynamical
matching requires

\[
I_\alpha(A_-)=I_\alpha(A_+),
\qquad
[K_A,I_\alpha]=0.
\]

The commutant condition makes every invariant eigenspace reducing for `K_A`,
so `U_A(chi)` preserves the matched invariant sector.

A stable recurring process must be selected by the same parent action before
ordinary time exists. With the unit-cycle convention only after that
selection,

\[
\frac{t_{\rm eff}}{\tau_{\rm ref}}=chi,
\qquad
H_{\rm eff}=\frac{\hbar}{\tau_{\rm ref}}K_A,
\qquad
E_{\rm eff}=\frac{\hbar}{\tau_{\rm ref}}\kappa.
\]

No external clock or absolute Aether time is introduced. The algebraic
calibration is consistent, but the archive does not yet derive a stable
reference cycle or its scale.

## 4. Exact regular BHSM recovery

At zero process depth,

\[
U_A(0)=I.
\]

For an identity event whose contribution and first regular-field variations
vanish,

\[
\Gamma_{\rm total}[\Phi,E_{\rm id}]
=\Gamma_{\rm BHSM}[\Phi],
\qquad
\frac{\delta\Gamma_{\rm total}}{\delta\Phi}
=\frac{\delta\Gamma_{\rm BHSM}}{\delta\Phi}.
\]

The symbolic verification uses the retained metric--eta density class

\[
\sqrt g\left(
\kappa_1R-\frac{\kappa_0}{2}
+\frac{\kappa_1X}{2}+\frac{X^4}{8}
\right)
\]

and a completely generic formal event response multiplied by an identity
deviation marker. Substituting the identity marker before comparing the
metric, curvature, and eta-`X` Euler derivatives returns exact symbolic zeros.
The marker is bookkeeping, not an adopted physical coefficient.

Therefore the regular recovery gate closes exactly and without artifact or
prediction drift. It does not prove that any nontrivial core transition
occurs.

## 5. Constructive generator nonuniqueness theorem

Self-adjointness, invariant matching, unitary composition, clock consistency,
and identity recovery still do not select `K_A`. Consider the same invariant

\[
I=\operatorname{diag}(1,-1)
\]

and the two fixed generators

\[
K_A^{(1)}=\operatorname{diag}(0,1),
\qquad
K_A^{(2)}=\operatorname{diag}(0,2).
\]

Both are self-adjoint, both commute with `I`, both yield unitary kernels, and
both have the exact identity limit. Their spectra differ, so they are not
unitarily equivalent, and their kernels differ at unit process depth.

No continuous family or adjustable coefficient is used in this witness. It
proves that the declared closed conditions are insufficient to choose a
physical event law.

## 6. Action-ownership boundary

The requested v15.1 object is therefore only partially closed:

- universal relational action/kernel form: closed;
- self-adjoint relative-boundary theorem class: closed;
- norm conservation: closed conditionally;
- invariant commutant and clock map: closed conditionally;
- exact identity recovery: closed;
- physical core Hilbert module: not derived;
- physical Wentzell/Calderon response: not derived;
- physical generator `K_A`: not selected;
- stable reference clock cycle: not selected;
- nontrivial physical event amplitude: not derived.

## Hindsight 20/20

### Validated

- Additive relational order and unitary composition fix the universal
  first-order action and exponential kernel form.
- Hermitian Wentzell data give an exact self-adjoint attachment domain.
- The boundary Green form vanishes and norm is conserved.
- Commuting generators preserve parent invariant sectors.
- Clock calibration produces conventional energy only after a stable
  reference process exists.
- Identity transport exactly recovers the regular metric--eta action and
  field equations.

### Invalidated

- Self-adjointness uniquely selects the event generator.
- Invariant matching plus identity recovery selects a unique kernel.
- The existing archive already contains a physical core-boundary Hilbert
  representation.
- A clock period may be inserted as an external Aether parameter.
- A theorem-class Wentzell matrix is already the physical BHSM response.

### Reclassified

- The v15.0 event span now carries a universal conditional unitary-action
  class.
- The self-adjointness blocker is physical generator and response-block
  provenance, not theorem-class existence.
- Regular recovery is an exact identity-limit theorem, not evidence of a
  nontrivial event.

### Open

Exactly one next irreducible object:

`ACTION_DERIVED_PREGEOMETRIC_EVENT_GENERATOR_K_A_ON_AN_ACTION_DERIVED_CORE_BOUNDARY_HILBERT_MODULE_WITH_PHYSICAL_WENTZELL_CALDERON_BLOCKS_INVARIANT_COMMUTANT_AND_STABLE_REFERENCE_CLOCK_CYCLE`

## Completion boundary

```text
STRUCTURAL_EVENT_DYNAMICS_THEOREM_CLASS_DERIVED = TRUE
PHYSICAL_EVENT_LAW_DERIVED = FALSE
REQUESTED_NEXT_OBJECT_FULLY_CLOSED = FALSE
FULL_BHSM_COMPLETE = FALSE
MARK_III = NOT_REACHED
USB_SYNCHRONIZATION_ELIGIBLE = FALSE
```

Frozen predictions and official prediction logic are unchanged. No new
continuous parameter, primitive field, preferred frame, empirical input, or
USB operation enters v15.1.
