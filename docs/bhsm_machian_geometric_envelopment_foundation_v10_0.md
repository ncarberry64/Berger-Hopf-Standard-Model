# BHSM v10.0 Machian Geometric Envelopment Foundation

Primary campaign result:
`BHSM_DYNAMIC_ENVELOPMENT_ACTION_AND_COMPLETION_ARCHITECTURE_CONSTRUCTED_CONDITIONALLY`.

## 1. Canonical doctrine

BHSM adopts Machian Geometric Envelopment as a `STRUCTURAL_POSTULATE`:

> The universe is one closed, interconnected geometric system. A physical
> particle is not a rigid object inserted into spacetime. It is a persistent,
> topologically organized, dynamically maintained energy--geometry
> differential of the parent spacetime. Its identity is the stability of its
> dynamical and topological class, not a constant instantaneous radius.

The reciprocal architecture is

\[
\text{global geometry}\longrightarrow\text{local envelopment},\qquad
\text{local envelopment}\longrightarrow\text{global geometry},
\]

with hierarchy

\[
\text{cosmos}\supset\text{environment}\supset
\text{envelopment}\supset\text{sub-envelopment}.
\]

This is now the canonical interpretive foundation of the repository. It is
not represented as a theorem following from the action.

## 2. Classification vocabulary

Every v10 result uses exactly one of:

- `DERIVED`;
- `DERIVED_CONDITIONAL`;
- `STRUCTURAL_POSTULATE`;
- `CANDIDATE`;
- `PROXY_ONLY`;
- `INVALIDATED`;
- `OPEN`;
- `BLOCKED_EXACT_OBJECT_PROVED`.

The distinction is substantive. In particular, adopting a field or ontology
does not prove that a physical solution exists.

## 3. Complete state and particle

The complete state is registered as

\[
\Phi(\tau)=(G_{AB},\chi,\sigma,\eta,A_{\rm gauge},
\text{boundary/collar data}).
\]

The bulk v10 structural action owns (G_{AB},\chi,\sigma,\eta), and the
auxiliary multiplier (\Lambda_\eta). Gauge and boundary/collar fields remain
stratified; their mere appearance in the complete state does not move them
into the eight-dimensional bulk action.

A particle is a gauge- and diffeomorphism-equivalence class of a stable
relative-periodic solution,

\[
\Phi(\tau+T)=h\cdot\Phi(\tau),
\]

where (h) may include gauge, admissible diffeomorphism, Spin/(G_2),
triality, (C_3), or internal phase operations. A static soliton is the
constant-orbit special case. A rigid fixed radius is not required.

Status: `STRUCTURAL_POSTULATE`. No physical orbit has yet been derived.

## 4. Boundary formation

The undifferentiated branch is (\sigma=0). Formation is controlled by

\[
\lambda_{\min}(H_\sigma^{(0)}[\Phi])=0.
\]

The three signs mean:

| Sign | Meaning |
|---|---|
| (\lambda_{\min}>0) | undifferentiated branch locally stable |
| (\lambda_{\min}=0) | physicality mode marginal |
| (\lambda_{\min}<0) | nonlinear envelopment branch required |

A level-set surface is licensed only after a nonlinear (\sigma\ne0)
solution exists. It may breathe, deform, move, merge, bifurcate, collapse, or
relax. No fixed-coordinate shell is inserted.

## 5. Mass

For a charged self-envelopment,

\[
P^A=\int_{\Sigma_\tau}T_{\rm total}^{AB}n_B\,d\Sigma,
\qquad m_{\rm phys}^2=-P_AP^A.
\]

The total stress must contain geometry, scalars, eta/triality, gauge dressing,
boundary/collar, and background response. The repository emits no physical
mass until those terms are evaluated on a stable physical orbit.

For a neutrino, the target observable is a propagation quasi-energy or phase
splitting. No primitive stationary neutrino rest enclosure is assumed.

## 6. Generation

The three generations are targeted as three stable (C_3)/Floquet family
eigenbundles. The frozen ledgers remain unchanged:

\[
\mathcal F_\ell=((0,0),(5,2),(9,3)),
\]
\[
\mathcal F_u=((0,0),(6,0),(10,1)),
\qquad
\mathcal F_d=((0,0),(6,3),(8,2)).
\]

For the regular three-cycle (C), the code constructs

\[
P_r=\frac13\sum_{n=0}^2\omega^{-rn}C^n,
\qquad \omega=e^{2\pi i/3}.
\]

Idempotence, Hermiticity, mutual orthogonality, rank one, and completeness are
verified to numerical precision below (10^{-12}). This does not prove a
unique correspondence with the frozen ((k,j,q)) slots. The exact remaining
gate is
`OPEN_ACTION_OWNED_C3_FLOQUET_TO_FROZEN_KJQ_INTERTWINER`.

## 7. Sector ontology

- A charged lepton is targeted as a timelike, gauge-dressed, finite-energy
  relative-periodic self-envelopment. The electron-like orbit is first.
- A quark is a color-open sub-envelopment inside a color-neutral parent. An
  isolated quark soliton is not the primary target.
- A neutrino is a near-null propagation-supported envelopment with three
  physical monodromy sectors.
- A measurement is a nonlinear transition of the combined incoming,
  environment, and detector system. Verbal resonance is not a probability
  theorem.

## 8. Frozen hierarchy

The frozen operator

\[
\Theta_f=\exp[-\Lambda_f/(4\pi)]
\]

is retained numerically and reclassified as a `CANDIDATE` one-cycle transfer,
attenuation, survival, or residue operator. The action origin of (1/(4\pi))
is not derived here.

## 9. Static theory as a limit

If all cycle data are stationary, the v10 cycle averages reduce identically
to the v8.9 static kinetic Gram forms, response Hessians, and common-current
kernel. Thus the prior static lens theorem is embedded as a conditional limit,
not discarded.

## 10. Claim boundary

Foundational completion is `REACHED`. Conditional architecture is
`REACHED_CONDITIONALLY`. Physical derivation and empirical replacement remain
`OPEN`. No mass, CKM matrix, PMNS matrix, or Standard Model replacement claim
is made.
