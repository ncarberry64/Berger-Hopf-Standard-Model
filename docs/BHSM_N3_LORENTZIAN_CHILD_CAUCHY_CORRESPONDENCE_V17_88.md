# BHSM N=3 Lorentzian event-to-child correspondence (v17.88)

This result is reclassified. The complete child is not an additional
coordinate in the pre-event solve, but the archive does not own the joined
event action assumed by the first version of this note.

For the order-three event chart, the terminal fields and the child fields use
the same reduced variables. Varying

\[
S_{\rm joined}=S_{\rm parent}^{\tau<0}+S_{\rm child}^{\tau>0}
\]

would give, for its interior Cauchy part,

\[
q_{\rm child}(0)=q_{\rm event}(T),\qquad
\Pi_{\rm child}(0)=\Pi_{\rm event}(T).
\]

The prior assertion `W_phys=0` is invalidated. v14.67--v14.69 own a nonzero
attachment response and incidence theorem class, while v15.1 and v15.21 state
that its event-state-dependent physical pullback is open. Continuous traces
still occur only inside a canonical relation, so the pregeometric firewall
does not transport metric or momentum as primitives.

On the canonical relation the trace and momentum jumps vanish identically.
The complete local Dirac constraint component has seven rows: six terminal
lapse/shift equations plus the Hamiltonian energy constraint,

\[
F_{\rm child}^{\rm Dirac}(z_{\rm event})=C_{\rm terminal}^{\rm replacement}(z_{\rm event}).
\]

The six multiplier equations are rows 368--373 (zero based) of the existing
376-row KKT residual; the seventh local energy constraint is owned globally by
period stationarity. Together they have local differential rank seven. They
are necessary but do not replace the
outer-layer map

\[
F_{\rm child}^{\rm outer}=P_{\rm phys}
(\Gamma_1^{\rm event}+\Gamma_1^{\rm child}
+W_{\rm phys}(z_{\rm event})\Gamma_0).
\]

The current v17.75 candidate does not even pass the Dirac component: its terminal
replacement-row norm is about `0.0536828011`. The correspondence is therefore
partly derived, but that candidate is not promoted. The next scientific step
is the state-dependent attachment incidence and physical outer-layer
Calderon/Wentzell flux map. Direct N=3 solving remains deferred until then.
