# BHSM v8.1 mode-resolved curvature incidence

> Historical-status notice: v8.2 supersedes the broad v8.1 statement that no
> family module exists. Earlier BHSM layers already contained the frozen
> three-slot module; the missing object was its master-action incidence
> attachment and then its mode-resolved classical stress.

## Result

The complete Brown--York tensor and its first nonzero equatorial normal
response can be computed exactly. The v7.1 Hopf reduction also supplies a
genuine internal scalar associated-bundle tower. These two facts do not,
however, define a physical family-incidence matrix: the localized fermion
bundle has no Hopf-associated tensor factor, the localized Dirac operator has
`D_Hopf_twist=0`, and the action selects no finite internal mode projector.

The strongest exact result is

`BHSM_FAMILY_RESOLUTION_REQUIRES_NEW_INTERNAL_FERMION_BUNDLE_EXTENSION`.

This is stronger than the v8.0 universal-trace result. It retains the
traceless Brown--York response, distinguishes internal modes from spacetime
momentum, and localizes the obstruction at the missing bundle/action map.

## Authoritative structures

The stratified action owns the Standard Model fields intrinsically on \(M_4\).
The physical fermion operator is

\[
D_{M_4}
=i\gamma^\mu\bigl(\nabla_\mu^{S_h}+\rho(A_{\rm SM})_\mu\bigr)-Y(H)
\]

on

\[
S_h\mathbin{\otimes}E_{\rm SM}
\mathbin{\otimes}\mathbb C^3_{\rm family}.
\]

The last factor is supplied field content. It is not an index, kernel, or
family multiplicity derived by the Hopf geometry. The action has
\(D_{\rm Hopf/twist}=0\), and it does not select a maximal-isotropic fermion
domain.

Independently, the exact scalar Hopf reduction supplies

\[
\mathcal H_{(J,m)}
=S^7\times_{\rho_J}V_J\longrightarrow S^4,
\qquad
\operatorname{rank}\mathcal H_{(J,m)}=2J+1,
\]

with

\[
\mathcal O_{(J,m)}
=-D_A^*D_A+\lambda_{(J,m)}(L_1,L_2)+E_{(J,m)}.
\]

Here \(A\) is the quaternionic-Hopf \(Sp(1)\) connection, \(V_J\) is the
left \(Sp(1)\) irrep, the right \(U(1)\) weight \(m\) is preserved, and the
minimal scalar reduction has \(E_{(J,m)}=0\). This is the strongest exact
internal geometric carrier already in the repository. It is a scalar tower,
not an internal fermion bundle, and no action-selected finite projector
retains one collection of its blocks as physical families.

## Carrier audit

The candidates separate as follows.

- An internal Hopf-associated Dirac operator is absent: no parent spinor
  action, internal spinor bundle, chirality operator, lower-order
  endomorphism, or domain is supplied.

- The localized tangential Dirac-Yukawa operator is exact and physical, but
  the Hopf connection does not act on it. Its seam shape response is ordinary
  spacetime stress/momentum.

- A Hopf-twisted localized Dirac operator would be a new action term; the
  historical twist and boundary-strength proxies are not admissible.

- The Berger scalar operator \(\mathcal O_{(J,m)}\) is exact as an internal
  geometric carrier but has no fermion chirality, physical Yukawa incidence,
  or finite action-selected projector.

- The scalar/topographic Hessian, constrained cap response, and scalar
  representation-resolved Dirichlet-to-Neumann map act on their declared
  scalar/geometric domains. None supplies a common internal-fermion domain or
  a nonzero localized Yukawa cross-block.

Consequently there is no eligible \(\mathscr A_f\) on a derived physical
family module. No proxy, inserted zero mode, finite `k_max`, empirical scalar
entry, or mass-selected mode ledger is used.

## Internal-mode firewall

The associated fibers \(V_J\) are internal geometric representation spaces.
By contrast, eigenmodes of tangential \(M_4\) or physical \(S^3\) operators
are ordinary spacetime momentum states. A collar response diagonal in those
spacetime eigenmodes is momentum dependence, not family structure.

A physical extension could have the form

\[
S_h\otimes E_{\rm SM}\otimes\mathcal F_{\rm BH},
\]

but the authoritative action contains no \(\mathcal F_{\rm BH}\), no bundle
map from \(\mathcal H_{(J,m)}\) or an internal spinor analogue into the
localized fermion field, and no common domain. The geometric internal module
therefore exists, so the weaker “no internal mode module” verdict would be
incorrect; what is absent is its physical fermion attachment.

## Hopf direction and triality

The internal Hopf fiber has the canonical covariant splitting

\[
TS^3=\mathcal H\oplus\mathcal V,\qquad
P_{\mathcal V}=\xi\otimes\eta,\qquad
P_{\mathcal H}=I-P_{\mathcal V}.
\]

This licenses vertical and horizontal contractions on the internal fiber. It
does not license importing the Reeb direction as a preferred direction on
the round physical seam. On the round branch the spatial Brown--York response
is isotropic and

\[
\Delta\kappa
=P_{\mathcal V}^{ab}\pi_{{\rm env},ab}
-\frac12P_{\mathcal H}^{ab}\pi_{{\rm env},ab}=0.
\]

An internal Berger anisotropy can distinguish vertical and horizontal
directions, but the action supplies no collar profile \(L_1(\rho)\),
\(L_2(\rho)\), or localized fermion action of that anisotropy. Assigning one
would introduce new data.

The Spin(8) triality projectors on
\(8_v\oplus8_s\oplus8_c\) are exact representation data. Their
identification with three physical families remains conditional on explicit
outer-automorphism carrier maps. The localized action neither makes those
three images independent same-charge fermion states nor gives them distinct
curvature responses. Triality therefore does not supply the missing
projector or family incidence.

## Full Brown--York tensor

Use the repository collar

\[
c_\epsilon(t,x,\rho)
=(t,\pi/2-\epsilon\rho,x),
\]

where \(\rho\) increases from the seam into either cap. On the round cap,

\[
h(\rho)=-N(t)^2dt^2+a(t)^2\cos^2\rho\,\gamma_{S^3}.
\]

In the pulled-back inward convention,

\[
K^a{}_b=\operatorname{diag}(0,s,s,s),
\qquad s=-{\tan\rho\over a},
\]

and the complete density-free canonical momentum is

\[
\pi_{\rm env}{}^a{}_b
=\kappa_1(K^a{}_b-K\delta^a{}_b)
=\kappa_1s\,\operatorname{diag}(-3,-2,-2,-2).
\]

The cap momenta are first pulled to the common seam with the repository
orientation factor and then averaged. The overall sign changes if the
opposite normal convention is used; the collar order, tensor decomposition,
centrality, and obstruction do not.

Because \(M_4\) is four-dimensional, the trace/traceless decomposition is

\[
\pi_{\rm env}^{ab}
={1\over4}(h_{cd}\pi_{\rm env}^{cd})h^{ab}
+\widetilde\pi_{\rm env}^{ab},
\qquad
h_{ab}\widetilde\pi_{\rm env}^{ab}=0.
\]

The \(1/3\) coefficient proposed for the full seam tensor is not traceless:
\(h_{ab}h^{ab}=4\). It would apply only to a purely spatial three-block. In
mixed form,

\[
\pi_{\rm env}=-9\kappa_1s,\qquad
\widetilde\pi_{\rm env}{}^a{}_b
={\kappa_1s\over4}\operatorname{diag}(-3,1,1,1).
\]

Thus the full response contains a nonzero spacetime traceless structure away
from the equator. It distinguishes time from space but is proportional to the
identity on the spatial block and on every internal representation.

## First nonzero equatorial response

At the equator \(K_{ab}=0\), hence
\(\pi_{\rm env}^{ab}=0\). The Riccati equation

\[
\nabla_nK_{ij}
=-R_{injn}+K_{ik}K^k{}_j,
\qquad
R_{injn}=a^{-2}h_{ij},
\]

with \(n=a^{-1}\partial_\rho\), gives the first nonzero cap-identified
one-sided response:

\[
\left.\nabla_n\pi_{\rm env}{}^a{}_b\right|_0
={\kappa_1\over a^2}\operatorname{diag}(3,2,2,2),
\]

\[
\left.\nabla_n\widetilde\pi_{\rm env}{}^a{}_b\right|_0
={\kappa_1\over4a^2}\operatorname{diag}(3,-1,-1,-1).
\]

The Brown--York traction therefore starts at collar order one after the two
inward collars are cap-identified.

The signed intrinsic geometry is even:

\[
h_{\rm spatial}(\rho)=\cos^2\rho\,h_{\rm spatial}(0),
\qquad
J(\rho)=\cos^3\rho.
\]

For a tangential Laplace principal part,

\[
\mathscr A(\rho)=\sec^2\rho\,\mathscr A(0),
\qquad
\mathscr A'(0)=0,\qquad
\mathscr A''(0)=2\mathscr A(0).
\]

Including the local action measure,

\[
J(\rho)\mathscr A(\rho)=\cos\rho\,\mathscr A(0),
\qquad
(J\mathscr A)''(0)=-\mathscr A(0).
\]

This second response is diagonal in ordinary spacetime eigenmodes. It cannot
be reinterpreted as a family matrix. The internal vertical eigenvalue
\(\lambda_{(J,m)}(L_1,L_2)\) has no action-derived collar derivative because
no \(\rho\)-dependent \(L_1,L_2,A\) profile is supplied.

The invariant identity is
\(\mathcal L_n h_{ab}=2K_{ab}\). In the repository's dimensionless collar,
\(g_{\rho\rho}=a^2\) and \(n=a^{-1}\partial_\rho\), so the precise coordinate
identity is

\[
\partial_\rho h_{ab}=2aK_{ab},
\]

not \(\partial_\rho h_{ab}=2K_{ab}\). It equates contraction of the metric
variation with the first shape variation only on a fixed domain and with the
measure variation included. No moving endpoint or \(x\)-dependent embedding
is introduced; doing so would silently enlarge the variational domain
excluded by the v6.24 audit.

## Mode stress and centrality

For a valid common carrier one would define

\[
\mathcal T_{ab}^{ij}
=\left\langle u_i,{\delta\mathscr A\over\delta h^{ab}}u_j\right\rangle,
\qquad
\mathscr R_{ij}
=\int_{M_4}\pi_{\rm env}^{ab}\mathcal T_{ab}^{ij}\,d\mu_h.
\]

The exact associated scalar calculation instead proves a centrality
statement. For fixed \((J,m)\), the round collar response commutes with the
left \(Sp(1)\) action on the irreducible \(V_J\). Hence

\[
\operatorname{End}_{Sp(1)}(V_J)
=\mathbb C\,I_{V_J}
\]

and Schur's lemma makes the response central within each irreducible block.
Different \((J,m)\) blocks can carry different scalar eigenvalues, but the
action selects no finite collection of such blocks, no multiplicity-space
basis, and no theorem identifying them as fermions with identical Standard
Model quantum numbers. A triality transport also remains central because the
Brown--York/collar action has no triality-specific coupling.

The physical localized mode-stress matrix is therefore undefined, not
\(I_3\). Reporting \(I_3\) here would merely repeat the supplied family factor
and the v8.0 trace projection.

## Family space, masses, and mixing

The exact geometric tower has ranks \(2J+1\) for
\(J=0,\tfrac12,1,\ldots\). It does not select \(J=1\). Choosing that block
because it has rank three would be measured-family matching, and it would
still be a scalar \(Sp(1)\) module rather than a localized chiral-fermion
family theorem.

There is no action-derived finite family projector, isolated physical
spectral cluster, selected chirality, common gap theorem, or fourth-mode
exclusion. Consequently:

- physical family dimension: undefined;
- charged-lepton, up, and down response matrices: undefined;
- ranks and singular values: undefined;
- action normalization: undefined;
- mass ratios: undefined;
- CKM matrix, angles, phase, and Jarlskog invariant: undefined.

No Yukawa matrix input is removed. The v7.2 observable transport has no new
dimensionless singular values or left singular vectors to run at
\(\mu_\star=\ell_\star^{-1}\).

## Freeze and comparison

Before examining any historical mass or mixing record, v8.1 freezes:

- no eligible physical carrier;
- the unselected scalar tower \(\{\mathcal H_{(J,m)}\}\);
- left \(Sp(1)\) centrality on each \(V_J\);
- distinct scalar and localized-fermion domains;
- Brown--York collar order one and intrinsic even-operator order two;
- the complete trace and traceless Brown--York tensors;
- no physical response matrix, normalization, family dimension, ratios, or
  mixing prediction;
- the `overline_MS` v7.2 reference convention;
- the exact action/bundle obstruction and its falsifier.

The deterministic artifact records the freeze hash. Post-freeze historical
mode screens and external mass/mixing references are classified
`UNRELATED_OBSERVABLE`, because there is no v8.1 physical numerical
prediction to compare or retune.

The obstruction is lifted only if the existing action can exhibit a nonzero
equivariant map from an action-selected internal Hopf spinor or associated
module into localized fermion Yukawa incidence, together with its finite
projector and domain. The repository audit finds no such object.

## Completion result

RB-15 remains `BLOCKED_EXACT_OBJECT_PROVED`. RB-16 remains
`DOWNSTREAM_BLOCKED`, and no release package is generated. No fit,
measured-mode selection, spacetime-harmonic relabeling, assumed
three-family module, arbitrary matrix, arbitrary collar coefficient,
inserted zero mode, mediator, second scale, hidden calibration, or
post-comparison retuning is used.

Final exact verdict:

`BHSM_FAMILY_RESOLUTION_REQUIRES_NEW_INTERNAL_FERMION_BUNDLE_EXTENSION`.
