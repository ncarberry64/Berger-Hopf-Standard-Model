# BHSM v7.3 distinct action-derived prediction campaign

## Result

All six independent construction routes have been evaluated against the
authoritative stratified action and the v7.2 observable transport. No proxy
operator, empirical scalar entry, inserted zero mode, assumed gap, or
mass-selected projector is used.

The campaign proves the stronger singular obstruction

`NONUNIVERSAL_BHSM_TO_LOCALIZED_PHYSICAL_SECTOR_ACTION_COUPLING`.

The corresponding repository verdict is

`BHSM_DISTINCT_PREDICTION_REQUIRES_NEW_BULK_BOUNDARY_COUPLING_NOT_PRESENT_IN_ACTION`.

This is not the v7.2 statement that no candidate happened to survive. It is
an action-incidence theorem identifying why every legitimate route fails.

## Action-incidence theorem

The authoritative action is

\[
S_{\rm BHSM}^{\rm strat}
=S_8+\sum_{\epsilon=\pm}(S_{5,\epsilon}+S_{{\rm GHY},\epsilon})
+S_{4,\rm localized}+S_{\rm compatibility}.
\]

The distinctive variables are the Hopf connection \(\omega\), fiber
metric/radion \(a_F\), Berger spectral data, cap scalar \(\sigma\), fixed-\(h\)
\(D_0\) KKT mode, and cap/seam matcher data. The physical fields
\(A_{\rm SM},\Psi,H\) are intrinsic \(M_4\) fields. They are not extended into
\(M_5\) or \(M_8\).

The compatibility action contains metric trace/pushforward constraints and
\(\sigma_5=P_0\sigma_8\). It contains no term that maps \(\omega\), \(a_F\),
Berger eigenmodes, the \(D_0\) amplitude, or a derived generation projector
into the localized Dirac, Higgs, or gauge operators. At fixed seam metric,

\[
\frac{\delta^2S}{\delta\omega\,\delta\Psi}
=\frac{\delta^2S}{\delta\omega\,\delta A_{\rm SM}}
=\frac{\delta^2S}{\delta a_F\,\delta\Psi}
=\frac{\delta^2S}{\delta a_F\,\delta H}
=\frac{\delta^2S}{\delta\sigma\,\delta H}=0.
\]

The seam metric \(h\) is an existing universal gravitational coupling. Its
background and moduli are not uniquely selected, and it does not insert a
Hopf charge, Berger eigenvalue, \(D_0\) amplitude, or family projector into a
physical operator. It therefore cannot replace the missing non-universal
cross block.

Consequently, eliminating internal variables cannot produce a distinctive
coefficient in a v7.2 observable. A nonzero map

\[
C_{\rm BHSM\to phys}
[\omega,a_F,\sigma_\perp,KKT;
 A_{\rm SM},\Psi,H,\Pi_{\rm generation}]
\]

would require a new gauge-covariant action term and selected domain.

## Route A: exact twisted Dirac operator

The fermion operator actually supplied by the action acts on

\[
S_h\otimes E_{\rm SM}\otimes\mathbb C^3_{\rm family}
\quad\hbox{over intrinsic }M_4
\]

and is

\[
D_{M_4}
=i\gamma^\mu
\left(\nabla_\mu^{S_h}\otimes1
+1\otimes\rho(A_{\rm SM})_\mu\right)-Y(H).
\]

In the requested decomposition,

\[
D_{\rm spin}=i\gamma^\mu\nabla_\mu^{S_h},\qquad
D_{\rm gauge}=i\gamma^\mu\rho(A_{\rm SM})_\mu,
\]

while

\[
D_{\rm Hopf/twist}=0,\qquad D_{\rm boundary}=0
\]

as action summands. The Yukawa background is the displayed zero-order term.
The canonical Sp(1) connection transports retained \(M_8\to M_5\) associated
modes; it does not act on the localized \(\Psi\) bundle.

The principal symbol is

\[
\sigma_1(D_{M_4})(x,\xi)
=i\gamma^\mu\xi_\mu\otimes
1_{E_{\rm SM}\otimes\mathbb C^3}.
\]

Gauge covariance follows from the Standard Model covariant derivative.
Chirality is the intrinsic four-dimensional grading fixed by the supplied
representation ledger. The Green identity is

\[
\langle D\Psi,\Phi\rangle-\langle\Psi,D\Phi\rangle
=\int_{\partial M_4}\langle\Psi,i\gamma\!\cdot\!n\,\Phi\rangle.
\]

Self-adjointness holds after choosing a maximal-isotropic subspace of this
boundary pairing. The current action does not choose its graph unitary, bag
angle, APS cut, or junction fermion term.

Thus the exact action-supported object is the Standard Model EFT
Dirac-Yukawa operator on the seam metric, not a BHSM twisted Dirac operator.

## Kernel and family index

The Hopf bundle has \(c_2=1\), but it does not act on \(\Psi\). The index of
the available chiral operator depends on the seam metric, Standard Model
connection and topological sector, representation bundle, and selected
boundary domain. Those data are not jointly selected by the action.

The factor \(\mathbb C^3_{\rm family}\) is already in the declared field
bundle. Counting its three components repeats an input; it does not compute
an index. Triality projectors are exact representation projectors conditional
on the supplied carrier, not a protected kernel theorem.

No fixed chiral kernel dimension, exactly-three-family theorem, or
fourth-generation exclusion follows.

## Fermionic gap

The requested quantity

\[
\widehat\Delta_D
=\inf\operatorname{spec}
\left(D_{\rm BHSM}^\dagger D_{\rm BHSM}
\big|_{\ker D_{\rm BHSM}^{\perp}}\right)
\]

is undefined because \(D_{\rm BHSM}\) is absent. The available \(M_4\) gap is
conditional on \(h,A_{\rm SM},H,Y_f\), and a selected self-adjoint domain.
Moreover, the allowed theory-input space contains arbitrarily small Yukawa
singular values and no uniform geometric/domain gap. Hence the infimum over
allowed inputs is zero. No positive universal bound can be transported to
\(\sqrt{\widehat\Delta_D}/\ell_\star\).

## Route B: scalar/topographic Hessian

At fixed seam metric the full retained scalar Hessian has the block form

\[
\mathcal H_{\rm scalar}
=\mathcal H_{\sigma,\rm strat}
\oplus\mathcal H_{H,M_4},
\qquad
\frac{\delta^2S}{\delta\sigma\,\delta H}=0.
\]

\(\mathcal H_{\sigma,\rm strat}\) is the regular-pole, fixed-\(h\),
Dirichlet/KKT cap operator. \(\mathcal H_{H,M_4}\) is the covariant Hessian of

\[
|DH|^2-m_H^2H^\dagger H-\lambda_H(H^\dagger H)^2.
\]

The cap coefficients and \(m_H^2,\lambda_H=\lambda_5\) are independent
inputs. Constraint-reduced metric Schur blocks are background- and
coefficient-dependent; they do not identify \(\sigma\) with \(H\) or derive
an orthogonal physical scalar projector.

Therefore \(\mathcal H_{\rm Higgs}^{\perp}\) and
\(\widehat\Delta_\perp\) are not action-derived physical objects. No
extra-scalar, resonance, or fifth-force exclusion follows.

## Route C: internal geometry selection

Varying \(a_F\) in the pushforward of \(S_8\) produces a scalar-tensor radion
equation depending on the independent bulk coefficients and background
fields. The stored \(S_5\) action is an independent target-stratum Wilson
action, not equal to \(\pi_!S_8\).

The Berger shape \(a\) is classified as a fixed independent theory input.
No reduction/source law makes it an active coordinate of
\(S_{\rm BHSM}^{\rm strat}\), so

\[
\frac{\partial S_{\rm BHSM}^{\rm strat}}{\partial a}
\]

is not defined. Cap and collar moduli likewise have no simultaneous
input-independent stationarity system. No measured \(\alpha\) is used.

The action selects neither a unique geometry nor a discrete branch from
which a physical spectral ratio could follow.

## Route D: mode and generation selection

The representation and triality projectors are finite typed data. Sector
projectors are independent inputs. Generation/mode projectors are
conditional retained subspaces, and the historical mode ledger is
screen-only.

The present theory contains three supplied family components. This is a
physical field-content statement but an input tautology. It is not an
action-derived generation-count prediction.

## Route E: input-cancelling sum rules

The exhaustive cancellation audit gives:

- charged-fermion masses and CKM invariants retain independent \(Y_f\);
- gauge ratios retain independent \(g_1,g_2,g_3\);
- scalar/electroweak ratios retain \(m_H^2,\lambda_5\);
- scalar/fermion gap ratios are undefined because both required gaps are
  absent;
- anomaly and representation traces are structural identities conditional
  on the supplied representation ledger;
- \(c_2=1\) and orientation signs are exact structural invariants but have no
  localized physical transport term.

Eliminating the one common \(\ell_\star\) leaves no input-free, distinct,
physical BHSM observable relation.

## Route F: fixed-\(h\) critical sector

The exact \(D_0\) Lyapunov--Schmidt result remains

\[
V_E(\phi)=V_0+\frac{g_4}{4!}\phi^4+O(\phi^6),
\]

with

\[
V_E'(0)=V_E''(0)=V_E'''(0)=0,
\]

and

\[
g_4
=5.84444718718846\,G_5/Z_5^2
+81.5773688846122/\kappa_1.
\]

The first same-family Einstein interaction is therefore quartic. This is
action-derived and distinctively BHSM, but it is not physical: \(\phi\) is a
fixed-\(h\) cap/KKT amplitude, not the Higgs or a particle-normalized
\(M_4\) field. It has no v7.2 observable transport, and \(G_5\) remains an
independent input.

## Proxy retirement

The following are rejected as theorem inputs:

- manually inserted zero modes and `zero_mode_count`;
- `complement_floor`;
- arbitrary twist and boundary strengths;
- finite `k_max` convergence;
- the empirical 125.10 GeV scalar entry;
- an assumed \(4\pi^2v\) threshold;
- mass-selected or hand-classified modes.

## Candidate and release status

No candidate satisfies all of distinctiveness, action provenance, physical
transport, and falsifiability. Therefore no formula, uncertainty, comparison
target, or kill threshold is frozen as a new official prediction.

RB-15 remains `BLOCKED_EXACT_OBJECT_PROVED`, now with the exact resolution
`NONUNIVERSAL_BHSM_TO_LOCALIZED_PHYSICAL_SECTOR_ACTION_COUPLING`.

RB-16 remains `DOWNSTREAM_BLOCKED`. No release package is generated and no
public completion claim is made.

## Reproduction

```text
python -m bhsm.interface distinct-prediction-status --format json
python -m bhsm.interface distinct-prediction-status --format markdown
python -m bhsm.interface.master_action.distinct_prediction --materialize
```
