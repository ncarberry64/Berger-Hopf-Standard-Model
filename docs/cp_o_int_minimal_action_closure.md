# CP Holonomy Minimal Action Decision

Holonomy status: `ARTIFACT_BACKED`.

Standalone CP `O_int` production target: `RETIRED_TARGET`.

`artifacts/CP_no_fit_holonomy_output_v1.json` records the CP boundary holonomy
as closed, the Z6 phase as `delta_BH = pi/3`, and the CKM/PMNS phase seeds as
active. Under the author ontology, CP is this boundary holonomy constraint and
its attachment to admissible structures. It is not required to be a standalone
interaction vertex.

The historical symbolic density

```text
G_raw exp(i delta_BH) O_int + h.c.
```

remains a representation-only candidate. Retiring it as a core target does not
make it production eligible and does not establish FeynRules, UFO, or MadGraph
readiness.
