# CP O_int Minimal Action Decision

Status: `OPEN_MISSING_ACTION_SOURCE`.

Established locally: the CP phase, CKM/PMNS phase attachment, source-traced
symbolic density, and callable candidate evaluator.

The remaining object is an action-derived `O_int` source that fixes the
coupling normalization, integration measure, variation, and production rule.
The existing expression

```text
G_raw exp(i delta_BH) O_int + h.c.
```

does not supply that derivation. No production or runtime gate changes.
