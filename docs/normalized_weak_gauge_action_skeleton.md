# Normalized Weak Gauge Action Skeleton

The located conditional skeleton is

```text
S_gauge = k[Tr(F_cyc^2) + Tr(F_orient^2) + eta_Y F_Y^2].
```

Its status is `CONDITIONAL_NORMALIZED_WEAK_GAUGE_ACTION_SKELETON`. The overall coefficient `k` is not fixed or identified with a derived `g2_BH`; therefore the skeleton does not derive the coupling.
