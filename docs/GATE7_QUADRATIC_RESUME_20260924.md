# Gate 7 quadratic compiler: Git stopping point

The user requested a clean Git update at 10% remaining usage. This checkpoint
freezes completed work and records the unfinished signed screen explicitly.
The scientific authority remains `cef38b6b`; Layer A and B remain inherited.
Do not rerun frozen endpoints, midpoints, cells, graphs or remainders.

## Completed

- Sparse degree-two compiler with canonical shared physical monomials,
  disk-backed coefficient circuits, bounded frontier and 512-bit Arb.
- Raw dominant midpoint descriptor: OLD 10.01247127802717;
  NEW <= 5.004286125; approximately 2.000779 times reduction; 22500 monomials.
- Scalar tail <= 4.998578356; 99.9981552% is q-based implicit-correction loss.
- Two independent descriptor forward checkpoints are byte-identical.
- All 405 selected target models and 3607128 frozen ancestors compiled.
  This includes all 99 second-incidence components and common-border
  normalization products. No full-interval compilation was performed.
- 13 focused tests pass. Compact retained artifacts were materialized twice
  byte-identically in `artifacts/flagship_integration/gate7_quadratic_models_20260924/`.

The owned selected-target worker was stopped after its completed forward
checkpoint and SQLite commit, before more output support. No owned scientific
or numerical worker is intended to continue in the background.

## Resume without forward replay

Run from the repository with `C:\Python314\python.exe`. The local working
directory is `tmp/gate7_quadratic_20260924/`; retain it. In particular, retain
`descriptor_repeat/quadratics.sqlite` and `targets_first/quadratics.sqlite`.
Their companion `compiled_roots.json` files and the supported descriptor
report already exist. These large numerical caches remain local, outside Git.
Compact forward checkpoints are archived in Git, but require the caches or a
new numerical replay of the frozen DAG to recover individual Q circuits.

```powershell
C:\Python314\python.exe scripts/reuse_n12_gate7_descriptor_support.py --descriptor-checkpoint tmp/gate7_quadratic_20260924/descriptor_repeat/compiled_roots.json --target-checkpoint tmp/gate7_quadratic_20260924/targets_first/compiled_roots.json --descriptor-report tmp/gate7_quadratic_20260924/descriptor_supported.json --out tmp/gate7_quadratic_20260924/targets_reused.json
C:\Python314\python.exe scripts/screen_n12_gate7_quadratic_targets.py --compiled tmp/gate7_quadratic_20260924/targets_reused.json --evidence-root C:\Users\carbe\OneDrive\Documents\CODEX\BHSM-ae32-crossing-correction --out tmp/gate7_quadratic_20260924/screen_first.json
```

The reuse check compares numerical coefficient circuits and shared-node
aliases; it does not re-prove branch identity. If it refuses reuse, support
the saved selected checkpoint with the existing batched compiler arithmetic;
do not silently bypass the identity check or rerun a scientific producer.

The pending screen combines the three J assignments, full second incidence,
and common-border normalization, then subtracts frozen LL/LT on the same keys
and supports the result after frozen center causal transport to node 370.
Report cancellation, residual source classes and normalized selected support.
This is not a full interval remainder: omitted terms and causal-map error are
outside this prototype. Repeat the numerical screen for byte identity and
retain its result in a new artifact directory, preserving this checkpoint.

## Boundary and first missing object

No new kappa values, contraction, decay, instability, or physical failure are
established. No physical budget debit. No intervals 14–18.
`Gate7_closed = False`. Frozen diagnostics remain
`kappa_L <= 2.98824e10`, `kappa_T <= 5.77960e12`.

The first missing representation object is the shared mixed bordered-resolvent
correction jet `((I-E(theta))^-1-I) R rhs_uv(theta,u,v)`, with
`E(theta)=I-R B(theta)`, preserving signed directions and common-border row
coupling. Frozen midpoint example leaves 17172972 and 20485749 contain only
`w_i*q/(1-q)*norm_w(R rhs)`. Higher polynomial order cannot restore the
dependence absent from those scalar leaves. The classification remains
`CASE_1 = NUMERICAL_COMPILER_CORRELATION_LOSS`.

The raw prototype does not justify expanding to all interval-13 roots.
Signed booking cancellation is **not yet measured** at this stopping point.

## Workspace preservation

Pre-existing source, historical handoff and mission-ledger material is kept
in a separate preservation commit. Its inclusion is not scientific
recertification. Historical hidden work directories are retained on disk and
explicitly ignored as caches. No frozen science artifact was deleted.
The pre-existing pytest process (observed PID 13600) was not stopped by this
task; it may belong to another task. Its outputs are not this checkpoint's
validation evidence.
