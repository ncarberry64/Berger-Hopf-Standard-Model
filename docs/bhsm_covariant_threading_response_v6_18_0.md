# BHSM v6.18.0 covariant threading response

Primary result:
`BHSM_INDUCED_THREADING_ACTION_REPRODUCES_CONSTRAINT_RESPONSE`.

The corrected resting principle fixes only the source-free homogeneous
integration constant, \(C_\Sigma=0\). It does not set
\(\partial_qS_\Sigma=0\). The v6.17 hard dynamical extension remains
rejected.

## Induced action

The v6.16 P1 Hessian and v6.17 momentum constraint are integrable into the
symmetric-background constrained-collar functional
\[
\Gamma_\Sigma^{\rm eff}
=\frac12\langle S,K_\Sigma S\rangle
+\left\langle S,K_\Sigma\tau\frac{\pi\chi_1}{16}q\right\rangle
+\Gamma_0[q]+\cdots ,
\]
with
\[
\langle f,g\rangle_\Sigma
=\int_{\mathrm{B1}}\sqrt{|\gamma|}\,\bar f g .
\]
This is an induced Hamilton--Jacobi/on-shell response functional from the
existing P1+GHY+B1+matcher+scalar theory, not a new fundamental boundary
term. The common Z2 interface is counted once; the two reflected caps share
the inherited overall P1 normalization.

On round spatial S3, the v6.16 Bochner calculation gives
\[
K_{\Sigma,\ell}=-\frac{2\ell(\ell+2)}{a^4}.
\]
The homogeneous mode is the integration constant. Every \(\ell\ge1\) mode
is invertible, and variation yields
\[
\Pi_\perp S_\Sigma
=-\tau\frac{\pi\chi_1}{16}\Pi_\perp q .
\]
Thus the action reproduces the exact v6.17 response on both sheets and is
independent of scalar sign.

The general covariant Dirichlet-to-Neumann operator is nonlocal. The parent
density is fourth order, while the constant-curvature Bochner identity
reduces its integrated spatial form to a second-order curvature-weighted
operator. The displayed Lorentzian spatial eigenvalues are negative, but
neither the Euclidean Hessian nor canonical Hamiltonian sign is certified
without the full constrained continuation. This is not a ghost theorem and
no stiffness is introduced.

## No threshold

The effective source is
\[
J_\Sigma[q]
=K_\Sigma\tau\frac{\pi\chi_1}{16}\Pi_\perp q .
\]
It vanishes whenever \(D_\mu q=0\). Therefore rest--response--rest follows
at the level of stationary constraint solutions without an energy threshold:
`BHSM_FOLD_SOURCE_VANISHING_REPLACES_EXPLICIT_ENERGY_THRESHOLD`.

The fold discriminant, fold Hessian eigenvalue, scalar-wall invariants,
normal stress, and intrinsic curvature generally remain nonzero in static
fold backgrounds and do not distinguish transition from rest. \(Dq^2\)
does distinguish them but is not positive energy in Lorentz signature. A
true energy density needs a derived timelike observer. No \(E_{\rm crit}\),
observer, scale, fitted switch, or Heaviside function is added.

The initial and final source-free states both select \(C_\Sigma=0\). The
intermediate response is elliptic/algebraic on each M4 slice in the derived
approximation. Retarded relaxation, dissipation, and cosmological dilution
are not derived. A white-hole-like reading remains a conditional BHSM
identification, and no time is assigned to the common core.

## Domain and kinetic status

The dynamic response equation replaces the rejected hard zero condition.
It is gauge invariant, Z2 compatible, pole regular, independent of the Ward
identity, and does not duplicate the metric junction. The dynamic domain is
nonempty and the trace count closes from one to zero after \(C_\Sigma=0\).

The threading block is now invertible on nonconstant harmonics, but the
complete finite-q coupled ADM operator for
\(Y=(A,B,\psi,E,\delta\sigma,\zeta)\), its absolute normalization, and its
Green operator remain unconstructed. Hence
\(K_{\rm shift+endpoint}^{\rm red}\) and \(k_q^E\) remain undefined.
\(K_{\rm scalar}\ge2\) and
\(K_{\rm Weyl}=1.220620174933802\ldots\) are preserved.
The kinetic result remains
`BHSM_FOLD_KINETIC_REMAINS_UNRESOLVED_BY_EXACT_OPERATOR_OBSTRUCTION`.

No measured input, fitted threshold, new primitive, new scale, new
fundamental action, boundary tension, `tau_J`, neutral work, physical bulk
Dirac law, frozen-prediction change, or official prediction-logic change is
introduced.
