# Theorem input sandbox

`data/theorem_inputs/theorem_input_template.json` is reserved for future
author-supplied theorem candidates. A valid author input can produce only
`DERIVED_CONDITIONAL_FROM_AUTHOR_AXIOM`; it cannot silently promote production
readiness or alter frozen predictions.
