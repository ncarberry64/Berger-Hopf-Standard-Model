# BHSM v11.0 Core-Stratum Action

The regular domain is `0<upsilon<=1`, equivalently
`0<=q_D<+infinity`. Thus the core endpoint `upsilon=0` lies at infinite Haar
field distance. It is not a finite regular vacuum of the canonical scalar
equations.

Variation of the canonical support term gives the boundary form

`-int_Sigma sqrt|h| (n^A nabla_A q_D) delta q_D`

and momentum `Pi_D=sqrt|h| n^A nabla_A q_D`. A core functional would have to
satisfy

`Pi_D,regular + delta S_core-total/delta q_D = 0`,

together with metric, stress, gauge, topological, and symplectic flux balance.
These equations determine the form of matching, not the missing core response.

Conservation alone does not choose a transfer law. Terminal Dirichlet and
reflecting Neumann cutoff ensembles both have zero regular symplectic flux and
are conservative, yet neither implements absorption or emission. A reversible
transfer model requires a core phase space and a self-adjoint or symplectic
transfer operator, plus an asymptotic prescription at `q_D=+infinity`.

Verdict:
`BHSM_CORE_JUNCTION_REMAINS_OPEN_AT_INFINITE_HAAR_DISTANCE_WITHOUT_A_CORE_RESPONSE_OPERATOR`.

Exact next object:
`CORE_BOUNDARY_PHASE_SPACE_AND_SELF_ADJOINT_TRANSFER_OPERATOR_AT_QD_INFINITY`.
