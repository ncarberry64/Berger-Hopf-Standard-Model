# Uniform local integration — 12 September 2026

Uniform physical first derivatives and local Hermite–Simpson integration blocks
are now available for a selected six-interval neighborhood of the retained
action. **Their current enclosures do not establish contraction or close Gate 7.**
They are not Standard Model observable predictions or a physical quotient proof.

## Completed numerical chain

Independent invocations reproduce byte-identical evidence for the complete
endpoint derivatives at endpoints 13–19 and the complete actual-midpoint
derivatives at intervals 13–18. Each matrix includes all 99 weighted augmented
basis directions, including the descriptor. The midpoint inputs are actual HS
outer domains built from the paired uniform endpoint fields.

The original physical variation retains both bordered variation solves,
configuration-source differentiation, third- and fourth-action descriptor terms,
and normalization differentiation. The same proved coupled inverse bound is
used throughout. No physical trial radius, measured-data input, branch selection
or frozen prediction is changed.

The endpoint and midpoint derivatives are combined with the existing fixed
frames and frozen inverse to produce independently paired local `C`, `DL` and
`DR` blocks for intervals 13–18. This replaces center-only derivatives with
uniform derivatives on the specified domains in that local calculation.

## Width remains the limiting issue

For interval 13, the original uniform `DL` and `DR` component radii reach about
`1.0131e9` and `1.1486e9`. Such broad boxes cannot establish the needed
contraction. This is a limitation of the enclosure, not evidence of physical
instability. The frozen `C` block is sharply enclosed, with maximum absolute
entry below `2.885347` and interval radius below `4.208756e-153`.

The normalization refinement differentiates the same coupled identities
`psi^T psi=1` and `psi^T hard=0` after canceling the nonzero common border scale.
It retains the original complete action contractions and both actual bounded
variations. An initial nine-column diagnostic reduced maximum derivative radii
by about 65 times at endpoint 13 and 44 times at midpoint 13. The complete
refined endpoint matrices at 13–19, midpoint matrices at 13–18, and all six
refined local integrations now also reproduce independently. The full refined
matrix radii at endpoint/midpoint 13 are below `76989.066` and `52103.025`.

Refined interval-13 `DL` and `DR` radii are below `6.271552e7` and `7.372878e7`,
about 16 times narrower than the original local boxes. They remain too broad
to certify closure. Both versions are preserved in the evidence index.

The next numerical improvement is to
bound the variation residual around a fresh verified point variation rather
than a zero center, retaining signed preconditioned action contractions. That
improvement has not yet been implemented or certified in this report.

## Evidence and execution

The accompanying
`artifacts/flagship_integration/BHSM_N12_GATE7_UNIFORM_LOCAL_INTEGRATION_20260912.json`
records paired artifact digests, coverage and matrix dimensions/radii. Complete
arrays remain in the execution workspace's corresponding uniform and refined
work directories. Every accepted entry requires a separate recomputation with
byte-identical output.

Repeated input hashing is reduced on Windows by retaining native read handles
that deny writes and replacement until each invocation ends. The original raw
and normalized hash functions are preserved, output directories are excluded,
and other platforms fall back to uncached hashing. One reproduced endpoint-13
run reused 83,947 of 88,942 hash requests while holding 4,036 input files
immutable. Each independent invocation starts with fresh handles and hashes.

Validation: **31 focused tests pass**, including derivative identities against a
smooth normalized/orthogonal family, full-basis and domain routing, complete
contraction capture, restoration after failure, existing HS block algebra,
and actual Windows write/replacement exclusion.

The main curvature campaign reached endpoint 16 and continues around the
immediate six-worker proof batches. No scheduled task is created. The full
370-interval uniform causal bound, useful remainder estimates, physical quotient,
observable closure, Museum observable data and manuscript physical dependencies
remain open. `Gate7_closed=false` and `FULL_BHSM_COMPLETE=false`.
