# X_ch Interaction Operator Resolution Attempt

Machine-readable artifact:

```text
artifacts/BHSM_x_ch_interaction_operator_resolution_attempt_v1_0.json
```

## Result

For CKM/PMNS charged-current target vertices, the charged weak current mediator
may be treated as the standard target-current `W_mu` interface convention.
This is a partial resolution only and does not make the vertices
FeynRules-ready.

For the separate `charged_boundary_response_matrix`, `X_ch` remains unresolved.
The missing theorem is the derivation of the separate charged
boundary-response interaction operator from the BHSM boundary-to-4D projection.

