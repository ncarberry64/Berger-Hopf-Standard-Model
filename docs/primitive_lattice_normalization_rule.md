# Primitive Lattice Normalization Rule

Status: `OPEN_MISSING_ACTION_PRIMITIVE_LATTICE_NORMALIZATION_RULE`. Candidate status: `CONDITIONAL_PRIMITIVE_LATTICE_NORMALIZATION_RULE`.

The primitive lattice normalization rule is not action-derived unless the BHSM action is shown to quotient common incidence rescalings.
