# BHSM v10.1 Topological Buoyancy Audit

Topological Buoyancy is an author-required `STRUCTURAL_POSTULATE`: gravity is
to arise from covariant radial/normal equilibrium between local geometric
density and the closed cosmic background. No force term is inserted.

## Geometry and variation

The repository has three distinct radial objects:

- `rho`, the dimensionless M5 collar normal with physical element
  `ds=a(t) d rho`;
- `a_F`, the Hopf-fiber radius/radion, whose variation makes the pushed-forward
  M8 action scalar--tensor gravity;
- `R`, the v10 normalized texture-scale proxy, which is not a parent
  coordinate.

No action theorem selects one combined buoyancy coordinate. The candidate
covariant equation remains `B[Phi]=delta S_BHSM/delta r=0`, assembled from
normal stress, Brown--York/GHY variation, collar Jacobian, radion equation,
ADM constraints, and global constraints.

For the controlled v10 proxy,

\[
B_R=\frac{dV}{dR}=5\kappa_1A_2R^4-\frac{A_8}{R^2},
\]

so

\[
R_0=\left(\frac{A_8}{5\kappa_1A_2}\right)^{1/6},\qquad
V''(R_0)=30\kappa_1A_2R_0^3>0.
\]

This is `DERIVED_CONDITIONAL`, not a physical gravity solution. The U-shaped
potential does not determine the author's compact-energy/depth ordering.
Covariance, weak-field/Newtonian recovery, equivalence-principle behavior,
and the global-local solution map remain open. Eta contributes stress but is
not introduced as an independent gravity mediator.

Verdict:
`BHSM_TOPOLOGICAL_BUOYANCY_NOT_GENERATED_BY_CURRENT_PARENT_ACTION`.

Exact missing object: `COVARIANT_RADIAL_BUOYANCY_FUNCTIONAL`.
