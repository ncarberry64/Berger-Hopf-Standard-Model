# BHSM v11.3 Reciprocal Core–Surface Attachment

The recovered action-owned slot is the v7.1 compatibility matcher

`S_85 = int_M5 dmu5 <Lambda85, g5-Q_H(G8)>`.

With `I_C=Q_H(G8)` and `I_W=g5`, the adopted reciprocal principle gives

`S_attach = int_M5 dmu5 <Lambda85, upsilon^(-1/2) I_W-upsilon^(1/2) I_C>`.

This ordering preserves the inherited sign. Multiplier variation yields `I_W=upsilon I_C`; at `upsilon=1` it is exactly the inherited matcher. The multiplier normalization remains redundant, so no field, mediator, coefficient, or physical parameter is added.

