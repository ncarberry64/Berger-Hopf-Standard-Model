# Admissible Neutral Positivity Proof

On the response cone,

```text
q(x)=x^T K_nu x
    =3*x2^2+(5/3)*x3^2+(2/3)*x1*x2+(1/3)*x2*x3 >= 0.
```

Every term is nonnegative for `x_i>=0`, so the proof is exact and uses no
threshold clipping. The normalized-cone minimum is zero at `(1,0,0)`.

Admissible neutral positivity is proven only on the stated admissible domain and is not a raw-kernel PSD claim.

The status is
`CONDITIONAL_MEASUREMENT_SUPPORTED_NEUTRAL_POSITIVITY_CANDIDATE` because the
response-cone identification follows from author ontology but is not yet
uniquely generated by the complete neutral action.

Partial variational boundary/collar support is now recorded, but it does not
promote the cone to complete-action derived.
