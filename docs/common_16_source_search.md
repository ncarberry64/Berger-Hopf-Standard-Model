# Common-16 Source Search

The v1.8 source search locates the charged incidence kernel, bridge source,
charged-stiffness branch audit, historical CKM exponent audit, same-scale
transport artifact, and charged beta table. All sources are local and the
search runs offline.

The search finds the values needed to test a common-16 identity, but it also
finds that the historical CKM `1/16` candidate was selected after residual
comparison. That residual is recorded as provenance and is not used as a
theorem input.

Status: `COMMON_16_SOURCE_SET_LOCATED`.
