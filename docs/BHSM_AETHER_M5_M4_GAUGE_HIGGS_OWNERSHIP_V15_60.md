# BHSM v15.60 — gauge pushforward and Higgs ownership

The round quotient base has

\[
 ds_5^2=-dt^2+R_F^2d\chi^2+r(\chi)^2d\Omega_3^2,
 \qquad r(\chi)=R_F\sin\chi\cos\chi,
 \quad 0\leq\chi\leq\frac\pi4.
\]

For (A_\mu(\chi,x)=u(\chi)a_\mu(x)), electric and magnetic curvatures have
radial weights

\[
 I_E=\int d\chi,R_Fr u^2,
 \qquad I_B=\int d\chi,R_F\frac{u^2}{r}.
\]

A Lorentz-invariant boundary Maxwell term requires
(I_E=r_b^2I_B), where (r_b=R_F/2). For every regular profile
(u_p=\sin^p(2\chi)),

\[
 \frac{I_E}{r_b^2I_B}=\frac{2p}{2p+1}<1.
\]

Equality occurs only as (p\to\infty), a distribution supported at the
boundary. Therefore no smooth bulk connection mode produces the intrinsic
local (M_4) Yang--Mills action. A boundary/Wentzell gauge term is required,
and its dimensionless normalization is not fixed by the bulk Einstein term.

The active parent bosonic tangent bundle contains a metric tensor, a real
singlet response, a color (3\oplus\bar3) eta tangent, and an adjoint weak
connection one-form. It contains no

\[
 (1,2)_{1/2}
\]

scalar section. A principal-fiber coordinate is gauge redundancy and cannot
be relabelled as that section. Consequently the v15.53 Higgs doublet is
intrinsic foundational (M_4) data, not a fluctuation derived from the
current parent field bundle.

The smallest required microscopic completion must generate, in one
covariant reduction, the boundary gauge normalization, Higgs potential, and
four Yukawa operators. Assigning them unit or measured values is not a
derivation.
