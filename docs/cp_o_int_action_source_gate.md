# CP O_int action/source gate

The source ledger already writes `G_raw exp(i delta_BH) O_int + h.c.` and marks
it blocked by the missing interaction attachment. It supplies no integration
measure, locality theorem, variation, or equations-of-motion effect. The
theorem status is therefore `OPEN_MISSING_ACTION_SOURCE`.

A symbolic field/action candidate is not action-level closure.
