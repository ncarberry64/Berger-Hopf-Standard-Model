# Common-16 Incidence Audit

Under the explicit premises `Omega=(3,6,12)` and candidate `rho_ch=3`, exact
rational arithmetic gives

```text
s_f = Omega_f/rho_ch = (1,2,4)
S_ch = 1+2+4 = 7
Pi_f = (1/7,2/7,4/7)
N_16 = s_d^2 = 16
```

The identities are exact. Their status is
`CONDITIONAL_COMMON_16_GENERATOR_CANDIDATE` because `Omega_f` is structurally
integrated rather than complete-action derived and the charged action does not
select `rho_ch=3`.
