# BHSM N=3 event attachment-state incidence (v17.89)

The N=3 action already supplies the two scalar geometric functionals needed
to place a candidate event in the reciprocal attachment space:

\[
q_C=q_0,\qquad q_W=\log R_{\partial}(q),\qquad x_D=q_C-q_W.
\]

Consequently `-q_C+q_W+x_D=0` and its differential hold identically. The map
from the ten terminal N=3 geometry modes to `(q_C,q_W,x_D)` has rank two. Its
composition with the v14.68 four-stratum map also has rank two. This is the
usable form of the “whole system counts” principle: the attachment state is a
derived system-level map, not an eleventh geometry mode or a 377th KKT
unknown.

Pulling the archived representative v14.67 Wentzell operator back through
this differential produces a rank-two N=3 coordinate block. That calculation
is retained only as a provenance/shape check. It is not physical because the
archived core and depth curvatures were not Schur-derived on the current event
and the complete gauge-fixed Calderon projector is still open.

The next required calculation is therefore sharply defined: evaluate the
event-specific Schur curvature and the complete child Calderon flux on this
rank-two incidence image, then form

\[
F_{\rm child}^{\rm outer}
=P_{\rm phys}(\Gamma_1^{\rm event}+\Gamma_1^{\rm child}
+W_{\rm phys}(z_{\rm event})\Gamma_0).
\]

Only after that map exists should the direct nonlinear N=3 closure resume.
