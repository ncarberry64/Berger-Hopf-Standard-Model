# BHSM v10.2 Normal and Radion Geometry

The global parent remains `M8 = I_t x S7`. The seven-dimensional
`S3_fiber x M4` lifted seam is only a local bundle/collar description. The
physical action also retains independent M5 cap and intrinsic M4 owners.

For a unit non-null normal, the induced projector is

\[
h^A{}_B=\delta^A{}_B-\epsilon n^A n_B,
\qquad h^A{}_B n^B=0,
\qquad h^2=h.
\]

In a block Gaussian collar,

\[
\sqrt{|G|}=|N_\rho|\sqrt{|h|}.
\]

For a diagnostic deformation `delta X = psi n`, the standard identities are

\[
\delta h_{ij}=2\psi K_{ij},\qquad
\delta K=-\Delta_h\psi-(K_{ij}K^{ij}+\operatorname{Ric}(n,n))\psi.
\]

They do not make `psi` dynamical. The frozen M4 matcher uses a fixed embedding
`iota`; GHY completes Dirichlet metric variation but supplies no embedding
Euler equation.

The homogeneous M8 metric contains the legitimate geometric mode `a_F`:

\[
ds_8^2=-N^2dt^2+a_H^2g_{S^4}+a_F^2\langle\omega,\omega\rangle.
\]

Its spatial curvature is

\[
R_7=\frac{48}{a_H^2}+\frac6{a_F^2}-\frac{12a_F^2}{a_H^4},
\]

so

\[
\frac{\partial R_7}{\partial a_F}
=-\frac{12}{a_F^3}-\frac{24a_F}{a_H^4}<0.
\]

Thus the current homogeneous static constraint has no positive radion
equilibrium. `rho`, `a_F`, and the collective texture size `R` remain distinct.
