# BHSM v6.6.0: topological FR quantization and neutral dispersion

Primary result:
`BHSM_TOPOLOGICAL_FR_AND_NEUTRAL_DISPERSION_ARCHITECTURE_DERIVED_CONDITIONALLY`.

This sprint begins from protected `main` SHA
`ae14c0f9af8fe8b7933aed584e7bd924b9001ce4`, the history-preserving merge of
PR #165. Its scientific SHA
`746de9f8aee3b82b89a34c46f9feee5c68c450ba` is retained in `main`; its Python,
native, and ROOT checks passed and its remote scientific branch was not
deleted.

## Result in one paragraph

The selected BHSM identification

```text
Q_N = Map_*^N(S3,S3)
```

has `pi1(Q_N)=pi4(S3)=Z2`. Selecting the nontrivial
Finkelstein–Rubinstein (FR) character gives half-integer spin and fermionic
exchange for odd `N`, and the trivial sign for even `N`. This global
line-bundle quantization does **not** produce a local first-order M4 action.
The sprint therefore decisively selects Architecture B and adopts the unique
minimal boundary invariant with exactly one exposed dimensionless primitive,
`y_sigma`. A Hermitian geometric propagation response has a conditional
leading `L/E` phase, but the present action does not derive that response. A
constant Lorentz-invariant vacuum response would be a mass-squared operator,
whatever it is called.

## Established mathematics and BHSM identification

The function-space adjunction gives

```text
pi1(Map_*^N(S3,S3))
 = [S1,Map_*(S3,S3)]_*
 = [S1 smash S3,S3]_*
 = pi4(S3).
```

Hatcher records `pi4(S3)=Z2`, with generator the suspension of the Hopf map.
Because `S3=SU(2)` is a topological group, pointwise multiplication translates
degree components by homotopy equivalences. Thus the result is independent of
`N`. The selected compact-open/k-space function-space convention follows
May's standard mapping-space setup.

The unbased evaluation fibration has the same fundamental group because
`pi1(S3)=pi2(S3)=0`. A free quotient by global target `SU(2)` also does not add
a fundamental-group generator because that fiber is simply connected. No
local gauge quotient is silently imposed. Berger deformation, triality, and
the `G2/SU(3)` polarization are associated data, not extra mapping-space
factors.

Sources:

- [Hatcher, *Spectral Sequences in Algebraic Topology*, Chapter 1](https://pi.math.cornell.edu/~hatcher/SSAT/SSch1.pdf)
- [May, *A Concise Course in Algebraic Topology*](https://people.math.rochester.edu/faculty/doug/otherpapers/MayConcise.pdf)
- [Auckly and Speight, configuration spaces for Skyrme and Faddeev–Hopf models](https://arxiv.org/abs/hep-th/0411010)

`Q_N` is an **adopted BHSM identification** of the simplest topology already
present, not a parent-action theorem about the final physical quotient.

## FR quantization

There are two one-dimensional characters of `Z2`: the trivial character and
the nontrivial sign character. BHSM selects the nontrivial character,

```text
chi_FR(0)=+1,  chi_FR(1)=-1.
```

It defines a two-sheeted universal cover and the flat line bundle

```text
L_FR = Qtilde_N x_chi C,
Psi(gamma q) = chi_FR(gamma) Psi(q).
```

For the standard degree-`N` soliton rotation and exchange loops, the class is
`N mod 2`; hence the sign is `(-1)^N`. The internal `2pi` target rotation has
the same parity in this standard mapping-space sector. Conjugation sends
`N -> -N` and preserves parity. The triality cycle and wall-orientation
reversal are not identified with the FR generator. The construction does not
by itself prove exactly one fermion representation per BHSM particle slot or
exclude additional family sectors.

Sources:

- [Finkelstein and Rubinstein, *Connection between spin, statistics, and kinks*](https://www.osti.gov/biblio/4830115)
- [Krusch, *Homotopy of rational maps and the quantization of skyrmions*](https://arxiv.org/abs/hep-th/0210310)
- [Auckly and Speight](https://arxiv.org/abs/hep-th/0411010)

No monopole, Dirac string, monopole harmonic, or magnetic charge enters.

## Collective dynamics and the Architecture B decision

The frozen bosonic/topological field naturally gives a second-order
collective action,

```text
S_coll = integral dt [
  G_AB qdot^A qdot^B/2 + A_A qdot^A - V(q)
].
```

The FR line bundle has nontrivial discrete holonomy but a locally flat
connection. No continuous Berry curvature or Berry one-form coefficient is
generated by the frozen action. A spinorial collective Hilbert space is not
the same object as a local M4 Clifford bundle.

Architecture A therefore fails at a precise point: the repository has no
transgression giving the local M4 associated bundle, transition functions,
Lorentzian principal symbol, self-adjoint boundary domain, or the coefficient
of `sigma Gamma_star`.

Architecture B is selected. The adopted invariant is

```text
S_F,partial = integral_M4 sqrt(-h)
  <Psi,[C_BHSM + y_sigma sigma Gamma_star]Psi>.
```

This is an **adopted BHSM action invariant**, not parent-derived. It is the
lowest-order family-universal, wall-odd localizing invariant compatible with
the existing representation ledger, `Y_BH`, and `Q_em`. The declared
maximal-isotropic domain makes it symmetric/Hermitian for real `y_sigma`;
general first-order elliptic boundary-domain machinery is documented by Bär
and Ballmann:

- [Bär and Ballmann, *Boundary value problems for elliptic differential operators of first order*](https://arxiv.org/abs/1101.1196)

Canonical field normalization cannot remove the invariant ratio `y_sigma`.
FR topology fixes a discrete sign, not its magnitude. No relation to
`Z_sigma`, wall width, or collective inertia is derived. Exactly one new
dimensionless primitive remains, with no sector-dependent Yukawa fits.

## Compact spectrum boundary

The v6.5 rectangular maximal-isotropic diagnostic is recomputed at three
values of `y_sigma` times wall amplitude. It retains index one, one normalized
zero mode, and a positive first massive level. This establishes coefficient
dependence within the tested domain.

It is not the full B1 cap spectrum: the action has not selected the domain,
and exported upper/lower-sheet scalar profiles are insufficient for the
requested sheet-resolved normal problem. The squared operator must eventually
retain wall gradient, curvature, connection curvature, Berger, polarization,
and domain contributions.

## Neutral high-energy propagation

The conditional Hermitian operator is

```text
H_neutral(E,x)
 = E I + K_prop(x)/(2E) + A_0(x) + O(E^-2).
```

After removal of the common trace, path-ordered unitary evolution gives

```text
Delta phi_ij
 = integral_gamma [
   Delta kappa_ij(x)/(2E) + Delta A_0,ij(x) + ...
 ] d ell.
```

The deterministic representative test validates unitarity, common-phase
removal, path reversal, segment-refinement invariance, linear baseline
scaling, and phase halving when energy doubles. A flavor-dependent static
`A0` produces `L E^0`, preserving the v6.5 rejection of static holonomy as an
`L/E` mechanism. The standard high-energy `1/(2E)` dispersion structure is
established physics; no measured mass splitting or oscillation parameter is
used:

- [Akhmedov, Maltoni, and Smirnov, high-energy neutrino propagation](https://arxiv.org/abs/hep-ph/0506064)

The BHSM-specific identification of `K_prop` remains conditional. It must be
derived from curvature, boundary response, connection curvature, transverse
stiffness, or another propagation-activated geometric source.

## Zero-rest-mass audit

A constant Lorentz-invariant vacuum eigenvalue in

```text
E_i^2 = p^2 + kappa_i
```

is operationally a mass-squared eigenvalue. It cannot be protected by
renaming it geometric. A propagation-supported interpretation requires path
or medium dependence, activation by propagation geometry, disappearance in
the declared flat/non-propagating limit, and no static rest-energy pole.

The current action does not derive such a `K_prop` and cannot decide the
vacuum dispersion. Therefore the zero-fundamental-rest-mass doctrine is
conditionally compatible with a propagation-supported `K_prop`, but has not
been validated by this sprint.

The PMNS attachment remains structural,

```text
U_PMNS = U_l^dagger U_neutral U_nu,
```

and distinct from

```text
V_CKM = U_u^dagger U_color U_d.
```

No matrix is fitted or inserted.

## Polarization and forward links

An `S3/SU(2)`-valued degree field does not canonically define a section of
`G2/SU(3)=S6`. No equivariant bundle morphism is present, so dynamic
polarization remains open and its invariant one-section potential stays flat.
The scalar–Berger pair `(sigma,beta)`, exact
`tau_nested/tau_transverse=exp(2 beta)`, `Q_em` null direction, and scale
transfer gate are preserved without measured inputs or an assumption
`Z_g=Z_A`.

## Reproduction

```bash
python scripts/materialize_topological_fr_neutral_dispersion_v6_6_0.py
python -m pytest -q tests/test_bhsm_topological_fr_neutral_dispersion_v6_6_0.py
python -m bhsm.interface topological-fr-neutral-dispersion-status --format markdown
```

Open gate:
`V6_6_0_LOCAL_CARRIER_PARENT_SOURCE_PROPAGATION_RESPONSE_AND_FULL_COMPACT_B1_SPECTRUM_OPEN`.

`FULL_BHSM_NOT_COMPLETE`
