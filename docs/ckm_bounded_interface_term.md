# CKM Bounded Interface Term

Status: `ARTIFACT_BACKED_BOUNDED_CKM_INTERFACE_TERM`.

`L_CKM_charged_current_bounded` is located in the minimal bounded collider-interface subset. Its symbolic target uses `V_CKM_BH`, a runtime coefficient, an up/down bilinear, and `+ h.c.`.

L_CKM_charged_current_bounded is a bounded interface term, not automatically a normalized action-selected transport operator.

The v2.8 measure audit does not promote this term.
