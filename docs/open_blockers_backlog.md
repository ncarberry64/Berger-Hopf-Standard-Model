# Open Blockers Backlog

Current public status: structural architecture integrated conditional; numerical closure open.

Each blocker must be handled without fitting observed data after comparison and without changing frozen predictions.

| Blocker | Current status | Why it matters | Next valid action | Forbidden invalid action |
| --- | --- | --- | --- | --- |
| `DELTA_Y_NU_NEUTRAL_SADDLE_DISPLACEMENT_OPEN` | localized as `OPEN_LOCALIZABLE` | Needed for the Hessian/barrier candidate formula. | Derive `Delta y_nu` from stationary-point equations or finite-width centroids before comparison. | Choose `Delta y_nu` to reproduce observed neutrino mass scale or PMNS residuals. |
| `S_EFF_NU_NEUTRAL_ACTION_OPEN` | localized as `OPEN_LOCALIZABLE` | Needed for the stationary-point formula. | Derive `g_sub`, `ellapse_nu`, neutral boundary tensors, neutral boundary conditions, and neutral profile from the boundary/topographic action. | Insert an effective action, projection map, or apparent propagation parameter chosen from neutrino residuals. |
| `G_SUB_ELLAPSE_NU_PROJECTION_GEOMETRY_OPEN` | localized as `OPEN_LOCALIZABLE` | Needed to make the subsurface neutral-channel action mathematically auditable. | Derive `g_sub`, `ellapse_nu`, and `Pi_sub_to_ext` from scalar/topographic profile, projection convention, positivity/causality proof, and neutral boundary tensors. | Fit projection geometry to neutrino masses, PMNS data, or FTL/anomaly data. |
| `G_SUB_INTERNAL_METRIC_OPEN` | localized as `OPEN_LOCALIZABLE` | Needed for the subsurface neutral topographic channel. | Derive the internal/subsurface metric from the internal topographic action. | Choose a metric to tune neutrino scales. |
| `ELLAPSE_NU_PROJECTION_MAP_OPEN` | localized as `OPEN_LOCALIZABLE` | Needed to relate subsurface neutral paths to exterior-surface projections. | Derive the projection/lapse map and its causality interpretation. | Fit apparent propagation behavior to data. |
| `PI_SUB_TO_EXT_PROJECTION_MAP_OPEN` | localized as `OPEN_LOCALIZABLE` | Needed to map subsurface neutral coordinates to exterior surface coordinates. | Derive the coordinate-invariant projection map from the internal metric and neutral channel geometry. | Pick a projection map after seeing neutrino or anomaly residuals. |
| `EXPLICIT_SCALAR_TOPOGRAPHIC_BOUNDARY_VARIATION_OPEN` | upgraded to `DERIVED_CONDITIONAL` as a symbolic variation | Needed to connect `S_bulk + S_partial^(nu)` to the neutral boundary condition. | Derive the normal-coupling convention, tensor values, neutral profile, and positivity/stability proof from the complete scalar/topographic action. | Treat the symbolic condition as a numerical neutrino prediction or fit it to data. |
| `R_NU_NORMAL_COUPLING_COLLAR_CONVENTION_OPEN` | localized as `OPEN_LOCALIZABLE` | Needed to resolve the `lambda_nu Phi n.grad Phi` contribution inside the symbolic neutral boundary condition. | Derive the collar measure, normal orientation, inner-edge condition, admissible variation data, and any Robin coefficients from the complete scalar/topographic action. | Fit `R_nu`, collar convention, or Robin coefficients to neutrino masses, PMNS data, or FTL/anomaly data. |
| `COLLAR_GEOMETRY_PACKAGE_OPEN` | localized as `OPEN_LOCALIZABLE` | Needed to make the neutral normal-coupling collar convention mathematically auditable. | Derive the collar measure, normal orientation, inner-edge condition, admissible variation data, and Robin coefficient extraction from the complete scalar/topographic boundary action. | Fit collar coordinate, measure, orientation, edge condition, variation data, or Robin coefficients to neutrino masses, PMNS data, or FTL/anomaly data. |
| `COMPLETE_SCALAR_TOPOGRAPHIC_COLLAR_ACTION_OPEN` | audited as `OPEN_LOCALIZABLE` | Needed to decide whether the existing scalar/topographic action fixes the PO-BH-55 collar package. | Derive `L_collar`, `J(Y,rho)`, `s_n`, edge data, admissible variations, and symbolic Robin coefficients from the complete scalar/topographic boundary action. | Fit `L_collar`, `J(Y,rho)`, `s_n`, edge data, `A_nu`, or `B_nu` to neutrino masses, PMNS data, or FTL/anomaly data. |
| `COLLAR_MEASURE_EXTRINSIC_GEOMETRY_OPEN` | formula upgraded to `DERIVED_CONDITIONAL`; `K(Y)` and `S` formulas now `DERIVED_CONDITIONAL` with numerical/function values open | Needed to make the collar measure symbolic rather than an unconstrained Jacobian. | Derive the boundary embedding, scalar/topographic profile, metric data, and normal orientation needed to evaluate `S(Y)` and `K(Y)`. | Fit `J(Y,rho)`, `K(Y)`, shape operator `S`, boundary embedding, or curvature data to neutrino masses, PMNS data, or FTL/anomaly data. |
| `BOUNDARY_EMBEDDING_INDUCED_METRIC_SHAPE_OPERATOR_OPEN` | formulas localized/derived conditionally; `boundary_embedding_X` and scalar/topographic profile remain `OPEN_LOCALIZABLE` | Needed to evaluate `J(Y,rho)=det(I+rho S(Y))` and `K(Y)=tr(S)(Y)` inside BHSM. | Derive the BHSM boundary embedding and scalar/topographic level-set profile before comparison. | Choose `X`, `h_AB`, `n`, `K_AB`, `S`, or `K` from neutrino, PMNS, or FTL/anomaly residuals. |
| `SCALAR_TOPOGRAPHIC_LEVEL_SET_BOUNDARY_OPEN` | two candidate level-set routes localized; regular-level-set geometry `DERIVED_CONDITIONAL`; profile values remain open | Needed to turn the PO-BH-58 formulas into evaluable BHSM geometry. | Derive `T_0`, `Phi_0`, explicit `T(x,t)` and `Phi(y)` profiles, nonzero-gradient proof, metrics, orientation, and `X` before comparison. | Fit thresholds, profiles, embedding, `S_nu_topo`, or `epsilon_nu_topo` to neutrino, PMNS, CKM, FTL/anomaly, or propulsion data. |
| `SCALAR_TOPOGRAPHIC_PROFILE_INPUTS_OPEN` | input ledger classified; Gate 1 partially localized; Gates 2-4 remain `OPEN_LOCALIZABLE` | Needed to know which profile inputs must be derived before numerical closure. | Derive threshold selection, explicit profile equations/solutions, metric/profile evaluation, and neutral action evaluation before comparison. | Use observed masses, CKM, PMNS, neutrino, anomaly/FTL, propulsion/anomaly, or target values to choose profile inputs. |
| `SCALAR_TOPOGRAPHIC_PROFILE_EOM_SOURCE_OPEN` | PO-BH-61 audit completed; partial source found; complete EOM and profile solutions remain `OPEN_LOCALIZABLE` | Needed to turn schematic scalar/boundary variation into actual solvable `T(x,t)` and `Phi(y)` systems. | Derive explicit spacetime/internal profile EOMs, thresholds, boundary data, and profile solutions from BHSM action structure before comparison. | Treat the partial variational source as a computed profile or fit EOM/source terms to neutrino, PMNS, CKM, anomaly/FTL, propulsion/anomaly, or target values. |
| `SCALAR_TOPOGRAPHIC_BOUNDARY_CONDITION_COEFFICIENTS_OPEN` | normal-form boundary conditions are `DERIVED_CONDITIONAL`; coefficients, thresholds, and profile solutions remain `OPEN_LOCALIZABLE` | Needed to turn the normal form into actual boundary data for solving `T` and `Phi`. | Derive `alpha`, `beta`, `gamma`, and source terms from the scalar/topographic boundary action before comparison. | Fit boundary coefficients, thresholds, profile solutions, `S_nu_topo`, or `epsilon_nu_topo` after comparison. |
| `SCALAR_TOPOGRAPHIC_COEFFICIENT_THRESHOLD_VALUES_OPEN` | PO-BH-63 audit completed; coefficient families/dimensions conditionally organized; values, ratios, sources, and thresholds remain `OPEN_LOCALIZABLE` | Needed before the normal form can become a profile-solving boundary-value problem. | Derive coefficient normalization, ratios, source terms, and threshold values from BHSM action/topographic structure before comparison. | Fit coefficient values, coefficient ratios, source terms, `T_0`, `Phi_0`, profile solutions, `S_nu_topo`, or `epsilon_nu_topo` after comparison. |
| `CHARGED_HESSIAN_ANISOTROPY_RHO_CH_OPEN` | `rho_ch` remains `OPEN_LOCALIZABLE`; `rho=1` and `rho=3` are candidates only | Needed before exact charged-sector Hessian costs can be promoted. | Derive `rho_ch` from charged-sector action/Hessian structure. | Choose `rho_ch` from masses, CKM, PMNS, neutrino data, measured alpha, observed generation data, or target values. |
| `CHARGED_HESSIAN_ACTION_SOURCE_OPEN` | PO-BH-65 audit completed; no charged action/Hessian source decides `rho_ch` | Needed to decide between the minimal `rho_ch=1`, cyclic `rho_ch=3`, or another derived charged metric value. | Derive the charged Hessian anisotropy from explicit charged-sector action, boundary Hessian, or local quadratic form before comparison. | Choose `rho_ch` from masses, CKM, PMNS, neutrino data, measured alpha, empirical generation data, or target values. |
| `CHARGED_QJ_CROSS_TERM_SOURCE_OPEN` | charged `qj` cross-term is `FORBIDDEN_CONDITIONAL_UNLESS_ACTION_DERIVED`; neutral/topographic `qj` mixing remains `OPEN_ALLOWED` | Needed to prevent neutral/topographic anisotropy from silently leaking into charged-sector numerical closure. | Derive an explicit charged-sector coupling if a charged `qj` cross-term is to be admitted. | Import neutral/topographic mixing into charged costs without a charged action source, or fit a cross-term after comparison. |
| `ETA_L_PROJECTION_CONSTANTS_OPEN` | eta projection/self-screening route documented; `Pi_l`, `alpha_geom`, and exact `eta_l` remain open | Needed before charged-lepton precision dressing can be claimed as derived. | Derive `Pi_l` and `alpha_geom` internally before comparison. | Use CODATA alpha as derivation or fit `Pi_l` after seeing charged-lepton residuals. |
| `Z_VIRT_U2_APPLICABILITY_OPEN` | dimension-ratio path strengthened; applicability proof remains open | Needed before `Z_virt^{u,2}=1/2` is fully derived. | Prove that the up-sector virtual correction samples the two-door virtual pair. | Adopt the ratio as fully derived without the applicability proof. |
| `Z_VIRT_U2_VIRTUAL_DOOR_APPLICABILITY_OPEN` | PO-BH-66 formalizes `dim(V_pair^u)=2`, `rank(A_virt^u)=1`, and ratio `1/2`; applicability remains `OPEN_LOCALIZABLE` | Needed to promote the virtual-door dimension ratio from candidate route to applicable up-sector correction. | Derive that the relevant middle-up virtual correction samples `V_pair^u=span{door_u,door_d}` before comparison. | Use charm/top, up/top, CKM, PMNS, neutrino data, measured alpha, or target ratios to justify the factor. |
| `Z_VIRT_U2_DRESSING_DEPENDENCY_LINK_OPEN` | PO-BH-67 localizes the actual path through `pure_fiber_middle_up_rule`, but finds no explicit two-door dependency | Needed before the released middle-up dressing can be claimed as derived from the virtual-door pair. | Derive an explicit link from the actual up-sector virtual correction source to `V_pair^u`. | Treat frozen prediction appearance, charm/top agreement, or comparison rows as derivation. |
| `ROBIN_COEFFICIENTS_A_B_OPEN` | localized as `OPEN_LOCALIZABLE` | Needed to reduce `R_nu` to `A_nu Phi + B_nu n.grad Phi` without hidden fitted knobs. | Derive `A_nu` and `B_nu` from `lambda_nu`, `epsilon`, `J`, orientation, and edge data after the collar package is fixed. | Fit Robin coefficients to masses, PMNS values, or anomaly/FTL data. |
| `NEUTRAL_BOUNDARY_TENSORS_CONDITION_OPEN` | symbolic boundary condition `DERIVED_CONDITIONAL`; tensor values remain `OPEN_LOCALIZABLE` | Needed as the shared bridge between `S_eff_nu`, subsurface projection geometry, `Delta_y_nu`, and `S_nu_topo`. | Derive the tensor values and exact normal-coupling convention from the scalar/topographic boundary action, subsurface metric/projection geometry, `W_nu`, and positivity/stability proof. | Fit `chi_nu`, `lambda_nu`, or the boundary condition to neutrino masses, PMNS data, or FTL/anomaly data. |
| `CHI_NU_LAMBDA_NU_BOUNDARY_TENSORS_OPEN` | localized as `OPEN_LOCALIZABLE` | Needed for the boundary scalar/topographic route. | Derive `chi_nu^{AB}`, `lambda_nu`, and the neutral boundary condition. | Pick tensors to match PMNS or neutrino masses. |
| `NEUTRAL_PROFILE_W_NU_OPEN` | open `S_eff_nu` dependency | Needed for the finite-width neutral profile action. | Derive `W_nu`, `V_eff^(nu)`, and the width/regularization term. | Choose a profile width after comparison. |
| `CAUSALITY_PROJECTION_THEOREM_OPEN` | open interpretive dependency | Needed to formalize apparent exterior anomalous propagation from the internal metric/projection. | Prove the projection statement from `g_sub` and `ellapse_nu`. | Claim literal local causality violation or experimentally established FTL. |
| `S_EFF_H_HIGGS_ACTION_OPEN` | open `Delta_y_nu` dependency | Needed to define the charged/Higgs saddle `y_H`. | Derive `S_eff^(H)` from the Higgs/charged scalar/topographic action. | Choose a Higgs saddle to make the neutral displacement work. |
| `H_H_HIGGS_SADDLE_HESSIAN_OPEN` | open `Delta_y_nu` dependency | Needed for `Delta y_nu = - H_H^{-1} grad_y[delta S_eff^(nu-H)]|_{y_H}`. | Derive the Higgs/charged Hessian at `y_H`. | Insert a fitted inverse Hessian. |
| `W_NU_W_H_CENTROID_WEIGHTS_OPEN` | open `Delta_y_nu` dependency | Needed for the finite-width centroid route. | Derive `W_nu`, `W_H`, and the invariant centroid convention. | Pick centroids after looking at neutrino data. |
| `H_TOPO_NU_HESSIAN_DERIVATION_OPEN` | open localizable `S_nu_topo` dependency | Needed to define `G_nu_topo = 1/2 E_nu^T H_topo^(nu) E_nu`. | Derive the neutral topographic Hessian from the full boundary/topographic action. | Insert a Hessian eigenvalue fitted to neutrino data. |
| `E_NU_LABEL_TO_DISTANCE_MAP_OPEN` | open localizable `S_nu_topo` dependency | Needed to map neutral labels into the topographic displacement metric. | Derive `E_nu` from neutral sector boundary tensors and finite-width geometry. | Pick an embedding that makes the suppression numerically work. |
| `S_BARRIER_NEUTRAL_TOPOGRAPHIC_OPEN` | open localizable `S_nu_topo` dependency | Needed to decide whether a positive barrier term contributes to suppression. | Derive or prove unnecessary `S_barrier` from the neutral boundary action. | Fit a barrier term to the neutrino scale. |
| `SCALAR_TOPOGRAPHIC_PROFILE_SOLUTION_OPEN` | open theorem/scaffold dependency | Needed for the full scalar/topographic profile and neutral finite-width saddle/path. | Solve or bound the profile from the scalar/topographic action. | Tune profile parameters to hide residuals or extra light modes. |
| `POSITIVITY_PROOF_S_NU_TOPO_OPEN` | open proof dependency | Needed to justify suppression as positive action rather than a fitted damping factor. | Prove positivity of the derived neutral suppression action. | Assume positivity from the desired exponential hierarchy. |
| `CKM_1_16_EXPONENT_NOT_DERIVED` | open localized numerical-closure object | Controls the candidate CKM 2-3 dressing exponent. | Derive or reject the exponent from BHSM structure before comparison. | Fit `1/16` to CKM residuals after comparison. |
| `S_NU_TOPO_DERIVATION_OPEN` | open localized numerical-closure object | Sets the neutral topographic suppression scale. | Derive or constrain `S_nu_topo` from topographic/boundary action structure before comparison. | Fit `S_nu_topo` to neutrino mass scale. |
| `SCALAR_TOPOGRAPHIC_DECOUPLING_OPEN` | open theorem blocker | Needed to exclude unwanted light scalar/topographic modes. | Prove or bound scalar/topographic decoupling in the action/scaffold. | Hide extra light states by tuning profile parameters. |
| `HIGHER_LOOP_THRESHOLDS_OPEN` | open comparison blocker | Required for precision RG/QCD comparison. | Implement validated higher-loop/threshold matching with labeled inputs. | Use mixed-scale masses as precision truth. |
| `NUMERICAL_MASS_RATIO_LOCK_OPEN` | open pre-comparison lock | Needed before claiming mass-ratio prediction. | Lock all symbolic mass inputs and tolerances before comparison. | Adjust metrics or prefactors after seeing residuals. |
| `CKM_NUMERICAL_LOCK_OPEN` | open pre-comparison lock | Needed before claiming numerical CKM prediction. | Lock sector displacement, phase, and finite-width inputs before comparison. | Tune CKM phases or mixing metrics to observed CKM. |
| `PMNS_NUMERICAL_LOCK_OPEN` | open pre-comparison lock | Needed before claiming numerical PMNS prediction. | Lock neutral operator parameters and phase loop before comparison. | Tune PMNS angles or CP phase to data. |
| `NEUTRINO_ORDERING_OPEN` | open physics-output blocker | Needed before any neutrino ordering claim. | Derive ordering from the neutral operator after pre-comparison lock. | Choose ordering from observed oscillation data. |
| `STABILITY_AND_COUPLING_BOUNDS_OPEN` | open theorem blocker | Needed for robust low-energy equivalence and decoupling claims. | Prove stability and coupling bounds in the relevant operator/domain scaffold. | Assert stability from finite tests alone. |
