# BHSM completion: foundational sigma-selection obstruction

The validated formation-to-shape transfer is retained. The obstruction occurs
at the next required physical step: the present action does not determine a
unique nonzero sigma material response.

## Exact invariant submanifold

The retained Hamiltonian sub-block is

\[
H=\frac{p_q^2}{2M_q(1+g\sigma^2)}
+\frac{p_\sigma^2}{2Z_\sigma}+V_q(q)
+\frac12K_\sigma\sigma^2+\frac14G_0\sigma^4.
\]

Its sigma momentum equation has the form

\[
\dot p_\sigma=\sigma\left[
\frac{g p_q^2}{M_q(1+g\sigma^2)^2}-K_\sigma-G_0\sigma^2
\right].
\]

Consequently, locally unique classical evolution with
\(\sigma=p_\sigma=0\) remains exactly on

\[
\boxed{\sigma(\tau)=p_\sigma(\tau)=0}.
\]

The parity-even \((q,s)\) transfer does not change this theorem. A negative
tangent curvature amplifies a displacement, while positive inertial
saturation bounds a displaced trajectory. Neither creates a classical seed
or selects its sign.

## Nonlinear integration

The complete conservative \((q,\sigma)\) Hamilton equations were integrated
through both pulse flanks with energy conservation. Deterministic controls
show:

- an exact zero seed remains zero;
- changing the retained but unselected response coefficient changes sigma
  amplification by orders of magnitude;
- with the same action coefficients, changing the unselected seed changes the
  nonlinear saturated trajectory.

These controls are not predictions. They are counterexamples to uniqueness
from the presently supplied data.

## Independent coefficient-family theorem

The v15.10 inverse already constructs inequivalent stable triples
\((g,A_0,G_0)/Z_\sigma\) that share the same sigma-zero parent and first
variation. Field variation cannot select constants that are independent,
unvaried action data.

The exact missing action-owned information is:

1. the physical response jet
   \((S_\sigma,\partial_XS_\sigma,\lambda_\sigma)\);
2. a sigma symmetry-breaking initial-state or quantum-state selection rule;
3. a pregeometric-to-regular pairing that derives both without empirical fit.

The repository specifies the algebraic inverse from such a response jet, but
contains no physical generator, its \(X\)-derivative, nonlinear response, or
state-selection law.

## Consequence

Without those data, the current repository defines a family of inequivalent
theories and trajectories rather than one BHSM. It cannot uniquely determine
a material skin. Therefore geometric separation, contact/ejection,
reconstruction loss, the Aether transition input, the persistent child, and
child-evaluated Standard Model sectors are not well-defined downstream
objects.

Assigning a convenient response triple, noise seed, or sign would be a new
postulate and would violate the no-fitting/no-arbitrary-coefficient rules.

`FULL_BHSM_COMPLETE = FALSE` because unconditional completion is
mathematically underdetermined by the supplied action—not because another
versioned gate was chosen as a stopping point.
