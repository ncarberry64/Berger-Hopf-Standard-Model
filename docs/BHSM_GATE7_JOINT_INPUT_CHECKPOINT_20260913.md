# Gate 7 checkpoint: preserved inputs, insufficient joint remainder

**Decision:** do not scale this method or launch another seed refinement.
The bounded saved-data calculation is finished, independently reproduced,
and negative for its explicitly defined relaxation. The fully coupled
physical enclosure is **not established**. `FULL_BHSM_COMPLETE=false`.

- **Recovered/reused:** owner protocol from Downloads, SHA256
  `3890326AE387015D18EC2ED95B2568C79B148173ECE713F2C52CCA15F7EAC78D`;
  durable copy in `tmp/bhsm_joint_input_column_20260913/`. PR #440 is merged
  at `e6e92375efeb9821122164e83b0bf8bf119b46d1`; the DEV base for this update
  was `acfa63eb2edc6ff909df1dcc92ba10180eb8e768`. The original right-column
  obstruction and paired endpoint/midpoint jets are unchanged. First/repeat
  bytes, common source bindings, physical operands, axes and original
  domains were reconciled. No action derivative was recomputed.
- **Authoritative target:** existential finite-encapsulation physical
  history with its required operator/readout dependencies. The current
  gate ledger explicitly exempts the post-encapsulation reset child from
  having to hit the event again. The older protocol does not override that
  later definition. Reset remains set-valued; no selector, component
  separator, recurrence requirement, or favorable-domain restriction is
  introduced. The original two-radius endpoint domain and all 249 midpoint
  directions remain covered.
- **Actual result:** the anchored joint-input outer model retains full
  uniform-minus-point Hessian remainders, point uncertainty, the endpoint
  chain and the original center shift. Midpoint point-linearization norm
  upper bound: `0.0001742429`; midpoint remainder upper bound: `53.21519326`.
  Endpoint remainder upper bound: `2.81404770`. Complete local transverse
  column norm upper bound: `55.95756527`. A rigorously exhibited pair in
  this relaxation gives gain **at least `48.720352347851`**, hence relaxed
  contraction margin **at most `-47.720352347851`**. No particular old
  Cartesian corner has been proved physically excluded.
- **Certification:** require
  `B_i=Y_i+sum_j Z_ij r_j+C_i r_L^2+2M_i r_L r_T+T_i r_T^2 < r_i`
  for both outputs, and
  `max_i sum_j (partial_j B_i) r_j/r_i < 1`, with all coefficients covering
  the same physical map and tube. At unchanged radii
  `(9.160189721418071e-7,1.9369612593815614e-9)`, stored polynomial margins
  remain `(2.9861380823659324e-7,6.314316577071288e-10)` and stored algebraic
  `q=0.7974712031541378`. The **physical global margin remains unknown**.
  The old Cartesian lower bound `54.843547458166` is preserved; the new
  relaxed lower bound is not a measured reduction of the certification gap.
- **Dominant obstruction:** retaining a shared input after the implicit
  response/Hessian errors have become independent coordinate intervals
  leaves large unattainable combinations in the numerical outer set.
  Saved matrices do not recover the parameter-dependent implicit solves,
  normalization, or exact endpoint-to-midpoint attainable relation.
- **One next action:** implement one coupled projected residual support
  certificate for this same complete right column, starting with a
  separating output covector. Use the saved point jets in
  `G(theta,Uhat(theta)+e)=0`, bound its preconditioned residual with common
  `theta`, and retain inclusion, nonlinear remainder, normalization,
  branch/inertia and boundary obligations. A sufficient remaining
  **local vector remainder** budget for this test is less than
  `0.9997125743857969`; a single scalar support success would only remove
  an obstruction, not certify the vector norm or global operator. This
  bound is not supplied by existing saved independent residual ranges.
  The precise residual equation and scope are in the theory note. No
  executable validated coupled-residual calculation is presently available
  to launch; no additional point-Hessian campaign is justified by this test.
- **Remaining gates:** uniform physical neighborhood/full-history
  certification and frame/quotient identification; complete joint
  finite-history operator with geometry jets and endpoint form; signed
  projected force, KKT root and constrained physical Hessian; continuum
  right-inverse/strong-graph closure. Full completion additionally requires
  action-owned state/scale/observable maps, the no-fit prediction and
  benchmark ledger, resulting physical data in existing Museum engines,
  and a complete submission manuscript. No gate, frozen prediction, or
  Museum engine was changed.
- **Workers:** native process checks confirmed controller `6232`
  (birth `134337397863219009`) and its then-current child `18136`
  (birth `134337679610562223`) live; phase `H_midpoint_033_repeat`.
  This is a dated snapshot, not a perpetual liveness assertion. The
  six-worker campaign owns its sequential continuation and file logs;
  model-turn attachment is unnecessary. The wrapper and child launchers
  write durable logs and reuse completed manifests/paired checkpoints.
  Sleep pauses work; reboot recovery is not automatic; app-shutdown
  survival has not been verified. Terminal failures, source mismatch or
  disk-reserve limits require recovery. No scheduler or new worker was added.
- **Validation:** final arithmetic records are byte-identical from fresh
  processes; 15 focused mathematical tests passed. Forbidden-claims,
  status, frozen-prediction integrity, public-readiness and precision audits
  passed. Full historical tests and coordinate timing benchmarks were not
  rerun for this local saved-data calculation. All unrelated work is retained.

The full derivation is
[`n12_gate7_joint_input_column_sufficiency.md`](../theory/n12_gate7_joint_input_column_sufficiency.md).
The [result](../artifacts/flagship_integration/BHSM_N12_GATE7_JOINT_INPUT_COLUMN_SUFFICIENCY_20260913.json)
has SHA256
`F9BBF59B0E695DC96228C4ADAE2CB0CEFA2FB93D8B05B033703B3FF8B5075CA8`;
its adjacent reproduction record distinguishes arithmetic replay from new
physical derivatives. Detailed local validation and worker snapshots are
under `tmp/bhsm_joint_input_column_20260913/`.

Exact replay entry point, from the DEV repository root, using a new output:

```powershell
C:/Python314/python.exe scripts/evaluate_n12_gate7_joint_input_column.py --evidence-root ../BHSM-ae32-crossing-correction --out tmp/bhsm_joint_input_column_20260913/resume.json
```

Use the saved result for ordinary resumption; do not rerun this command to
poll the campaign. Its independent numerical continuation is
`scripts/run_n12_gate7_curvature_with_read_retry.py` in LIVE. Do not start a
second controller. A restart is appropriate only after the old controller
and child are verified exited; preserve all existing work directories and
use a new hidden launcher with fresh log files. The one-shot recovery
wrapper is not a reusable restart command.
