# BHSM v14.29 View 2 authoritative action campaign

## Outcome

The subsequent scientific proof audit reclassifies this campaign as Outcome B:

`BHSM_VIEW2_MINIMALLY_GAUGED_ETA_ACTION_AND_COMPOSITE_THETA_CURRENT_ARE_CONSTRUCTED_CONDITIONALLY_BUT_FULL_MASTER_ACTION_OWNERSHIP_REMAINS_BLOCKED`

The initial software-consistency result did not prove the missing `M8` eta-to-physical-SU3 common-domain reduction or the FR/Dirac matching theorem. The controlling derivation and corrected current convention are in [BHSM_VIEW2_SCIENTIFIC_PROOF_AUDIT_V14_29.md](BHSM_VIEW2_SCIENTIFIC_PROOF_AUDIT_V14_29.md). This is not a claim that BHSM, confinement, the physical scale, or flavor is complete.

## Validated

- For the retained color bundle `P_color`, the extension `Q_G2=P_color x_SU3 G2` and associated `S6=G2/SU3` bundle retain arbitrary parent `c2` sectors. The vertical tangent is `P_color x_SU3 m`, with `dim_R m=6` and `m_C=3+bar3`.
- `theta=Theta_eta(D_A eta)` is composite. The independent configuration variables are `A_physical` and `eta`; the Hessian has the ordinary eight Yang-Mills vector symbols and the six existing eta scalar tangent symbols, with no extra vector pole.
- A conditional candidate covariantizes the retained `p=2+p=8` eta density without a new coefficient. It is not derived from the prior stratified action because eta and `A_physical` live on different owned strata with no common reduction/measure theorem.
- Direct variation gives the source `J_a^mu=+2w(kappa1+X_eta^3) Re[(t_a xi)^dagger D^mu xi]` in the local complex tangent convention when `delta S=-int J delta A`. Gauge symmetry gives the convention-corrected off-shell identity and on-shell conservation for the candidate.
- The stabilizer selector and a featureless pure normal wall have zero current. A nontrivial eta tangent excitation has an explicit noncentral current and positive dimensionless reference `Z_eta` on the retained stable branch.
- The exact Wilson-dressed meson/baryon source functionals have zero total singlet charge and mesonic summed endpoint correlation `-4/3`.

## Reclassified or invalidated

- The FR Dirac current is a conditional collective quantization of the eta zero-mode current, not an additive second Gauss source. Its first-order action equivalence and self-adjoint relative-knot domain remain conditional.
- A stable finite-width tube would not by itself prove an area law. The Gaussian collar result remains a no-go for nonzero asymptotic string tension.
- Representation traces constrain ratios but do not determine the common Yang-Mills normalization.
- The recovered v14.20 theorem excludes rank-`dim(g)-1` projectors as unbroken simple-factor kinetic forms and identifies `6pi2` as an unnormalized `S3` measure times family trace, not a physical coupling derivation.
- The recovered v14.21 theorem gives one normalizable chirality on a single degree-one eta wall; its formal adjoint partner is nonnormalizable. Identical normalized eta profiles with an intrinsic-M4 Higgs give unit, family-central overlap and cannot produce hierarchy or mixing.

## Downstream gate

The exact non-Abelian coupled BVP has been specified with background-covariant gauge fixing, a required FP/zero-mode domain, parent-relative subtraction, collar transmission conditions, topological and Wilson center sectors, and a nonradial continuation plan. It has not been replaced by an Abelian proxy or falsely reported as solved.

The reduced transverse functional and its wallless/wall-only extrema are reproduced, together with the convexity criterion and a relative-determinant/no-double-counting contract. The campaign stops at the first exact unsolved object:

`GAUGE_FIXED_COUPLED_ETA_SU3_COLLAR_WILSON_SINGLET_BOUNDARY_VALUE_PROBLEM_WITH_SELF_ADJOINT_DOMAIN_PARENT_RELATIVE_SUBTRACTION_AND_NONRADIAL_HESSIAN`

No CKM, PMNS, absolute mass, mass splitting, gauge normalization, or `c_sigma` output is emitted. The family charged-current kernel remains `I3` pending an action-derived noncentral family response.
