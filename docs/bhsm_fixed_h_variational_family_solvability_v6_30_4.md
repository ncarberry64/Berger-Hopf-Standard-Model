# BHSM v6.30.4 fixed-\(h\) variational-family solvability

## Executive result

The v6.30.3 domain mismatch is repaired at second order by selecting one
family and keeping it fixed: D0, the strict fixed-\(h\), fixed-curvature
family. Its order-two source lies in the range of the v6.30.2 KKT operator,
the Fredholm coefficient is exactly zero, and the complement response is
constructed without a control unfolding:

\[
\boxed{\Omega_2=0},\qquad
\boxed{\Phi_2=(A_2,0,0,\eta_2)} .
\]

The phase verdict is

`BHSM_STRICT_FIXED_H_NONLINEAR_FAMILY_SECOND_ORDER_SOLVABLE`.

This is an order-two result, not an all-orders nonlinear-family theorem.

## Fixed starting state and incompatible tangents

The starting main SHA is
`24b6be33871911dcd7932503ed56553867462ff8`. The fixed-\(h\) kernel and
adjoint kernel are both generated by

\[
\Phi_1=\Phi_1^\dagger=(0,0,u_1,0),
\]

with scalar normalization

\[
\int_0^{\pi/4}4\sin^4\rho\,u_1^2\,d\rho=1.
\]

Thus \(F_1=0\). The historical \(F_{1,\tau}\ne0\) uses the distinct tangent
\(X_1=\tau\chi_1\). It is retained as D2 provenance but is not used in the
selected family.

## Domain ledger

| Domain | Curvature data | First tangent | Status |
|---|---|---|---|
| D0 | \(r(q)=r_0\) at every order | \((0,0,u_1,0)\) | selected |
| D1 | proposed even \(r_2,r_4,\ldots\) | \((0,0,u_1,0)\) | not fixed-\(h\) \(\Gamma[h,q]\) |
| D2 | \(r_1=X_1=\tau\chi_1\) | metric plus scalar | historical external/on-shell probe family |
| D3 | covariant amplitude multiplier | \((0,0,u_1,0)\) | no frozen-action source provenance |

D1 does not add a new field: the independent \(M_4\) metric exists in the
parent action. It is nevertheless external fixed data during extraction of
\(\Gamma[h,q]\). It may be varied only after \(F(q)\) and \(V_J(q)\) have
been obtained. D3 would require a new action-level multiplier/source that is
not present in the frozen action, so it is not used.

## Reflection and parity

The exact reflection is

\[
(q,\tau)\longmapsto(-q,\tau).
\]

In D0 this gives

\[
\delta\sigma=q\,u_1+\frac{q^3}{3!}\sigma_3+\cdots ,
\quad
A=\frac{q^2}{2!}A_2+\cdots ,
\quad
\psi=\frac{q^2}{2!}\psi_2+\cdots .
\]

The matcher reaction is even. The curvature probe is constant, and both
\(F(q)\) and \(V_J(q)\) are even. The label \(\tau\) is fixed and is not
\(\operatorname{sign}(q)\). D2 differs: its nonzero \(\tau q\) metric
tangent makes \(F_{1,\tau}\) nonzero at fixed \(\tau\).

## Taylor convention and complete order-two residual

Every coefficient uses the factorial convention

\[
\Phi(q)=\Phi_0+q\Phi_1+\frac{q^2}{2!}\Phi_2+O(q^3).
\]

In proper radial coordinate, \(a_0=\sqrt2\sin\rho\),
\(H_0=\cot\rho\), and

\[
u_1''+4\cot\rho\,u_1'+\mu_cu_1=0.
\]

The complete nonzero bulk source is quadratic scalar stress:

\[
S_{2,N}=Z_5\left(u_1'^2+\mu_cu_1^2\right),
\]

\[
S_{2,a}=Z_5\left(-u_1'^2+\mu_cu_1^2\right).
\]

Reflection gives

\[
S_{2,\sigma}=0.
\]

Because \(\alpha_1(1)=0\), the nonlinear trace recurrence gives
\(\alpha_2(1)=\psi_2(1)=0\). The scalar Dirichlet source also vanishes.

## KKT pairing and Fredholm condition

The adjoint generator has only a scalar component. Therefore

\[
\Omega_2
=
\int_0^{\pi/4}
4\sin^4\rho\,u_1S_{2,\sigma}\,d\rho
+\Omega_{2,\partial}.
\]

The bulk term is zero because \(S_{2,\sigma}=0\). The extended endpoint
pairing is zero because the adjoint vector has no matcher component and
\(u_1(\pi/4)=0\). Hence

\[
\boxed{\Omega_2=0}.
\]

This is the full KKT result; it is not an unweighted scalar \(L^2\)
substitution.

Existing controls have \(D_a=0\) at this order. An \(r_2\) or scalar-action
coefficient response first multiplies the order-one scalar at odd order.
No control coefficient is needed, selected, or left free.

## Constructive complement solution

Use fixed-\(h\) areal gauge \(\psi_2=0\). The lapse constraint gives

\[
\boxed{
A_2(\rho)=
-{Z_5\over12\kappa_1}\tan^2\rho
\left(u_1'^2+\mu_cu_1^2\right)
}.
\]

Reflection and complement normalization give \(\sigma_2=0\). The multiplier
reaction is generated from the exact P1+GHY momentum:

\[
\eta_2=-24\kappa_1A_2(\pi/4)
=2Z_5u_1'(\pi/4)^2.
\]

For \(Z_5/\kappa_1=1\), independent methods give

\[
\mu_c=29.43091835294756,\quad
A_2(\pi/4)=-6.93876695733808,\quad
\eta_2=166.530406976114.
\]

The 60-digit hypergeometric route and adaptive regular-pole shooting agree
on the 33-node \(A_2\) profile within \(1.2\times10^{-13}\). The inherited
positive complement gap remains \(64.0147366689857\).

## Noether compatibility

The exact off-shell identity remains

\[
a'E_a+\sigma'E_\sigma-N(E_N)'=0.
\]

At order one it reduces to the Jacobi equation. At order two, substitution
of \(A_2\) solves the Hamiltonian equation identically. Differentiating that
equation and using the Jacobi equation reduces the tangential Einstein
residual to zero:

\[
6\kappa_1
\left[
A_2-\frac12\cot\rho\,A_2'-\cot^2\rho\,A_2
\right]
-Z_5(-u_1'^2+\mu_cu_1^2)=0.
\]

At the cap center \(A_2=O(\rho^2)\). At B1,
\(\psi_2(1)=0\) and the matcher reaction cancels the canonical-momentum
endpoint current. Gauge fixing was applied only after deriving the
covariant identity.

## Interpretation and claim boundaries

Validated:

- D0 is second-order Fredholm solvable;
- \(\Phi_2\) is a regular fixed-\(h\) complement response;
- parity, Noether compatibility, and matcher compatibility hold;
- no even-order control unfolding is required.

Invalidated:

- importing D2 coefficients into D0;
- treating D1 as the fixed-\(h\) effective-potential family;
- adding an unlicensed D3 amplitude source;
- the claim that quadratic scalar stress by itself obstructs fixed-\(h\)
  second-order solvability.

Open:

- the order-three and higher Fredholm projections;
- \(\Phi_3,\Phi_4\);
- same-domain \(F_2,V_2\) and Einstein-frame interactions;
- local stability and every scale question.

The v6.30.3 blocker is resolved at order two by an explicit domain choice,
not silently deleted. v6.30.5 is permitted under
`BHSM_FIXED_H_NONLINEAR_FAMILY_HIGHER_ORDER_CONSTRUCTION_PERMITTED`.
v6.31 remains forbidden until the same-domain potential closes.
