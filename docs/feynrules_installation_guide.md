# FeynRules Installation Guide

Machine-readable artifact:

```text
artifacts/BHSM_feynrules_installation_status_v1_7.json
```

Supported environment variable:

```text
FEYNRULES_PATH
```

FeynRules should be obtained only from an official/legal source. The repository
does not bundle FeynRules and does not run the FeynRules load test unless a
Wolfram runtime is available.

Current status:

```text
feynrules_detected = false
load_test_attempted = false
load_test_passed = false
ready_for_validation = false
```

