# BHSM v15.57 — full Sobolev hybrid actualization
Let (s>11/2). On a seven-dimensional spatial slice this gives
(H^s\hookrightarrow C^2), so the pole data, material interface, and the
constraint-quotiented Euler--Dirac rank-loss event are well defined.  The
later refined trajectory shows that this event need not satisfy
\(\min_\chi(1+X_\eta^3)=0\). After the Einstein, eta, response, gauge,
and diffeomorphism constraints are imposed, denote the physical phase space
by

\[
 \mathcal X^s_{\rm phys}=\mathcal C^s/
 (\mathrm{Diff}^{s+1}_0\times\mathrm{Gauge}^{s+1}_0).
\]

At the firewall, two traces are Aether-equivalent when their degree,
orientation, FR parity, response-endpoint order, incidence, boundary
identities, and child cobordism class agree. The selected event component is
the single quotient class ({\mathcal I_*\}). The regular child interior
survives as the (B^4\times S^3) reconstruction carrier; this does not mean
that a metric, momentum, velocity, curvature, or local-energy coordinate is
transported through the event.

The already selected constrained reset state defines

\[
 \mathcal R_s:\{\mathcal I_*\}\longrightarrow\mathcal X^s_{\rm phys},
 \qquad \mathcal R_s(\mathcal I_*)=z_*.
\]

On the event basin (mathcal D_{\mathcal I_*}), its extension is constant:

\[
 \widehat{\mathcal R}_s(z)=z_*,
 \qquad D\widehat{\mathcal R}_s=0.
\]

Therefore the full return operator is

\[
 \mathcal P_s=\mathcal R_s\circ\mathcal E\circ\Phi_T,
 \qquad \mathcal P_s(z)=z_*\quad(z\in\mathcal D_{\mathcal I_*}),
\]

and

\[
 \operatorname{Fix}(\mathcal P_s|\mathcal D_{\mathcal I_*})=\{z_*\},
 \qquad D\mathcal P_s=0,
 \qquad r(D\mathcal P_s)=0.
\]

Thus every continuous perturbation in the selected event basin is captured
in one event in every (H^s) norm. The odd FR ray returns projectively with
multiplier one because parity is discrete, not a continuous tangent. The
Standard Model bundle returns to the same isomorphism class and its selected
zero-field conformal background returns exactly.

This is unique actualization on the selected degree-one negative-child hybrid
component. It does not assert that arbitrary smooth initial data outside that
event basin reach the firewall.
