# Phase Three-N Runtime Scripts

These scripts inspect local runtime availability for the bounded minimal
collider-interface FeynRules path.

They do not install Mathematica, bypass licenses, fabricate validation logs,
or generate fake event files. Logs from real local runs are written under:

```text
runs/phase_three_n/
```

The committed repository artifacts record whether commands were actually
attempted and whether readiness gates passed.

