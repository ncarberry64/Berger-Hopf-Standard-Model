# N12 forward terminal-chart reachability gate

## Result

The local continuum singular-event theorem is closed, but the existing
retained action does not yet prove that any certified forward complete-child
history reaches its terminal chart before an existing physical-domain exit.
Nor does it prove that no such history exists.

This is a localization of the first global proof obstruction, not a new
physical equation and not a numerical nonconvergence claim.

## What is already controlled

For the certified continuum child:

- the initial ordered eigenvalue is strictly positive;
- the first analytic continuum-flow interval is event-free;
- every solution that enters the certified positive-lambda terminal chart
  hits the singular event in finite forward time;
- the event-to-complete-child reset is a regular set-valued relation;
- the maximal retained flow exists until strong-norm blowup, an existing
  physical-domain boundary, Dirac inverse loss, or event/eigenline stopping;
- physical time has the single existing positive-lapse, positive-duration
  orientation; formal reversal is an algebraic/chiral pairing inside that
  domain, not a competing temporal sector or gauge quotient.

The stored positive-duration witness is endpoint evidence only. Its final
ordered eigenvalue is larger than its initial value. It neither proves nor
excludes a later terminal hit.

## Why the local theorem does not globalize from current action assets

Let `u=e_ord^2`. In the terminal chart,

    u_dot = 2 c_psi b_psi + 2 e_ord R(Y)

has a certified negative sign. Away from that chart, no component-restricted
integrated upper bound for `u_dot` has been derived. Exact formal reflection
rules out promoting a single global sign on a reflection-invariant union of
paired Cauchy states; it does not rule out a theorem on one separately
controlled state component. This is not a choice of physical time direction.

The standard recurrence/global-continuation shortcut also fails with the
currently owned charges. The constraint-reduced Legendre energy is identically
zero. The unreduced ADM velocity form has an explicit unbounded zero-energy
sequence, so its level sets do not control the strong topology. No complete
boundary-improved child Hamiltonian, compact invariant child shell,
action-selected reference cycle, or return degree/index is available.

Consequently the existing theorems produce only the exact alternative

    terminal-chart entry / existing domain exit / strong blowup /
    Dirac or eigenline degeneration,

without selecting an outcome.

## First localized retained-action obstruction

The first missing object is an action-owned global control on at least one
unquotiented complete-child component. A sufficient lemma would be either:

1. a compact forward-trapping/coercive estimate with uniform existing domain
   margins, plus a component-restricted integrated transport estimate forcing
   entry into the certified terminal chart; or
2. a direct analytic continuation cover which proves entry into that chart or
   certifies the first existing physical-domain exit.

No experimental value, solver basin, formal reflection quotient, trajectory
sampling, parent substitution, or new event sign may supply this object.

Therefore the honest hard outcome is

    FORWARD_TERMINAL_CHART_REACHABILITY_NOT_YET_PROVED;
    FIRST_RETAINED_ACTION_GLOBAL_CONTROL_OBSTRUCTION_LOCALIZED.

`FULL_BHSM_COMPLETE` remains false. The matched-parent `Q_xi`, relative
energy, observable, blind prediction, and manuscript promotion remain locked.
