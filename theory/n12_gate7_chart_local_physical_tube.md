# Chart-local physical-tube promotion

Status: `INTERVAL13_PHYSICAL_TUBE_CLOSED_REMAINDER_ENCLOSURE_NOT_VIABLE`.

## Reproduced result

Interval 13 now has a complete augmented physical-tube atlas at the original
radii, using eight closed time cells and the two inherited moving-link charts.
Every cell independently reproduced identical bytes. The maximum inverse
defect is 0.6353864808947848; the maximum self-map bound is
0.7041038838508931. The augmented descriptor image gives a uniform physical
normalization lower bound greater than 3.047800796095928e-8. No endpoint was
added, and no Layer-A calculation was repeated. The complete rate is smooth
on this image by the inherited branch, the local inverse, positive action
inertia and positive physical norm.

All three actual HS evaluation sites also have complete 99-output mixed-rate
enclosures, with every implicit border, normalization and descriptor term.
The tighter calculation imports their original primal and first-variation
certificates and contracts incidence maps before direction support. Its
mixed-rate norm bounds are approximately 14.7191, 24028886.7014 and 13.1997
at the left endpoint, actual midpoint and right endpoint. These values are
upper bounds from coordinate enclosures, not observed physical curvature.
They and the complete remainder assembly reproduced byte-for-byte.

The signed interval source includes the non-affine midpoint incidence,
subtracts the booked LL/LT functions with the correct factor two, and is
transported to all 357 causal destination nodes with the frozen-map error.
The isolated interval-13 upper bounds are

```text
kappa_L,13 <= 1.3113695368730564e16
kappa_T,13 <= 1.2540364054668333e19.
```

These displays are rounded; certificate rationals control. They do not meet
the current sufficient targets. They are not global kappas, lower bounds,
evidence of growth, or evidence of decay. The first broader radial-coordinate
calculation was even weaker (about 4.73e70 and 4.50e73).
The unresolved object is a sufficiently sharp signed remainder, especially
through the actual midpoint, not an uncovered interval-13 tube.

Accordingly the conditional extension to intervals 14--18 has not been made.
The next calculation must retain shared state/direction coefficients and
mixed monomials through the implicit solves, descriptor normalization,
booked subtraction and output contraction. Reusing a value/first-derivative
certificate improves the enclosure but does not recover correlations already
lost by coordinate boxing. The existing endpoint-19 shared mixed machinery
remains frozen and reusable on its recorded domain. There is no reason here
to add endpoints, shrink radii, return to a fixed inverse, or infer decay.

Evidence is under
`artifacts/flagship_integration/gate7_physical_tube_20260924/`.
Gate 7 and global contraction remain open.

The owner freezes `452c80a7`: Layer A is closed for the endpoint-13 through
endpoint-19 evaluation atlas. Its identity, D4 center-line links, gaps,
endpoint domains and actual-HS midpoint domains are inputs here. They are
not recomputed. No fixed-anchor continuation or new endpoint is introduced.

## Exact image and local chart obligation

In the existing weighted augmented 99 coordinates, let z0,z1 be the two
original affine endpoint domains at the unchanged rL,rT, and let f0,f1 be
their paired uniform physical rates. With the same endpoint parameters in
every occurrence, the cubic Hermite image is

```text
X(tau) = a z0 + b z1 + c f0 + d f1,
a = 1-3 tau^2+2 tau^3,       b = 3 tau^2-2 tau^3,
c = h(tau-2 tau^2+tau^3),   d = h(-tau^2+tau^3).
```

At tau=1/2 this is exactly `(z0+z1)/2+h(f0-f1)/8`, the actual HS midpoint.
This is the retained dense-output image, not a separately asserted exact
ODE trajectory. Uniform endpoint-rate intervals are auxiliary enclosing
errors; their unknown correlations with endpoint parameters are not invented.
The descriptor remains the 99th coordinate and is not included in the
98-component physical normalization norm.

On each half interval, let L(s),p(s),R(s) be the already-certified affine
state line, eigenpair predictor and numerical inverse; s=2 tau on the first
half and s=2 tau-1 on the second. Restrict tau to a closed time cell. Define
delta=X_raw(tau)-L(s) and the radial extension

```text
Z(sigma,tau,theta) = L(s)+sigma delta(tau,theta),  0<=sigma<=1.
```

Every actual raw physical image in the cell occurs at sigma=1. Its center-line
base occurs at sigma=0. The same tau parameter occurs in L, p and R.
Partitioning tau does not shrink either physical radius or either complete
transverse ball. A chart certificate must cover all endpoint perturbations
and enclosing rate errors in its cell, not merely the unperturbed curve.

The new action quantity is exclusively the radial off-center correction

```text
R(s) (H(Z)-H(L(s))) eta
 = integral_0^sigma R(s) D3S(L(s)+t delta)[e_red,eta,delta] dt.
```

Use eta=p(s) for the new residual and every vector in the witness box for
the derivative defect. The norm border and nonlinear p/lambda variation
remain the exact inherited algebra. No D4 identity calculation is repeated.
The radial argument must stay in the positive action domain; its global
inertia lower bound is recorded with every contraction.

If qA,Y_A are the frozen center-line base defect and residual in weights w,
M bounds the normalized new radial Hessian action, and r is a proposed
componentwise eigenpair radius, sufficient row bounds are

```text
v_i = w_i [qA max_j(r_j/w_j) + sum_j M_ij (r_j/w_j)] + N_i(r),
Y_i = w_i Y_A + new_radial_residual_i,
q = max_i v_i/r_i < 1,
max_i (Y_i+v_i)/r_i < 1.
```

N(r) is the existing quadratic eigenpair/normalization-border bound.
Every r must contain the frozen center-line witness; changing r changes
only the proof enclosure. Proposal iterations have no physical authority:
only the final outward rational inequalities certify a cell.

## Coverage and handoff

A complete cover needs closed cells whose union is [0,1], not a collection
of successful samples. A per-cell certificate must be joined to the exact
same endpoint/rate/HS operands by hashes. The midpoint occurrence must be
the actual-HS domain already owned by Layer A.

For componentwise rebalanced witnesses, neighboring boxes need not be nested.
The rigorous handoff instead uses their common radial homotopy: at a shared
tau and fixed physical parameters both begin at the same frozen Layer-A
root at sigma=0 and follow the same Z(sigma) with nonsingular bordered
derivative. Local uniqueness and continuation imply agreement at sigma=1.
At tau=1/2 the original complete midpoint chart is also an overlap authority.
A bare overlap of coordinate hulls would not prove equality of roots.

Raw eigenchart coverage is still distinct from physical-rate authority.
An augmented physical-tube certificate must additionally bind the descriptor
image and all conditions needed to define the rate on those local domains.
No successful cell is extrapolated to an uncovered cell or another interval.

## Exact physical-rate promotion terms

Reuse the existing machinery with newly established domain authority:

| Object | Required same-domain addition |
|---|---|
| Eigenpair values | Off-center witness and inverse bound on the complete local tube; retain normalized oriented p and lambda |
| Eigenline first/mixed jets | Solve the differentiated bordered equations with S3 and S4 contractions; retain `-p_u.p_v` in the mixed norm border |
| Physical response | Complete forcing from the gradient and configuration-Hessian expression, including moving configuration legs; solve `K q_uv=F_uv-K_uv q-K_u q_v-K_v q_u` |
| Response orthogonality | Retain `p.h=0`, both first derivatives, and `p.h_uv+p_u.h_v+p_v.h_u+p_uv.h=0` |
| Descriptor numerator | All 16 mixed assignments in each `C=S3[p,p,a]` and `J=S3[p,p,d]`; include S5 and every moving-leg S4/S3 term |
| Descriptor incidence | Preserve s,s_u,s_v,s_uv from the actual augmented image; endpoint affine s_uv=0 does not imply midpoint/image s_uv=0 |
| Normalization | Use the same N and its jets, with a same-domain positive norm; cancel a common positive response border only when its sign is certified there |
| Output and quotient | Differentiate genuinely state-dependent physical readouts; keep the external frozen Newton/test/causal matrices fixed, with their certified arithmetic errors |

Endpoint-19 mixed eigenline, response and complete-rate certificates are
reusable only on their recorded original domain and direction families.
Their algorithms can be applied elsewhere; their numerical bounds cannot be
copied to a new child chart. A value model's remainder is not a derivative.

A pointwise moving numerical preconditioner can be used in the residual
solve `z=zhat+(I-RK)(z-zhat)+R(b-K zhat)`. The physical derivative equations
are derived from Kz=b. Differentiating R is not an extra physical force term.
If one instead differentiates a numerical representation, all derivative
terms of that representation must be retained and reconciled with Kz=b.

## Non-affine incidence and signed remainder

For endpoint-direction images b0(u),b1(u), the true midpoint jets are

```text
m_u = (b0(u)+b1(u))/2 + h(A0 b0(u)-A1 b1(u))/8,
m_uv = h(H0[b0(u),b0(v)]-H1[b1(u),b1(v)])/8.
```

For every dense-output state map, compose the physical Hessian as
`D2(f o X)[u,v] = H_f[X_u,X_v]+A_f X_uv`. The complete HS source is

```text
r_uv = -h/6 (H0[b0u,b0v] + 4 Hm[m_u,m_v]
                         + 4 Am m_uv + H1[b1u,b1v]).
```

The `4 Am m_uv` term is mandatory. In particular, a field Hessian enclosed
on a midpoint chart is not yet the Hessian of the HS composition.
The shared incidence implementation preserves all 99 outputs and these
terms. It supplies algebra, not missing physical operands.

On a completed interval i, form the signed local source and subtract the
matching booked LL/LT quadratic derivative before support and causal
aggregation. There is no Taylor half in the Hessian; the booked quadratic
contributes 2Q. On interval 14, apply its booked right-column entry mask
(73,14) to the matching local Jacobian derivative, as in the existing
[uniform remainder formulation](n12_gate7_uniform_remainder_formulation.md).

For each output node k, the remaining operator is

```text
Pi_beta,k / r_beta * [-G_(k,i) B_i r_i,uv - booked_i,uv],
```

with the same shared variables, certified projections and frozen-map
arithmetic. Accumulate the signed local sources before taking any support
that the representation permits. Positive coefficient bounds cannot be
subtracted from other positive bounds as a substitute for booked functions.
Only the resulting complete normalized operator enters kappa_L/kappa_T.

The sufficient current targets remain kappa_L < 0.6542911248664067 and
kappa_T < 0.7452414479024602 (display values; ledger rationals control).
An isolated interval contribution does not close the global ledger. A failed
upper-bound screen establishes insufficiency of that enclosure, not physical
decay or loss of a child. The interval-13 source now exists, but its isolated
enclosure is not viable. Both global kappas remain uncomputed.
