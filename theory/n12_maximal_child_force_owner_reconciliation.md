# N12 maximal-child force owner reconciliation

Status: `MAXIMAL_HISTORY_COEFFICIENT_ORACLE_AND_FIRST_JET_SINGLE_FORCE_OWNER`.

The action-owned maximal source domain is already unique.  For every
event-generated complete child, the retained maximal flow supplies its
forward interval.  A later certified event uses the retained reset graph;
an infinite or excluded maximal end uses the Friedrichs closure of the
nonnegative minimal form.  No Robin parameter or validation cutoff remains.

The common source incidence is also assembled.  Gauge, ghost, rank-16 Weyl,
unit and complex Hubbard--Stratonovich pair/contact vertices live in one
domain-parametric direct sum and reproduce their retained periodic lineage
exactly.  The inverse-free Weyl solver then returns value, first jet, and
second jet from any supplied action-owned coefficient pencil.

For the first heat-minus-zeta force root, the intrinsic quotient theorem
removes another stale dependency: exact time invariance makes the raw
constraint-tangent KKT equation equivalent to the physical quotient root.
An explicit hybrid time generator is not required until Hessian mode
classification.

Therefore the single earliest missing force input is

`xi -> (Y_xi(tau), P_xi, D_xi P_xi)`

on a nonempty regular reset stratum, or the equivalent exterior bundle

`xi -> (M_C(z;xi), D_xi M_C(z;xi))`.

This is the action-owned maximal-history coefficient realization and first
geometry jet.  The existing positive-duration witness and two certified
chords do not supply it because their endpoints are validation cutoffs, not
events or canonical exits.  The local expanding Briot--Bouquet branch also
does not supply it because no reset-to-asymptotic intersection theorem is
proved.

Once this single input is certified, the already-derived operator cotangent,
moving-endpoint projection, Euler--Dirac adjoint pullback, and raw bordered
root test evaluate Gate 7 without choosing a reset representative.  If the
force is nonzero, the second operator jet and intrinsic/bordered geometry KKT
Hessian remain required for the corrected saddle.

No chord 3, terminal recurrence, endpoint, selector, scale, fit, action term,
time direction, or new gate is introduced.
