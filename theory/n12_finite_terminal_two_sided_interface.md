# N12 finite-terminal two-sided forward event/child interface

The certified 58-row root is a double-event incidence pair.  Its original child
half has `c_psi b_psi<0` and is approached in finite positive forward time.
The event half has `c_psi b_psi>0` and moves away from the singular locus in
positive forward time.  The latter sign is certified by the same directed
Decimal symmetric-difference, retained `D5`, selected-line graph, and exact
product-rule transfer used on the incoming half.

On the locus where both selected event functions vanish, the retained historical reset
equations are swap invariant.  The constraint blocks exchange, while the
zero trace and canonical-momentum differences only change sign.  Hence if
`(E_*,C_*)` is the certified root, `(C_*,E_*)` is also a reset pair.  It is the
correct new-event to new-child orientation.  Here and below, "reset" names the
historical API only; its physical semantic alias is the forward
event-to-new-child glue.

The reset relation has dimension 139.  Its event-coordinate derivative has
the certified structural rank 32: 25 constraint rows, one simple selected
event row, four trace rows, and two canonical-momentum rows, with independence
supplied by the already-certified Legendre, eigenline, and canonical-lift
margins.  Therefore the child projection of the reset tangent has rank

`139-(98-32)=73`,

which is the dimension of the constrained child manifold.  The projection is
locally a submersion.  Constraint propagation places the incoming terminal
germ in that manifold, so every sufficiently near pre-event point on the germ
lifts to at least one retained event-to-child pair.  Thus an event
`E0(lambda_0)` creates a child `C1=Y_-(lambda_0)`.  The retained flow carries
that child forward to the distinct new event `E1=C_*` after a finite `T>0`.
The swapped incidence then supplies the forward event-to-new-child glue from
`E1=C_*` to the new outgoing child `C2=E_*`.

This proves a nonempty local family with chronology
`E0 -> C1 ->[T>0] E1 -> C2` and action-owned endpoint graphs.  It does not
select a member, prove universal terminal reachability, identify `E1` with
`E0`, or introduce recurrence, an action term, scale, chord, or gate.  Gate 7
advances to construction of the compact finite-history operator and its first
physical event/child-quotient jet on this family, followed by the existing
heat-minus-zeta force root.

`FULL_BHSM_COMPLETE=false`.
