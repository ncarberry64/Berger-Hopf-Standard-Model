# Corrected complete forward-history and particle-class gates

The BHSM particle is the complete forward history of the encapsulated child,
not a privileged Cauchy representative.  Encapsulation is a one-time birth
event.  Reset remains the existing de-encapsulation/interaction relation; it
is not a recurrence condition for identity.

## Gate 5: maximal admissible forward history

Let `C_infinity` be the certified continuum complete child in the existing
open strong child domain `U_infinity`.  The local Galerkin/Duhamel theorem
gives a unique positive-duration continuum Euler--Dirac flow.  The recentered
maximal-flow theorem then gives a unique maximal solution

    Y_C : [0,T_max) -> U_infinity.

If `T_max` is finite, the strong norm diverges, an existing metric/lapse/eta/
inertia/trace/gauge margin tends to zero, or the gauge-fixed Euler--Dirac
inverse degenerates.  Otherwise the same retained local theorem recenters and
extends the history.  This is exactly the required continuation through the
ordinary propagation domain until an existing strong/admissible-domain exit.
No return to the encapsulation surface is required.

The already-certified forward covers independently demonstrate nontrivial
propagation with positive ordered-event, eta, boundary, Dirac, constraint, and
continuum-tail margins.  They support the theorem but are not needed to turn
maximal-flow existence into a recurrence result.

Therefore corrected Gate 5 is closed.

## Gate 6: history class with physical state freedom

For each admissible complete child representative `C`, let `Phi_C` denote its
unique maximal forward history.  Define the generic encapsulated-child class
map by

    C -> [Phi_C]_(existing gauge, time-origin),

typed by the certified continuum event-to-child relation component, fixed
child-domain topology and boundary incidence, forward orientation, and all
retained conserved or discrete labels.  Two histories belong to the same
generic class when they lie in the same connected component of this
action-defined admissible history space and share those labels, modulo only
the existing gauge and time-origin quotient.

Smooth fixed-domain evolution cannot change topology or boundary incidence;
such a change is an existing exit/de-encapsulation event.  Constraint and
trace incidence propagate under the retained flow.  Legitimate tangent
directions are not quotiented: they remain physical position, momentum,
orientation, phase, and other state/history freedom.

This closes the generic action-defined particle/history-class architecture.
It does not manufacture species labels.  Gauge representation, flavor,
family, mass, CKM, neutral, and PMNS classification remain the downstream
sector Gates 7--10.

The unfinished first `1e-8` Hermite shadowing construction is preserved as a
stronger supporting forward-propagation theorem and post-closure backlog.  It
is no longer a prerequisite for Gates 5 or 6.
