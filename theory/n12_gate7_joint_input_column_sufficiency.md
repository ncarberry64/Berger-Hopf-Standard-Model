# Interval 13: what joint dependence can be recovered from the saved jets

This is a bounded **saved-evidence sufficiency test**, not a certificate of
the fully coupled physical response requested by the owner. It evaluates no
action derivatives. It preserves the old Cartesian obstruction, the original
domain, and every completed numerical result. No physical gate is promoted.

## The theorem target is existential, with its physical readout obligations

The recovered `BHSM_Gate7_Codex_Owner_Decision_Protocol.json` says explicitly
that repository proofs and retained definitions override its historical
frontier. Its rule against a fabricated selector, universal success over
unrelated children, or local-to-global promotion remains applicable.

There is a substantive later scope clarification in `theory/gate_ledger.md`,
under the Norman finite-encapsulation physical-domain reclassification:
the exact reset image is **post-encapsulation and need not hit the event
again**. Finite encapsulation existence is recorded as closed locally;
universal reachability and post-event recurrence are not required. The
restriction is explicitly owner-authorized, not an action-derived invariant.
This supersedes interpreting the older protocol as a demand for recurrence
of every, or even a selected, post-event child. It does not remove any
neighborhood, continuum, operator, or physical-readout obligation.

The admissible relation remains set-valued. The relevant component contains
an action-generated complete child, with positive lapse and duration and
the retained constraints, orientation, simplicity, inertia, trace, gauge,
eta, and Euler-Dirac domain margins. No new separator is proved here, and
no unfavorable direction is excluded. The current operator target remains
the complete action-owned finite-history realization, or equivalent
two-sided Weyl-Calderon oracle, with endpoint form and required geometry
jets; then the signed joint projected force, KKT root, and constrained
physical Hessian. See `docs/BHSM_1_0_DEFINITION_OF_DONE.md`,
`theory/n12_joint_finite_history_operator_data_gate.md`, and
`theory/n12_gate7_joint_kkt_information_gate.md`.

## The unchanged local problem

Use interval 13, right trial column 14, first affected output node 14.
Let `e` be its frozen 99-component trial, `h` its stored positive step,
`P=R^{-1}T` its unchanged frozen output map, `a` the stored node-14 axis,
and `Q=I-aa^T`. Exact unit length of `a` is not assumed.

Write `A=DF(Z_R)e`, `M=DF(Z_M)`, and retain the original exact midpoint
direction `w0`. The full right column is

    D = u_14 - P e + (h/6) P A
        + (2h/3) P [ DF(Z_M)w0 + M(e/2 - h A/8 - w0) ].

This identity includes the chain direction and center shift. It contains
the full uniform `M`, not its point approximation. It does not identify
moving frames or the physical quotient. No later interval can cancel this
first affected causal output.

The endpoints retain `z_i + E_i(a_i ell_i + t_i)` with
`|ell_i|<=r_L`, `a_i^T t_i=0`, `||t_i||<=r_T`, using

    r_L = 9.160189721418071e-7,
    r_T = 1.9369612593815614e-9.

The existing endpoint outer parametrization has 75 scaled directions.
The actual midpoint's certified outer parametrization has 249: two scalar
interval groups, two 74-dimensional Euclidean groups, and a 99-dimensional
box group. The latter retains the endpoint-field remainder. These outer
sets cover the original variations; they are not proved to equal the
action-owned admissible component. No radii, domain, or proof parameters
are changed in this test.

## The constructed and evaluated enclosure

For the midpoint put `B(xi)=DF(z0+V xi)w0`. The paired evidence supplies
the verified anchor `B0`, point Hessian columns, and a uniform interval
matrix `[H]` containing `D2F(z)[w0,V]` on the same convex outer family.
All seven implicit solves and scalar contractions, normalization, branch
orientation, and inertia checks belong to the bound's source chain.
The mean-value identity gives

    B(xi) = B0 + H0 xi + Ebar xi,
    Ebar = integral_0^1 (H(z0+t V xi)-H0) dt in [H]-H0.

`H0` is the exact midpoint of the saved point-Hessian balls. Their
uncertainty remains in `[H]-H0`; uncertainty in `B0` is also retained.
This uses the full uniform Hessian remainder, without assuming a small
third derivative or replacing a neighborhood by point evidence. Apply
`S=(2h/3)QP` before ranges. The same construction is applied to the
75-direction endpoint action, with its coefficient
`Q[(h/6)P-(h^2/12)PM]`, preserving its point uncertainty and full
uniform remainder. These two expansions are substituted into the complete
column above. Product-group supports and the Euclidean output norm give
an outward upper bound for that local transverse column.

This retains **one common input vector across the output coordinates of
each expansion**. It does **not** recover the same-input response graph
through all implicit solves. The saved `uniform_solve_i`, residual,
normalization and Hessian arrays contain interval entries, not functions
of the common parameters. Nor does this construction recover the exact
endpoint-to-midpoint attainable relation from its outer product domain.
These limitations are explicit false fields in the result, not silently
assumed certificates.

## A rigorous kill test for the remaining relaxation

For the 249-dimensional midpoint parameter choose one exact vector:
scalar and box entries are 1; entries in each Euclidean group are `1/9`.
Each Euclidean squared norm is `74/81<=1`. This is a witness in the
declared **outer parameter domain**, not an asserted physical state.

Let `rho_ik` be downward-rounded exact saved coefficient radii. At this
same input, the independent coefficient relaxation permits two matrices
whose difference is twice a rowwise sign choice times `rho_ik`. Put

    d_i = sign(S_73,i) sum_k rho_ik |xi_k|.

The two projected columns differ by `2 S d`, with all other relaxed
operands fixed. Thus at least one has norm at least `|(S d)_73|`,
regardless of the common center. Outward evaluation proves

    sup_relaxation ||Q D|| >= 48.720352347851 > 1.

The input `r_T u_14` is admissible in the original two-radius trial ball,
as exactly proved in PR #440. Input and output `r_T` cancel. The relaxed
contraction margin is therefore at most **-47.720352347851**. This lower
bound is for the tested coefficient relaxation; it is neither a physical
derivative lower bound nor an improvement of the full certification gap.

The signed midpoint point-linearization upper bound is approximately
`0.0001742429`; its retained uniform-minus-point remainder upper bound is
approximately `53.21519326`. The exact rational bounds and complete local
column upper bound are in the generated record. The small point term
cannot justify deleting the much larger remainder.

The final complete-column calculation also uses the endpoint's common
75-direction parameter before taking ranges. Its point-linearization
contribution has norm upper bound `0.00009757655`, and its retained
uniform-minus-point remainder has bound `2.81404770`. Anchor uncertainty
and the original center-shift terms contribute a combined bound
`0.00001560618`. The complete local transverse column has outward norm
upper bound `55.95756527`. These are separate upper bounds, not separately
attained contributions, and their sum is not the complete bound. Even a
perfect midpoint estimate would leave the endpoint remainder to address.
All statements here concern the fixed-frame local column; the complete
history's boundary form and physical quotient still require their own
certificates.

## Which Cartesian combinations have been excluded?

In a true fixed-coefficient representation `y=c+H0 xi`, a covector `v`
with `v^T H0=0` forces `v^T(y-c)=0`; independent corners violating that
identity are excluded. The focused analytic test verifies this elementary
case. It is **not** a proof that interval 13's two particular old corners
are physically excluded.

For interval 13, no exclusion of either particular `c +/- rho_73 u_73`
from PR #440 is proved. They cannot simply be declared nonphysical.
The calculation instead proves that a different explicit bad pair survives
the retained independent coefficient errors at one common outer input.
Reintroducing a shared input after the coefficient errors became boxes
therefore does not repair the method. Whether intersecting all old bounds
and the new outer set excludes particular corners is also unproved; the
lower bound here is not claimed for that uncomputed intersection.

## Smallest missing calculation and why it matters

The next bounded target is one **joint projected residual support bound**
for this same right column on its unchanged two-endpoint neighborhood.
Use the retained point jets as Taylor anchors for the shared parameter
vector, and retain common parameters through the selected eigenline,
bordered response, their differentiated systems, and original normalization.
The constraints include `psi^T psi=1`, `psi^T hard=0` and their derivatives,
the auxiliary border and eigenvalue contractions; separate interval
certificates for each variable are insufficient.

The first scalar target should be a separating output covector, such as
the normalized `Q u_73` from the old obstruction. Bound its support of
the **complete** column, including the endpoint chain and midpoint map,
before taking coordinate ranges. A coupled Taylor/residual certificate
must bound, uniformly over that graph, the projected error of the saved
joint point model. In schematic form, for the same input theta and exact
coupled solution U(theta), prove

    |v^T [Q D(theta,U(theta)) - J_anchor(theta)]| <= epsilon_v.

The function `J_anchor` includes its certified anchor uncertainty;
`epsilon_v` includes the nonlinear neighborhood remainder, common
normalization, full chain, center shift, boundary terms, and inertia/
branch/domain guarantees. No new physical action derivative is justified
until that residual form is implemented and its required input bounds
are enumerated. Existing saved matrix radii do not supply a sufficiently
small `epsilon_v`: the witnessed relaxation has gain above 48.72.
This is the precise missing bound, not a request to recompute all Hessians.
An implementable residual formulation is to stack the unchanged implicit
equations as `G(theta,U)=0`, including the HS midpoint relation and every
normalization identity. Build `Uhat(theta)` from the saved certified point
jets. For an anchor inverse `J0^{-1}`, the correction must satisfy

    e = -J0^{-1} G(theta,Uhat(theta))
        + [I-J0^{-1} integral_0^1 D_U G(theta,Uhat(theta)+t e) dt] e.

The missing numerical datum is a uniform support bound for this
preconditioned residual and correction after the chosen output pullback,
with the **same theta** in every term, plus a valid inclusion bound for
`e`. Evaluating its coordinates as independent interval residuals would
repeat the failure proved above. Start with the one scalar output support;
reuse all anchor solves. If additional action contractions are needed,
compute only those required for this joint residual remainder on interval
13, not new point-Hessian inventories or another seed bootstrap. There is
currently no executable, validated coupled-residual evaluator to launch.
Joint residual support can remove the independent row-sign choices that
produce the witnessed floor. Whether it gives the required reduction is
unknown. A scalar success is only an obstruction screen; full local norm,
all required columns, ordered-event transport and the complete certificate
must then pass before scaling or closure. No further refinement is launched.

## Certification and separate completion gates

For `i in {L,T}`, the compatible two-radius majorant must obey

    B_i(r)=Y_i+sum_j Z_ij r_j+C_i r_L^2+2 M_i r_L r_T+T_i r_T^2,
    r_i-B_i(r)>0,
    q(r)=max_i sum_j (partial_j B_i(r)) r_j/r_i < 1.

The stored polynomial margins are `2.9861380823659324e-7` and
`6.314316577071288e-10`; its algebraic `q` is about
`0.7974712031541378`. These numbers are conditional on those coefficients
bounding the same physical map and tube. The full physical global margin
is **unknown**. Neither local column widths nor the new negative relaxed
gain are substituted for a global `Z` coefficient.

Eigenpair inclusion, full-neighborhood and full-history certification,
physical quotient identification, Gate 7 and full BHSM completion remain
distinct. Continuum right-inverse/strong-graph closure, action-owned state
and observable maps, physical scale and no-fit benchmark/prediction ledger
remain open. Updating the existing Museum engines with those resulting
physical data and completing the submission manuscript remain downstream.

Run from the repository root:

    C:/Python314/python.exe scripts/evaluate_n12_gate7_joint_input_column.py --evidence-root ../BHSM-ae32-crossing-correction --out tmp/bhsm_joint_input_column_20260913/resume.json

The output must be new. Reuse the paired results instead of rerunning this
entry point merely to read status. It invokes no worker or derivative
campaign. The missing coupled residual certificate is the next mathematical
entry point, not an automatic numerical continuation.
