# Gate-7 open reset-subball propagation through the 1222 core

The compact-family reserve audit found two rows whose stored binary64 endpoint-tube upper bound equals the stored local radius.  Equality of the rounded values does not imply equality of the retained computation.  Both rows were originally formed by adding a binary64 ideal-tube value and a binary64 center-rounding defect in high-precision `Decimal` arithmetic, followed only afterward by upward conversion for storage.

Replaying global rows 791 and 1064 from their stored predecessor centers reproduces the selected allocation, positive descriptor step, branch, and local radius exactly.  The unrounded endpoint tubes lie strictly below their local radii.  Directed conversion gives a positive reserve lower bound at both rows without changing a center, step, history, action term, or domain.

Let `rho_open` be one quarter of the smallest positive initial-radius allowance from the other 1220 rows.  This is a derived proof radius and has no physical role.  If `G_i` is the retained cumulative state-Jacobi upper bound and `eta` is the reset normal graph, then

`delta_i <= G_i (1 + ||D eta||) rho_open`.

After replacing only the two rounded-zero entries by their directed exact-replay lower bounds, this displacement is strictly below every one of the 1222 output reserves.  Therefore the full open 72-dimensional reset-quotient subball propagates through the retained finite core while remaining inside all certified regular-domain balls.

The same variational majorant gives an inverse singular-value bound for the regular flow: `sigma_min(D Phi_i) >= 1/G_i`.  Composed with the uniform reset-quotient first-jet lower bound, the first quotient jet remains strictly nonzero through the terminal core node.

This closes reset-to-1222-core propagation only.  The next geometric owner is propagation of this same open family from the core endpoint to the existing NHIM capture surface or to a first actual retained event/canonical stop.
