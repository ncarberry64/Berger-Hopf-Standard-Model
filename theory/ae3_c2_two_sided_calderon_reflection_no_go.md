# AE3 current-C2 two-sided Calderon reflection no-go

The AE3 reciprocal profile is odd about the enclosure interface,
`sigma(pi/2-chi)=-sigma(chi)`. The round radius and localization weight are
even there. Consequently reflection maps the regular exterior gauge/ghost
problem unitarily to the derived interior problem. In half-cap coordinates,
`rho_inside=2 chi` and `rho_exterior=pi-2 chi`, so the electric, transverse
spatial, and radial-gradient weights agree. The interface Dirichlet trace is
fixed, the two regular pole conditions are exchanged, and the constraint,
gauge-fixing, and Faddeev--Popov blocks carry through the same reset. Thus the
complete regular Maxwell--BRST domain, not only its transverse static slice,
is conjugate:

```text
N_exterior(omega,k)=N_inside(omega,k),
N_total=2 N_inside.
```

The static and frequency derivatives both double, so the two-sided ratio is
unchanged:

```text
Z_t/Z_s = 0.590609601652908 < 1.
```

Thus the unique retained coefficient-free route selected by the preceding
screen is evaluated and fails. No retained AE3 action/domain route now yields
a local Lorentzian Maxwell residue. The resulting finite physical-theory
decision surface is: derive a more microscopic fixed boundary/collar action,
derive an action-selected nonreflection exterior or independent boundary-field
domain, or retain AE3 and accept that this parent trace has no local photon
sector. This theorem chooses none of those new physical inputs.

The exact trigonometric identities provide the proof; the machine certificate
also samples them densely as an implementation audit. No periodic frequency,
surface contact term, gauge normalization, coupling, cone adjustment, or
fitted residue is introduced.
