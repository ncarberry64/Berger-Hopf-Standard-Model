# N=12 bordered graph uniform nonlinear defect

Status: `UNIFORM_NONLINEAR_GRAPH_DEFECT_CERTIFIED_EXISTENCE_SCALE`.

Scope: supplementary quantitative refinement of the nonrealized expanding
formation branch; not the physical Gate-7 owner.

This theorem closes the operator-defect part of the asymptotic capture owner
on a positive, deliberately conservative subball.  It does not close the
inhomogeneous lower-weight remainder or reset entry.

Let `rho_geom<0.1` be the certified geometric product-ball radius.  In the
common product norm, every row of the ten-variable local action map has
Euclidean norm below 200.  Indeed, the largest rows are the spatial
derivative and shift-derivative rows, bounded respectively by
`sqrt(12)*46<160` and `sqrt(12)*48<167`.  Therefore every local perturbation
has absolute value below 20 throughout the geometric ball.

After expansion, the exact weight-seven density contains fewer than 100
kinematic monomials, integer coefficients below 49, and polynomial degree at
most four in the local variables.  Four differentiations increase a
coefficient by at most `4!=24`.  The exponential factor is at most
`exp(40)<1e18`.  The round `tan(x)` and `cot(x)` powers have total degree at
most two, and the `cos(x)^3 sin(x)^3` density bounds every such product by
one on `(0,pi/4)`.  Hence `1e30` bounds every density-weighted local third or
fourth derivative coefficient on the full geometric ball.

Contracting four local slots costs at most `200^4`, and summing all local
indices costs at most `10^4`.  Thus `1e50` is an inflated uniform bound for
each projected `D4 L7` entry; the same number also bounds projected `D3 L7`.
Differentiating the explicit `7 H` and `5 H` recurrence products adds the two
Hubble--`D3 L7` product-rule terms.  Since `|H|<1` on the ball, the exact
entrywise second bordered-operator variation is below the further inflated
bound

`C2=1e60`.

Scaling by `W^-1` cannot increase an entry.  A 74 by 74 matrix with entries
bounded by `C2` has Frobenius norm at most `74 C2`.  The directed determinant
theorem supplies

`||Z||_prod <= E_det ||Btilde0 Z||_2`, with `E_det<1e1143`.

Consequently the uniform second relative derivative obeys

`M4 <= 74 * 1e60 * 1e1143 = 7.4e1204`.

The structured first-jet theorem gives `M3<2.38e17`.  Taylor's formula with
integral remainder therefore yields

`theta(rho) <= M3 rho + (1/2) M4 rho^2`.

For

`rho_nl=min(rho_geom,1/(4 M3),sqrt(1/(2 M4)))`,

both terms are at most one quarter and `theta(rho_nl)<=1/2<1`.  This radius
is positive and is of order `10^-603`.  Its tiny size is the expected cost
of using the determinant/Frobenius graph equivalence as an existence
fallback.  It proves uniform bordered invertibility and a strict nonlinear
relative graph-defect margin without forming any inverse.  A useful radius
still calls for a structured repeated-solve `D4` refinement.

The retained analytic Briot--Bouquet theorem already absorbs every positive
epsilon lower weight and proves `H4->H0>0` on this mathematical branch.  The
branch is a nonrealized formation history under owner finite-encapsulation
ontology, so no invariant formation-capture surface or reset-to-capture
overlap is a physical Gate-7 requirement.  The completion-critical owner is
instead the event-generated maximal-child Calderon/Weyl family and its
heat-minus-zeta physical-quotient covector root.  No selector, scale, fit,
endpoint, action term, time direction, gate, or chord is added.
