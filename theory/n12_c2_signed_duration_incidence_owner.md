# N12 C2 signed moving-duration incidence owner

Status: `SIGNED_DURATION_INCIDENCE_AND_REFERENCE_DDELTA_BALL_CERTIFIED_FAMILY_TRANSPORT_OPEN`.

The retained attachment gives exact local action-dual covectors for
`x=log R4` and `log N_boundary`.  Along the fixed-`s` C2 flow the proper-time
density is

`q_tau(Y,s)=N_boundary(Y) s/Delta(Y,s)`.

Hence its exact signed local first variation is

`D_Y q_tau=q_tau (D_Y log N_boundary-D_Y Delta/Delta)`.

The first term is executable directly from the BHSM boundary attachment.  At
the retained fixed-`s` reference center, the selected-line and hard-complement
calculation now gives the signed partial `D_Y Delta` plus a closed Euclidean
remainder ball.  Its partial norm is about `1.603e-9` and its remainder is at
most `4.467e-12`, a relative radius below `2.79e-3`.  This is a certified
local seed ball, not an exact-family or physical-history value.

No committed theorem yet transports that signed ball across the exact
parametric family.  The older moving-duration enclosure supplies only global
norm control.  The required transport is owned by the selected-line and
hard-complement second variation of the exact fixed-`s` action field; it may
not be replaced by differentiating the adaptive proof-center algorithm.

After `D_Y Delta` is supplied, its segment integral gives the signed
`h_Y,j` consumed by the already-derived reverse coefficient-history adjoint.
The transposed exact segment-map action and actual graded heat-minus-zeta
source remain separate required inputs.
