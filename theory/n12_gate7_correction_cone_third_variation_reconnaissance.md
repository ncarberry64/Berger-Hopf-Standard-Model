# Gate-7 correction-cone third-variation reconnaissance

Finite differences of the graph Hessian are subtraction-limited on the
Gate-7 owner nodes and have no certification authority.  The correct center
quantity is obtained by automatic directional differentiation after two
input legs have been contracted with the actual signed Green correction
direction:

`D3f[delta, c_hat, c_hat]`.

This produces a 72-by-72 map on the local flow-transverse constraint tangent.
Its operator norm controls the first variation of the directional quadratic
source without replacing it by the complete third tensor.  Contracting the
last leg also gives `D3f[c_hat,c_hat,c_hat]`.

The calculation uses the same calibrated JAX graph and the same 48 retained
constraint-tangent frames as the anisotropic `D2f` reconnaissance.  It is
therefore a center and owner-localization result only.  The authoritative
outward theorem must still combine the retained-action `D5` majorants with
the branchwise selected-line and bordered-response interval calculus.

The first-order tube-inflated directional curvature is inserted into the
already-derived causal vector-radius recursion while the mixed and transverse
groups remain at their center values.  This measures whether the directional
outward term is plausibly closing; it does not replace the missing retained
remainder or the first-hit/domain transfer.
