# Covariant BHSM bubble/interface mechanics

## Selected minimal action on regular geometry

The owner clarification supplies the missing selection principle for the
previously open `ORD1/IF5` class: encapsulation uses covariant interface
mechanics.  On each regular geometric stratum let `W_s` be the interface
worldvolume, with induced metric `h_ab`, oriented normal `n`, and dimension
`m_s`.  The minimal local, reparametrization-invariant first-jet member is

```text
S_Sigma=-sum_s integral_(W_s) gamma_s dmu_h.
```

No curvature-square coefficient is needed.  Although the density contains
only the first embedding jet, its embedding variation produces the mean
curvature vector and its time component supplies membrane inertia.  The
already-owned GHY/Hayward terms remain separate and are not counted again.

This action is defined only on regular `G_A`.  No pressure, area, curvature,
velocity, or classical membrane variable is inserted on `C_A`.  For an
emergence transition, the law begins on the first carrier on which regular
geometry has actually been produced.

## FSC stiffness and dimensions

In units with `hbar=1`, a worldvolume density has dimension `L^(-m_s)`.
The environmental scale signature supplies the regular length `ell_s`, using
the existing `ell_kappa` reference when no finer derived scale exists.  The
owner FSC rule therefore fixes

```text
gamma_BHSM^(s)=alpha_FSC ell_s^(-m_s).
```

This is `GAMMA1`: the power and dimensionless multiplier are fixed, while the
existing BHSM scale remains symbolic.  No observed decimal value of
`alpha_EM`, fitted coefficient, channel-by-channel tension, or FSC exponent is
used.

## Stress jump and tangential balance

With surface stress `S_Sigma^(ab)=-gamma_s h^(ab)`, total-action variation
gives

```text
jump(T^(mu nu)n_nu)+nabla_a(S_Sigma^(ab)e_b^mu)=0.
```

Choose `K=-n_mu H^mu`.  The normal projection is the BHSM Young--Laplace
analogue

```text
jump(T_nn)=gamma_s K+I_bulk,response,
```

where the response term is computed from the action-owned bulk fields rather
than inserted phenomenologically.  The tangential projection is

```text
h_a^mu jump(T_mu n)=D_a gamma_s.
```

It vanishes on a fixed stratum with constant FSC reference and scale.  Scalar,
gauge, fermion, ghost, higher-spin, and geometric contributions enter through
the total bulk traction.  This avoids duplicating their action terms on the
surface.

## Moving-interface equation

For a symmetric spatial `p`-surface of areal radius `R`,

```text
L_Sigma=-gamma_s Omega_p R^p sqrt(1-Rdot^2).
```

Its exact Euler equation per unit area is

```text
gamma_s[Rddot/(1-Rdot^2)^(3/2)+p/(R sqrt(1-Rdot^2))]
  =Delta P_event+P_bulk,response.
```

This is the covariant BHSM Rayleigh-type equation.  It is not a copied
Newtonian fluid law.  No viscosity or damping is added.  Any loss term must be
the computed outward Noether/radiative flux of the existing action.

## Nucleation, harmonics, and the formation number

The constrained formation Hessian is

```text
H_form=H_bulk,constrained+gamma_s J_Sigma-H_event,drive,
J_Sigma=-Delta_Sigma-(|A|^2+Ric(n,n)).
```

Formation begins at a simple oriented first crossing of
`lambda_min(H_form)=0`.  A normal deformation
`delta R=sum_lambda a_lambda Y_lambda` obeys

```text
I_lambda D_tau^2 a_lambda
 +(gamma_s j_lambda+Z_lambda)a_lambda=f_event,lambda.
```

The initiating event projector, rather than an independent child choice,
selects populated modes and any owned amplitude or phase.  The natural
dimensionless formation number is the spectral quotient

```text
rho_B=sup <psi,H_event,drive psi>
          /<psi,(gamma_s J_Sigma+H_impedance)psi>.
```

This is `RHO-B2`, a corrected operator form of the historical
`E_mode/E_impedance`.  The scalar historical ratio is recovered only after a
valid one-mode, common-charge reduction.

## Carrier, reset, and remaining boundary

The carrier is the first regular future stationary worldvolume at a simple
constrained crossing.  This is `CARR2_CONDITIONAL`: uniqueness modulo
reparametrization still requires a well-posed coupled full-field interface IVP
and an actually unique first crossing.

When that solution exists, `F_B` is its event-to-child worldvolume flow with
spin/bundle parallel transport, not identity by convention.  The physical
nonfermion relation is generated by the same surface action:

```text
q_e=C_s(F_B)q_c,
Pi_c+C_s(F_B)^*Pi_e=-delta S_Sigma/delta q_c.
```

It is Lagrangian on the reduced trace space when the displayed exact
generating functional and domains are instantiated.  The active differential
is the right-hand side; its geometric normal component is `gamma_s K`.

The constitutive loop is removed conceptually because event/environment/mode
now precede the carrier and charges.  The executable repository classification
remains `LOOP3` until the coupled IVP, first crossing, induced `F_B`, and full
operator domains are constructed.  Track 1 is neither read nor promoted by
this package.

## Claim boundary

The regular minimal interface action, dimensional FSC stiffness, stress jump,
moving-interface equation, nucleation operator, harmonic law, and `RHO-B2`
quotient are derived.  The physical carrier, `F_B`, full reset domain,
persistence/decay, Track-1 Gate 7 result, and full BHSM completion are not.

The one independent next Track-2 object is:

```text
COUPLED_CONSTRAINT_REDUCED_FULL_FIELD_INTERFACE_IVP_WITH_A_PROVED_UNIQUE_
SIMPLE_FIRST_CROSSING_AND_INDUCED_SPIN_BUNDLE_RESET_MAP
```
