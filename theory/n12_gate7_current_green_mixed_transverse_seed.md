# Current-C2 Green mixed/transverse polarization seed

The current Green image selects a longitudinal unit axis in the frozen
74-dimensional proof-coordinate frame.  Its orthogonal complement is the
transverse sector.  BHSM already used this split on the older 48-seam center,
but those numerical maps are not authority for the current 371-node center.

This certificate reconstructs the mixed map directly from the retained
action at four decisive current nodes: the coarse obstruction and historical
mixed-owner node 1, both endpoints of the first componentwise midpoint loss,
and the terminal owner of the certified causal central scalar.  For every
projected coordinate column it uses the exact polarization identity

\[
 D^2F[u_G,v]
 =\frac{D^2F[u_G+v,u_G+v]-D^2F[u_G-v,u_G-v]}{4}.
\]

Both quadratic operands are evaluated independently in 512-bit Arb through
the current action-derived selected eigenline, bordered response, normalized
field, and descriptor rate.  The map is assembled before its operator and
outward Frobenius bounds are taken.  This preserves the Green-axis/transverse
correlation that the component-box route discarded.

All four maps are finite.  The outward Frobenius uppers are
`30.44882953782609`, `7.991624894256848`, `8.097138523344938`, and
`9.568503872339862` at nodes 1, 355, 356, and 370 respectively.  Node 1 is
the seed owner, matching the historical mechanism's localization while using
entirely current-center values.  The projected maps annihilate the Green axis
at center to at worst `4.2293283692679946e-14`.

The result is a seed, not the global remainder theorem.  The same map still
has to be evaluated at every current endpoint and Hermite--Simpson midpoint,
then causally composed with an action-derived transverse--transverse
remainder before the two-radius test can be promoted.
