# N12 C2 fresh-chart fixed-s growth transfer

The fresh branch-24 eigenline chart is larger than the moving-cubic quotient
chart required by `F_0=Psi/c`.  The retained mixed `D4/D5` bounds and the fresh
Kato graph therefore derive, rather than choose, the maximal subradius on which
both `c>0` and `D lambda != 0` remain strict.

On that subchart the complete product rule bounds `D2c`, the quotient Hessian
bounds `D2(Psi/c)`, and the unknown signed fixed-line `D4` column is retained as
an absolute action-owned operator ball.  The resulting full action-coordinate
operator is valid on the exact fixed-descriptor fiber and is strong enough to
absorb predictor-center uncertainty without permitting physical normal motion.

The same action differentiation supplies fresh covariant hard-response inputs:
the RHS first/second variations, hard-block `D3`, selected-hard coupling, and
mixed `D4` controls.  These are the only inputs passed to the next continuation;
no old chart radius, redundant inverse reserve, or endpoint condition is reused.
