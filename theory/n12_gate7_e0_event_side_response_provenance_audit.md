# Gate-7 E0 event-side response provenance audit

Status: `E0_EVENT_SIDE_PROVENANCE_EXHAUSTED_REALIZED_PARENT_ARM_OPEN`.

The missing block in the closed two-seam operator is the outward-conormal
Calderón family of the physical parent arm ending at `E0`, denoted
`M_E0(z;xi)`. It must act on the same event trace space as `W_E0`, be defined
for every retained graded level on the real negative resolvent axis, and
carry its covariant first reset-quotient jet.

The repository candidates do not fill this slot:

- The N12 positive-duration Calderón-history artifact evolves the certified
  event state backward for one sampled collar step. It proves only a numerical
  local-history observation. It has no certified whole parent interval, no
  action-owned far-end graph, and no parent Weyl value or first jet.
- The event-normal Riccati equation is a valid arm-transfer identity, but its
  one-sided `M(0,z)=W_phys` initialization was explicitly superseded by the
  two-sided AE2 seam theorem. An ODE identity without the physical opposite
  endpoint load does not determine `M_E0`.
- The abstract maximal-forward source-domain theorem fixes how a realized
  forward history is closed at an actual event, canonical stop, or excluded
  end. It does not provide the pre-E0 parent coefficient history or identify
  which endpoint outcome occurs.
- `M_C2` belongs to a distinct post-E1 child history. Reusing it at E0 would
  insert recurrence or a reflected-history identification that the retained
  chronology does not supply.
- Historical relative-determinant and reduced collar objects lack the current
  N12 history, fields, domain, and graded operator bundle.
- The event/complete-child correspondence and AE2 action fix the trace,
  conormal, and reset-lift types, not the nonzero parent-arm response value.
- The old compact-history endpoint-role statement used a zero-source
  Dirichlet reference at birth. That interpretation is superseded by the
  owner-authorized source ontology and cannot be used as the physical `M_E0`.

The minimal missing data package is therefore sharply localized: the
action-generated pre-E0 parent coefficient history from its own physical
birth/end class to E0, the corresponding retained sector forms and endpoint
domain, and their first quotient jets. The existing inverse-free
Calderón/Riccati machinery then evaluates `M_E0` without a new operator
theory.

No source, seam force, recurrence, reflection, boundary selector, endpoint,
scale, action term, gate, or chord is added. Gate 7 remains open and
`FULL_BHSM_COMPLETE=false`.
