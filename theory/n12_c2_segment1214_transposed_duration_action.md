# N12 C2 segment-1214 transposed duration action

On the fixed positive descriptor fiber, segment 1214 has fixed internal
descriptor endpoints.  They are proof-partition values, not physical moving
boundaries, so their endpoint-shape covectors vanish.  The moving duration is
therefore owned entirely by the state dependence of the exact BHSM density

```text
q_tau=N_boundary*s/Delta.
```

For the exact segment map with state Jacobi action `J(s)`, its initial-state
duration covector is

```text
h_Y = integral D_Y q_tau(Y(s),s) J(s) ds.
```

The full segment-joint certificate supplies one signed centered ball for
`D_Y q_tau` on every admissible point of the segment.  The retained
fixed-descriptor continuation supplies `||J(s)|| <= G`.  The variational
identity also gives `||J(s)-I|| <= G-1`; hence the integral is enclosed about
`Delta_s D_Y q_tau(center)` without forming or inverting the transition
matrix.  Density motion, Jacobi motion, and decimal-to-binary representation
of `Delta_s` are all retained in the action-dual radius.

This closes only segment 1214.  It neither substitutes a proof center for a
physical history nor claims the remaining 1,221 segment actions, the complete
signed reverse sweep, the graded heat-minus-zeta force, or Gate 7.
