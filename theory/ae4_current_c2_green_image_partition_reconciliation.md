# Current-C2 Green-image partition reconciliation

BHSM already identified the relevant anisotropy before the accepted replay
center existed.  The signed minus-defect Green image defines a longitudinal
axis, and its causal orthogonal complement defines the transverse sector.  The
older 48-seam calculation was only calibrated-JAX reconnaissance and used a
different center lineage, so its numerical curvature values cannot be
promoted on the current center.  Its mechanism is nevertheless reusable.

The current 384-bit accepted-center calculation now supplies the missing
instantiation: the complete causal Green-image coordinate at every one of 371
nodes.  The reset node is fixed at zero and all 370 later images have strictly
positive norm.  Thus, for every post-reset node,

```text
u_G = A(-d) / ||A(-d)||,
P_parallel = u_G u_G^T,
P_perp = I - P_parallel.
```

This partition is selected by the BHSM shadow equation itself.  It is not a
fitted coordinate scaling or an arbitrary subdivision of the 73 tangent
coordinates.

The coarse-route curvature witness is causal coordinate 61 at node 1.  Its
certified absolute projection onto the current Green axis is at most
`0.001360430650320728`; its transverse projection is greater than
`0.9999990746137947`.  The witness therefore obstructs a common radius and the
coarse 73+1 field/descriptor split, but it does not obstruct a narrow
Green-image cone with a separately controlled transverse radius.

The next calculation is the outward retained-action version of the already
defined Green-image test: directional `D2f[u_G,u_G]`, mixed
`D2f[.,u_G]`, the transverse-transverse remainder at the declared small
radius, and their complete causal two-radius composition.  The same accepted
center, action, branch, preconditioner, and trajectory remain frozen.
