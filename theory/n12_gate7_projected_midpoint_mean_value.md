# Fixed output projection of the paired midpoint mixed jets

For the frozen local map P=R^-1 T, linearity gives D2(PF)=P D2F and
DF(z)w0 mapped by P equals P DF(anchor)w0 plus the segment integral of
P D2F along the original midpoint directions. P is fixed; this does not
establish a physical quotient or permit omission of moving-frame derivatives.

Reuse the complete independently paired seven-solve midpoint jets, all scalar
contractions and the identical primal family. Do not evaluate new action
derivatives. Write the original numerator as W+t V, where W contains the line
and scalar cpsi and V contains the configuration, response and remainder.
Apply P to W, V and all their first/mixed variations before multiplying by the
shared uncertain scalar coefficients. Keep the original unprojected physical
norm and all its scalar variations; normalizing PF instead of F would be wrong.

Compare this expression with direct projection of the saved uniform Hessian.
Both are valid enclosures of the same fixed-map mixed derivatives. Use narrower
overlapping coordinate bounds, retain both forms and require containment of
every projected point column. Apply the complete original 249-direction product
support and retain the full projected point-derivative uncertainty.

The result encloses P DF_M(w0) on the unchanged domain. Pair the new output
before local integration. It establishes no full trial basis, full-path
contraction, physical quotient or BHSM completion.
