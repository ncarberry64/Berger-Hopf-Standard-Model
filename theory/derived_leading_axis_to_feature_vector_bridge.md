# Derived Leading Axis To Feature Vector Bridge

The leading feature-vector bridge may use `D^(k/2)_(q/2,q/2)` only after the `y0` axis sampling theorem is derived.

Status: `FEATURE_VECTOR_BRIDGE_CANDIDATE_ONLY`.
