# N12 Gate-7 exact-affine 72D history first jet

The complete reset-quotient seed is the certified `98 x 72` projected lift.
Project it into the 73-dimensional physical tangent frame at the reset and
compose it with the 47 correlated 256-bit Arb interaction-Taylor26 macro
maps.  This produces the fixed-action-time Jacobi path on all 48 retained
history nodes without choosing one reset-family member.

The transverse terminal stop supplies the moving-duration correction

`D_xi T = -D_xi lambda_24 / Dlambda_24[F]`,

and therefore

`D_xi X_hit = J(T) + F(T) D_xi T`.

For normalized proper time `s=tau/T`, the action-owned coefficient
`x=log R4` has first jet

`D_xi x(s) = DlogR4[J(Ts)] + s DlogR4[F(Ts)] D_xi T`.

The stored artifact outwardly materializes these arrays on the certified
affine carrier.  It does not identify that affine variational path with the
first jet of the nonlinear exact solution family.  That remaining transfer
must use the already-certified exact-center `D2F` tubes before the compact
Weyl first jet and heat-minus-zeta force can be promoted.

`FULL_BHSM_COMPLETE = FALSE`.
