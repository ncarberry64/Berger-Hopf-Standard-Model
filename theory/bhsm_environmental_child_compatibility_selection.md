# BHSM environmental child-compatibility selection

Action version: `BHSM-AE-3.2.8-ENVIRONMENTAL-CHILD-COMPATIBILITY`

Status: `RECOVERED_OWNER_POSTULATE_E5_F4_L4_D4_G4_U_OUTCOME_D`.

## Provenance correction

The environmental selection principle is pre-existing deep owner work newly
materialized in the active Track-2 authority. It is not newly invented physics
and is not a theorem derived from the thirteen-term action.

Repository antecedents already state that:

- the spacetime edge is the owner-authorized limit where geometric variables
  cease to be the operative description and pure energy/Aether is the
  environment;
- pure energy without ordinary spacetime support is the core;
- the child channel has the form
  ((I_{m event},I_{m environment},B_{m SM})\mapsto
  ({\rm admissible\ child\ sector},R_{\rm rec},z_{\rm return}));
- arbitrary cross-superselection-sector branch choice is forbidden.

The sprint formalizes those existing semantics as an admissibility relation
on the conditional reset family. It does not introduce a seam action.

## Owner-authorized recovered postulate

The scale, scenario, and local energy/spacetime environment of the
encapsulation event select the physically admissible child. A child requiring
a physical structure that the event environment neither supports nor produces
through an owned transition cannot be produced by that event.

The rule is a support predicate, not a minimum-energy prescription.

## Minimal environmental state

The smallest descriptor that can exercise every part of the postulate is

\[
\mathcal E_s=(\alpha_s,\tau_s,I_s,\Lambda_s,B_s).
\]

### Structure stratum \(\alpha_s\)

This records which categories of mathematical object exist. It is a
diffeomorphism-invariant discrete type label. Current authority separates the
regular geometric stratum (G_A) from the pregeometric core (C_A).

The support scalar \(\upsilon\) is a dimensionless diffeomorphism scalar only
on (G_A), where (0<\upsilon\leq1) and the metric remains nondegenerate.
The core is a separate stratum: (C_A\ne\{\upsilon=0\}). Consequently no
inverse-metric equation may be evaluated at a fictitious interior
\(\upsilon=0\) point.

### Scenario \(\tau_s\)

The typed event class determines whether absent structures may legitimately
be produced or removed. It is a dimensionless ordered event label, not a
coordinate time.

The retained event order is

\[
{\rm formation}
\to {\rm constraint\ solved\ shear}
\to {\rm surface\ separation}
\to {\rm Legendre\ firewall}
\to {\rm oriented\ cut}
\to {\rm child\ reconstruction}.
\]

Persistence/return and AE4 future collapse are additional typed classes. A
canonical stop, chart failure, finite proof cutoff, or Legendre firewall is not
automatically the spacetime edge.

### Transported signature \(I_s\)

The metric-free event signature is

\[
I_s=(d,\epsilon,{\rm FR},o_\sigma,\mathcal I,
[B_{\rm Spin\times G_{SM}}],P_{\rm family}),
\]

containing degree, orientation branch, FR parity, response-endpoint order,
incidence, returned bundle isomorphism class, and family projectors. These are
dimensionless discrete, topological, or representation-valued invariants.

For the retained event:

\[
d=1,qquad \epsilon={\rm child}\ x<0,qquad {\rm FR}=-1,
\]

with separate child and parent boundary copies of
\(\Sigma=S^3\times S^3\).

### Scale signature \(\Lambda_s\)

The action-owned length anchor and dimensionless scale variables are

\[
\ell_\kappa=\kappa_1^{-1/6},
\qquad
R_\star=2.0232708255441265\,\ell_\kappa,
\qquad
x_s=\log(B/A)|_{\sigma=0}.
\]

Here \(\ell_\kappa\) and (R_\star) have length units, while
\(R_\star/\ell_\kappa\) and (x_s) are dimensionless scalars. The
event-conditioned return scale

\[
R_{\rm rec}[I_{\rm event},I_{\rm environment}]
\]

remains an open broken-reconstruction BVP output. No threshold is invented.

### Boundary/energy support \(B_s\)

This is the gauge-covariant, constraint-compatible boundary Cauchy and
Noether-flux data extracted from the physical event layer, including the
causal boundary-value sign. It collects only information needed for support:
Hamiltonian and momentum constraints, stress/flux data, the positive
\(\eta\)-Legendre domain, Gauss/Noether balance, and AE4 retarded support.

Its entries carry their sector's action-density or canonical units. Current
finite Galerkin energies use the repository normalization. There is no single
conventional energy variable on (C_A).

Every component is necessary: removing \(\alpha_s\) loses the geometric/core
distinction; removing \(\tau_s\) loses direct-versus-transition semantics;
removing (I_s) loses topology, spin, bundle, and incidence selection;
removing \(\Lambda_s\) loses scale selection; removing (B_s) loses energy,
constraint, flux, and causality selection.

## Environmental regime space

The repository supports the following typed stratification.

| Regime | Available structure | Limitation |
|---|---|---|
| Regular Lorentzian \(G_A\) | nondegenerate metric/inverse, causal cones, connection, curvature, canonical phase space, conventional energy | requires (0<\upsilon\leq1) |
| Regular Euclidean \(G_A\) | nondegenerate metric/inverse, connection, curvature | no Lorentzian causal cones or Hamiltonian energy; signature not selected |
| Depleted regular \(G_A\) | the same geometric object classes with (0<\upsilon<1) | not the core |
| Legendre-firewall face | last regular trace plus degree, orientation, FR parity, incidence | not proved to be the spacetime edge |
| Pregeometric \(C_A\) | abstract invariant signature | no coordinates, metric, inverse, causal cones, conventional time/energy, curvature, or metric size |
| Reconstructed Lorentzian \(G_A\) | restored regular geometry and retarded support | conditional on the reconstruction BVP and event-boundary map |
| Emergence/collapse transition | whatever its owned map produces or removes | general core/geometry transfer remains open |

The firewall ledger explicitly does not transport metric, proper distance,
proper time, velocity, curvature, local energy density, or canonical metric
momentum as pregeometric primitives.

## Child requirement signature

For a candidate child (X_c), define

\[
\mathcal R(X_c)=
(A_R,T_R,I_R,D_R^{\rm scale},D_R^{\rm energy},D_R^{\rm causal}),
\]

where (A_R) is the required structure set, (T_R) the allowed event
classes, (I_R) the required invariant signature, and the remaining factors
are exact owned scale, energy/constraint, and causal domains.

The retained degree-one Lorentzian full-preimage child requires:

- a smooth manifold, nondegenerate metric/inverse, orientation, causal cones,
  spin structure, \({\rm Spin}\times G_{SM}\) bundle, connection and curvature;
- canonical phase space, constraint-solved boundary data, and retarded child
  support;
- the degree-one \(\eta\) full-preimage/Hopf structure with seam
  \(S^3\times S^3\), post-cut topology \(B^4\times S^3\), negative child
  orientation branch, FR parity \(-1\), and returned bundle class;
- the exact reset radius relation and any future event-conditioned return
  scale derived by the broken reconstruction BVP.

The pregeometric child instead requires a positive core state sector, core
adjacency, and a core transfer operator. Those objects remain open and it does
not require a spacetime metric.

## Compatibility relation

Let \({\rm Avail}(\mathcal E_s)\) be structures already present, and let
\({\rm Produced}(\tau_s)\) be structures created by an owned transition.
Then

\[
\mathcal E_s\models\mathcal R(X_c)
\]

iff

\[
A_R\subseteq
{\rm Avail}(\mathcal E_s)\cup{\rm Produced}(\tau_s),
\]

and all invariant, scale, energy/constraint, causal, bundle, and incidence
predicates hold.

Evaluation is three-valued:

- `SUPPORTED`: every required predicate is established;
- `EXCLUDED`: at least one owned predicate explicitly fails;
- `UNRESOLVED`: none explicitly fails, but required data, a map, or a threshold
  remains open.

Thus unknown mathematics is neither silently accepted nor falsely excluded.

## “Spacetime in knots / no spacetime”

The strongest repository-supported formal correspondent of “spacetime in
knots” is the degree-one \(\eta\) full-preimage geometry, its
\(S^3\times S^3\) seam and Hopf structure, instantiated with a regular
Lorentzian child metric. This is not promoted to an action-derived theorem
that the owner's phrase has one exact mathematical synonym.

“No spacetime” is (C_A), where coordinates, metric/inverse, causal cones,
conventional time/energy, curvature, and metric size are undefined.

A direct (C_A\to\) knotted-spacetime reset is `EXCLUDED` because the child
requires unavailable geometric structures. A direct regular-spacetime
\(\to C_A\) reset is likewise `EXCLUDED` when the scenario supplies no
structure-removal map.

The “vice versa” statement is therefore scenario-relative. An emergence or
collapse transition may mediate either direction only after BHSM owns the
corresponding production/removal map. This avoids a false binary rule while
preserving the owner's incompatibility principle.

## Adversarial results

Environmental compatibility rejects:

- a same-scale, same-energy candidate with incompatible spin structure;
- topology change with the wrong degree;
- incompatible incidence;
- a holonomy-requiring child where no connection exists;
- a disconnected attachment without solved boundary data;
- advanced AE4 support.

The positive-energy relative Hopf rotor does not change the required-structure
signature and therefore remains unselected. Finite Galerkin intertwining is
required but does not select a continuum attachment.

## Application to the reset family

Let \(\mathfrak A_0\) be the prior conditional family. The environmental
filter gives

\[
\mathfrak A(\mathcal E_s)
=
\{(F_B,L_s)\in\mathfrak A_0:
{\rm Compatibility}(\mathcal E_s,\mathcal R(F_B,L_s))
\ne {\rm EXCLUDED}\}.
\]

This removes cross-sector mismatches before variation. It does not select a
member within a supported sector.

The previous two-map witness remains: identity and an inner-automorphism map
have the same degree, orientation, metric, volume, incidence, and
environmental requirement signature, but different tangent maps and
connection pullbacks. The two nonfermion maximal-isotropic graph witnesses
likewise have the same environmental signature while retaining different
graph jets and boundary Hessians.

All four base-map construction routes must satisfy compatibility, but none is
selected. Polarizations requiring absent structures or violating causal,
constraint, bundle, or incidence data are rejected; within-sector
polarization freedom remains.

Therefore:

- E1: false;
- E2: false;
- E3: false;
- E4: false;
- E5: true.

The surviving family is an infinite-dimensional, environmentally compatible
family of base maps and nonfermion Lagrangian correspondences.

## Consequence for the previous deficit

The previous phrase “new-physics deficit” is reclassified. At this stage no
new owner principle has been identified as necessary: environmental selection
already existed in the owner's deeper work. What remains is a recovery or
mathematical-implementation deficit:

`ONE_UNRECOVERED_ENVIRONMENT_CONDITIONED_RESET_SELECTION_FUNCTOR_OR_EQUIVALENT_INTERFACE_GENERATING_FUNCTIONAL`.

Compatibility narrows the domain of an eventual selector, but does not reduce
its functional dimension to a finite set. No interface functional is chosen or
implemented.

## Generator and downstream status

Because E5 does not produce a selected reset domain, the relative generator
remains locked:

- F4, L4, D4;
- G4 differentiability;
- \(Q_e=Q_c=Q_{\rm rel}={\rm None}\);
- charge class U;
- Outcome D;
- reduced reset and \(\beta\) absent;
- S1 reference-slice only;
- S2--S4 blocked.

## Hindsight ledger

### VALIDATED

- Environmental compatibility is a typed support relation.
- Regime, topology, spin, incidence, causal, and owned scale mismatches are
  genuine exclusions.
- “Vice versa” is scenario-relative.
- Existing deep owner semantics predate this materialization.

### INVALIDATED

- The owner postulate is newly invented physics.
- The core is an interior \(\upsilon=0\) regular state.
- A firewall or canonical stop is automatically the spacetime edge.
- Scale or positive energy alone establishes compatibility.
- Support compatibility selects one pointwise attachment or polarization.

### REDUNDANT

The thirteen-term action selector search and moving-graph, covariance,
groupoid, BRST, and R4 audits are inherited.

### OPEN

`RECOVER_OR_OWNER_SUPPLY_THE_ENVIRONMENT_CONDITIONED_FULL_FIELD_RESET_SELECTION_FUNCTOR_E_s_TO_EQUIVALENCE_CLASS_OF_(F_B,L_s)_WITH_BOUNDARY_CAUCHY_NOETHER_SCALE_AND_STRUCTURE_SUPPORT`

### DECISION POWER

The recovered postulate removes impossible superselection sectors, but leaves
E5 within supported sectors. F4, L4, D4, G4, U, and Outcome D remain.

### OWNER-POSTULATE CONSEQUENCE

Cross-sector incompatibilities are eliminated before variation; no unique
reset is earned.

### NEW-PHYSICS DEFICIT

Not classified as new physics. The current deficit is recovery or mathematical
implementation of deep owner work.

`FULL_BHSM_COMPLETE=false`.
