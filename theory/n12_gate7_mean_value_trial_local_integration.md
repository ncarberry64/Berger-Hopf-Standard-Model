# Local HS integration of paired mean-value trial actions

Consume the independently paired uniform endpoint trial-action enclosures from
the complete 75-direction mixed-variation calculation. Verify their identical
physical families and frozen trial geometry before use.

Reuse the existing paired midpoint center direction w0, its uniform action B0,
and the full midpoint derivative M on the original actual HS outer domain.
For each new endpoint action A, the actual chain direction is E/2 +/- h A/8.
Retain its complete difference from w0, including both its center displacement
and uncertainty. By derivative linearity, B0 + M(E/2 +/- h A/8-w0) encloses
the required midpoint chain action. No new midpoint domain or eigenpair theorem
is asserted and no center displacement is omitted.

Apply the previously tested preconditioned-tail and combined-endpoint-tail
associations, with the same frozen P, L, R, frames, and step. Select narrower
valid overlapping coordinate bounds against the preceding paired local column.
This uses no new action derivative evaluations. Independently reproduce the new
local record and data before downstream use.

The scope remains one frozen trial column of one local operator. Physical
quotient, moving-frame derivatives, complete causal contraction, and BHSM
completion remain open. No measured data or fitting is introduced.
