# Child boundary Hamiltonian ownership gate

The constraint-reduced local Legendre energy cannot globalize the continuum
child flow because it is an exact zero row of the complete-child map.  The
next possible action-owned energy route would be a differentiable,
boundary-improved child Hamiltonian generated by the retained time flow.

For a covariant action this requires, on one common field/domain convention,

\[
\delta H_\xi^{\rm child}
=\int_{\partial\Sigma}
  (\delta Q_\xi-i_\xi\Theta_{\rm retained})
-\delta B_\xi^{\rm child}.
\]

The current repository does not own the three ingredients needed to evaluate
this expression.  The fail-closed covariant phase-space ledger records the
complete retained symplectic potential, current, and flux as absent.  The
boundary variational ledger records the complete counterterm and selected
canonical ensemble as absent.  The historical recovery audit confirms that
the complete local supported action and core response were not recovered.
The v7.1 stratified reduction architecture fixes measures, orientations,
pushforwards, GHY transport, and compatibility maps, but it does not supply
the missing local support/core action data or a complete charge assembler.

The explicit N12 reduced action does supply its canonical one-form and local
Legendre transform.  After the actual lapse/shift and energy constraints are
imposed, however, the latter is the zero function.  Its boundary Casimir term
contains no new time derivative and does not by itself define the missing
covariant \(Q_\xi\), \(\Theta_{\rm retained}\), or differentiability
counterterm.  Therefore the reduced action cannot be used to manufacture a
nonzero child boundary charge by relabeling its zero constraint.

This is a localized action-ownership no-go, not evidence that a properly
completed covariant action would have no boundary Hamiltonian.  Deriving such
a Hamiltonian would first require the missing action-owned support/core
attachment term, complete covariant symplectic potential, and selected child
boundary ensemble.  Choosing any of them for the purpose of obtaining a
positive energy would introduce new physics and is not authorized here.

The no-parent result is unchanged.  Even a future child-only Hamiltonian
would not be the composite-minus-matched-parent \(\Delta H\), and it could
not be called mass without the independently missing parent section.

For the structures BHSM presently owns, the shortest flagship path therefore
bypasses energy globalisation: prove a direct strong-(S_2) continuation-or-
physical-domain-exit estimate for the existing continuum Euler--Dirac flow,
then evaluate the already-derived ordered-event return/exit readout.  This is
an analytic theorem dependency, not authorization for another trajectory or
solver campaign.
