# Common-Mode Cancellation in Mass Ratios

Status: `COMMON_MODE_CANCELLATION_DERIVED`

A common factor `Z_common I_f` cancels from intra-sector ratios, so it cannot produce relative hierarchy dressing.
