# N12 forward fixed-channel transfer variations

Status: `FIXED_CHANNEL_TRANSFER_AND_WEYL_VARIATION_EQUATIONS_DERIVED`.

Let `T'=G(x)T` be any retained two-dimensional fixed-channel transfer system.
For two action directions `h,k`, write `x_h`, `x_k`, and `x_hk` for the first
and mixed-second log-radius Jacobi histories. The exact triangular variation
system is

\[
 T_h'=GT_h+G_hT,\qquad T_k'=GT_k+G_kT,
\]

\[
 T_{hk}'=GT_{hk}+G_hT_k+G_kT_h+G_{hk}T.
\]

For an action-independent reset trace, the birth conditions are `T=I` and
`T_h=T_k=T_hk=0`. The scalar-channel generator has
`D_hD_k(c exp(-2x))=(4 x_h x_k-2 x_hk)c exp(-2x)`. The product-Dirac generator
has the corresponding exact `exp(-x)` chain rule. Thus the bulk transfer jets
require only `x,x_h,x_k,x_hk`; no independent temporal coefficient or moving
spatial basis appears.

If `T=[[a,b],[c,d]]` and `mu` is a terminal or Friedrichs admittance, the
birth Weyl value is

\[
 m=\frac{c-\mu a}{\mu b-d}.
\]

Its first and mixed-second action jets are the exact quotient jets of the
transfer and terminal-admittance jets. Consequently all endpoint or maximal
domain variation is isolated in `mu,mu_h,mu_k,mu_hk`; it is not a second bulk
operator coefficient. Regular propagation requires either a positive margin
for `|mu b-d|` or a finite Weyl-chart cover. This is a chart condition, not a
terminal-event recurrence assumption.

The remaining owner is now explicit: action-level maximal-forward enclosures
for `x,x_h,x_k,x_hk`, the Friedrichs/terminal graph jets, and a regular chart
cover. Gate 7 remains active, Gate 8 remains locked, chord 3 is unauthorized,
and no frozen prediction or new physical input has been added.
