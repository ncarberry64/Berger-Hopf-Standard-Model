# Gate-7 local-trust second Hermite--Simpson endpoints

Apply the locally sampled trust fraction of the second block direction and
recompute every projected endpoint and descriptor fiber.  Midpoint replay is
the acceptance test; first-hit reconstruction remains downstream.

`FULL_BHSM_COMPLETE = FALSE`.
