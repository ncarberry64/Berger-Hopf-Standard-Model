# BHSM owner-authorized encapsulation interface action

## Scientific result

Norman P. Carberry has authorized `P-A*` as one genuinely new physical
primitive: the interface action is the physical work/process by which the
initiating event mode envelops its local spacetime environment.  It must
dynamically determine the carrier, attachment map, nonfermion boundary
relation, and active boundary differential.  Its strength may depend only on
the initiating event mode, the local energy/spacetime condition, and physical
scale; independently fitted interface parameters are forbidden.

This authorization determines the primitive's physical role and its argument
types.  It does not determine a constitutive density.  Covariance,
dimensional analysis, canonicality, and Noether balance leave arbitrary
invariant scalar, matrix-valued, and potentially operator-valued response
functions.  The earned classifications are therefore

```text
ORD1  minimal first-order local class
IF5   infinite-dimensional functional freedom remains
UA5   the actualization solution set remains infinite-dimensional
LOOP3 the executable reset/charge/carrier cycle remains unbroken
```

No member of the class is selected in this sprint.

## Authority separation

### Existing BHSM authority

The prior 13-term stratified action supplies sectorwise bulk equations,
Green forms, canonical covectors, GHY completion on declared geometric caps,
the AE2 fermion graph, conditional reset naturality, environmental typing,
scale coordinates, and partial incidence/Calderón structure.  It contains no
variation in the full-field attachment map, nonfermion Lagrangian relation,
or physical carrier embedding.

### Owner-authorized new primitive

The new authority is `P-A*`.  It is recorded as a fourteenth action authority
class rather than retroactively inserted into the old action.

### Newly derived consequences

The authorization fixes the admissible variables, symmetry class, minimum
derivative order, formal Euler system, active differential identity, and the
Noether implementation of no-ex-nihilo.  It does not fix the functions that
appear in the density.

## Interface variables

Use one abstract compact oriented stratified carrier
\(\Sigma_{\rm enc}\).  On every regular geometric stratum \(s\), its piece
\(\Sigma_{\rm enc}^{(s)}\) is a hypersurface,

\[
\dim\Sigma_{\rm enc}^{(s)}=\dim M^{(s)}-1.
\]

The active regular pieces may lie in the registered M8, M5+/M5-, or M4
strata.  Codimension-two corners are boundaries of these pieces and do not
constitute a separate carrier law.  No ordinary embedded carrier is asserted
inside the pregeometric \(C_A\) stratum.

The event and child sides are two trace copies of the same abstract carrier,
with

\[
\iota_{\rm enc}=(\iota_e,\iota_c),\qquad
\iota_e:\Sigma_{\rm enc}\hookrightarrow M_e,
\quad
\iota_c:\Sigma_{\rm enc}\hookrightarrow M_c.
\]

The event outward conormal and child outward conormal have opposite signs.
Where the induced metric is non-null this gives unit normals; otherwise the
canonical object is a conormal density.  The retained branch is orientation
preserving.

The attachment is a dynamical map

\[
F_B:\Sigma_e\longrightarrow\Sigma_c,
\]

with natural sector transport
\(C_s(F_B):Q_{c,s}\to Q_{e,s}\) and cotangent adjoint
\(C_s(F_B)^*\).  Its variation is represented by

\[
\eta_c=\delta F_B\circ F_B^{-1}.
\]

For every nonfermion sector, \(L_s\) is a section of the Lagrangian
Grassmannian bundle of the reduced boundary trace symplectic space.  Its
variation lies in \(T_{L_s}\operatorname{Lag}(\mathbb T_s)\).  It is not
replaced by an arbitrary matrix.  The separately owned AE2 fermion graph is
not refitted.

An adequate Sobolev model uses H^(s+1) embeddings and attachment maps with
\(s>\dim(\Sigma)/2+1\), or smooth representatives.  This supports traces,
\(D F_B\), normals, natural pullbacks, and first shape variation.  Degree,
topology, incidence, spin lift, FR parity, and bundle class remain frozen
superselection data unless an independently owned transition vertex changes
them.

## Controls and frozen data

The control state is

\[
\Xi_{\rm enc}=(\mathcal M_{\rm event},\mathcal E_s,\Lambda_s).
\]

The dynamical variables are \(X_e,X_c,\iota_e,\iota_c,F_B\), and the
nonfermion \(L_s\).  The controls are the initiating mode, environmental
descriptor, and scale signature.  The environmental descriptor remains

\[
\mathcal E_s=(\alpha_s,\tau_s,I_s,\Lambda_s,B_s),
\]

where \(B_s\) contains gauge-covariant constraint, Cauchy, Noether-flux,
energy-support, and retarded-sign data.  The scale signature is

\[
\Lambda_s=(\ell_\kappa,R_{\rm reset}/\ell_\kappa,x_s),\qquad
\ell_\kappa=\kappa_1^{-1/6},\qquad
x_s=\log(B/A)|_{\sigma=0}.
\]

The event mode may contribute only already-owned basis-invariant amplitude,
event eigenline, forward orientation, representation/Hopf data, and geometry.
A phase or polarization enters only if it is already physical and selected by
a basis-invariant projector.  Independent child-mode coefficients, fitted
thresholds, and arbitrary harmonic phases are forbidden.

## Symmetry and invariant density families

The action must be a real, dimensionless, ghost-number-zero, Grassmann-even
scalar.  It is covariant under admissible diagonal stratum-preserving spatial
diffeomorphisms, the SM gauge bundle, BRST structure, earned spin lifts,
orientation/FR parity, and incidence/projector transport.  Once a carrier is
selected, it must also be equivariant under the actual event stabilizer.

Full Diff across incompatible strata, universal Spin(4), and basis-dependent
rotations inside an unresolved degenerate eigenspace are not assumed.

At local derivative order at most one, the complete allowed families are:

- canonical generating contractions of event and transported-child traces,
  canonical covectors, and \(L_s\);
- basis-invariant event-mode singlets in the appropriate intertwiner spaces;
- scalar contractions of \(B_s\) with induced metric, conormal, trace, and
  canonical data;
- arbitrary dimensionless functions of owned event/environment invariants,
  \(R_{\rm reset}/\ell_\kappa\), and \(x_s\);
- invariants of \(D F_B\), its Jacobian, induced metric/coframe transport,
  and lifted connection transport;
- induced carrier metric, volume, normal/conormal, and first embedding jets;
- tangential covariant field derivatives and already-owned normal
  Legendre/Green data;
- gauge traces, scalar/topographic contractions, Grassmann-even spinor
  bilinears, and BRST-closed ghost pairs;
- basis-invariant incidence/projector-intertwining defects, once physical
  projectors exist;
- independently quantized topological/holonomy terms.

New extrinsic-curvature squares, second attachment jets, and second embedding
jets are not required.  Existing GHY/Hayward terms remain part of the old
geometric action and cannot be counted again as the new envelopment strength.

## Derivative-order result

`ORD0` fails because dynamical connection, coframe, tensor, and density
transport depends on \(D F_B\).  First carrier jets are also needed for the
induced measure, conormal, and shape work.  `ORD1` is therefore the minimal
class.

`ORD2` is not forced.  Adding curvature-square or second-map-jet terms would
be a separate higher-derivative physical choice.  Global topological or
holonomy terms may occur, but a nonlocal constitutive law is not unavoidable.

## Most general action class

For \(n_s=\dim\Sigma_{\rm enc}^{(s)}\), the maximally constrained local form is

\[
S_{\rm total}=S_{13}+S_{\rm enc},
\]

\[
S_{\rm enc}
=\sum_s\int_{\Sigma_{\rm enc}^{(s)}}
\mu_{\gamma_s}\,\ell_\kappa^{-n_s}
\mathcal W_s(
I_{\rm mode},I_{\rm env},I_{\rm scale};
q_e,C_s(F_B)q_c,
\Pi_e,C_s(F_B)^{-*}\Pi_c,
L_s,j^1F_B,j^1\iota_{\rm enc})
+T_{\rm top}.
\]

Each \(\mathcal W_s\) is a real invariant scalar function.  No independent
fitted interface coefficient is permitted.  The action is defined modulo
carrier total divergences with fixed corner data, exact canonical changes
that leave the generated relation unchanged, physically null BRST-exact
terms, and independently quantized topological phases.

This is a class, not a selected density.

## Energy, spacetime, and scale dependence

The smallest presently typed continuous invariant arguments are:

- the owned basis-invariant norm/action amplitude and geometric invariants of
  the initiating event mode;
- dimensionless contractions of \(B_s\) after a physical conormal and
  reference exist;
- \(R_{\rm reset}/\ell_\kappa\);
- \(x_s\).

The anchor \(\ell_\kappa\) supplies dimensions.  It does not select the
dimensionless response function.  The previously typed
\(\rho_{E/ST}\) slot still lacks a unique invariant contraction, units, sign,
and range.  `rho_hold` cannot be used because the common charges needed to
define it remain absent.  Historical synthetic energy/impedance values are
not imported.

## First variation

For every ORD1 member, integration by parts gives

\[
\delta S_{\rm enc}
=\sum_s\left[
\langle\mathcal E_{X_e},\delta X_e\rangle
+\langle\mathcal E_{X_c},\delta X_c\rangle
+\langle\mathcal E_\iota,V\rangle
+\langle\mathcal E_F,\eta_c\rangle
+\langle\mathcal E_L,\ell_s\rangle
\right]
+\int_{\partial\Sigma}\theta_{\rm enc},
\]

where

\[
\mathcal E_Y(\mathcal W)
=\frac{\partial\mathcal W}{\partial Y}
-\nabla_a\frac{\partial\mathcal W}{\partial(\nabla_aY)}.
\]

Here \(V=\delta\iota_{\rm enc}\) splits into tangential
reparametrization and physical normal deformation,
\(\eta_c=\delta F_B\circ F_B^{-1}\), and
\(\ell_s\in T_{L_s}\operatorname{Lag}(\mathbb T_s)\).  A selected member
must supply a well-posed corner/ensemble treatment; no new corner coefficient
is inferred from the class.

## Euler-Lagrange system

The carrier equation of the total action is

\[
\operatorname{Pr}_{\perp}
\left([T_{\rm bulk}\cdot n]_{e,c}
+\mathcal E_\iota(S_{\rm enc})\right)=0.
\]

The tangential embedding variation is the reparametrization/Noether identity,
not an independent carrier-selection equation.

The attachment equation is

\[
\sum_s\langle\Pi_{e,s},D_FC_s(F_B)[\eta]q_{c,s}\rangle
+D_FS_{\rm enc}[\eta]=0
\quad\text{for every admissible }\eta,
\]

modulo proven carrier reparametrization and internal gauge/spin redundancy.

The nonfermion boundary-relation equation is

\[
\operatorname{Pr}_{T_{L_s}\operatorname{Lag}(\mathbb T_s)}
D_{L_s}S_{\rm enc}=0.
\]

The event and child seam equations are

\[
\Pi_e+\mathcal E_{q_e}(S_{\rm enc})=0,
\qquad
\Pi_c+\mathcal E_{q_c}(S_{\rm enc})=0.
\]

All equations must be projected to the Hamiltonian, momentum, Gauss, BRST,
incidence, FR, and retarded admissible directions.  These formulas identify
the equations that a selected density would provide; their values and ranks
cannot be evaluated while \(\mathcal W_s\) is unknown.

## Active encapsulation differential

Combining the two field seam equations gives, in the retained orientation
convention,

\[
\boxed{
\Delta_{\rm enc}
:=\Pi_c+C(F_B)^*\Pi_e
=-\left[
\mathcal E_{q_c}(S_{\rm enc})
+C(F_B)^*\mathcal E_{q_e}(S_{\rm enc})
\right]
=:J_{\rm enc}.}
\]

Thus \(\Delta_{\rm enc}\) is derived from the interface action as a reduced
boundary cotangent source.  Its support is the support of the selected density
and any explicit environment seam variables.  Its sign is fixed by the
opposite outward conormals.  Its transformation is the cotangent pullback and
the appropriate adjoint gauge covariance.

Its amplitude and physical mode content are not yet derived.  A crucial
adversarial result is that an action depending only on the passive mismatch
\(q_e-C(F_B)q_c\) gives

\[
\mathcal E_{q_c}+C(F_B)^*\mathcal E_{q_e}=0,
\qquad \Delta_{\rm enc}=0.
\]

Therefore a trace-mismatch penalty alone cannot implement the authorized
active creation, erasure, or redistribution of boundary information.

## No-ex-nihilo implementation

No scalar energy equation is postulated.  Diagonal symmetry of the closed
extended action produces a local Ward identity

\[
dJ_\xi^{\rm total}=0,
\]

and the oriented seam identity

\[
J_{\rm event}+J_{\rm child}+J_{\rm interface}+J_{\rm environment}=0.
\]

The integrated Hamiltonian balance quoted by the owner is available only
after a common generator, reference, ensemble, and domain exist.  If
\(\mathcal E_s\) is held as an external control, its variation is an explicit
work/source term.  It cannot be hidden in the child or described as
closed-system conservation.  Noninvertible information reduction likewise
requires complementary interface/environment degrees to carry the missing
canonical/Noether data.

## Canonical/Lagrangian status

Locally, the critical locus of a selected generating family can define an
exact isotropic correspondence in
\(\mathcal P_e^-\times\mathcal P_c\).  It is Lagrangian only when it is
immersed and has half the dimension of the fully reduced target.  Coisotropic
Hamiltonian, momentum, Gauss, and BRST reduction must occur before this
maximality test.

The generating-family class is canonically meaningful.  Actual isotropy and
Lagrangian maximality are not verified because no density, carrier, complete
reduced symplectic domain, or Hessian/transversality data are selected.

## Parameter count

The result is `IF5`.

The owner rule excludes independent phenomenological interface constants,
independent child amplitudes, and mode/environment-blind laws.  It does not
choose:

- universal scalar response functions of the allowed invariant controls;
- matrix-valued functions on matched irreducible sectors with multiplicity;
- potentially operator-valued kernels on the full boundary section spaces;
- the interface/environment degrees that carry active information and work;
- topological/holonomy classes whose levels are not already fixed.

Dimensional analysis fixes powers of \(\ell_\kappa\), but arbitrary
dimensionless functions remain.  The unresolved carrier, stabilizer,
projectors, and multiplicities prevent even a finite function count.  Thus
`IF0`--`IF4` are not earned.

## Historical candidate comparison

- The v6.10 junction action is a zero/optional member and selects no graph.
- v14.60--61 is a finite synthetic truncation, not a physical member.
- Historical boundary-action branches are structural subclasses with unproved
  normalization and quotient rules.
- v15 master/Unique Actualization semantics supply no constitutive function.
- AE4 impedance/core data could become invariant arguments but are presently
  undefined as common charges on a selected carrier.
- Calderón/DtN/Wentzell blocks are conditional response operators; they need a
  physical domain and do not determine the source law.

No historical candidate is the unique member of the new class.

## Closure, rank, and actualization

No carrier, \(F_B\), or \(L_s\) is selected.  Consequently there is no
physical reset domain and the relative generator tangency test is not reached.
`LOOP3` remains the executable classification.  Charge classifications remain
`XI5/CHG4/REF5/MODEE5/ENVH5/DOMH4/AE4R5`; `rho_hold` is undefined.

The formal carrier, map, relation, and field equations have unknown raw
equation counts on the physical domain and contribute zero earned independent
rank.  N12 remains 67-dimensional, or 66 after the already-owned time
quotient.

The solution set is a union over an unselected infinite-dimensional functional
class, carriers, maps, relations, and BVP solutions.  The actualization result
is `UA5`, not theorem-level Unique Actualization.

Graph jets, beta, S1--S4, and the full-field attachment remain blocked.

## Exact next object

`ACTION_LEVEL_CONSTITUTIVE_ENVELOPMENT_WORK_DENSITY_OR_EQUIVALENT_FUNCTIONAL_SELECTION_PRINCIPLE_FIXING_THE_IF5_RESPONSE_FUNCTIONS_WITHOUT_FITTED_INTERFACE_PARAMETERS`

No owner question is formulated because `IF5` is not a small ambiguity.
Asking Norman to provide an arbitrary function would violate the task.
