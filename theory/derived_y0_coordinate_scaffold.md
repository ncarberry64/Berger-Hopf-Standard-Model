# Derived y0 Coordinate Scaffold

The generic internal sampling point is represented symbolically as:

\[y_0=(\alpha_0,\beta_0,\gamma_0).\]

This is a coordinate scaffold, not a numerical derivation.
