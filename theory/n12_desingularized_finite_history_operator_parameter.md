# N12 desingularized finite-history operator parameter

Status: `ACTION_OWNED_FORMATION_AMPLITUDE_DURATION_JET_CERTIFIED`.

On the certified incoming selected line, use the signed simple eigenvalue
`lambda` as the branch parameter, with `lambda>0` before the terminal event
and `lambda=0` at it.  The retained singular Euler--Dirac flow becomes the
regular equation

`dY/dlambda=(b_psi Psi+lambda V_hard)/(c_psi b_psi+lambda R)`.

At the endpoint,

`dY/dlambda=Psi_E/c_psi(E)`.

The terminal event state is fixed along this one-parameter incoming germ.
Consequently its terminal `log R4` and proper radius rate do not move with
the formation amplitude; their variation across the distinct 73-dimensional
event/child family remains separate.

The proper duration satisfies

`T(lambda)=a lambda^2+o(lambda^2)`,

`a=1/(-2 c_psi(E)b_psi(E))>0`.

Thus `D_lambda T(0)=0` and

`D_lambda^2 T(0)=1/(-c_psi(E)b_psi(E))=2a>0`.

For every sufficiently small `lambda>0`, the compact Weyl map is finite.  Its
leading amplitude behavior is

`M_C(lambda,z)=(a lambda^2)^(-1)L(1+o(1))+C_E+O(lambda^2)`,

where `C_E=0` in scalar channels and
`C_E=diag(-s_E,s_E)` in product-Dirac channels.  This is an action-owned
singular formation-amplitude germ, not a selected history and not a
validation cutoff.

The result supplies the leading moving-duration jet.  A force value at a
positive member still requires the complete spectral realization and the
regular branch beyond its endpoint germ; evaluating only `lambda=0` is
impossible because the physical duration is zero there.

HINDSIGHT: the formation amplitude is an action coordinate and its quadratic
duration law is required.  Choosing a numerical positive amplitude would be
a selector unless it is subsequently determined by the same-action saddle.

`FULL_BHSM_COMPLETE=false`.
