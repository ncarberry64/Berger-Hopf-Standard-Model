# Derived Raw Mode To Feature Vector Bridge

```text
F_{k,j,m}(y0)=(psi, d_a psi, d_a d_b psi)|_{y0}
```

The bridge is symbolic; explicit feature values remain open.

Status: `RAW_MODE_TO_FEATURE_VECTOR_BRIDGE_DERIVED_CONDITIONAL`.
