# C2 1,064-segment finite-core descriptor pencil

The full certified continuation stores 1,065 chronological C2 proof centers
and 1,064 positive proper-time intervals.  Their joins are exact at the stored
action-coordinate precision.  Each node also carries a certified action tube,
which produces an interval enclosure for the action-owned boundary log radius.
These data now supply a genuine coefficient-path enclosure for one nested
finite form core, superseding the earlier endpoint-only inventory at that
limited scope.

For piecewise-linear temporal form functions on each interval of length `h`,
the scalar channel uses

`K_e = h^-1 [[1,-1],[-1,1]] + c exp(-2x_mid) h/6 [[2,1],[1,2]]`.

The two product-Dirac channels use the positive factor form

`integral |u' + W u|^2 d tau`,  `W=+/- lambda exp(-x_mid)`,

whose exact element matrix is

`K_e = h^-1 S + W^2 h A/6 + W diag(-1,1)`.

The mass pencil is `M_e=h A/6`.  Differentiating these formulas gives exact
element-local `D_xmid K`, `D_h K`, and `D_h M`, so an action-owned reset
Jacobi path can later be contracted by the chain rule without reconstructing
or inverting a dense kinetic/Dirac block.

The birth node is retained as the Calderon/Weyl trace.  The last node is
eliminated only as the Dirichlet edge of a nested Friedrichs form core; it is
not a physical endpoint and carries no terminal load.  Scalar positivity is
uniform from the positive potential.  Product-Dirac positivity follows from
the one-sided Poincare estimate because `pi/(2T)` overwhelmingly exceeds the
bounded superpotential on this very short certified core.

This certificate assembles `K`, its mass pencil, the event/child partition,
and the coefficient-level derivative adapter on actual BHSM continuation
data.  It does not fabricate the missing reset-quotient Jacobi path, a
maximal-tail estimate, a far endpoint, or a numerical zero-source force.  The
next owner is to pull the combined heat-minus-zeta cotangent through this path
and prove the projected finite-core force net is Cauchy on the maximal
exhaustion, or to reach an actual finite event/canonical stop.
