# Gate-7 quarter-corrected recentered-cone boundary-cluster spectrum

The retained quarter-step stop history is the base center.  Its matched fine
signed Green correction is part of the proof center, while the independently
certified causal Taylor radius is only the nonlinear remainder halo.  This
distinction is essential: the matched center correction is about
`3.486903140325637e-6`, whereas the nonlinear action radius is
`1.243972269022099e-12`; the latter cannot contain the former.

The corrected center and nonlinear halo are covered on the union refinement of
the quarter-step center mesh and the fine correction mesh.  On each union cell
the base cubic plus the piecewise-linear correction is exactly cubic.

If `P` is its three-coordinate action projection and `rho` is the global
nonlinear action radius, then

`sqrt(2) [P, rho I_98]`

contains the product of the corrected path ellipsoid and the nonlinear ball.
The established correlated boundary-cluster Kato proof is evaluated on this
101-dimensional projection.  Retained exact action Hessians fix the centers;
batched automatic differentiation of the same action formula accelerates only
the center `D3` directions; and retained MixedBound `D4` is the outward
authority.

Internal hard-branch 26/27 proximity remains inside the invariant positive
cluster 25--27 and is never used as a selected-line denominator.

The resulting 3,009-cell proof retains branch 24 everywhere.  Its minimum
selected-line boundary gap is `1.7205464630704569e-7`, and the maximum selected
line shift is `5.586614648001691e-12`.  This closes the spectrum carrier only;
the projector graph, bordered inverse, response transfer, and literal radii
polynomial remain separately scoped certificates.
