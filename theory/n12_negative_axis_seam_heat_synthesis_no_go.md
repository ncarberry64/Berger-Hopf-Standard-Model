# N12 negative-axis seam heat-synthesis no-go

Status: `BROAD_NEGATIVE_AXIS_SEAM_FAMILY_CANNOT_DECIDE_HEAT_MINUS_ZETA_FORCE_SIGN`.

The AE2 seam comparison theorem covers every negative real spectral probe,
and the separate low-energy source-Dini and high-energy trace estimates remain
valid.  Coverage alone is not enough to determine the heat-minus-zeta force.

For `K=sqrt(kappa^2+a)` and `T_ref>=T_core`, the scalar comparison interval
contains both canonical regular far loads because

`kappa tanh(kappa T_core)
 <= K tanh(K T_ref)
 <= K coth(K T_ref)
 <= K coth(K T_core)`.

The first interior load is the Neumann far-end family and the second is the
Dirichlet far-end family.  The product-Dirac comparison class also contains
both.  In its high-probe regime its retained upper envelope is

`S+(2/sqrt(3))*sqrt(S^2+kappa^2)`,

which strictly exceeds the constant-channel load; in the low/mid regime the
zero-extended Dirichlet trial is the retained finite-core upper bound.

At identical `R4=1` and `T_ref=3`, with identical retained spatial spectra,
multiplicities, and supertrace signs, rigorous heat sums give

- Neumann far load: `F_rep > 3.46484200887244046924`;
- Dirichlet far load: `F_rep < -4.10073296251316019097`.

Each uncancelled angular/temporal tail is below `1.66e-121`.  Hence the
currently certified broad whole-axis class contains two complete spectral
families with opposite force signs.  No integration, interpolation, or
additional sampling of that same class can produce a valid unique sign.

The reference families are not asserted to be the physical N12 history.
This no-go instead proves that the retained action-owned far endpoint load
must be sharpened enough to exclude one family.  The next object is the
maximal finite event/canonical-stop history with its Weyl geometry jet, or the
equivalent joint finite-history operator.  No additional negative-axis probes
within the unchanged broad class have proof value.

Gate 7 remains active; Gate 8 is locked; chord 3 is unauthorized; no action
term, endpoint selector, scale, fit, or frozen-prediction change is made.
