# Neutrino Conjugate-Cover Mass Engine

Status labels:

```text
NEUTRINO_CONJUGATE_COVER_MASS_ENGINE_CANDIDATE
NEUTRINO_NORMAL_ORDERING_PREFERENCE_CANDIDATE
PMNS_EQUAL_DEGREE_CONJUGATE_COVER_CANDIDATE
PMNS_BASE_HOLONOMY_PHASE_CANDIDATE
```

Neutrino sector operator:

```text
Omega_nu = -q - 2j = -k
|Omega_nu_star| = 3
```

Candidate ledger:

```text
(k,j): (0,0), (3,0), (3,1)
(q,j): (0,0), (3,0), (1,1)
```

Neutral mass opening:

```text
Omega_nu + Omega_nu_c = 0
M_nu = (v^2 / (2 Lambda_nu)) * B_nu
```

Candidate leading rank-two structure:

```text
M_nu^(0) =
m0 * [[0,0,0],
      [0,A,B exp(i phi)],
      [0,B exp(i phi),C]]
```

Candidate implications:

```text
m1 approximately 0 at leading order
normal ordering preferred
m3 associated with lower-action (q,j)=(1,1)
m2 associated with (q,j)=(3,0)
```

PMNS phase candidate:

```text
A_l = -A_q + 2 A_j
A_nu = -A_q - 2 A_j
A_l - A_nu = 4 A_j
```

Thus `delta_PMNS` is a Berger-base holonomy dominated candidate. This is not a
minimal Standard Model prediction and does not alter frozen PMNS screens.
