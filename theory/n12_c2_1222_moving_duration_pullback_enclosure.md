# N12 C2 1222-core moving-duration pullback enclosure

The fixed-descriptor proper-time integrand is

`q_tau(s,Y)=N_boundary(Y) s/Delta(Y)`.

On every retained regular proof ball, `N_boundary>0` and `Delta>0`.  Hence

`D log q_tau = D log N_boundary - D log Delta`.

Let `h_e^+` be the certified upper proper duration, `G_e` the local state
Jacobi-growth bound, `L_N=||D log N_boundary||_*`, `Delta_e^-` the positive
denominator lower bound, and `L_Delta,e` its action-coordinate derivative
bound.  Direct integration gives

`||D_Ystart h_e|| <= h_e^+ G_e (L_N + L_Delta,e/Delta_e^-)`.

Multiplication by the previously certified state-Jacobi product before the
segment pulls this covector back to the C2 birth state.  Absolute contraction
with the inverse-free Weyl duration cotangent then gives a finite operator
norm on every unit C2 reset-image direction.  Orthogonal gauge/time quotient
projection cannot increase the bound.

All inputs are BHSM-owned.  The compensated/adaptive and fixed-descriptor
balls are replayed from their stored centers and action majorants.  The final
eight matrix-Lohner denominator derivatives are reconstructed from the same
certified `c`, `b_psi`, hard-response, selected-line, and remainder first and
second variation bounds used by those segment certificates.

This closes existence and a rigorous norm enclosure of the moving-duration
first jet on the finite core.  It does not determine the signed reset
covector: that requires the inverse-free backward center adjoint.  It also
does not control the maximal tail beyond the finite 1,222-core truncation boundary.

No duration interval or proof tube is substituted for a first jet.  No event,
stop, recurrence, selector, fitted scale, action term, gate, or chord is added.
