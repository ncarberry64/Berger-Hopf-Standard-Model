# Gate-7 captured-NHIM rank-72 relative-tail theorem

Status: `CAPTURED_NHIM_RANK72_SIGNED_RELATIVE_TAIL_CAUCHY_RESET_ENTRY_OPEN`.

This theorem concerns the source-contracted relative Gate-7 cotangent, not
the absolute infinite-volume heat trace.  The latter remains divergent on a
finite-optical NHIM history and is neither restored nor required here.

Let a regular post-event C2 history enter the open analytic capture basin of
the retained finite-N12 normally attracting invariant family.  Write
`epsilon=R4^-2=exp(-2x)`.  The capture theorem gives, after some `T0`,

`epsilon(tau) <= epsilon(T0) exp(-H0(tau-T0))`, `H0>0`.

The stable-foliation theorem also applies to the first variation of every
nearby history in the open captured family.  In the moving scale section its
center part converges to a finite tangent of the 24-parameter boundary
family, while its 25 stable velocity-normal components decay at the normal
rate and its radial component decays at the retained radial rate.  Thus the
log-radius and shape Jacobi components are bounded and their nonconstant
rate components decay.

The exact fixed-channel reduction contains no other history coefficient.
Its variable pieces have weights

`s_mu=chi*mu*exp(-x)`, `V_c=c*exp(-2x)`,

with pair vertices proportional to `exp(-x)`, Laplace--de Rham blocks
proportional to `exp(-2x)`, and retained contact blocks independent of `x`.
Consequently every propagated captured-family seed has

`|D s_mu| <= C_j*(1+mu)*exp(-H0(tau-T0)/2)`,

`|D V_c| <= C_j*(1+mu)^2*exp(-H0(tau-T0))`,

and the same bounds, with a fixed finite polynomial angular order, for all
retained incidence and form vertices.  These tails are integrable in proper
time.  The direct non-scale zeta variation is bounded by the same
`exp(-x)` tail; the common-scale zeta variation remains exactly zero by its
separate moving-duration Ward identity.

For the angular sum, the certified positive first C2 collar lies between the
birth seam and every far-tail insertion.  The two Poisson factors in the
relative-form derivative therefore supply

`exp(-ell_0*mu/R4_max)`

for the low-energy part, while the high-energy part supplies

`exp(-a*mu^2/4)`, `a=exp(-2*x_max)>0`.

Both dominate the quadratic retained multiplicities and every fixed finite
polynomial order contributed by the coefficient jets.  Fubini/Tonelli may
therefore be applied to the absolute majorant after the signed joint
heat-minus-zeta density has been assembled.  The direct increment is Cauchy,
and for all 72 reset-generated seed directions

`g_T-g_S = integral_S^T (U(t,0)B_seed)^dagger q_rep(t) dt + d_T-d_S -> 0`.

Hence the signed rank-72 source-contracted relative-form tail is Cauchy on
every open reset-generated family that is actually captured by this NHIM.
No absolute heat trace, arbitrary endpoint, or ambient adjoint norm is used.

The theorem is conditional only at the geometric connection step.  The
repository still has no validated capture surface and has not proved that a
nonempty event-generated AE2 reset-quotient family enters the basin.  Gate 7
therefore advances to that reset-to-capture connection (or an actual later
retained event/canonical stop), followed by evaluation and rooting of the
complete projected covector.

Only the external Cauchy/birth source is zero.  All internal responses and
contacts remain in the joint operator exactly once.  No selector, seam
source, endpoint, recurrence condition, scale, fit, gate, or chord is added.

`FULL_BHSM_COMPLETE=false`.
