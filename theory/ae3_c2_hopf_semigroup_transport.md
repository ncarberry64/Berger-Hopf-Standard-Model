# Current-C2 Hopf-semigroup transport theorem

The historical BHSM overlap rule has two geometrically distinct factors that
must not be conflated. The actual AE3 reset supplies the current C2 carrier
history. The frozen Berger shape belongs to the independent internal
mode/family fiber. The repository field ledger explicitly types it as an
internal S3 datum, while the localization theorem factors the state space as

\[
 L^2(C_2)_{\rm carrier}\otimes(\mathrm{Spin}\times G_{\rm SM})
 \otimes\mathcal F_{\rm family}.
\]

The round current-C2 birth diagnostic gives the dimensionless generators

\[
 K_\ell=\operatorname{diag}(0,35,99),\qquad
 K_u=\operatorname{diag}(0,48,120),\qquad
 K_d=\operatorname{diag}(0,48,80).
\]

Using the frozen BHSM overlap width gives

\[
 S=\frac{1}{4\pi},\qquad T_f(t)=e^{-tK_f}.
\]

Direct functional calculus proves that every operator is self-adjoint,
positive, contractive, commutes with the exact family projectors, satisfies
the semigroup law, and has generator

\[
 \left.\frac{dT_f}{dt}\right|_{t=0}=-K_f.
\]

All three round diagnostic operators are family-noncentral and order the
retained slots as heavy, middle, light. This explains why the zero-mode slot
can be the heaviest: the relevant object is a decreasing response, not a
positive local gradient energy.

## Unchanged frozen internal operator

The round carrier does not set the independent internal Berger shape to one.
Keeping the frozen BHSM value

\[
 a=1.157054135733433
\]

on the internal factor gives

\[
 K_f(a)=\sum_n\left[k_n(k_n+2)+(a^2-1)q_n^2\right]P_{f,n},
 \qquad T_f=e^{-K_f(a)/(4\pi)}.
\]

The resulting unchanged ratio screens are

\[
 T_\ell:\ (1,\ 0.06007447093260976,\ 0.00029729106456492414),
\]

\[
 T_u:\ (1,\ 0.008310500554068288,\ 1.2690463017606151\times10^{-5}),
\]

\[
 T_d:\ (1,\ 0.021933971495439474,\ 0.0011165200546001757).
\]

These exactly reproduce the frozen BHSM bare hierarchy assets; nothing is
recomputed from observed masses. Because each operator acts only on the
family factor, it commutes exactly with the AE2 reset lift, the AE3 enclosure
restriction, and the smooth localization carrier. Thus the frozen internal
Hopf response operator is now attached over the actual current C2 history.
The round result remains a useful diagnostic, not a replacement for the
frozen internal geometry.

## Exact action boundary

This closes the finite-family response-operator transport, not the mass
theorem. The first failure occurs before any absolute scale is needed:

```text
CURRENT_AE3_INTRINSIC_M4_LR_HIGGS_COUPLING_WITH_T_C2 = ABSENT
```

The historical v11.3 packet varied a term of the form

\[
 -\int_{M_4}\left(\bar L_L\,Y_0T_\ell H e_R+\mathrm{h.c.}\right),
\]

but that term was not carried into `BHSM-AE-3.0.0`. The frozen overlap width
and internal ratio operator are now valid attached upstream assets, yet
neither is itself an AE3 variational coupling. The trace-normalized source and
profile lift likewise remain outside the present AE3 action.

After the missing coupling, AE3 still lacks a nonzero action-selected
Higgs/HS saddle, family-resolved fermion poles, and a matched-parent
\(\Delta H_\xi\) comparison. Formally inserting \(H_*=0\) into

\[
 M_f=\frac{H_*}{\sqrt2}Y_0T_f
\]

gives the zero matrix in every sector. That algebraic zero is not a physical
massless-pole theorem because the dynamical HS kernel and pole problem are not
present.

## Result and next derivation

- `CURRENT_C2_FINITE_FAMILY_HOPF_RESPONSE_SHAPE_DERIVED = TRUE`
- `FROZEN_INTERNAL_HOPF_RESPONSE_OPERATOR_ATTACHED_TO_CURRENT_C2 = TRUE`
- `FROZEN_MASS_RATIO_SCREENS_TRANSPORTED_UNCHANGED = TRUE`
- `CURRENT_AE3_YUKAWA_OPERATOR_DERIVED = FALSE`
- `CURRENT_AE3_FAMILY_MASS_HIERARCHY_DERIVED = FALSE`
- `PHYSICAL_FERMION_POLES_DERIVED = FALSE`

The next science unit is no longer an open-ended recovery exercise. It is to
version and vary the retained intrinsic M4 LR--Higgs semigroup coupling on
current C2, without an independent family coefficient, and then derive its
broken saddle and fermion poles. Only those poles, or an equivalent
matched-parent charge calculation, may be promoted as physical masses.
