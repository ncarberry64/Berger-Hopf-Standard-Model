# Gate 7 native source-readout necessity audit

Status: `NATIVE_DEPENDENCY_LOCALIZED_GATE7_UNCHANGED`.

The retained forward source action does not require a terminal-event return in
order to own its operator domain.  At an actual retained reset hit it uses the
existing reset graph; at an infinite or excluded maximal end it uses the
Friedrichs closure.  Terminal reachability is therefore one sufficient finite
endpoint realization, not a native prerequisite of the Gate-7 source Hessian.

The earliest unresolved executable-readout object is instead prior to the
numerical Hessian evaluation.  The inherited formula

\[
 Z_{g,i}=\left.\partial_{p^2}
 \langle a_i,K_{A,i}(p)a_i\rangle\right|_{p^2=\mu_\star^2}
\]

originates as a formal contract in the v15.69 \(M_5\to M_4\) pushforward.
Its first common numerical operator realization in v15.96 is periodic.  On
the corrected forward domain, that Fourier realization is retired, but no
replacement construction defines either the family \(K_A(p)\), an admissible
BRST-transverse source family \(a_i(p)\), or the meaning of \(p^2\).  The
existing spatial DtN derivative supplies only spatial and Gauss coefficients;
its own claim boundary says that the dynamic frequency response and a single
Lorentzian Maxwell coefficient remain open.  Consequently the displayed
formula is a predeclared formal contract, not yet an executable forward-domain
evaluator.

This is not cured by compact source support.  If the source core and exterior
decompose a positive operator as

\[
 P-z=\begin{pmatrix}A-z&B\\B^*&C-z\end{pmatrix},
\]

then the exact compressed resolvent is

\[
 \Pi_c(P-z)^{-1}\Pi_c^*
 =\bigl(A-z-B(C-z)^{-1}B^*\bigr)^{-1}.
\]

Thus the exterior history enters a compact-core heat response only through
its Weyl--Calderón function.  A complete coefficient oracle or an actual
finite endpoint graph is sufficient to obtain that function, but neither is
logically necessary if the same exterior response can be derived directly.

The exact next dependency is therefore:

1. construct from the retained forward action an admissible one-parameter
   BRST-transverse source or operator family whose parameter is the declared
   \(p^2/\mu_\star^2\), and prove differentiability of the pair-plus-contact
   Hessian along it;
2. evaluate or rigorously enclose the exterior Weyl--Calderón response at the
   boundary of the certified source core;
3. then evaluate the weak geometry force, certify the same-action saddle, and
   compute the full pair-plus-contact residue with Ward/BRST control.

This audit changes no action, endpoint, source, scale, gate, prediction, or
physical time direction.  Chord 3 remains unauthorized, Gate 7 remains open,
and Gate 8 remains locked.
