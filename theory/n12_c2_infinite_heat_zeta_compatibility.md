# C2 infinite-route heat--zeta compatibility

The retained Standard Model zeta subtraction is history distributed:

`D Gamma_SM^zeta[h]=(59/30) integral h(tau) d_tau/R4(tau)`.

Write `d s_opt=d_tau/R4` and let `h_cs` be the Jacobi modulation
generated by the retained physical common-scale reset direction.  The exact
termwise zeta tail criterion is

`lim_(S,T->infinity) integral_S^T h_cs d s_opt = 0`.

Absolute optical integrability of `h_cs` is sufficient but not necessary.
The direction cannot be discarded: the reset radius Cauchy jet has rank two,
one time quotient cannot remove it, and lower action weights plus the boundary
Casimir make common scale a physical center rather than a gauge orbit.

This termwise criterion is sufficient, but it is not necessary for the full
replacement force.  The retained no-double-counting identity is exact:

`Gamma_Q=Gamma_parent+Gamma_heat
        =Gamma_attached^zeta-Gamma_SM^zeta+Gamma_heat`.

Consequently the weakest owner remains the Cauchy tail of the combined
physical replacement covector `q_rep=q_heat-q_zeta`.  A cancellation may be
used only if it is proved by this action accounting and the actual operator;
no difference of unrelated divergent terms or chosen relative reference is
allowed.

The accounting combines with the existing angular heat result as follows.

1. If the optical length is finite, every bounded `h_cs` has a finite zeta
   variation, but the exact positive-chirality transfer lower bound prevents
   the absolute graded heat terms from tending to zero.  A finite optical end
   therefore does not define the full infinite-route Gate-7 force.
2. If the optical length is infinite, the heat angular barrier route is not
   excluded.  A termwise construction must prove the scalar optical Cauchy
   condition above together with the full graded heat-cotangent convergence.
   Alternatively, the action may prove the combined projected `q_rep` Cauchy
   tail directly.  Separate zeta convergence is then not required.
3. A finite later event or canonical stop avoids both infinite-tail conditions
   and uses the retained compact endpoint theorem.

Thus radius history alone cannot close the infinite route.  For example,
`R4=1+tau` has infinite optical length: `h_cs=1` gives logarithmic divergence,
whereas `h_cs=(1+tau)^(-epsilon)` is absolutely integrable for every
`epsilon>0`.  This is a mathematical sharpness witness, not a BHSM tail.

No current artifact proves either the termwise pair or a direct combined
replacement cancellation on the actual C2 history.  The exact live
infinite-route owner is therefore the projected `q_rep` Cauchy tail; the
common-scale zeta optical condition is the sharp extra obligation only for a
termwise heat/zeta construction.
