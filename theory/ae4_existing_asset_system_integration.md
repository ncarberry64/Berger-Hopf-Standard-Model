# AE4 existing-asset systems integration

BHSM completion no longer treats each downstream target as requiring a new
mechanism. Hindsight shows that many broad “open” labels referred to operator
shapes or identity bridges already present elsewhere in the repository.

The following assets are reused without rebuilding the particle spectrum:

- frozen particle, family, representation, projector and current ledgers;
- the charged-lepton family-noncentral M4 operator and conditional local poles;
- color-singlet exterior-charge cancellation and Schur-polarizability sign;
- the `r2`/ell-two capture selection and ell-zero-to-ell-two mixing channel;
- the capture-created `nu_e` source and family-central no-oscillation theorem;
- neutral family-noncentral semigroup/wake shapes, two gaps and the exact
  historical-to-current mode identification;
- the AE3.1 outer-Calderón exhaustion theorem, now used as the reason AE4 must
  supply the new causal relative-boundary domain.
- the v17.84 event-to-child first variation and exact `F_child` boundary
  canonical-relation formula;
- the evaluated v17.86 finite-chart metric/lapse child Dirichlet slice and its
  KKT-multiplier DtN flux;
- the v17.87 persistent nonequilibrium child definition and first-domain-exit
  decay law.
- the v17.88--v17.98 dynamic Wentzell/Cauchy, attachment, scalar,
  zero-background gauge--spinor--ghost--HS, and discrete-firewall chain,
  which closes the retained event-to-child boundary map;
- the v17.99 positive-duration, constraint-consistent moving-child witness;
- the v21.35 N3--N6 exact-attachment weak-conormal complete persistent
  children, fixed-background Calderon graph convergence, weak bulk-tail
  decay, exact N-minus-two shell bound, and asymptotic high-shell inverse.
- the later direct N12 complete persistent child and resolution-independent
  continuum event-child certificate, which supersede the v21.35 finite
  N6-to-M0 bridge and the historical v21.37 fixed-chart frontier;
- the AE3 coefficient-free `sigma=0` local same-spacetime enclosure carrier,
  its resolved interface variation, and the commuting transport of all nine
  charged-sector/family state fibers into that enclosure;
- the one-forward-time domain and local singular hitting/reset relation.

This reduces several old blockers:

```text
new family-noncentral mechanism
  -> existing charged and neutral noncentral operator shapes;

independent M8/M5/M4 Wilson coefficients
  -> retired by the single AE4 Dirac-zeta owner;

new particle/cutoff scale
  -> ell_star from the first future collapse-impedance surface;

separate gauge, fermion and scalar outer oracles
  -> one direct-sum BRST stratified operator;

particle-spectrum reconstruction
  -> prohibited and unnecessary.

stationary soliton or exact-return particle requirement
  -> superseded by the already-derived and positively evolved complete
     persistent nonequilibrium children.
```

The actual remaining root object is therefore one operator:

```text
D_strat_ret[Phi_star,Sigma_star]
```

on the parent-to-child future-collapse relative-boundary domain, with the
complete BRST/ghost and zero-mode quotient. The same AE4 functional and its
retarded resolvent must simultaneously produce:

- the Maxwell residue, neutral Hessian and photon pole;
- charged and neutral dressed fermion poles;
- the action-evaluated neutral wake and weak-flavor intertwiner;
- the `r2` capture Hessian and outgoing neutrino boundary mode;
- the color-singlet returned resolvent and residual hadronic response;
- Ward-related vertices, LSZ amplitudes and collision channels;
- metric/stress variations used by gravity and cosmology.

The child calculation is complete through the continuum certificate and must
not be solved again. Global forward reachability remains a parallel dependency
for `Q_xi` and parent-relative energy, but it does not reopen localization:
AE3 already selected the local same-spacetime `sigma=0` enclosure. The primary
systems join is now the one background-covariant AE4 current-C2 stratified
operator with explicit gauge--ghost, nonzero fermion, HS-source, and response
multiplier blocks on the reset-glued maximal-history domain. Its event
canonical flux and complete Noether--Hamiltonian balance are then evaluable.

This is dependency compression, not physical promotion. The global operator,
stationary background, `ell_star`, dressed poles and amplitudes remain
unevaluated. No fitted repair or independent oracle is authorized.

The machine integration also defines the museum export firewall. Derived,
conditional, simulated and external experimental data must remain visibly
distinct; conditional poles are not measured masses and neutral shape gaps
are not neutrino mass splittings.

Scientific milestones:

```text
AE4_EXISTING_PARTICLE_AND_ENCLOSURE_ASSETS_SYSTEM_INTEGRATED = TRUE
AE4_DUPLICATE_BROAD_GATES_REDUCED_TO_ONE_OPERATOR_EVALUATION = TRUE
AE4_N3_TO_N6_EXACT_ATTACHMENT_COMPLETE_PERSISTENT_CHILDREN_REUSED = TRUE
AE4_FINITE_N6_TO_M0_NORMAL_SCHUR_BRIDGE_CERTIFIED = TRUE
AE4_CONTINUUM_EVENT_CHILD_CERTIFICATE_REUSED = TRUE
AE4_GLOBAL_FORWARD_TERMINAL_CHART_REACHABILITY_DERIVED = FALSE
AE4_GLOBAL_RETARDED_STRATIFIED_OPERATOR_REALIZED = FALSE
```

`FULL_BHSM_COMPLETE = FALSE`.
