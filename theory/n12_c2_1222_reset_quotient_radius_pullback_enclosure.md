# N12 C2 1222-core reset-quotient radius pullback enclosure

## Scope

This theorem addresses one exact slot in the Gate-7 forward-event diagram: the
pullback of the stored 1222-segment Weyl cotangent through the child
log-radius coefficients.  It does not replace the moving-duration term, the
graded heat-minus-zeta source contraction, or the physical force root.

## Jacobi provenance reconstruction

Each certified continuation segment carries a local action-coordinate
Jacobi-growth bound.  The first 436 segments, the final 150 scalar/matrix
segments, and the eight matrix-Lohner segments already serialize that factor.
The compensated, adaptive, and recentered-adaptive continuations used the same
factor in their validated tube recurrences but did not serialize it.  Their
stored centers, descriptor increments, incoming tubes, and parent majorants
deterministically replay the retained ball generator.  The replay is checked
against the stored denominator and radius data before the factors are used.

For node `i`, let `G_e >= 1` be the certified local factor on segment `e`.
Then the fixed-descriptor state variation obeys

`||D_{Y0} Y_i|| <= product_{e<i} G_e`.

Products are accumulated in logarithmic space.  Recentring proof balls does
not reset the physical tangent propagation.

## Radius-covector pullback

The exact attachment coordinate is `x=log R4`.  Its only nonlinear coordinate
dependence is the boundary Berger term.  On a node tube of action radius `r`,

`||D_Y x||_* <= ||D_Y x(Y_c)||_* + 2 ||b_signs||_*^2 r`.

If `c_i=D_{x_i}M_C(z)` is the inverse-free backward Weyl cotangent, the fixed
node-radius contribution therefore satisfies

`||D_{Y0}M_C|_x|| <= sum_i |c_i| ||D_Yx_i||_* product_{e<i}G_e`.

The C2 block of a unit vector in the orthonormal reset tangent has norm at most
one.  Passing to an orthogonal gauge/time quotient cannot enlarge this bound.
Thus the same number bounds every physical reset-image direction, including
the retained common-scale direction.

## Exact remaining term

The stored Weyl cotangent also has a nonzero coefficient
`D_{h_e}M_C` for every proper segment duration `h_e`.  A duration interval is
not a duration first jet, and a proof-tube width is not a physical variation.
The missing object is the same-action moving-time covector

`D h_e[h] = integral_e D_Y(N_boundary s/Delta)[J_h] ds`

with its transverse moving-edge term, pulled back through the fixed-descriptor
state propagator and reset tangent.  Until that contraction is certified, the
full `D_xi M_C`, graded force, saddle, and Hessian remain open.

No reset semantics, recurrence, selector, scale fit, action term, terminal
recurrence, or third chord is introduced.
