# Gate-7 maximal compliance-seam contraction

Status: `MAXIMAL_CHILD_LOAD_CANNOT_AMPLIFY_FIXED_TERMINAL_INCOMING_COMPLIANCE_COTANGENT`.

Work in one retained scalar, transverse-gauge, Hubbard--Stratonovich, or
product-Dirac chirality channel at a neutral probe `z=-kappa^2<0`.  Along the
certified fixed-terminal incoming-amplitude family, the event/child state,
reset frame, contact, and outgoing C2 history are fixed.  Only the incoming
compliance

`C_f=M_f^-1>0`

varies.  The complete internal load in the event frame is

`L=U_R^dagger M_C2^max U_R+W_phys>=0`.

The scalar seam Green response has the two exactly equivalent forms

`G_S=(M_f+L)^-1=C_f/(1+C_f L)`.

No value or first jet of `M_C2^max` is needed in this direction.  Holding the
fixed-terminal load fixed and differentiating the joint response gives

`D G_S=(D C_f)/(1+C_f L)^2`.

Consequently

`|D G_S|<=|D C_f|`

on the whole negative real resolvent axis.  The inequality is uniform over
every nonnegative action-owned maximal child/contact load and becomes strict
unless the load vanishes.  Since the existing compliance chart gives
`D_lambda C_f=O(lambda)`, the unknown maximal C2 load cannot restore the
short-arm Dirichlet-to-Neumann pole or amplify its fixed-channel incoming
amplitude cotangent.

This is a source-contracted statement for the incoming-amplitude direction,
not a full noncompact Weyl-jet theorem.  It does not compare heat traces:
pointwise resolvent contraction does not determine the signed nonlinear
heat-minus-zeta functional, and the retained negative-axis no-go remains in
force.  The unresolved object is now localized to the actual complete graded
source-contracted seam spectral measure (or an equivalent functional-calculus
cotangent) and its physical-quotient maximal Cauchy tail.  A separate
`D_lambda M_C2^max` is not part of this fixed-terminal direction.

Only the external Cauchy/birth datum is zero.  No internal response is zeroed,
no extra seam source or force is introduced, and no selector, endpoint,
recurrence condition, scale, fit, gate, or chord is added.
