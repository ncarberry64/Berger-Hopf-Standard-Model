# BHSM-AE-2.0.0 nonfermionic zero-threshold margin

Status: `AE2_NONFERMION_ZERO_THRESHOLD_RESONANCE_EXCLUDED`.

On the AE2 global transmission domain the zero-energy seam operator is

\[
 M_e(0)+U_R^*M_c(0)U_R+W_{\rm phys}.
\]

The event and child arms are nonnegative source forms, unitary conjugation by
the reset lift preserves quadratic-form order, and the retained Wentzell block
is nonnegative. Consequently every positive scalar or de Rham channel inherits
the already-certified child-core impedance lower bound
`6.37052204298831e-8`; no unknown nonnegative event exterior can cancel it.

For transverse gauge modes, the action-derived block

\[
 K_F^{(5)}c_G(\Delta_1^{\rm coexact})^{1/2}
\]

adds a lower bound of `650.627973541321` at the minimum retained group ray,
the lowest coexact unit-radius eigenvalue `4`, and the certified radius upper.
The constant scalar channel has zero first log-radius vertex, while the global
gauge zero mode is already removed by the retained quotient.

This excludes the critical Robin countergraph for the nonfermionic source
blocks without evaluating the event Calderon map. It does not close the
factorized Weyl channels: `A_s u=0` still gives a valid local zero-transport
mechanism there. A boundary-uniform limiting-absorption estimate, the graded
angular tail, and the numerical zero-source force also remain open. No new
endpoint, reference operator, coefficient, scale, source profile, or physics
is introduced.
