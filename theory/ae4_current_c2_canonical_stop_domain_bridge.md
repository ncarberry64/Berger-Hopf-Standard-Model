# AE4 current-C2 canonical-stop domain bridge

## Recovered endpoint result

The current repository already proves more than the older maximal-child
wording reflected.  One exact forward reset history reaches the action-owned
branch-24 canonical earliest stop; the crossing is transverse, its first-hit
time has a certified interval, and continuous dependence promotes the center
witness to a nonempty open 72-dimensional stop-reaching reset stratum.

The retained endpoint theorem already decides the domain at such a stop:

```text
canonical stop -> Friedrichs form closure of the retained nonnegative
                  minimal form.
```

Therefore the stop branch does not require an independently selected finite
child Weyl load or independently supplied terminal-load HS jets.  In the
factorized first-order representation this is the Dirichlet/Friedrichs
form-core terminal graph, represented by `terminal_load=None`.

## What remains dynamical

This endpoint result does not identify the 1,222-segment proof cutoff with the
physical stop.  It also does not freeze the physical endpoint.  The nonlinear
current-C2 coefficient path must be assembled through the certified stop, and
the variation must retain both bulk-coefficient and moving-stop terms.  Those
terms include any HS-induced stop motion selected by the full AE4 Hessian.

The alternative event branch is unchanged: if a history encounters another
actual event instead of this stop, its child Weyl family and HS jets are still
required.  The present result closes the terminal-domain ambiguity only on
the already-proved nonempty canonical-stop stratum.

## Consequence

The exact next operator calculation is no longer “choose or fit a terminal
load.”  It is:

```text
stop-matched nonlinear log(R4) and proper-duration path
  + HS bulk/moving-endpoint jets
  + action-selected Friedrichs terminal graph
  -> full first-order product-Dirac Weyl jet
  -> AE4 E1 Hessian.
```

No particle spectrum, child construction, boundary coefficient, or backward
time branch is introduced.

`FULL_BHSM_COMPLETE = FALSE`.
