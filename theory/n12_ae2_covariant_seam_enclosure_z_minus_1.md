# N12 AE2 covariant seam enclosure at z=-1

Status: `AE2_TWO_SIDED_CHILD_LOAD_AND_COMPACT_COVARIANT_JETS_ENCLOSED_BROADLY_AT_Z_MINUS_1`.

The reset lift `U_R` is a transition map of the one global
`Spin x G_SM` bundle selected in AE2.  With the compatible event-child
connection it is covariantly parallel,

`nabla_Phi U_R=0`.

Thus the ordinary derivatives of a moving frame do not constitute a new
physical source.  They are absorbed into the covariant derivative of the
child response.  For

`B_event=U_R^dagger M_child U_R+W_phys`,

the exact first and mixed covariant jets are

`nabla_Phi B_event=U_R^dagger(nabla_Phi M_child)U_R+nabla_Phi W_phys`,

`nabla_PhiPsi B_event=U_R^dagger(nabla_PhiPsi M_child)U_R`
`+nabla_PhiPsi W_phys`.

This does not erase physical event-child orientation.  Relative orientation
is encoded in the covariantly pulled-back child response relative to the
event response, rather than in a separately counted frame derivative.

The retained certified two-chord child core already supplies broad,
future-independent Calderon and compact-support first/mixed jet bounds at
the native resolvent probe `z=-1`.  Unitary pullback preserves the
quadratic-form intervals and operator-norm jet bounds.  For AE2 fermions
`W_phys=0`, so the stored product-Dirac child intervals pull back unchanged
to the event effective-load intervals.  The scalar/de Rham enclosures transfer
the same way, with their retained nonnegative Wentzell terms added where
present.

This closes the corrected two-sided seam at one negative resolvent probe in
a broad interval sense.  It does not supply the complete spectral functional
needed by `exp(-ell^2 P)P^-1`, a numerical heat-minus-zeta force, a reset-fiber
selector, or a saddle.  The next theorem must extend the seam values and jets
to that complete spectral functional or materialize the equivalent joint
finite-history operator.

`FULL_BHSM_COMPLETE=false`.
