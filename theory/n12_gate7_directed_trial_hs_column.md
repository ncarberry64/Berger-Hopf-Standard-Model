# Frozen trial directions inside the physical HS variation

This pilot changes interval evaluation order, not the retained physical action.
For one frozen trial column e_L or e_R, enclose the complete variations
A_L = DF_L e_L and A_R = DF_R e_R directly. Retain every original D3/D4
contraction, selected-line variation, physical-response variation, and coupled
normalization derivative. Configuration and descriptor directions are the actual
supplied weighted directions, not an ambient identity-column index.

The actual Hermite–Simpson midpoint chain directions are
W_L = e_L/2 + h A_L/8 and W_R = e_R/2 - h A_R/8.
Enclose B_L = DF_M W_L and B_R = DF_M W_R on the original independently paired
actual midpoint outer domain. Interval W relaxes crosspoint correlations and
therefore may overestimate. It is not a new domain or a new eigenpair theorem.
The point check uses the midpoint of each input-direction ball while the uniform
calculation retains its full uncertainty.

With P = R_frozen^-1 T_frozen, the defect columns are

    DL_j = P ((L_frozen + I)e_L + h A_L/6 + 2h B_L/3)
    DR_j = unit_j + P (-e_R + h A_R/6 + 2h B_R/3).

These follow by differentiating the original HS residual convention. Algebraic
association can change interval widths; either enclosure remains valid. Each
coordinate may select the smaller radius of independently valid overlapping
enclosures, including the already paired ambient-matrix baseline.

The pilot uses the exact paired primal psi/response sources used by the compared
full ambient derivatives, retaining lambda and all frozen geometry. Every stage
must independently reproduce before downstream use. One column does not certify
all 74 columns, physical quotient or frame derivatives, global contraction,
continuum closure, or BHSM completion. No measured data or fitting enters.
