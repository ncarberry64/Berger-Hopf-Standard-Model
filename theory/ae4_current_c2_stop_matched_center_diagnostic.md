# AE4 current-C2 stop-matched center diagnostic

## Calculation

The retained cancelled-flow identity supplies physical proper time without
identifying the stored action-arc coordinate with time:

```text
d tau / d r = N_boundary s / ||G_theta||_action.
```

The accepted 371-node correlated center is truncated at the left, midpoint,
and right values of the certified canonical first-hit interval.  At each
representative endpoint the action-owned stop descriptor is set to zero, the
coefficient path is pulled back to proper time, and the product-Dirac Weyl
graph is propagated with the already-selected Friedrichs terminal domain.
No finite terminal load is inserted.

The midpoint center has proper duration

```text
T_center = 1.4769525610799176e-4.
```

At `z=-1`, the finest piecewise-linear subdivision gives

```text
chirality +: M_birth = 6769.190672162319
              D_H M = -0.9998515506191017
            D_H^2 M = 9.846350179442918e-5

chirality -: M_birth = 6772.205986753127
              D_H M = -1.0001484493810138
            D_H^2 M = 9.846350187105560e-5.
```

Subdividing every proper-time segment from one to eight pieces changes the
birth Weyl value by less than `4e-11` and either HS jet by less than `1e-12`.
This is numerical discretization stability, not an interval shadowing proof.

## Scientific consequence

The physical-duration stop diagnostic is qualitatively different from the
`1.255e-27`-duration 1,222-segment birth-local proof prefix.  In particular,
the stop-matched center second HS jet is near `9.846e-5`, whereas the prefix
result was suppressed by its tiny proof duration.  The prefix therefore
cannot stand in for the physical stop-matched operator.

## Claim boundary

The canonical endpoint domain and first-hit interval are certified inputs.
The accepted center path is not yet a correlation-preserving outward
nonlinear shadow, and its HS-induced moving-stop jet is not yet derived.
Consequently these values are a BHSM-native operator diagnostic, not a
physical scalar residue, Higgs mass, or completed AE4 Hessian.

`FULL_BHSM_COMPLETE = FALSE`.
