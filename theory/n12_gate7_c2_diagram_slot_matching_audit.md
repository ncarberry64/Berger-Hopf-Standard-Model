# N12 Gate-7 C2 diagram-slot matching audit

Status: `C2_WEYL_THEORY_MATCHED_ACTUAL_RESET_SELECTED_REALIZATION_OPEN`.

The forward-event diagram is a calculation, not a mnemonic:

`C1 --M_f--> E1 --(U_R,W_phys)--> C2 --M_C2--> maximal endpoint`.

Eliminating the `C2` arm requires a Weyl--Calderon map

`M_C2(z;xi): H_C2 -> H_C2`,

where `H_C2` is the retained BRST-quotiented birth trace space in the child
frame, `z` lies in the Dirichlet-reference resolvent set (in particular real
`z<0`), and the conormal has the outward orientation of the child arm.  The
same object must be available as a spectral family, not only at `z=-1`, and
its first and second reset-quotient covariant geometry jets are required for
the force and Hessian.  Its far end is the already retained maximal endpoint
class: a later AE2 event graph if hit, otherwise the canonical Friedrichs
closure at a retained canonical stop.  This is the exact mathematical type
of the `C2` leg.

The repository already defines that type.  The action-owned maximal-forward
source-form theorem constructs `M_C(z)` on any retained reset-generated child
history.  Applying the existing construction to the reset output at `E1` is
the leg relabeling

`M_C2 := M_C^max[Y_C2, endpoint_class_C2]`.

This is a valid provenance-preserving match, not equality with the incoming
`C1` response.  AE2 supplies one global `Spin x G_SM` bundle, so the reset
lift is the transition map between the event and child frames.  The exact
event-frame load and seam are therefore

`B_E1 = U_R^dagger M_C2 U_R + W_phys`,

`S_AE2 = M_f + B_E1`.

The existing covariant seam routine implements precisely these compositions.
The maximal Friedrichs exhaustion and fixed-finite-stratum oracle are valid
construction/evaluation engines once the actual `C2` coefficient form,
endpoint stratum, quotient, and jets are supplied.

The matching audit rejects the apparent substitutes:

- the compact two-boundary `C1` response and `M_f` occupy the incoming leg;
- `U_R` is a transition map and `W_phys` is a vertex/contact block;
- the N12 event-child `7 x 7` symbol is a local constraint/reaction graph,
  not a temporal, `z`-dependent exterior Weyl family;
- the N3 zero-background Calderon row is a zero-field compatibility result,
  not the N12 nonzero response;
- the event-shell operator is a weighted spatial M5 response;
- the terminal Laurent germ and two-chord enclosures are partial incoming or
  finite-core data and do not determine the full `C2` spectral family;
- one negative-axis probe or broad seam enclosure cannot synthesize the
  retained heat functional;
- historical periodic, round-collar, and reduced scalar constructions have
  the wrong temporal domain or physical provenance.

Accordingly, `M_C2` is **not genuinely new as an operator theory or diagram
object**.  What is actually missing is its action-selected N12 realization:
the reset-produced `C2` coefficient/form history (or equivalent joint
operator), its retained endpoint outcome, and the first two covariant
reset-quotient jets.  The word "missing" in earlier Gate-7 ledgers is henceforth
read as "missing realized oracle data," not "missing child-response theory."

The next calculation is fixed: instantiate the existing maximal-forward Weyl
construction on the `E1 -> C2` reset output, route a finite regular stratum
through the inverse-free parametric oracle or a Friedrichs route through core
exhaustion, conjugate by `U_R`, add `W_phys`, and then combine with `M_f`.
No new child theory, selector, endpoint, coupling, scale, time direction,
recurrence, or gate is introduced.

Gate 7 remains active at actual `C2` realization and force evaluation;
chord 3 remains unauthorized and `FULL_BHSM_COMPLETE=false`.
