# N12 finite-history spectral-realization provenance

Status: `TWO_BOUNDARY_RESPONSE_REDUCTION_DERIVED_PHYSICAL_TERMINAL_LOAD_OPEN`.

The certified chronology

`E0 -> C1 ->[T>0] E1 -> C2`

is not reopened here.  On its compact formation segment, the retained
quadratic action supplies `K(xi)`, `D_xi K`, and the free two-boundary
Calderon response

`(n_0,n_1)^T=M_C(u_0,u_1)^T`,

with endpoint order `(birth,new_event)` and outward conormals
`(-p_birth,+p_new_event)`.  This is response data, not by itself the domain of
a positive self-adjoint heat operator.

If the terminal action graph supplies

`n_1+B u_1=0`,

then its action-owned reduction is exact:

`(M_11+B)X=M_10`,

`M_birth=M_00-M_01 X`.

The first and mixed geometry jets are obtained by the triangular bordered
systems

`H X_h=M_10,h-H_h X`,

`H X_k=M_10,k-H_k X`,

`H X_hk=M_10,hk-H_hk X-H_h X_k-H_k X_h`,

where `H=M_11+B`.  Differentiating `M_00-M_01 X` then gives the reduced
first and mixed jets.  The implementation forms no inverse.

At an AE2 event the terminal load is not a free Robin datum.  The retained
two-sided seam gives

`B_AE2=U_R^dagger M_C2 U_R+W_phys`,

with the corresponding covariant geometry jets.  Consequently the already
certified formation-segment `M_C` and terminal Laurent germ do not determine
the physical spectral realization until that child response is supplied, or
until the equivalent joint two-sided finite-history operator is assembled.
Replacing it by zero, Friedrichs, Dirichlet, Neumann, periodic, reflected, or
a validation endpoint would change the domain and is not authorized.

This also fixes the force provenance.  The zero-source heat force

`(1/2) Tr[exp(-ell^2 P) P^-1 D_xi P]`

requires the complete positive self-adjoint `P(xi)` and its domain jet.  A
single Weyl probe, the fixed-duration coefficient part of `D_xi M_C`, or the
owner finite-encapsulation ontology cannot be substituted for that operator.
The zeta contribution is already derived, but the total force, same-action
saddle, and constrained physical Hessian remain downstream of the missing
AE2 terminal response/full joint realization.

HINDSIGHT: the compact history and its endpoint labels are closed; the
remaining issue is operator-domain propagation into the immediately
following child phase, not forward-history existence, reset recurrence, or a
new physical selector.

`FULL_BHSM_COMPLETE=false`.
