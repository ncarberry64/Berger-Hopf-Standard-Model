# N12 asymptotic-NHIM angular-force no-go

Status: `FINITE_OPTICAL_NHIM_CHILD_ROUTE_EXCLUDED_FROM_ABSOLUTE_GRADED_FORCE_DOMAIN`.

The finite-N12 normally attracting asymptotic family has

`epsilon=R4^-2`, `epsilon'=-2 H4 epsilon`, and `H4 -> H0>0`.

For every history captured by that family, choose `T` so that
`H4(tau)>=H0/2` for `tau>=T`.  Then

`epsilon(tau)<=epsilon(T) exp(-H0(tau-T))`

and therefore

`integral_T^infinity d_tau/R4(tau)
 <=2 sqrt(epsilon(T))/H0<infinity`.

The initial regular segment has finite optical length, so the complete
optical length `I=int_0^infinity d_tau/R4(tau)` is finite and positive.

This places every captured infinite child history in the exact obstruction
class already derived by the AE2 angular source-Dini audit.  For a retained
nonnegative compact log-radius source with nonzero weighted integral, the
positive-chirality level coefficient obeys

`C_mu >= c_h exp(2 mu I)`, `c_h>0`,

while `mu_n=n+3/2` and `d_n=48(n+1)(n+2)`.  Hence the terms
`d_n C_mu_n` do not tend to zero and the absolute graded angular source-Dini
sum diverges.  Every fixed channel remains source-Dini finite; the failure is
only the required absolute direct sum over retained internal angular levels.

The retained BRST pairing does not cure this failure.  It cancels the
longitudinal--ghost pair, but the physical transverse/HS/Weyl graded leading
coefficient is nonzero, and Gate 7 has no action-owned relative reference
that replaces the absolute source class.  The retained subtraction is
explicitly local:

`D Gamma_SM^zeta[h]=(59/30) integral h d_tau/R4`.

For the compact source above and finite optical length this is one finite
number.  Subtracting it cannot turn an absolute angular series whose positive
terms fail to tend to zero into a convergent absolute series.  Thus the heat
part already fails the absolute graded differentiability requirement, and the
existing zeta subtraction does not make this route an admissible force root
in that class.

This is a conditional-route no-go, not a claim that the certified reset image
enters the asymptotic basin.  That connection remains unproved, but it is no
longer useful as a Gate-7 completion route: if a reset history enters the
basin, its infinite end does not carry the required absolute graded
heat-force covector.  The mathematical NHIM and its Friedrichs operator are
not deleted, and no new canonical stop is declared.

The live completion alternatives are therefore an actual finite later event
or retained canonical stop, where the compact-endpoint angular/source and
adjoint theorems apply, or an independently action-owned infinite route with
the missing optical-complete/relative-trace structure.  No arbitrary
infinite-tail campaign is reopened.  Gate 7 remains active, Gate 8 remains
locked, chord 3 remains unauthorized, and frozen predictions are unchanged.
