# Correlated central Green scalar causal composition

The all-interval correlated central Green scalar supplies a finite local
Hermite--Simpson second residual on every retained interval.  This certificate
applies the already frozen current-center test/trial frames, left Newton
blocks, and reduced right-block inverses to those residuals in their retained
order.  The reset second response is fixed at zero.  A 384-bit reconnaissance
localized precision amplification; the promoted causal recurrence is evaluated
at 512-bit Arb precision.

The maximum causal norm upper is `8.405509181456809` at terminal node 370,
with central representative norm `8.405500854737143` and terminal exported
radius norm `1.5732549213095888e-5`.

This is a scalar-line causal composition.  It introduces no new action,
center, trajectory, branch, scale, partition, or fit.  It does not absorb the
certified difference between the selected central axis and the exact Green
axis.  Therefore the mixed Green/transverse and transverse-transverse
neighborhood remainder remains explicit and must be combined with this result
before the longitudinal/transverse two-radius theorem can be tested.

At 384 bits the nodewise recurrence exposed precision amplification beginning
at node 325: the representative stayed finite while only the enclosure radius
grew.  Re-evaluating the same action, center, directions, and recurrence at 512
bits removes that numerical wrapping without a cutoff or fitted adjustment.
The resulting finite causal scalar is therefore suitable as the central
longitudinal operand, while the exact-axis neighborhood and two-radius theorem
remain separate obligations.
