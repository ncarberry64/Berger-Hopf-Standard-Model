# N=12 Gate-7 global connection obstruction

Status: `EXACT_GLOBAL_CONNECTION_OBSTRUCTION_LOCALIZED`.

The compact-domain input has since been sharpened: the retained terminal
reset radii theorem now certifies a closed 72-dimensional quotient ball with
nonempty relative interior, uniform regular-domain margins, and a strictly
positive first-jet singular margin.  The missing route-B object is therefore
the boundary-controlled propagation or first-hit map of that whole compact
domain, not the parameter domain itself.

This result consumes the sixteen-segment cancelled-descriptor cover, its
action-owned endpoint recenter, and the first sheared invariant-graph block
as closed local machinery.  It does not add another continuation block.

## Certified endpoint and observed finite-cover trend

The final expanded-cover tube has radius below `9.9093e-11`.  The endpoint
recenter retains branch 24, gives a fresh line chart above `2.2923e-8`, and
reconstructs the cancelled field with center `Delta=8.9204400254e-14`.
The first sheared block keeps

`Delta in [2.4678711968e-14,1.5373008855e-13]`,

positive lapse and proper duration, and a strictly positive correlated
descriptor interval.

Across the preceding sixteen segments, however, the accepted `theta` steps
decrease from `1.3513380e-8` to `2.9531773e-10`; the median successive ratio
is about `0.7747`.  The endpoint tube grows while the signed-descriptor center
increases.  This is a finite certified record, not a theorem that the same
ratio persists.

Applying the exact stored 98-state terminal projection to the seventeen
cover centers and the sheared endpoint resolves no binary64 change in
`log R4`.  The terminal product norm decreases only from about
`5.58720386279967e6` to `5.587203862792232e6`.  The endpoint has
`log epsilon=0.0101924947464`, whereas the certified capture tube requires
`log epsilon` below approximately `-4952.93110456745`.  These figures do not
prove nonconnection.  They prove that the existing finite prefix is not a
numerical overlap certificate and exposes no resolved long-range radius
trend that can be promoted.

## Globalization audit

The signed descriptor is monotone on the realized cover because the
invariant graph gives `E=s-lambda(Y)` constant and the certified local
`Delta` intervals are positive.  It is not the capture-distance functional.
No global inequality relates its growth to `epsilon`, the 25 center
coordinates, the 25 velocity normals, and the 24 multipliers in the terminal
chart.  Positivity of `H4` and decay of `epsilon` are certified inside the
capture tube, not throughout the precapture component.

The sheared graph and the asymptotic stable cone also do not yet overlap as
one theorem.  At the recentered endpoint the exact center tangent has
positive numerical abscissa, while the interval logarithmic-norm bound is
large and positive.  This does not exclude a better BHSM-native cone, but no
single cone field with uniform inequalities has been certified on a
connected region joining the two local domains.

The reset launch theorem supplies a 73-dimensional local manifold and the
terminal chart supplies an executable 74-component descriptor plus
`log epsilon`.  Dimension counts do not define a degree.  The repository has
no certified compact reset-parameter box, no validated propagated map from
that box to the terminal chart, no declared square transverse map, and no
boundary exclusion or covering-face data.  Thus a Brouwer degree is
undefined here, not zero.

Finally, the existing cover is finite only because a fixed 16-step budget was
chosen.  It terminates with its action domain open, has no uniform future
step lower bound, and supplies no finite bound on the number of required
recenters.  The first sheared recenter removes the old local proof-ball edge;
it does not compactify the remaining path.  No retained event or canonical
stop has been reached.

## Exact missing connector

One of the following finite BHSM-native objects is still required:

1. a connected precapture invariant region with uniform retained-domain
   margins and an action-owned inequality forcing finite capture-tube entry;
2. a compact nonempty reset-quotient parameter domain, a validated flow or
   first-hit map into the terminal chart, and either strict tube inclusion or
   a square transverse map with boundary exclusion and nonzero degree; or
3. a uniform transverse first-hit certificate for one already retained
   canonical stop, with every earlier domain margin preserved.

This is an obstruction to the current proof chain, not a proof that the
retained BHSM flow has no connecting history.  Another radius optimization,
proof-center recenter, or selected-line refinement does not supply the
missing global object.  Gate 7 therefore remains open on exactly one global
connector; the rank-72 force, KKT root, and constrained Hessian remain
downstream of that connector.

No selector, recurrence, chord, fitted scale, action term, physical time
direction, endpoint, or frozen prediction is added or changed.
