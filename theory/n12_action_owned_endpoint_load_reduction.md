# N12 action-owned endpoint-load reduction

Status: `ENDPOINT_DOMAIN_CLASS_OWNED_MAXIMAL_CHILD_WEYL_FAMILY_OPEN`.

The present Gate-7 obstruction is not a free choice between Neumann and
Dirichlet endpoints.  AE2 already fixes the domain class on each actual
maximal history.  At an event the physical operator uses the retained
two-sided trace/conormal transmission graph,

`B_event(z;xi)=U_R(xi)^dagger M_child(z;xi) U_R(xi)+W_phys(xi)`.

At a canonical stop the retained nonnegative minimal form is closed by its
Friedrichs closure.  A regular validation cutoff is not an endpoint, and a
free Neumann condition is admissible only if the action supplies an actual
regular terminal.  Thus the opposite-sign Neumann/Dirichlet counterpair
proves that the broad seam enclosure is information-insufficient; it does
not exhibit two competing physical AE2 theories.

Proper time, forward orientation, `D_tau`, and
`Delta_tau=D_tau^star D_tau` with the retained endpoint form are also already
action-owned.  The only unresolved bulk history coefficient in the current
source representation is `log R4(tau)`.  Consequently the minimal remaining
theorem is:

1. On a nonempty regular forward-reachable reset-quotient stratum, validate
   the action-determined maximal child coefficient family through its first
   actual event or canonical stop, preserving the retained margins.
2. On that same common `C2` form domain, construct or enclose
   `M_child`, `D_Phi M_child`, and `D_Phi2 M_child` on a nonempty native
   resolvent region.
3. Supply this family across the fixed reset stratum, or prove action-derived
   reset-fiber invariance before using one stored representative.

The equivalent route is a complete joint two-sided finite-history operator
with the same geometry jets.  This is an existence theorem on a nonempty
physical stratum, not universal terminal reachability.  Finite-encapsulation
existence is not reopened.  No boundary condition, endpoint, selector,
action term, scale, fit, time direction, or gate is added.

After this oracle is supplied, the existing inverse-free interface computes
the projected heat-minus-zeta force; the retained nullspace/bordered KKT
machinery then owns the same-action saddle and pair-plus-contact Hessian.

`Gate7=ACTIVE`, `Gate8=LOCKED`, `chord_03_authorized=false`, and
`FULL_BHSM_COMPLETE=false`.
