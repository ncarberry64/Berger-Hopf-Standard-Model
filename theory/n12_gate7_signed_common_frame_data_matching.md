# Gate-7 signed common-frame data matching

The retained objects fill every algebraic slot of the correlated shadowing
problem.  The adaptive cover supplies the complete 62-component bordered
response and exact path derivative on 8,692 cells.  The constraint geometry
supplies 98-to-73 physical quotient frames, center graph Jacobians, approximate
73-dimensional step maps, the correctly signed defect samples, and the
minus-defect Green correction.  The stored descriptor center already has its
certified first-hit theorem.

These objects must not be overpromoted.  The tangent, Jacobian, residual, and
Green records are center reconnaissance rather than interval theorems.  In
particular, a path derivative along the history is not the transverse
physical-tube derivative required for `Z2`.

The exhaustive match leaves exactly three implementation/certification
adapters:

1. an outward quadrature and interpolation remainder for
   `Y=||A(-d)||_P`;
2. a common-frame interval inverse-defect bound
   `Z1=||I-A L||_P`;
3. a 73-direction physical-tube `D2f` enclosure and correlated Green
   contraction for the literal `Z2` Lipschitz inequality.

Once those three bounds close the retained radii inequalities, the same tube
transfers the center first hit and every regular-domain margin.  This matching
introduces no action term, boundary condition, operator, selector, scale, or
new theory choice.
