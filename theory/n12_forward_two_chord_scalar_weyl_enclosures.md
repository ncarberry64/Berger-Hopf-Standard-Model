# N12 two-chord scalar/de Rham Weyl enclosures

Status: `TWO_CHORD_SCALAR_DERHAM_WEYL_AND_COMPACT_WEAK_JETS_ENCLOSED_BROADLY`.

For one fixed scalar or de Rham channel on the certified forward core, write

\[
 -u''+V_cu=-\kappa^2u,
 \qquad V_c(\tau)=c/R_4(\tau)^2,
 \qquad c\geq0.
\]

The action Green-form convention at birth is
\(M=-u'(0)/u(0)\). The unknown retained future of the same nonnegative
Friedrichs form induces a nonnegative inward graph at the end of the known
core, \(-u'(T)/u(T)\geq0\), with Dirichlet understood as the infinite limit.
If the certified core has duration at least \(T_-\) and
\(0\leq V_c\leq V_{\max}\), scalar Sturm comparison gives

\[
 \kappa\tanh(\kappa T_-)
 \leq M(-\kappa^2)
 \leq K\coth(KT_-),
 \qquad K=\sqrt{\kappa^2+V_{\max}}.
\]

The two certified chords give
\(T_-=2.2469219125168108\times10^{-8}\) and
\(R_4\geq0.9766928558375815\), hence
\(V_{\max}=c/R_{4,-}^2\). The artifact evaluates the exact stored round
scalar and one-form spectra through level three at the neutral probe
\(z=-1\). These rows are witnesses; the comparison formula applies
parametrically to every nonnegative fixed-channel eigenvalue. The constant
channel upper bound remains above \(4\times10^7\), so the result is rigorous
but intentionally records how weak the very short known core is.

For log-radius directions compactly supported in the certified two-chord core,
with
\(\|x_h\|_\infty,\|x_k\|_\infty\leq1\) and \(x_{hk}=0\),

\[
 \|V_h\|\leq2V_{\max},\qquad
 \|V_{hk}\|\leq4V_{\max},\qquad
 \|U\|_{L^2}^2\leq M_{\max}/\kappa^2.
\]

Together with \(\|R_D(-\kappa^2)\|\leq1/\kappa^2\), these identities give
finite first and mixed Weyl-variation enclosures. No future endpoint,
terminal event, third chord, momentum interpretation, or temporal tail has
been selected.

This does not yet enclose the product-Dirac channels, assemble the common
pair/contact incidence or certified spatial tail, or derive the zero-source
weak geometry force. Gate 7 remains active and Gate 8 remains locked.
