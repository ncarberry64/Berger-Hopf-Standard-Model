# Gate-7 signed-Green current-center macro tangents

At the 47 complete two-action-unit seams and the current numerical stop,
evaluate the direct 25-row action-constraint frame on the signed-Green
corrected center.  Its orthonormal nullspace is the 73-dimensional current
constraint tangent.  Only the projector onto this subspace is used, so basis
orientation is immaterial.

These tangents replace stale earlier-center seam projectors in the next
signed Green Newton prediction.  They are numerical center data, not an
interval variational carrier.

`FULL_BHSM_COMPLETE = FALSE`.
