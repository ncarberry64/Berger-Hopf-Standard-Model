# N12 reset-generated C2 launch chart

Status: `C2_RESET_GENERATED_72_PLUS_1_LAUNCH_CHART_CERTIFIED`.

Let `J_R : R^196 -> R^57` be the certified full-row-rank reset
linearization.  Its tangent has dimension `139`.  In the forward swapped
incidence, `E1=C_*` and the original event half `E_*` is the outgoing child
seed `C2`.  Projection of `ker J_R` onto that event half has rank `72`; its
kernel has dimension `67`.  This is the exact linear dimension identity

`139 = 72 + 67`.

The rank loss is physical bookkeeping, not a missing state variable: the seed
still lies on the ordered-event hypersurface.  Every projected reset tangent
therefore has zero selected-event derivative.  The retained exact fixed-`s`
field satisfies

`D lambda[F_s] = 1`

and has positive denominator at `s=0`.  It is consequently transverse to the
72-dimensional reset-event image.  The local forward launch chart is

`(xi,s) -> Flow_s(E(xi)),  xi in R^72, s >= 0`,

and has dimension `72+1=73`.  The extra direction is the action-generated
outgoing descriptor amplitude, not a chosen reset phase, endpoint condition,
or new coupling.

The stored SVD bases are numerical coordinate witnesses inside the already
certified reset root and event-eigenline balls.  They are not physical member
selectors.  No full Euler--Dirac or reset matrix inverse is formed.

This closes the local parametric C2 launch domain and removes the false need
to propagate all 139 reset-tangent directions as independent outgoing-child
data.  It does not control the maximal C2 tail.  The live Gate-7 owner remains
propagation of these 73 launch directions through the actual forward operator
and adjoint, followed by either a certified later event/canonical stop or the
physical heat-minus-zeta quotient-Cauchy tail.
