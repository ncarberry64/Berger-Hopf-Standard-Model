# N12 C2 birth-coefficient quotient jet

Status: `ACTUAL_RESET_SELECTED_C2_BIRTH_CAUCHY_JET_RANK_TWO_CERTIFIED`.

At the certified double-event incidence the stored ordering is `(E_*,C_*)`.
The two-sided interface theorem proves that the physical forward reset at the
new event is the swapped pair

`(E1,C2)=(C_*,E_*)`.

The swap must be applied to the complete retained reset map.  Merely taking
the first 98 rows of the original reset tangent gives the wrong projection
rank because the selected-event row belongs to the ordered first arm.  After
swapping the states and their action third-variation tensors and recomputing
the full reset residual and Jacobian, the residual remains zero within the
certified ball, the Jacobian has rank 57, and its 139-dimensional tangent
projects with rank 73 onto the actual outgoing `C2` arm.

The action-owned boundary coefficient map is

`C_C2(Y)=(x_C2,H_C2)=(log R4,D_tau log R4)`.

Its differential is evaluated on an orthonormal basis of the 73-dimensional
reset-selected `C2` image with the retained action weights.  Both singular
values are strictly positive, hence

`rank D C_C2 = 2`.

Because `x` and `H` are scalar geometry readouts, this differential is already
gauge covariant.  It is computed before the intrinsic one-dimensional time
quotient; that quotient cannot remove both independent coefficient
directions, and the physical common-scale direction remains retained.

The same certified joint root ball gives strictly positive lapse and a
strictly positive proper-time radius rate on the outgoing arm.  The reset
trace identity gives the same birth radius on the two arms, while their lapse
and proper radius rates are not identified.  This is expected: AE2 glues the
trace bundle and canonical interface data, not two complete Cauchy histories.

This certificate fills the first local realization slot of the already
matched maximal-forward Weyl family:

`M_C2 = M_C^max[Y_C2,endpoint_class_C2]`.

It supplies the actual birth coefficient and its reset-quotient first jet.
It does not supply the later `C2` coefficient path, its endpoint outcome, the
duration/interior Jacobi transfer, the complete spectral family, or the
second covariant jet.  No future endpoint, history member, recurrence,
selector, coupling, scale, action term, or chord is introduced.

Gate 7 therefore advances from a wholly unrealized `C2` oracle to an actual
certified birth Cauchy jet.  The next calculation is the outgoing
desingularized coefficient/Jacobi germ and its first validated regular
segment, followed by the existing inverse-free Weyl transfer.

`FULL_BHSM_COMPLETE=false`.
