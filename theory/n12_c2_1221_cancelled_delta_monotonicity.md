# N12 C2 realized-cover Delta monotonicity

The expanded cancelled-theta parent supplies a signed center value and first-
and second-variation bounds for `Delta`.  Evaluating its Taylor enclosure on
the actual tube-plus-center radius reached by the sixteen-segment cover gives
a strictly positive lower bound for `Delta` throughout that realized
subcover.

Therefore the signed descriptor is monotone there.  Integrating the signed
`Delta` interval yields a correlated descriptor interval whose lower endpoint
stays above the initial positive descriptor.  This supersedes the near-zero
lower endpoint produced by independently accumulating `|Delta|` on every
segment.

No turning point, event, canonical stop, or expanded-parent exhaustion has
occurred.  The next object is a recentered signed-`Delta` and cancelled-
response ball at the final predictor, initialized with the sharpened
descriptor interval.

