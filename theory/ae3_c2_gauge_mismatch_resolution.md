# AE3 current-C2 gauge-mismatch resolution screen

The current one-sided smooth parent trace has a strict Lorentz-residue
mismatch. Existing intrinsic boundary Yang--Mills candidates require an
unsourced coefficient and contradict AE3's zero surface-contact action.
Recovered v14.67 Wentzell matrices remain theorem-class data without physical
incidence and curvature provenance; the v17.89--v17.92 dynamic Wentzell laws
are reusable seam templates but do not supply current-C2 gauge Calderón data.
The relative spectral coefficient is likewise open.

The unique coefficient-free calculation route surviving these hard conditions
is the two-sided parent Schur complement at the internal `sigma=0` interface:

```text
N_total(omega,k)=N_inside(omega,k)+U_reset^* N_exterior(omega,k) U_reset.
```

This does not add a contact action. It integrates out the omitted exterior of
the same AE3 parent operator. The inside term is now derived; the exact missing
object is the continuous-frequency gauge/ghost Calderón operator on the actual
current-C2 maximal exterior. The far Friedrichs proof cutoff is not that
exterior.

Only if the derivatives of `N_total` yield one positive temporal/spatial
residue may the Maxwell residue be promoted. Otherwise the signed mismatch is
retained. No photon claim is made by this route selection.

## Subsequent evaluation

The selected calculation is now evaluated in
`ae3_c2_two_sided_calderon_reflection_no_go.md`. Exact reciprocal reflection
conjugates the regular exterior Maxwell--BRST problem to the interior one, so
both residues double and their ratio is unchanged. This screen remains the
provenance record of route selection; the later theorem owns the negative
evaluation and the resulting finite action/domain decision surface.
