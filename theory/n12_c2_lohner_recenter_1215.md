# N12 C2 Lohner recenter at segment 1215

The certified segment-1215 predictor and endpoint tube are used only as a new
proof center and uncertainty set.  The retained branch-24 Hessian, exact
complex-step `D3` columns, Kato eigenline derivative, action `D4` bounds,
lapse, and outward-radius coefficient are recomputed there.

The ambient ball certifies uniform line simplicity and domain margins.  The
physical propagation space remains the exact level set
`lambda_event(Y)=s`; normal motion is not admitted.  The selected radius is a
midpoint of the incoming tube and the maximal certified chart radius, hence a
derived proof choice rather than a physical cutoff.

Failure of a chart inequality would be outcome C proof exhaustion, not an
event or canonical stop.  A successful chart feeds the same bordered-response
and cancellation-preserving Lohner construction at the new center.
