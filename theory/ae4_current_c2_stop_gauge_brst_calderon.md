# AE4 current-C2 canonical-stop gauge/BRST Calderon block

## Result

The already-derived canonical stop now carries an evaluated gauge/BRST
boundary response on the accepted numerical center history.  No gauge
normalization or particle state is reconstructed.

The stop selects the Friedrichs form closure.  On that same terminal graph,
the lowest coexact mode uses the corrected action coefficient

```text
curl_0 = 2,       curl_0^2/R4^2 = 4/R4^2.
```

The stable inverse-free Riccati composition evaluates its birth Calderon
value at the negative-axis probe `z=-1`.  The three exact `+2` curl
coordinates therefore produce one threefold positive coexact boundary block.

The lowest nonconstant scalar harmonic has Laplacian `3/R4^2`.  The two real
constraint/gauge coordinates and the one complex ghost inherit this same
scalar operator and the same Friedrichs graph.  Their graded boundary blocks
are consequently

```text
+2 N_scalar  - 2 N_scalar = 0.
```

This transports the historical exact BRST cancellation onto the current
canonical-stop domain instead of rebuilding it.  The surviving gauge/BRST
supertrace is exactly the coexact block.

Left, midpoint, and right representatives of the certified first-hit
interval are evaluated, and subdivision of the same piecewise-linear proper
time path leaves both Weyl values stable.  These are center evaluations, not
outward interval authority.

## Claim boundary

The result derives the canonical-stop coexact and BRST-quotient Calderon
center block.  The negative-axis resolvent parameter is not a physical
Lorentzian frequency.  Therefore the AE3 temporal/spatial residue mismatch
remains recorded and is not renormalized away.  A physical event block still
requires the correlation-preserving nonlinear stop family, its moving
endpoint jets, and insertion together with the HS and fermion blocks in the
existing AE4 event-flux assembly.

```text
AE4_CURRENT_C2_CANONICAL_STOP_COEXACT_CALDERON_CENTER_EVALUATED = TRUE
AE4_CURRENT_C2_CANONICAL_STOP_BRST_QUOTIENT_CENTER_EVALUATED   = TRUE
AE4_CURRENT_C2_STOP_MATCHED_NONLINEAR_INTERVAL_GAUGE_BRST_BLOCK_DERIVED = FALSE
CURRENT_C2_LORENTZIAN_MAXWELL_RESIDUE_DERIVED                  = FALSE
FULL_BHSM_COMPLETE                                              = FALSE
```
