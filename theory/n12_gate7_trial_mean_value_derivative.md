# Uniform mean-value enclosure of the actual trial derivative

The small paired point sensitivities motivate a uniform calculation, not a
replacement of uniform bounds by point values. Evaluate the complete original
mixed physical variation graph on the frozen trial column E_j against every
scaled tube direction. Retain all seven bordered solves, scalar contractions,
coupled second normalization, and all 75 verified point-Hessian containment checks.

Use the identical paired primal family as the compared direct derivative.
The already paired directional line/response variations can narrow the first
trial-direction solves. Paired full 99-column first variations, multiplied by
the same 75 scaled directions, can narrow the corresponding tube-direction
solves. Every seed is a same-family derivative enclosure; select narrower
overlapping coordinate bounds. Only the 61 line coordinates are seeded; retain
the recomputed auxiliary line border and compute eigenvalue slopes from the
original scalar contractions. Response seeds use physical K coordinates after
the original solver's J/K conversion. No fake RHS or missing mixed solve is used.

The original normalized eigenpair and physical-value certificates must establish
strict implicit invertibility, positive inertia/reference overlap, and positive
physical norm on the same convex affine tube. On that smooth family, the
derivative action DF(z)E_j equals its verified anchor value plus the integral of
D2F along the segment. For each output, bound the longitudinal interval support
and the Euclidean support of all 74 transverse axes using the uniform mixed
enclosures. Retain full anchor uncertainty and every scalar term.

The result encloses one actual frozen trial derivative throughout this tube.
Independently reproduce before downstream use. It does not certify all trial
columns, the physical quotient, moving-frame derivatives, full-path contraction,
or BHSM completion. No measured data, fitting, or changed radii are introduced.
