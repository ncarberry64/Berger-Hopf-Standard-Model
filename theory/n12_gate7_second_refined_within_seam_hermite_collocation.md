# Gate-7 second refined within-seam Hermite collocation

Halve each of the 740 spans in the first refined candidate.  Each inserted
node uses the already-evaluated exact retained action field at the parent
Gauss-3 midpoint.  The resulting 1,481-node path is replayed at three Gauss
nodes on all 1,480 quarter-quarter spans.

The two successive defect-reduction factors distinguish a convergent
interpolation floor from a stale endpoint or linearization error.  This is a
numerical routing audit, not an interval shadowing theorem, a replacement of
the quarter-step proof center, or physical operator authority.

`FULL_BHSM_COMPLETE = FALSE`.
