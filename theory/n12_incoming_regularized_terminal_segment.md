# N12 incoming regularized terminal segment

The certified formation history reaches its fixed terminal event with a simple
selected eigenvalue `lambda>0` that decreases to zero in forward physical
time.  The singular Euler--Dirac rate is regular when the selected eigenvalue
itself is used as the state parameter:

`dY/dlambda=(b_psi Psi+lambda h)/Delta`,

where `Delta=c_psi b_psi+lambda R` and
`R=D lambda[(q_dot,h)]`.  On the incoming branch `c_psi<0`, `b_psi>0`, so
`Delta<0`.  Increasing `lambda` from zero therefore reconstructs the past
history from the terminal event, while the positive physical lookback
duration obeys

`dT_-/dlambda=-lambda/Delta>0`.

The terminal limit is finite:

`dY/dlambda|_E=Psi_E/c_psi(E)`.

This certificate uses the existing branch-23 solution ball, ordered
eigenline mixed majorants, retained-action derivative majorants, and terminal
orientation intervals.  Structured contractions bound `c_psi`, `b_psi`, the
hard solve, `R`, and the first derivative of the regularized vector field.
The root-relative tube is obtained from the inequality that keeps `-Delta`
at least one half of its certified terminal product.  Canonical dyadic safety
fractions then produce an explicit nonzero interval on which the path and its
first Jacobi field remain inside that tube.  These fractions define only a
proof domain; they do not select a physical formation amplitude or endpoint.

The result upgrades qualitative “sufficiently small positive lambda”
existence to an explicit uniform finite-amplitude action tube.  It uses no
Euler--Dirac block inverse, recurrence condition, fitted threshold, or new
physics.  The next operation is the inverse-free Volterra enclosure of the
normalized radius/coefficient path and its physical quotient pullback on
this certified interval.
