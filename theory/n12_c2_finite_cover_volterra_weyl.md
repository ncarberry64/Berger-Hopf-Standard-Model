# N12 C2 finite-cover Volterra/Weyl response

The 98 certified consecutive descriptor segments form one compact finite
history prefix.  Their proper-duration intervals are added, their state
Jacobi growth factors are composed multiplicatively, and the largest
root-relative translated ball supplies common boundary-coefficient bounds.
The smallest `Delta` margin and largest `Delta` derivative are retained for
the moving-duration pullback.

These data feed the unchanged scalar and product-Dirac short-history
Volterra construction.  The result is a free two-boundary response on the
whole certified prefix.  It imposes no terminal load and does not turn the
finite proof frontier into an encapsulation endpoint.
