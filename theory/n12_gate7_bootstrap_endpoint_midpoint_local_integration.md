# Integrating paired endpoint seed-bootstrap actions

Use the same local preconditioned split formula and exact midpoint center
directions as the paired endpoint-plus-midpoint mean-value integration. Replace
each endpoint action only with its independently paired seed-bootstrap bound
on the identical frozen trial direction and original physical family.

Require the complete 75-direction uniform endpoint graph, same-family
smoothness, verified point anchors and exact trial-axis match. Keep the paired
249-direction midpoint mean-value center actions and original full midpoint
derivative. Preserve the complete endpoint uncertainty and center-shift terms;
the bootstrap changes enclosures, not the underlying local operator.

Compare against the paired local result before endpoint bootstrap and retain
all prior and candidate bounds. Evaluate no new action derivatives in this
integration. Require fresh-process byte-identical reproduction before use.
Full trial coverage, quotient identification, moving-frame terms, full-path
contraction and BHSM completion remain open.
