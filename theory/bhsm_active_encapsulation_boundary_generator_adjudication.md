# Active encapsulation boundary-generator adjudication

Action version: `BHSM-AE-3.2.10-ACTIVE-ENCAPSULATION-ADJUDICATION`.

Status: `ACTIVE_ENCAPSULATION_NARROWED_TO_A_BOUNDARY_CANONICAL_TRANSFORMATION;_VALUE_AND_CARRIER_NOT_DERIVED`.

This report separates recovered prior BHSM, Norman's new clarification, and
consequences derived here. It adds no action term, interface functional,
coefficient, harmonic label, reset, particle claim, or prediction.

## 1. Provenance result

The historical pieces are semantically compatible with the clarified chain,
but repository dependencies do not show that they were one implemented map.

- **Recovered prior BHSM:** v6.0.5 already described physicality as an
  “action-supported localized or propagating energy--geometry differential or
  envelope at any scale.” v11.0 described relational envelopment holism,
  a particle as a stable localized or parent-bound spacetime envelopment, and
  an abstract `T_core` with trajectory target selection. v14.60-v14.61 showed
  in a synthetic reduced functional that child interior, seam value, seam
  traction, and nesting scale can be co-selected by global stationarity.
  v15.5 retained Unique Actualization while its master self-reconstruction
  map was absent. v17.82-v17.98 typed an event-conditioned child BVP but left
  `E_boundary` undefined.
- **Owner clarification:** encapsulation actively creates, erases, and
  redistributes boundary information; spacetime envelopment creates a
  differential; and the realization may depend on event harmonics or modes.
- **Newly derived here:** within the retained variational language, the
  strongest available type for that differential is a boundary canonical
  covector, or equivalently the source/translation of an affine Lagrangian
  boundary relation. This typing does not determine its value.

Thus the clarified semantic chain is historically corroborated as an intended
architecture, not recovered as a completed historical equation.

The closest historical mathematical bridge is also explicit. The recovered
v11.4/v14.67 attachment calculation constructs

\[
 W_{\rm att}=K_{\parallel}^{-1/2}H_{\parallel}K_{\parallel}^{-1/2}
\]

on a two-dimensional KKT tangent and calls for an “action-owned differential
incidence map” into the full `M8/M5+/M5-/M4` Calderon domain. Its representative
response matrix is

\[
\begin{pmatrix}
0.10512209545464106&-0.10620276995054592\\
-0.10620276995054592&0.6824723646442667
\end{pmatrix},
\]

with positive eigenvalues `0.08620600507952422` and
`0.7013884550193836`. This is real recovered provenance, but it remains
conditional on an author-selected finite-radius branch and a conditional depth
curvature. The full differential incidence map and the three moving-seam
channel amplitudes/phases were explicitly left open. It therefore cannot be
used as the present `J_enc`.

## 2. Mathematical envelopment

Until a physical route and carrier are selected, envelopment is a relation

\[
  \operatorname{Enc}\subset \mathcal X_{\rm pre}\times\mathcal X_{\rm post},
\]

not a single-valued map. The pre-state contains the gauge-quotiented
last-regular event, environment, admissible sector, and discrete
degree/orientation/FR/incidence/bundle data. The post-state must contain a
selected route, a domain and carrier `(D_enc,Sigma_enc)`, intrinsic and
extrinsic geometry when applicable, attachment `F_B`, child trace and
canonical covector, constraint/Noether level, and child causal domain.

The current bridge allows three routes: local same-spacetime enclosure,
geometric boundary/collar enclosure, and a spacetime-edge transition. No route
or carrier is selected. In particular, the pregeometric stratum is not an
ordinary metric geometry at a zero value of the support scalar:

\[
  C_A\ne\{\upsilon=0\}.
\]

On a regular route, metric and causal data persist and boundary normal,
extrinsic-curvature, and canonical pairs become meaningful once the carrier
is declared. At a spacetime edge, metric, proper time, curvature, local energy
density, and canonical metric momentum are not transported as pregeometric
primitives. A reconstructed Lorentzian child can acquire them only through a
subsequent well-posed BVP.

## 3. The active differential

Let a supplied configuration graph be written in a finite trace chart as

\[
  q_e=C(F_B)q_c.
\]

The retained two-sided boundary one-form restricts to

\[
  \Theta_\partial=
  \langle C^*\Pi_e+\Pi_c,\delta q_c\rangle
  +\langle\Pi_e,\delta C\,q_c\rangle.
\]

The strongest existing mathematical candidate for the created differential is

\[
  \boxed{\Delta_{\rm enc}:=\Pi_c+C(F_B)^*\Pi_e}
  \quad\in\quad T^*\mathcal Q_{\Sigma}^{\rm red}.
\]

It is a density-valued boundary covector after diffeomorphism, constraint, and
BRST reduction. Its sector components are the Brown--York/GHY/corner momentum,
Maxwell electric conormal, scalar/topographic normal Legendre momentum, and
the constrained Green-form counterparts. Ghost data must be BRST-induced
from the gauge map. The HS normal Legendre map has rank zero; that does not
select its algebraic trace incidence. AE2 already cancels the fermion
first-order Green form on its owned unitary graph.

Under a boundary diffeomorphism the covector uses the cotangent pullback and
boundary density; under gauge transformations it is adjoint-covariant; and
normal reversal changes the conormal sign. Its units are sector-specific and
dual to the corresponding boundary configuration, so that
`<Delta_enc,delta q>` has units of action. In stored N12 action coordinates it
is dimensionless after normalization. Its scale behavior must come from the
sector Legendre/DtN map and boundary measure; no universal active scaling law
is derived.

The retained action produces only

\[
  \Delta_{\rm enc}=0
\]

as natural transversality after a graph has already been supplied. The owner
clarification means that this zero-source member cannot be treated as the
general encapsulation law. An active law would have to assign

\[
  \Delta_{\rm enc}=J_{\rm enc}
  (z_{\rm event},\mathcal E_s,\{P_\alpha z_{\rm event}\}),
\]

on an action-owned carrier. Neither `J_enc` nor the configuration trace map is
presently defined.

## 4. Redistribution rather than copying

The correct closed-system form is an extended balance. With
`J_enc:=-Pi_enc` defining the child-side active source sign,

\[
  J_{\rm event}+J_{\rm child}+\Pi_{\rm enc}=0,
  \qquad
  \Pi_c+C^*\Pi_e=J_{\rm enc}.
\]

Boundary information may be created or erased in the reduced child description
only if the missing information is carried by seam/environment degrees of
freedom in the extended description. This does not waive Hamiltonian,
momentum, gauge, or Noether consistency. Degree, orientation, FR parity, and
incidence remain exact compatibility data. Conventional local energy is not
manufactured on `C_A`; only regular-domain Hamiltonian/Noether balances and
the invariant discrete data can cross the ontology boundary as currently
typed.

## 5. Harmonic and mode audit

The repository does not own

\[
  (h_s,m_s)=\mathcal M(z_{\rm event},\mathcal E_s).
\]

The simple ordered N12 branch-24 eigenline selects a stopping direction, not
a child boundary harmonic. The exact round-S7 scalar spectrum is a diagnostic
sub-spectrum whose physical parent operator/domain remains open. Berger--Hopf
family/mode labels are frozen upstream data, but the actual current-C2 slot and
its enclosure inheritance are open. The three noncentral v14.55 channels form
a rank-three basis while their amplitudes, phases, and order remain inputs.
Hopf rules constrain allowed overlaps but are not action selection.

The negative results are also binding: linear orthogonal modes do not select
relative phase, a degenerate basis label is not a physical vector,
commensurability does not imply coupling, and the historical cubic 10-4-4
trigger is killed by sigma parity at the stored background.

Once a physical self-adjoint boundary operator and domain exist, only the
basis-invariant decomposition

\[
  \Delta_{\rm enc}=\sum_\alpha P_\alpha\Delta_{\rm enc}
\]

is automatic. Writing coefficients
`a_alpha=<Y_alpha,Delta_enc>` decomposes an already known differential; it
does not generate it. The current full-field free data therefore remain an
infinite spectral sequence/boundary section. At N12 they remain the certified
67 continuous fixed-event directions before quotient.

## 6. Calderon/DtN/Wentzell connection

If the event and child interiors, attachment, and physical DtN operators were
known, then

\[
  \Pi_e=N_e Cq_c,
  \qquad \Pi_c=N_cq_c
\]

would turn the active balance into

\[
  \boxed{(N_c+C^*N_eC)q_c=J_{\rm enc}}.
\]

This identifies exactly how active encapsulation could provide the missing
input. If the two-sided operator were invertible modulo constraint, gauge,
and zero-mode quotients, it would determine `q_child`; the resulting affine
Calderon graph could then derive the nonfermion `L_s` rather than accept it as
free data.

Those hypotheses do not currently hold. `F_B` and the nonzero gauge connection
trace map are absent; the physical outer gauge--spinor--ghost Calderon
projector is not action-selected; `J_enc` is absent; the v14.67 two-dimensional
response has no full differential-incidence lift; and HS incidence remains
open. Reset transmission is not the physical outer Calderon projector.
Consequently `L_s` is only conditionally derivable, not derived, and `F_B`
remains **FB5**.

## 7. N12 rank ledger

For a fixed event, the existing 31 child rows are four attachment traces,
25 child constraints, and two canonical-momentum mismatch rows. They have
joint rank 31 in 98 child variables:

\[
  98-31=67.
\]

Environmental sector restrictions are discrete and add zero local rank. The
ordered event eigenline is in the event block and adds zero child rank.
Uninstantiated harmonic projectors add zero rows. The owner clarification is
semantics, not an equation, and adds zero rows. Hence the actual ledger remains

\[
  67\quad\hbox{before quotient},\qquad 66\quad\hbox{after the known time-flow quotient}.
\]

The chart is already gauge-fixed/quotiented and the row-rank gap is certified,
so these are not numerical null directions. One direction is the known time
flow. The remaining 66 have no proved redundancy or action-owned selection
equations. N12 uniqueness modulo that flow would require 66 additional
independent physical equations; a literal pre-quotient singleton would require
67. No such rank is claimed here.

## 8. Actualization and interpretation

The N12 geometry subsystem is **UA4**, with a 66-dimensional continuous family
after quotient. The complete full-field problem is **UA5**, because spatial
attachment and nonfermion boundary correspondence freedom remain
infinite-dimensional. Unique Actualization is not satisfied. No physical reset,
relative generator/charge, beta, or global graph jets are unlocked; outcome D
and the S1-reference/S2--S4 block remain unchanged.

The clarified mechanism is compatible with the older owner identification of
particles as stable localized or parent-bound spacetime envelopments. It does
not close physical-enclosure obligations PEI03--PEI09 or instantiate the C2
family/mode slot, so particle production remains a programmatic owner
hypothesis.

For propagation, an identified spacetime-enveloped child would use the existing
Lorentzian retarded causal domain. Ordinary spacetime speed is not a meaningful
primitive on `C_A`. The repository contains no continuous “degree of spacetime
locking”; only three unselected categorical routes and a proposed enclosure
signature. Norman's neutrino intermediate/environment-dependent interpretation
is therefore retained strictly as owner hypothesis. No observed FTL neutrino
claim or locally superluminal dynamics is made.

## 9. Decision

The clarification materially narrows the missing physics: the target is no
longer passive copying or an arbitrary untyped child graph. It is an active,
event-conditioned affine boundary canonical transformation on a physical
enclosure carrier. But its carrier, configuration half, mode projectors, and
canonical-covector value remain absent. It therefore has zero present N12 rank.

Exact next object:

`ACTION_OWNED_COVARIANT_ENCAPSULATION_CARRIER_SIGMA_ENC_AND_EVENT_ENVIRONMENT_MODE_TO_CREATED_CHILD_BOUNDARY_TRACE_AND_CANONICAL_COVECTOR_MAP_WITH_A_NONDEGENERATE_FULL_FIELD_GRAPH_JACOBIAN`.

Smallest owner question:

> What covariant event rule, on an action-owned encapsulation carrier, assigns
> both the child boundary trace `q_child` and the created canonical boundary
> covector `Delta_enc` for each event-selected harmonic component?

`FULL_BHSM_COMPLETE = FALSE`.
