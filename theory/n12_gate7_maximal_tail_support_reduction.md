# Gate-7 maximal-tail support reduction

Status: `FIXED_C2_UPSTREAM_INTERFACE_MAXIMAL_TAIL_CLOSED`.

Use the certified forward-swapped reset ordering `(C2,E1)`.  If

`J_R=[J_C2,J_E1]`

and `Z` is a basis of `ker J_R`, the outgoing-seed projection
`B=P_C2 Z` has rank 72 and

`ker B = K_fixedC2`, `dim K_fixedC2=67`.

The stored subspace identity is exact up to the certified numerical
projector residual:

`Z K_fixedC2 = {0}_C2 direct-sum ker J_E1`.

Consequently a variation in this 67-dimensional block changes no C2 birth
state and no coefficient of the maximal C2 differential expression.  In a
reset-compatible covariant frame,

`D_h M_C2^max(z)=0`, `h in K_fixedC2`.

The remaining variation of the one internal seam is therefore the variation
of the compact incoming `C1 -> E1` arm and the retained local AE2 contact
blocks.  It is not an additional seam source.  Per retained angular channel,
this is a finite-dimensional boundary perturbation of the same fixed maximal
C2 Friedrichs operator.  Its resolvent and heat-functional derivatives are
finite-rank relative functional-calculus quantities.

The full graded sum is also controlled without the unknown far C2 tail.  The
compact incoming arm has the already-certified finite-order transfer-jet
bound.  Every retained local contact jet has finite differential order.  On
the first positive C2 collar the low-energy Poisson pair has the Agmon factor

`exp(-ell_0 mu/R4_max)`,

and the complementary high-energy heat factor is

`exp(-exp(-2*x_max) mu^2/4)`.

The stored strict inequality

`ell_0/R4_max > c_transfer`

makes the first factor dominate any retained finite polynomial vertex order
and the quadratic angular multiplicities; the Gaussian dominates them on the
high-energy side.  Thus the fixed-C2 upstream/interface heat cotangent is an
absolutely summable Cauchy net under maximal form-core exhaustion.  Its zeta
part is supported on the compact incoming arm because the C2 geometry is
fixed, so it is Cauchy as well.

This closes the maximal-tail existence problem on the entire fixed-C2 block.
The intrinsic one-dimensional time quotient can only pass to a quotient of
this already-convergent finite-dimensional covector; an explicit hybrid time
generator is not needed for this support statement.

The formation amplitude is a separate coordinate, not a reset-fiber tangent:
its terminal `E1` Cauchy jet is fixed.  Its maximal full-graded boundary
cotangent was already closed by the incoming-compliance theorem and likewise
requires no C2 reverse tail.

Therefore the only remaining noncompact coefficient-Jacobi tail is supported
on the outgoing C2 launch block: the 72 reset-generated seed-image directions
plus the exact transverse descriptor-flow direction `F_0`, for an upper
dimension of 73.  This theorem proves support and convergence, not the signed
value of the compact upstream/interface covector, the outgoing 73-direction
tail, or a KKT root.

Only the external Cauchy/birth datum is zero.  No internal response is zeroed,
and no source, selector, endpoint, recurrence condition, scale, fit, gate, or
chord is introduced.
