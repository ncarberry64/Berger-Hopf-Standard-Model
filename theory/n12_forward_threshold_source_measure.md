# N12 forward threshold source-measure audit

Status:
`TWO_CHORD_SCALAR_THRESHOLD_MARGIN_AND_FACTORIZED_ZERO_ATOM_IDENTITIES_DERIVED_CONTINUOUS_THRESHOLD_OPEN`.

For a scalar or de Rham channel with unit-radius eigenvalue (c>0), the two
certified chords give (R_4\leq R_{4,+}) and proper duration
(T\geq T_-). Hence the channel potential obeys

\[
 V=c/R_4^2\geq c/R_{4,+}^2=:a^2.
\]

Sturm comparison against the constant potential (a^2), with any retained
nonnegative inward graph after the certified core, gives the child-side
zero-energy impedance bound

\[
 M_{\rm child}(0)\geq a\tanh(aT_-)>0.
\]

The (c=0) scalar channel has no such margin, but its first log-radius
operator vertex is exactly zero. Thus it contributes no first radius-source
weight.

For every product-Dirac square (P=A^*A), the first fixed-domain form jet is

\[
 D_hq[u]=2\operatorname{Re}\langle Au,A_hu\rangle.
\]

Every exact kernel vector therefore has zero first geometry weight. The
half-line factor (A=\partial_\tau+s), (s>0), has the normalized witness
(u=(2s)^{1/2}e^{-s\tau}), so factorization alone does not create a positive
threshold margin even though its exact zero atom is invisible to the first
form derivative.

The remaining issue is the continuous threshold. In the regular free
half-line model with (u'(0)=h u(0)), (h>0), the generalized eigenfunction
bound gives, for a compact multiplication source (q),

\[
 |\nu_q|([0,\Lambda])\leq
 \frac{2\|q\|_1}{3\pi}
 \left(\frac{1+hL}{h}\right)^2\Lambda^{3/2}.
\]

This has the required excess exponent (\varepsilon=1/2). At the Neumann
threshold, the unit compact source instead has leading behavior
(2L\sqrt{\Lambda}/\pi), which fails the E1 criterion. These free models
identify the exact dichotomy but are not promoted to the unknown N12
exterior.

Accordingly, the scalar core margin and factorized zero-atom cancellation do
not yet prove the actual N12 continuous spectral-counting exponent. The next
lemma must establish an action-owned limiting-absorption/Weyl threshold bound
for the actual physical birth graph and exterior, plus the E1-weighted
high-energy tail and graded angular summation. Gate 7 remains active, Gate 8
locked, and chord 3 unauthorized.
