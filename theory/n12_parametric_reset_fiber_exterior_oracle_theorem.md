# N12 parametric reset-fiber exterior-oracle theorem

Status: `FINITE_ENDPOINT_STRATIFIED_ORACLE_REGULARITY_DERIVED_ACTUAL_PARAMETRIC_ORACLE_OPEN`.

The complete-child reset is a set-valued local relation.  Its fixed-event
child tangent has raw dimension 67, while the retained physical count is 66
after the existing whole-system time quotient.  Neither the normal chart nor
the stored complete-child state is an action-owned selector.  It follows that
the Gate-7 replacement force cannot in general be evaluated from one chosen
reset representative.

On a fixed regular stratum of the physical reset quotient whose histories
reach one finite completed-encapsulation endpoint type or one retained
canonical stop, assume the retained forms have a common local form domain,
depend `C2` on the reset parameter, and have a uniform coercivity margin at a
real probe `z<0`.  Assume also that the endpoint graph, trace trivialization,
and retained gauge/time/common-scale quotient are `C2`.  The second resolvent
identity then gives operator-norm `C2` dependence of the Dirichlet resolvent.
The trace lift and Schur complement give `C2` dependence of the Poisson and
Weyl--Calderón maps.  Therefore the heat-minus-zeta force is a covector field
and its constraint-reduced geometry/reset Hessian is defined on that stratum.

In a fixed chart, writing

`M=A_bb-B*R*C`, `R=(A_ii-z)^(-1)`,

gives

`M'=A_bb'-B'*R*C-B*R*C'+B*R*A_ii'*R*C`.

The corresponding second directional derivative follows by differentiating
the same resolvent identity.  The stored theorem artifact checks both
derivatives independently with exact rational second-order dual arithmetic.
This uses only the coercive shifted interior block; it does not invert the
ill-conditioned Euler--Dirac kinetic block.

The requirement is sharp.  A constant family and
`M_star+xi*B` agree at one representative but have different first
variations.  A constant family and `M_star+(xi^2/2)*B` agree in value and
first variation there but have different Hessians.  Thus one representative
is sufficient only if the retained action proves the force and Hessian are
basic and invariant along the physical reset fiber, or the action itself
derives a unique saddle.  Neither statement is currently proved.

No smoothness is asserted across changes of endpoint outcome.  Such a change
is a stratum boundary or retained canonical stop until the action proves a
stronger result.  Infinite nonencapsulating histories remain valid
nonrealized mathematical histories and are not reintroduced into the
physical Gate-7 force domain.

The exact next dependency is consequently disjunctive but contains no owner
choice: derive the actual parametric exterior bundle
`M_C(z;xi), D_xi M_C, D_xi^2 M_C` on a nonempty fixed regular finite endpoint
or canonical-stop stratum, including the coupled time quotient; or prove
action-derived reset-fiber invariance of the replacement force and Hessian.
No reset selector, endpoint fit, scale, new gate, or chord is introduced.

`FULL_BHSM_COMPLETE=false`.
