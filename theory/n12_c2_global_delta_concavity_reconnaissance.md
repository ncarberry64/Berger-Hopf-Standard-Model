# N=12 C2 global Delta-concavity reconnaissance

Status:
`ACTION_OWNED_GLOBAL_DELTA_CONCAVITY_CANDIDATE_LOCALIZED; UNIFORM MOVING
CONE ENCLOSURE OPEN`.

The denominator-free coupled action-arclength system is

`dY/da=G_theta/(w ||G_theta||_2)` and
`ds/da=Delta/||G_theta||_2`.

A fourth-order, graph-preserving global center computation begins at the
last certified sheared C2 core.  It finds the descriptor maximum near
weighted action length `a=24`, retains branch 24 and every physical domain
margin through `a=92`, and brackets `s=0` before `a=94`.  This is a candidate
first Euler--Dirac stop, not yet a certified one.

The action identity used to audit the candidate is

`Delta=c b+s R`,

and therefore

`d_a Delta=(d_a c)b+c(d_a b)+(Delta/||G||)R+s(d_a R)`.

The selected-line derivative is obtained from its simple-line Kato
complement solve.  The hard-response derivative is obtained from the same
bordered inverse-free system already retained by Gate 7.  All scalar third-
and fourth-action contractions in the displayed derivative use the tracked
outward-rounded action tensor evaluator.

At global sample centers `a=0,24,48,54,72,92`, every pointwise outward-rounded
`d_a Delta` interval is strictly negative.  The center evidence therefore
supports one global concavity theorem: `Delta` decreases through zero and
continues decreasing until `s` reaches zero in finite action length.  This
route is BHSM-native because `s`, `Delta`, the selected eigenline, and the
hard response all come from the retained action.

Two tempting shortcuts are rejected.  An axis-aligned hull of even one
two-unit path segment destroys the reduced-Hessian enclosure by discarding
the strong velocity correlation.  A frozen principal ray also predicts a
different, false early crossing because the selected eigenline bends.  The
required global domain is a correlated moving-eigenline cone.

This audit does not certify that cone.  The binary center eigensolves and
bordered solves must be enclosed, and the derivative identity must be bounded
between sample centers on one finite correlated domain over `0<=a<=94`.
Once that uniform upper bound is strictly negative, elementary integration
of the scalar inequalities gives the transverse first hit of the already
retained canonical stop `s=0`; no capture-tail or further recentering is then
needed for this history.

No selector, recurrence, new stop, fitted scale, action term, chord, or time
direction is introduced.
