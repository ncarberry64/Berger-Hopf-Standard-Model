# Gate-7 outgoing descriptor-flow tail closure

Status: `OUTGOING_DESCRIPTOR_FLOW_MAXIMAL_TAIL_CLOSED`.

The extra direction in the certified `72+1` C2 launch chart is not an
independent deformation of the maximal child coefficients.  It is the exact
desingularized action flow through the reset-generated outgoing seed:

`D_s Y=F_s(Y)`, `D lambda[F_s]=1`.

Thus changing the launch coordinate `s` changes the birth section along one
and the same maximal C2 orbit.  It does not require a noncompact reset Jacobi
field.

For every retained channel and every finite Friedrichs form core, the Weyl
map at a moving birth section obeys the local Riccati/Lie derivative

`D_s M_T=(d tau/ds)[L_spatial(Y(s))-z I-M_T^2]`,

with the retained local contact derivative added once.  The identity is an
arm-transfer identity and does not use the superseded one-sided initialization
`M(0)=W_phys`.  The physical seam remains

`M_f+U_R^dagger M_C2 U_R+W_phys`.

The maximal Friedrichs theorem gives `M_T(z)->M_C2^max(z)` in operator norm
for each fixed negative `z`.  Since the Riccati right-hand side is a local
polynomial in `M_T` and the action-owned local coefficient, its descriptor-
flow derivative converges in operator norm as well.  Hence the `F_0`
component of the maximal Weyl first jet is Cauchy without differentiating the
far tail.

The corresponding heat derivative is a moving-boundary, finite-rank relative
functional-calculus term.  The first positive C2 collar supplies the same
low-energy Agmon pair and high-energy Gaussian used by the maximal incoming
theorem, so all retained graded angular multiplicities and every fixed local
differential order are summable.  The replacement-zeta derivative is the
local moving-lower-end term, with the action-owned `d tau/ds` factor; it has no
far temporal integral.

Therefore `F_0` is removed from the noncompact coefficient-Jacobi tail owner.
Together with the fixed-C2 support theorem, the only remaining noncompact
tail is carried by the rank-72 reset-generated outgoing C2 seed image.  This
does not assert that `F_0` is a new gauge direction, does not discard its
finite local force value, and does not evaluate the remaining 72-direction
tail or the KKT root.

Only the external Cauchy/birth datum is zero.  No internal response is zeroed,
and no source, selector, endpoint, recurrence condition, scale, fit, gate, or
chord is introduced.
