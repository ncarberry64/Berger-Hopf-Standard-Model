# N=12 asymptotic normally attracting capture basin

Status: `FINITE_N12_EXISTENTIAL_OPEN_CAPTURE_BASIN_DERIVED_RESET_ENTRY_OPEN`.

The complete weight-seven action has the exact isotropically expanding center
family proved in `n12_exact_weight_seven_center_family.md`.  Its physical
tangent dimension is 25: 24 fixed shape parameters plus the common-scale
orbit phase.  At the round member, the inverse-free descriptor has exactly
25 zero roots tangent to this family, 25 roots at `-7 H0`, no unstable finite
root, and only the algebraic modes removed by the certified analytic
constraint implicit-function reduction.

Hence, in the finite N=12 constraint-reduced and time/lapse-quotiented phase
space, the leading-weight family is a normally attracting invariant family
near the round member.  This conclusion uses the exact nonlinear invariance
identity `N7(a,0)=0`; it does not infer a basin from the two-jet alone.

For the full retained asymptotic system, put `epsilon=R4^-2`.  On a fixed
forward-scale section, `epsilon` replaces the common-scale orbit phase, so
the boundary family at `epsilon=0` has 24 shape parameters.  The exact
kinematic equation is

`epsilon'=-2 H4 epsilon`.

At the round boundary member its additional normal root is `-2 H0`.  The
normalized retained action and its analytically reduced vector field have
the scale decomposition

`Lhat=L7+epsilon L5+epsilon^2 L3+epsilon^3 L1+epsilon^4 L-1`

`    +epsilon^7 J/(I7+epsilon I5+epsilon^2 I3+epsilon^3 I1)`.

Thus every lower-weight perturbation vanishes at the invariant boundary and
is analytic there.  Normal hyperbolicity is open under analytic
perturbations.  The local stable-foliation theorem therefore supplies
positive, not presently quantified, constants `epsilon_star` and
`delta_star` such that an open relative neighborhood in the regular physical
constraint domain is forward captured by the boundary family.  Along every
history in this neighborhood,

`epsilon -> 0`, `H4 -> H0>0`, the normal velocities tend to zero, and the 24
shape variables converge to a nearby limiting member.

This is an existential local finite-N theorem.  It does not provide a
validated numerical capture surface, prove continuum-uniform attraction, or
show that the event-generated AE2 reset image enters the basin.  The current
Gate-7 owner is therefore a quantitative reset-to-capture connection (or a
later event/canonical stop before capture), including the retained domain
margins and quotient geometry jets.

The theorem concerns a possible post-event child exterior.  It does not
promote an infinitely nonencapsulating **formation** history into a physical
particle.  It adds no selector, action term, scale, fit, endpoint, recurrence
assumption, time direction, gate, or chord.  Gate 7 remains active and Gate 8
remains locked.
