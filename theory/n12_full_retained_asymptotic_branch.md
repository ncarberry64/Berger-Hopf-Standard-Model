# N12 full retained asymptotic branch

Status: `FULL_RETAINED_LOCAL_ANALYTIC_BRANCH_H4_TO_H0_POSITIVE_PROVED_NONREALIZED_SCOPE`.

Let `epsilon=R4^-2` and divide the retained action by its leading uniform
scale weight seven. Near the round expanding state its exact dependence is

`Lhat=L7+epsilon L5+epsilon^2 L3+epsilon^3 L1+epsilon^4 L-1`

`    +epsilon^7 J/(I7+epsilon I5+epsilon^2 I3+epsilon^3 I1)`.

The last term is the inverse-inertia contribution. Its leading denominator
is strictly positive at the round state because volume and the interior
localization are positive, the lapse is one, and
`eta_legendre=1+x_spatial^3>1`. The boundary Casimir is part of relative
order `epsilon^4`. Hence the complete finite N12 Euler--Lagrange system is
real analytic in `epsilon` and the quotient variables in a regular
neighborhood of `epsilon=0`.

After analytic constraint reduction and the retained time/lapse-chain gauge
slice, the invariant-graph equation is a finite-dimensional regular-singular
Briot--Bouquet system. At order `epsilon^k`, its linear recurrence pencil is

`A7+2 k H0 E7`.

The exact physical weight-seven roots are `0` and `-7H0`. For every positive
integer `k`, `-2kH0` hits neither root: the stable collision would require
`k=7/2`. The 24 algebraic modes are removed by the analytic constraint
implicit-function theorem. The directed first-order solution also satisfies
all twelve omitted gauge-chain equations, and the classical action Noether
identity propagates that compatibility to every order.

The analytic Briot--Bouquet theorem therefore gives an `epsilon_star>0` and
a complete retained-action branch

`Z(epsilon)=epsilon X5+epsilon^2 R(epsilon)`,

where `R` is analytic and uniformly bounded on each closed
`0<=epsilon<=epsilon_0<epsilon_star`. In particular,

`H4(epsilon)=H0+epsilon h1+epsilon^2 r_H(epsilon)`

with the directed interval `h1<0`, and

`lim_(epsilon->0+) H4=H0=sqrt(kappa0/42)>0`.

Thus the requested nonlinear dichotomy is outcome (a) on this local full
retained mathematical branch. Outcome (b), Osgood decay to zero, is false on
this branch, and no event or canonical stop occurs inside a sufficiently
small regular asymptotic neighborhood. This theorem is local at the infinite
end; it does not prove backward continuation to formation, universal event
reachability, or absence of a stop outside that neighborhood.

By Norman's finite-encapsulation ontology, the forever-expanding branch is a
nonrealized formation history and is not a physical particle observable. The
result closes the transverse nonlinear-modulation owner without reopening
infinite-tail analysis. Gate 7 returns to the physical finite-history
two-sided Calderon/zero-source-force owner.

No `R4^-2` eigenvalue is promoted. `Gate7=ACTIVE`, `Gate8=LOCKED`,
`chord_03_authorized=false`, and `FULL_BHSM_COMPLETE=false`.
