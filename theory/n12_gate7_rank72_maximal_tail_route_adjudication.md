# Gate-7 rank-72 maximal-tail route adjudication

Status: `FINITE_PREFIX_AND_FINITE_OPTICAL_SHORTCUTS_EXHAUSTED_PROJECTED_MAXIMAL_TAIL_OR_LATER_STOP_REQUIRED`.

The exact remaining Gate-7 object is the 72-vector net

`g_T-g_S = integral_S^T (U(t,0)B_seed)^dagger q_rep(t) d t + d_T-d_S`.

The following audit determines which retained results can certify that net.
It does not replace the signed projected criterion by an ambient norm.

## Finite-core heat suppression is not a maximal estimate

The stored fixed-channel heat bound is based on the mixed-boundary
Poincare rate `pi/(2T)`.  Along an unbounded Friedrichs exhaustion this rate
tends to zero.  More explicitly,

`g_scalar(T) >= (pi/(2T))^2+V_lower`,

`g_Dirac(T) >= max(0,pi/(2T)-||W||_infinity)^2`.

With the retained conservative value `V_lower=0`, neither formula supplies
a positive exhaustion-uniform gap.  The Dirac lower bound becomes identically
zero once `T >= pi/(2||W||_infinity)`.  This is a theorem about the proof
mechanism, not a claim that the realized maximal operator has zero gap.

The certified data display the loss directly.  From the 1,064 core to the
1,222 core the proper-duration upper endpoint grows by the recorded factor,
while the representative Poincare-derived gap falls by the recorded ratio.
The traces on both individual cores remain extraordinarily suppressed, but
that fact cannot be extrapolated past an artificial far Dirichlet edge.

## More finite prefixes are not a convergence proof

The arbitrary-precision 1,064-to-1,222 transfer composition is exact, including
its backward cotangent semigroup law.  Nevertheless the available low-axis
Dirichlet Weyl values change by more than 99 percent.  The net is therefore
not empirically converged at the current edge.  Since that edge is not an
event or canonical stop, neither its Weyl increment nor its tiny heat trace
is a physical force.

This is not merely a numerical caution.  The retained constant-exterior
counterfamily

`P_Q=-d_tau^2+Q`, `Q>=1`, `M_Q(-1)=sqrt(Q+1)`

has a common principal part, birth trace space, Friedrichs class, and positive
lower gap, while its Weyl value is unbounded.  Finite-prefix data plus a gap
and an abstract Friedrichs rule therefore cannot identify the maximal
response or its first geometry cotangent.

## Symmetry and asymptotic shortcuts do not remove the seed image

The reset-generated seed basis has rank 72 and its position block has rank 37,
the full retained coordinate dimension.  In particular its common log-radius
row is nonzero.  This is only a coordinate witness, but it rules out treating
the remaining seed image as a velocity-only or already-quotiented artifact.
The principal gauge slice is not a global Cauchy quotient, BRST leaves a
nonzero physical heat coefficient, and the exact time quotient acts only
after the covector exists.

The finite-N12 asymptotic NHIM also supplies no replacement proof.  Its 24
shape centers are physical, not gauge, and captured histories have finite
optical length.  The existing theorem excludes that route from the absolute
graded force domain.  It does not disprove a future signed, source-contracted
relative-trace cancellation theorem, but no such theorem is presently
action-derived.  The moving-duration Ward identity closes only the separate
common-scale zeta core net; it does not delete the heat term or the non-scale
zeta components.

## Exact adjudication

All presently certified shortcut routes are exhausted.  Gate 7 can now close
only by one of two BHSM-native results:

1. prove directly that the displayed rank-72 signed relative-form net is
   Cauchy on the action-owned maximal C2 history, including the full grading
   and the direct increment; or
2. certify that the relevant C2 history reaches an actual later retained
   event or canonical stop, where the existing compact-endpoint theorem
   applies.

The first alternative requires new global information about the propagated
reset seed and the complete closed-system heat-minus-zeta density.  Strong
resolvent convergence of the operators alone is insufficient because it does
not imply differentiability of the maximal functional with respect to a
noncompact coefficient history.  The second alternative requires an actual
stopping-locus certificate; a proof edge cannot be renamed as one.

Only the external Cauchy/birth source is zero.  No internal response is set to
zero, no seam term is added, and no selector, fitted threshold, new scale,
recurrence condition, endpoint, gate, or chord is introduced.

`FULL_BHSM_COMPLETE=false`.
