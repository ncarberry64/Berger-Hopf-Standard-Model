# Derived Electroweak-Breaking Generator

Choose the neutral vacuum:

```text
<H> = (0, v/sqrt(2))^T
```

The lower component has `Q=0`, so:

```text
Q <H> = 0
Q = T3 + Y/2
```

Therefore:

```text
SU(2)_orient x U(1)_Y -> U(1)_Q
```

Status: `ELECTROWEAK_BREAKING_GENERATOR_DERIVED_CONDITIONAL`.
