# N=12 reset-to-asymptotic capture overlap audit

Status: `EXISTING_RESET_AND_LOCAL_FLOW_CERTIFICATES_DO_NOT_YET_REACH_A_QUANTIFIED_CAPTURE_SURFACE`.

The action-owned reset-fiber certificate gives

`R4_reset=1.0023342201094778`,

so

`epsilon_reset=R4_reset^-2=0.995347235...`.

The retained Calderon graph is certified only on an action-coordinate ball of
radius `7.62939453125e-17` about each enclosed reset root.  The existing
Euler--Dirac variation theorem is likewise local and explicitly leaves the
promotion to bounded-margin component covers open.  These certificates prove
regular local departure and local geometry jets; they do not carry any reset
representative, much less the set-valued physical reset quotient, into the
asymptotic regime.

The normally attracting capture theorem supplies positive existential
constants `epsilon_star` and `delta_star`, but no numerical lower bounds.
Therefore no overlap comparison of the form

`epsilon(T)<=epsilon_star`, `dist(Z(T),M0)<=delta_star`

is presently evaluable.  It is invalid to compare the reset action-ball
radius directly with `epsilon_star`: the former is a radius in the full local
action-coordinate chart centered at the reset root, whereas the latter is a
compactified asymptotic phase-space margin centered at the invariant boundary
family.

The exact next certificate must contain all of the following:

1. an explicit asymptotic capture surface, with certified positive lapse,
   constraint-IFT, kinetic, inertia, metric, selected-line, and expansion
   margins;
2. a validated forward component cover carrying a nonempty stratum of the
   action-owned reset quotient to that surface, including first and mixed
   second quotient jets; or a certified later event/canonical stop first;
3. an overlap inequality in one common norm and chart, not a comparison of
   unrelated radii.

The reset radius Cauchy-jet has rank two on the raw reset tangent and retains
at least one physical direction after the one-dimensional time quotient.
Thus a single stored reset representative cannot stand in for the required
set-valued intersection theorem.

This audit adds no action, selector, scale, fit, endpoint, recurrence law,
time direction, gate, or chord.  Gate 7 remains active at the quantitative
capture-surface and reset-component intersection owner.
