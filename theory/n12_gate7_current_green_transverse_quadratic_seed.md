# Current-C2 Green transverse quadratic seed

The mixed-map seed leaves one independent local operand: the quadratic
curvature entirely inside the transverse complement of the current Green
axis.  This certificate evaluates two action-selected transverse directions
at each decisive node.  The first is the transverse projection of causal
coordinate 61, the already localized coarse obstruction.  The second is the
leading right-singular direction of that node's newly derived mixed map.

Each unit direction is reconstructed in the unchanged 74-dimensional proof
frame and passed through the same 512-bit retained-action second-rate
derivative used by the central Green scalar.  No historical transverse value,
new scale, center, trajectory, or fitted direction is introduced.

This is a directional seed, not a full transverse operator bound.  Its job is
to determine the current-center scale on the physically selected directions
and provide validation anchors for the remaining operator-majorant
calculation.  Promotion still requires a bound over the entire transverse
unit sphere, all endpoints and midpoints, and causal two-radius composition.

All eight 512-bit direction evaluations are finite.  The largest is
`283135.9524836309` at node 1 in the mixed-map leading right-singular
direction.  At the component-box failure endpoints 355 and 356 the same
direction gives `33040.72750401514` and `33052.68302416743`; at the terminal
causal-scalar owner it gives `33174.94833401804`.  These are current-center
action values and do not promote a supremum over the unsampled transverse
unit sphere.
