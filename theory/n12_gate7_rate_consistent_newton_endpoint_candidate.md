# Rate-consistent Newton endpoint candidate

The repaired ambient block correction is added in weighted action coordinates.
Each endpoint is constraint-projected when required.  A single retained exact
action jet then determines both the selected branch-24 eigenvalue and the
cancelled normalized field evaluated with that same eigenvalue as descriptor.
Thus no pre-recenter endpoint rate survives into the corrected center.

The terminal stop adapter remains disabled until the nonlinear collocation
center converges.  This is a numerical candidate, not interval authority.
