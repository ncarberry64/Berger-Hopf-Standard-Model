# N12 forward matter-domain no-go

## Result

The unchanged retained BHSM action does not define a unique full Gate‑7
operator.  It derives the transverse gauge DtN quadratic form, but the
retained matter-boundary theorem leaves a continuous
`U(1)_parent x U(1)_child` maximal-isotropic phase family because the normal
matter junction action is zero.  Boundary identity removes crosswise sewing;
it does not select the two remaining phases.

This does not contradict the earlier maximal-forward Friedrichs theorem.
Friedrichs closure fixes an infinite or excluded **far maximal endpoint** of
the nonnegative source form.  The missing object here is the distinct
**left birth/interface graph**.  The maximal-domain artifact lists that graph
as a separate input, v17.84 says its physical `W_phys` blocks are not yet
action-derived, and the local source-incidence artifact does not select its
temporal graph.  A closure prescription at the far end cannot manufacture
the missing matter law at birth.

The v17.96–v17.99 child lineage does not supersede that result.  It closes the
gravity/eta/scalar classical reset, the zero-field value `(trace,flux)=(0,0)`
in the gauge, Weyl, ghost and HS blocks, and a positive-duration classical
witness.  v17.97 explicitly records that the full nonzero fluctuation
Calderon matrices were not derived.  A linear graph containing the origin is
not determined by that fact.

## Exact resolvent separation

For the scalar representative

```text
K_h = -d^2/dx^2 + m^2,
u'(0) = h u(0),
kappa = sqrt(m^2-z) > 0,
```

the retained Cayley convention is

```text
U_h = (1-i h)/(1+i h).
```

The negative-energy resolvent kernel is

```text
G_h(x,y;z) = [exp(-kappa |x-y|)
              + r_h exp(-kappa(x+y))]/(2 kappa),
r_h = (kappa-h)/(kappa+h).
```

For the compact source `f=1_[0,L]`,

```text
<f,(R_h1-R_h0)f>
  = (r_h1-r_h0)(1-exp(-kappa L))^2/(2 kappa^3).
```

At `kappa=L=h1=1` and `h0=0`, this is

```text
-(1-exp(-1))^2/2 != 0.
```

Thus two allowed nonnegative self-adjoint phases give different native
compact-source resolvent contractions.  Self-adjointness, positivity, the
zero-background match, and the local pair/contact incidence do not remove
the missing phase.

## Claim boundary

This is a canonical no-go for closing Gate 7 from the **unchanged retained
action**.  It is not a theorem that no explicitly versioned action extension
could supply the missing normal matter generator, nor does it exclude a
future theorem proving that the complete graded observable is independent of
the entire surviving phase family.  No phase, selector, coupling, boundary
term, chord, gate, or prediction is added here.

The exact next dependency is therefore smaller than the prior generic birth
graph request:

```text
derive an action-owned normal matter boundary generator fixing the surviving
Cayley phases, or prove full graded Gate-7 phase independence on the complete
U(1)_parent x U(1)_child family.
```
