# N12 finite-history force-domain audit

Status: `LOCAL_ENCAPSULATION_EXISTS_COMPLETE_ACTION_OWNED_FORCE_INTERVAL_OPEN`.

The desingularized singular-event theorem proves local existence of a finite
pre-event branch.  It does not make an arbitrary local truncation into the
complete physical operator domain needed by the zero-source force.

This is already forced by the retained zeta/Casimir term.  For the common
scale variation `h=delta log R4=1` and any positive-radius extension from
`T1` to `T2`,

`F_zeta([0,T2])-F_zeta([0,T1])=(59/30) integral_(T1,T2) d_tau/R4(tau)>0`.

Therefore the local branch parameter `epsilon`, a validation-box endpoint,
or any other regular numerical cutoff changes an action term.  It cannot be
used as a boundary condition by convenience.  A cancellation with the heat
force for every cutoff has not been proved and cannot be assumed.

This does not reopen the nonrealized infinite-tail problem, terminal
recurrence, or universal reachability.  Nor does it require reconstructing a
complete trajectory when the same exterior information can be obtained
directly.  The current finite route is to derive the action-owned exterior
Weyl--Calderon response from the certified desingularized event graph, or to
certify an equivalent complete finite coefficient/operator oracle.  Either
route removes the arbitrary cutoff and supplies the heat-minus-zeta force for
the subsequent same-action saddle.
