# N12 C2 inverse-free Volterra/Weyl segment

The certified C2 launch is now inserted into the already-derived fixed-channel
operator.  For either the scalar or factorized product-Dirac channel, let `G0`
be the birth generator and let `K` bound the generator on the launch segment.
The Duhamel identity gives

`||T-I-Tau G0|| <= (K Tau)^2 exp(K Tau)/2 + L_x |D_tau x| Tau^2/2`.

Every retained channel has upper-right generator entry one.  Consequently the
transfer entry `b` lies in `Tau+[-r,r]`, and the certified remainder is much
smaller than the positive proper duration.  This proves the regular
two-boundary Calderón chart without integrating an ill-conditioned block.

With both endpoint values free, the response is the existing formula

`M=[[a/b,-1/b],[-1/b,d/b]]`,

where the off-diagonal equality uses the exact Wronskian `det T=1`.  Only
scalar interval reciprocals occur.  No terminal condition, downstream load,
or matrix inverse is introduced.

The first physical quotient bound propagates a unit reset-image direction
through the certified regularized state Jacobi estimate.  It also differentiates
the action-owned proper duration integral, so endpoint motion is not omitted.
The two explicit birth Cauchy jets are recorded, while a uniform norm bound
covers all 73 reset-image directions.

The launch edge remains a proof-domain edge.  This artifact is the first local
segment adapter in the maximal-forward `M_C2` composition, not the complete
child response.  The retained flow must next be continued to completed
encapsulation or a canonical stop and composed by the existing Möbius/Schur
rule.
