# N12 C2 cancellation-preserving Lohner segment 1216

This segment repeats the certified segment-1215 construction at the new
branch-24 proof center.  The center-local bordered response, relative tangent
tensor, fixed-descriptor field matrix, fresh `D3--D5` growth bounds, terminal
parent action majorants, and propagated incoming tube are its only inputs.

The bordered response is enclosed through second variation first.  The full
field remains in the exact form

`Psi/c+s(cV-RPsi)/(cDelta)`

until its signed correction derivatives have been combined.  The matrix
logarithmic norm and complete field second derivative then propagate the
incoming tube, while a first-order center predictor includes its certified
Taylor remainder.

The step and domain radius are derived proof quantities.  The predictor is not
a physical endpoint.  Strict branch, `c`, `b`, `Delta`, lapse, radius-rate,
physical-duration, and proper-duration checks are repeated.
