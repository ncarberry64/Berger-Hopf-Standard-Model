# Eigenpair enclosure on the actual HS midpoint outer domain

Apply the normalized eigenpair Banach argument from
`n12_gate7_affine_eigenpair_contraction.md` to the independently paired domain
from `n12_gate7_affine_hs_midpoint_domain.md`. The midpoint input is assembled
from paired uniform physical fields on both complete endpoint tubes. It is not
the midpoint of their center values alone.

Use the domain's exact raw center as a new action anchor. Compute and verify
its normalized, oriented index-24 eigenpair directly from the retained action.
Its witness must lie in the common proposed eigenpair box. The anchor belongs
to the outer domain even if it is not an attainable physical midpoint image.

There are 249 signed state/descriptor directions: two independent longitudinal
directions, two transverse groups of 74, and 99 interval-remainder directions.
The action uses their first 98 raw rows. Include every column in both the center
residual bound and the Jacobian-variation bound. Complete the signed retained
action contractions before applying each group's interval, Euclidean-ball or
box norm. In particular, independent endpoint directions cannot be canceled
against each other.

The p/lambda variation, fixed-center Jacobian defect, strict inclusion tests,
and deterministic growth of failed auxiliary proposal coordinates are unchanged.
No physical endpoint radius or midpoint domain is reduced to make a proposal
pass. Global inertia must be positive on the whole segment hull. The uniform
contraction, witness containment and positive reference orientation transfer
index 24 over the connected outer domain and therefore over every actual HS
midpoint image contained in it.

A failed bounded proposal search must remain a failure artifact; it does not
prove a physical singularity. A passing result must reproduce in a separate
invocation. Even a paired midpoint action-eigenpair enclosure leaves its physical
response, normalized field, uniform derivatives/remainder, global coverage and
physical quotient to be certified. Gate7 and full completion remain open.
