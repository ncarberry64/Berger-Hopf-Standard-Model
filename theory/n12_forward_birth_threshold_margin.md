# N12 physical birth threshold-margin audit

Status: `PHYSICAL_BIRTH_GRAPH_ZERO_THRESHOLD_MARGIN_REQUIRED_AND_UNASSEMBLED`.

The positive two-chord scalar barrier does not by itself determine the
continuous threshold class. Consider

\[
 K_h=-\partial_\tau^2+a^2\mathbf 1_{[0,T]},\qquad
 u'(0)=h u(0)
\]

on the half-line. At zero energy the unit birth solution on the barrier is

\[
 u_0(\tau)=\cosh(a\tau)+(h/a)\sinh(a\tau).
\]

Its derivative at the end of the barrier vanishes exactly for

\[
 h_{\rm crit}=-a\tanh(aT).
\]

The solution then continues as a nonzero constant and is a zero-threshold
resonance. The critical operator remains nonnegative: its positive zero
solution gives the ground-state transform

\[
 q_{h_{\rm crit}}[f]=\int_0^\infty
 u_0^2\left|(f/u_0)'\right|^2d\tau\geq0.
\]

For the same core with (h=0), the delta-normalized birth amplitude is
(O(k)), giving compact local counting (O(\Lambda^{3/2})). At the critical
graph the amplitude has a nonzero limit, giving only
(O(\Lambda^{1/2})). Therefore core positivity plus global operator
nonnegativity does not prove the strict excess required by the E1 criterion.

Applied to N12, the weakest certified positive child impedance is
(6.37052204298831\times10^{-8}). An unresolved event-plus-Wentzell birth
graph with the opposite zero-energy value can cancel it exactly without
violating nonnegativity.

The provenance is explicit. The zero-background graph closes only the zero
trace. The common nonzero incidence is local and domain-parametric and does
not select the temporal graph. The historical event--child record marks the
physical sector blocks as not action-derived, and the forward graph record
still marks the complete nonzero graph unassembled. The derived gauge
Wentzell formula cannot be assigned to all matter sectors.

The next action-owned object is thus the sector-resolved nonzero event flux
plus (W_{\rm phys}) birth matrix at zero energy, together with a strict
matrix Wronskian margin against the child Weyl map. A boundary-uniform
limiting-absorption theorem excluding the critical countergraph would be an
equivalent route. Gate 7 is unchanged and active; Gate 8 remains locked and
chord 3 unauthorized.
