# N12 C2 segment-1214 joint-domain extension

The endpoint-local C2 certificate has action radius
`5.5104723095444935e-11`.  The exact fixed-descriptor segment from node 1214
to node 1215 uses the slightly larger joint-feasibility radius

```text
5.5212888273161885e-11.
```

No endpoint-local derivative theorem is silently extended across the
difference.  The complete signed non-scale `D2(cb)` row sweep, the decisive
second-Jacobi contractions, and the complete global `s D2R` product-rule
bound are reissued at the joint radius.  The same action, center, physical
quotient, common-scale Weyl covariance, coefficient boxes, and terminal-parent
majorants are retained.

Only after the enlarged-domain `DDelta` and proper-duration-density covector
balls exclude zero may the transposed exact segment variational action be
applied.  This extension introduces no selector, recurrence, scale, fitted
threshold, new gate, new chord, or new physical time direction.
