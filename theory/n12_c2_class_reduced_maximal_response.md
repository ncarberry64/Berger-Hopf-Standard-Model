# N12 C2 class-reduced maximal response

The actual C2 reset output determines one unique maximal forward history in
the certified enclosure class.  The retained maximal source-domain theorem
then chooses the endpoint rule: an actual later event uses the AE2 reset graph,
while an infinite or excluded maximal end uses the Friedrichs form closure.
No validation-box edge is an endpoint condition.

For every real `z<0`, the existing maximal-forward theorem defines

`M_C2^max(z) a = Gamma1_birth gamma_C2(z) a`

on that history.  On the Friedrichs branch this is the unique operator-norm
limit of nested finite Dirichlet form-core Weyl maps.  Compactly supported
first and mixed-second form variations converge on the same exhaustion.  On
a finite regular event stratum, the retained inverse-free finite oracle is the
corresponding realization engine.

The 98 certified C2 segments are therefore one nested finite core inside one
physical class.  They support the definition and finite-prefix enclosures;
they do not define 98 operators or 98 physical objects.

Class reduction does not make the operator numerical value class-constant.
The retained Weyl family has a certified nonzero geometry variation and the
gap-only counterfamily shows that common class/domain/gap data do not bound a
Weyl value.  Actual continuous history coefficients and the endpoint outcome
remain required for numerical evaluation and noncompact reset-quotient jets.

For the zero-source force, the existing maximal-adjoint theorem avoids forming
all noncompact reset Jacobi columns.  It suffices to prove

`integral ||U(t,0)|| ||q_heat-zeta(t)|| dt < infinity`

or to reach a finite event/stop stratum.  This weighted load is the single
current Gate-7 owner after class reduction.
