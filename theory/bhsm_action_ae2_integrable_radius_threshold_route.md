# AE2 integrable-radius threshold route

For an infinite regular maximal forward history, each product-Dirac factor has `s_chi=chi*mu/R4`. If the geometry satisfies

`I_R=integral_0^infinity d_tau/R4(tau)<infinity`,

then the exact zero-transfer state approaches `exp(-chi*mu*I_R)`. The free-normalized squared threshold amplitude is therefore `(2/pi)exp(2*chi*mu*I_R)`, and the two-chirality sum is `(4/pi)cosh(2*mu*I_R)` before retained multiplicities. This supplies precisely the scalar normalization input required by the factorized source-measure reduction and yields `O(Lambda^(3/2))` for every fixed channel and compact log-radius source direction.

One sufficient geometry-only hypothesis is `R4(tau)>=R0(1+tau/T0)^(1+delta)` with `delta>0`, which gives `I_R<=T0/(R0*delta)`. No Standard Model observable or physical scale is inserted.

The current action ledger does not prove this global growth law or any equivalent `I_R` bound. Its local positive radius rate is kinematic, and the constraint-reduced Legendre energy is not coercive. Consequently the actual infinite-history branch remains open. The exact alternative is a direct source-normalization theorem for the non-`L1` supersymmetric tail; terminal recurrence and a strict spectral gap remain unnecessary.

`FULL_BHSM_COMPLETE=false`.
