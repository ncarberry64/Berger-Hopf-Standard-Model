# N12 C2 cancelled theta extension from the tracked 1,221 prefix

The fixed-descriptor proof divides by

`Delta=c_psi b_psi+s R`

and becomes badly conditioned near the birth eigenvalue.  The same-action
scaled field

`G_theta=Delta F_s=[s V_q,b_psi Psi+s V_hard]`

does not.  At the fully tracked 1,221 endpoint, recombination of the signed
`Delta` first jet with the fixed-`s` field matrix gives

- `||G_theta|| <= 4.294e-4`,
- center `||DG_theta||_2 = 6.289`, and
- a certified center-matrix remainder below `0.05`.

The complete bordered-response second-variation fixed point is then solved on
a ball strictly containing the inherited endpoint tube.  All large quotient
terms are cancelled before norms are taken.  The resulting interval
logarithmic norm, direct field second variation, descriptor-rate enclosure,
lapse enclosure, and positive signed-descriptor interval certify one strict
forward `theta` extension with positive proper duration.

This segment is an alternative continuation from the tracked 1,221 prefix.
Its predictor is not a selected physical child or an endpoint.  It proves
that the binary64 near-birth eigenvalue floor and division by `Delta` are not
termination mechanisms.  The remaining owner is recentered iteration of the
same construction, followed by log-descriptor continuation when the signed
descriptor is numerically separated, until strict capture inclusion or a
genuine retained stop.

