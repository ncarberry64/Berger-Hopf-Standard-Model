# AE3.1 current-C2 gauge composite HS action

The derived static nonlocal gauge current kernel can be rewritten exactly in
the transported quark LR channels.  This supplies the missing action-owned
odd *composite* endomorphism without adding an elementary scalar or importing
the globally obstructed Einstein--Cartan zero-mode kernel.

Let

```text
O = (bar(Q_L) u_R, bar(Q_L) d_R)^T,
K_LR = 2 G_C2 diag(7/5,13/10),       G_C2 > 0
```

on a positive coexact current-`C2` DtN mode.  Completing the square gives

```text
exp[+O^dagger K_LR O]
  proportional to
integral D H exp[-H^dagger K_LR^(-1) H + H^dagger O + O^dagger H],
```

with

```text
K_LR^(-1) = G_C2^(-1) diag(5/14,5/13).
```

This is an exact change of variables in the already-owned current interaction.
The unit up/down vertices are fixed by placing every channel coefficient in
the auxiliary quadratic block.  They are not fitted Yukawa couplings, and a
rescaling of the auxiliary coordinates changes the displayed vertex and
quadratic block together without changing the four-fermion interaction.

## Odd attachment and current-C2 domain

On the transported binary incidences,

```text
E_HS = H_u I_up + H_d I_down,
{Gamma_chi,I_up} = {Gamma_chi,I_down} = 0.
```

Thus the auxiliary rewrite realizes the odd endomorphism class that the
Higgs-selected connection and scalar-only profile could not supply.  Because
the incidences are finite internal operators tensoring the radial identity,
they commute with the current-`C2` radial carrier and preserve the reset domain
and retained birth trace.  The family factor remains `I3`.

The action ownership is deliberately limited: `E_HS` is an auxiliary
composite endomorphism inherited from the gauge current kernel.  It is not the
intrinsic AE3.1 Higgs endomorphism and does not yet identify a physical scalar.

## Remaining physical block

At bare level the auxiliary fields have no derivative kinetic term.  Their
dynamical two-point block must be generated by the same current-`C2` fermion
functional already factored as

```text
K_LR^(-1) - Pi_Had,sing I2 - Pi_fin[C].
```

The physical identification requires the mixed block

```text
H_mix = [[H_intrinsic, M_HS],
         [M_HS^dagger,
          K_LR^(-1)-Pi_Had,sing I2-Pi_fin[C]]].
```

`M_HS` is not present in the retained action and is not set to zero by
definition.  It must be obtained from the same-domain mixed intrinsic/
composite variation.  Until the derivative residue and this mixing are
derived, the unit auxiliary vertex cannot be called a canonical quark Yukawa,
and no physical Higgs direction, gap, quark mass, or CKM matrix is promoted.

This route uses no endpoint cutoff and no EC counterterm.  It does not revive
the failed global EC elimination: its parent is the regular positive coexact
gauge kernel.
