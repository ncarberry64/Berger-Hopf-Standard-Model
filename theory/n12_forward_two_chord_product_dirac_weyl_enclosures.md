# N12 two-chord product-Dirac Weyl enclosures

Status: `TWO_CHORD_PRODUCT_DIRAC_WEYL_AND_COMPACT_WEAK_JETS_ENCLOSED_BROADLY`.

For a fixed product-Dirac channel, retain the action-owned factorization

\[
 A_\lambda=\partial_\tau+s_\lambda,
 \qquad s_\lambda=\pm\lambda/R_4,
 \qquad P_\lambda=A_\lambda^*A_\lambda.
\]

At the probe \(z=-\kappa^2\), the Poisson energy is

\[
 \|A_\lambda u\|^2+\kappa^2\|u\|^2.
\]

Use the birth conormal \(M=-A_\lambda u(0)/u(0)\). Positivity of the retained
future gives \(M\geq0\). On the certified core, let
\(S=|\lambda|/R_{4,-}\). The admissible Dirichlet trial
\(u(\tau)=1-\tau/T_-\), extended by zero after \(T_-\), gives

\[
 0\leq M(-\kappa^2)
 \leq \frac1{T_-}+S+\frac{(S^2+\kappa^2)T_-}{3}.
\]

This proof stays in the factorized quadratic form. It therefore never expands
\(A_\lambda^*A_\lambda\) into a potential containing \(s'\), which is not an
independent action-owned history coefficient.

For log-radius variations supported in the certified core,
\(|s_h|\leq SH\) and \(|s_{hk}|\leq S(H^2+L)\). Coercivity controls both
\(\|A_\lambda u\|\) and \(\kappa\|u\|\). The first form derivative is bounded
by \(2SHM_{\max}/\kappa\). The mixed bound combines the direct mixed form
jet with both Dirichlet-resolvent pair contractions; all terms are explicit in
the artifact.

The stored round Dirac blocks through level three give witness rows for
\(|\lambda|=3/2,5/2,7/2,9/2\) and include signed eigenvalues of both signs.
The bound depends only on \(|\lambda|\), so it covers either chirality/sign
branch parametrically. It selects no future endpoint,
terminal event, third chord, source profile, or momentum interpretation.

Scalar/de Rham and product-Dirac base/weak channel bounds are now available at
`z=-1`. Common pair/contact incidence, its certified spatial-tail summation,
and the zero-source weak geometry force remain open. Gate 7 remains active.
