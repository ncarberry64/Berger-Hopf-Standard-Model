# Gate-7 formation/decay chronology supersession

## Scope

This audit reconciles Norman's finite-encapsulation ontology with the retained
maximal-forward source domain and the later finite-endpoint KKT construction.
It changes no action equation or endpoint law.

## Chronology

The physical chronology is

`pre-event formation -> finite encapsulation event -> AE2 reset -> child decay/evolution`.

The desingularized local branch proves existence of at least one pre-event
formation history that reaches the encapsulation event in finite positive
time.  The infinite regular branch excluded by owner ontology is an infinite
*nonencapsulating formation* history.  The owner restriction does not require
the post-event complete child to encounter a second event in finite time.

## Retained post-event source domain

The already-retained maximal-forward source-domain theorem gives three
action-owned exterior rules for the child arm:

1. use the retained event/reset graph if a later event is actually hit;
2. use Friedrichs closure at a finite strong/domain/Dirac exit; and
3. use Friedrichs closure at an infinite or excluded maximal end.

An arbitrary finite validation cutoff remains forbidden.  Thus the physical
event-centered operator still needs the child Calderón response, but that
response need not come from a second finite terminal event.

## Consequence for the KKT frontier

The finite-endpoint forward--adjoint quotient KKT system remains correct on a
nonempty finite event-or-stop stratum and is a sufficient completion route.
It is not the unique native Gate-7 requirement.  Promoting existence of a
finite-endpoint KKT root to the sole current owner incorrectly transfers the
finite-encapsulation condition from the incoming formation arm to the
outgoing decay/evolution arm.

The same-action continuation and direct heat-coercivity audits remain valid
within their stated finite-endpoint scope.  They do not rule out an
event-generated maximal child exterior with an infinite Friedrichs end.

## Correct current owner

On a nonempty regular physical reset-quotient stratum, certify the maximal
child Calderón/Weyl family and its required quotient geometry jet using the
retained endpoint rule, allowing either a later event, a canonical exit, or
an infinite Friedrichs end.  Then evaluate the heat-minus-zeta covector and
certify its physical quotient root, equivalently by a simultaneous
forward/operator/adjoint KKT solve on that actual maximal domain.

No universal reachability, post-event recurrence, chord 3, reset selector,
new endpoint, new action term, scale, fit, or frozen-prediction change is
introduced.  Gate 7 remains active and Gate 8 remains locked.
