# Selected-cone internal response and causal Taylor `Z2`

This certificate is the narrow adapter from the retained quarter-step
history to the Gate-7 radii-polynomial inputs.  It changes neither the action
nor the physical source ontology.  The external Cauchy/birth source is zero;
the incoming response, child response, reset transport, contact terms, and
selected-line response remain one internal action-owned seam object.

For the symmetric selected-line border

`K(xi) x(xi) = rhs(xi),  x=(h,b),`

the calculation differentiates the bordered equation rather than an
inverse.  In particular,

`K x' = rhs' - K' x`

and

`K x'' = rhs'' - K'' x - 2 K' x'`.

The multiplier is bounded through the exact identity

`b = <psi,rhs>`.

The physical reduced numerator contains `b psi + s h`.  Equivalently the
hard solve is scaled before differentiation, so `s h` is retained before a
norm is taken.  This prevents an artificial product of a bare hard-response
third derivative with the selected descriptor.

The selected descriptor's third transverse coefficient is assembled in the
common eigenframe.  Its center cross terms are the symmetric contraction of
`psi'` with `H'' psi` and of `psi''` with `H' psi`.  Retained signed interval
majorants supply the outward remainder.  No global `D3 f` tensor is formed.
The resulting scalar majorant for normalized-field third variation is

`||D3(G/||G||)|| <= N3/g + 9 A1 A2/g^2 + 15 A1^3/g^3`.

The causal enclosure does not replace the exact tensor by its Frobenius norm
at second order.  At each source seam it first forms

`q_j = (1/2) D2 f_j[v_j,v_j]`

with the actual signed transverse causal vector, then transports `q_j`
through the retained 73-dimensional step maps.  Only the residual error is
normed.  Since the Volterra Green matrix is strictly lower triangular, the
error radii are obtained in one forward sweep:

`e_i = sum_(j<i) G_ij [ M_j c_j e_j`

`    + C_j (||v_j|| e_j + e_j^2/2)`

`    + L_j (||v_j||+e_j)^3/6 ].`

Here `M_j` is the retained mixed curvature bound, `C_j` is the exact center
`D2 f` Frobenius bound, and `L_j` is the selected-cone `D3 f` majorant.  The
last two seam tubes alone are widened to the already-certified global cone
radius after the initial causal replay identifies them as the only local
radius owners.  This is an owner-only proof refinement; it introduces no new
radius and no new physical domain.

Certification means that every bordered and normalization denominator is
positive, the same-formula center derivatives agree with the retained exact
jets, the signed Taylor--Volterra radii remain inside their local proof tubes,
and their maximum remains strictly inside the selected DOP853 cone.

This closes only the complete internal-response tube and the componentwise
causal `Z2` input.  The signed defect `Y`, outward propagator term `Z1`, their
joint radii polynomial, and the first-hit/domain transfer remain open.  Gate
7 and `FULL_BHSM_COMPLETE` therefore remain active/false.
