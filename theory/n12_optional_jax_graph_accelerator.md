# N=12 optional JAX graph-Jacobian accelerator

Status: `CROSS_VALIDATED_RECONNAISSANCE_ACCELERATOR; NOT_INTERVAL_AUTHORITY`.

The retained 96-point BHSM action Jet remains the source of action values,
gradients, Hessians, selected eigenlines, and every certificate datum.  An
optional JAX realization supplies the third action tensor used to accelerate
the dense graph-Jacobian profile.  It does not change the action, field,
endpoint, branch rule, or proof norm.

At five nodes spanning the retained half-step stop history, the JAX action
value, gradient, and Hessian agree with the retained Jet to maximum relative
residuals `4.211762983581353e-15`, `1.0375012862076502e-14`, and
`6.551949589445655e-16`.  The assembled hybrid graph Jacobian and descriptor
gradient agree with 98-direction retained replay to `1.0853000603641994e-10`
and `9.685069704482086e-10` relative residual, respectively.  The audit records
the exact accelerator versions and backend.

This comparison permits the hybrid profile to choose and diagnose a better
numerical center.  It does not promote JAX arithmetic, point samples, or the
hybrid third tensor to interval authority.  The final Krawczyk inequalities
must use outward-rounded retained-action bounds, including the between-node
Jacobian remainder.
