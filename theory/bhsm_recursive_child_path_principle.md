# Recursive child-path principle

Status: `OWNER_HYPOTHESIS_WITH_PARTIAL_ACTION_LEVEL_REALIZATION`.

Owner continuation of 24 September 2026, on
`codex/g7-vector-endpoint-final-closure`. The inherited numerical checkpoint
is `ea526ad9`; the interval-13 child-chart certificate from `86b05bc9` was
integrated as `7f180488` without repeating its calculations. This note extends
[the child-closure candidate](bhsm_child_closure_cross_scale_candidate.md),
retaining its assumptions, counterexamples and open verdicts.

An admissibly persistent child can become the parent of a later event.
The event may preserve its identity, produce ordinary daughters, form a bond
or collective child, exchange or restructure children, or admit a core
transition where an action-owned route exists. The relation is recursive;
there is no universal outcome of forcing and no law of rigidity in constituent
count. Coherent closure can support rigidity only under the corresponding
action-derived susceptibility conditions.

Gate 7 remains OPEN. `FULL_BHSM_COMPLETE = FALSE`. No physical radius, frozen
prediction, action term, selector or gate criterion is changed here.

## EXISTING THEOREMS

These results retain their original domains and hypotheses. Environmental
support is owner-supplied physics; the conditional filtering consequences
do not derive that postulate.

| Authority | Established scope | Limit |
|---|---|---|
| [Local finite encapsulation](n12_finite_encapsulation_local_branch.md) | Local finite-positive-time formation and positive-duration child persistence | No global return, universal reachability or unique child |
| [Reset and child relation](bhsm_environment_conditioned_reset_selector_recovery.md) | Nonempty conditional child family; the N12 fixed-event block has rank 31 in 98 variables | Its 67-dimensional fiber, or 66 after the time quotient, is not a list of decay channels; no full-field selector |
| [Environmental compatibility](bhsm_environmental_child_compatibility_selection.md) | Typed support, invariant, scale, boundary and causality filtering | `SUPPORTED`, `EXCLUDED`, `UNRESOLVED` are distinct; supported sectors need not have selected members |
| [Fixed-history nonuniqueness](ae31_c2_fixed_history_state_nonuniqueness.md) | A continuous pure Hadamard covariance family survives fixed history and reset transport | Selecting a background does not select a quantum state |
| [Collective susceptibility](bhsm_collective_encapsulation_response_candidate.md) | Constrained KKT response and conditional energy-norm/reducing-subspace comparison results | Ordinary-norm response can increase under restriction; coherence alone gives no susceptibility bound |
| [Child-coherent eigenbranch links](n12_gate7_child_coherent_eigenbranch_links.md) | Two certified links through the actual HS midpoint of interval 13, preserving the oriented simple index-24 eigenpair | A connected evaluation cover, not a full dense-output physical tube |

In particular, the inherited example `H=[[1,2],[2,5]], B=[2,5]^T,
W=span(e1)` has unrestricted response `[0,1]^T` and restricted response
`[2,0]^T`. Thus even a positive Hessian and tangent restriction do not prove
ordinary-norm rigidity. Adding constituents need not be tangent restriction
in the first place.

## OWNER HYPOTHESIS

### Typed recursive relation

Use `E` for the existing event/environment data, including a parent trace
where required, and `A` for the action with its owned field/operator domains,
constraints, boundary ensemble, gauge quotient and scale/stratum maps.
The existing descriptor `(alpha_s,tau_s,I_s,Lambda_s,B_s)` retains its
structure-stratum, scenario, invariants, scale and boundary meanings;
`tau_s` is not a new time coordinate.

`C(E,A)` denotes the existing reset/child relation restricted by these
conditions, or a later transition relation once the action derives it.
It is a typed relation/set, not a phenomenological state machine. Record
supported, excluded and unresolved candidates separately. An unevaluated
condition cannot be treated as satisfied. A realized member is `X_C`;
the relation alone does not select that member.

At a regular child, `T_C` is the physical constraint tangent with the owned
residual gauge quotient, boundary domain and norm. The environmental
susceptibility `S_C = D_E X_C` exists only where a differentiable realized
branch and the required inverse are available. If constraints depend on E,
differentiate the complete KKT system, including `C_E`; replacing it by an
unconstrained Hessian inverse loses physical reaction terms.

Closure is a conjunction of the predicates required by that sector:
environment compatibility, constraint equations, admissible boundary data,
gauge/topological compatibility, reset relation where applicable, positive
action domains, branch persistence, and the relevant spectral-gap/Hessian
conditions. A static positive Hessian is not a universal dynamical closure
criterion. No arbitrary scalar closure score or forcing threshold is added.

The conditional path is

```text
parent -> owned environment/action event -> admissible child relation
       -> realized child -> its owned evolution -> later event or continuation.
```

Each realized ordinary daughter, bond or collective child can be a new
parent when its next event is defined. This includes competing children,
exchange and restructuring. A schematic `CLOSED -> BOUNDARY -> NEXT CHILD
SELECTION` requires demonstrated failure of physical conditions, followed
by an owned transition and selection mechanism. Failure to prove an
inequality or reaching a finite computation cutoff is not that boundary.

### Distinct outcomes and their limits

| Candidate outcome | Required interpretation |
|---|---|
| Persistence | The declared identity continues under the full relevant dynamics; its state and local representative can change |
| Decay `C_i -> C_j + C_k + ...` | Realized loss of parent persistence into dynamically accessible ordinary daughter sectors, which may themselves later decay |
| Bonding `C_i + C_j + E -> C_ij` | An action-supported collective child and its formation/persistence, not merely a permissible tensor product |
| Re-encapsulation/restructuring | A new admissible child reached through the appropriate owned transition relation |
| Core transition `C_i -> C_A` | A genuine action-owned route to the pregeometric sector; the general route is OPEN |
| Reconstruction `C_A -> C_new` | An independently owned reconstruction, after which the new child may become a parent; no universal existence is assumed |

Spontaneous decay need not wait for external forcing or loss of an
instantaneous local minimum. Metastable configurations can remain locally
admissible while full dynamics permits leakage. One accessible daughter
channel suffices. Environmental forcing therefore tests closure without
uniquely predicting decay, bonding, exchange or any other outcome.

Excessive forcing followed by a core slip is an owner hypothesis, not a
derived threshold or transition law. The pregeometric `C_A` stratum does
not acquire an inverse metric or time evolution at `upsilon=0` from notation.
Provisional S8, relative S5|4 and intrinsic S4 descriptions still require
their explicit compatibility maps.

### Force-specific realization

The same architecture does not identify the forces. Existing sector evidence
and the external examples are catalogued in the
[cross-scale note](bhsm_child_closure_cross_scale_candidate.md#open-physical-derivations).

For QCD, the retained color representation admits the invariant epsilon
contraction: `epsilon_abc U^a_d U^b_e U^c_f = det(U) epsilon_def`.
For SU(3) this gives an invariant color line. Different pair-first couplings
to that line can describe one collective child. The existing C2 quark LR
kernel with color Casimir `4/3` and relative weights `7/5,13/10` is a bilinear
exchange result, not a three-quark bound-state equation. Establishing a
baryon child still requires the owned gauge/BRST and boundary domain,
gauge-invariant state or correlator, binding/pole evidence and channel
analysis. Singlet algebra alone proves neither binding nor confinement.
Thus a generic three-body problem and a color-constrained collective child
need not have the same structure, but no BHSM baryon closure is promoted.

For electroweak transitions, use the owned parent/daughter representations,
vertices and physical external states. The existing charged-current
coefficient form `g2_BH/sqrt(2)` does not supply the physical coupling or
decay width. Same-action masses, residues, flavor projectors, amplitudes
and phase space remain necessary. The local Maxwell-residue obstruction
and nonlocal DtN distinction remain. Weak interactions are not a generic
three-body force.

For electromagnetic/material sectors, pairing, atoms, molecules and lattices
are later collective-child targets. The owner's Cooper-pair example motivates
environment-dependent admissible structure; it is not a BHSM pairing
derivation or a free-space binding claim. For gravity, capture, binaries,
hierarchical triples, exchange, ejection, merger, fragmentation and larger
structures require their gravitational dynamics and domains. These are
distinct realization tasks, not consequences of constituent count.

## NEW DERIVATIONS

### Conditional finite composition

Suppose a finite sequence has (i) an owned within-child evolution on each
segment, (ii) endpoint traces in the next relation's domain, and (iii) an
admissible realized child satisfying each event relation. Ordinary finite
relational composition yields a path satisfying every segment/event condition.
Proof is induction: append one admissible relation and its owned continuation
to the already admissible finite path. This formalizes recursive reuse of a
child as a parent. It proves neither existence of the next event nor selection,
uniqueness, universal reconstruction, or nonaccumulation of infinite events.

### Chart changes preserve an already connected eigenbranch

The action-level numerical instance uses the inherited normalized bordered
eigenpair equation. Interpolate the predictor and preconditioner using the
same `s in [0,1]`. With `D_i=I-R_i J_i`, the exact surrogate identity is

```text
I-R(s)J_aff(s) = (1-s)D_0+s D_1+s(1-s)(R_1-R_0)(J_1-J_0).
```

Action-derived D4 contractions bound the physical Hessian interpolation
error. The signed predictor residual includes the normalization border;
the witness correction retains the nonlinear eigenpair terms. A uniform
self-map and contraction with endpoint root-box containment connects the
same normalized simple eigenbranch. Concatenation through the same certified
midpoint root then preserves identity across both links. Changing R or the
witness is not a physical transition.

The imported interval-13 proof is reused unchanged. The new interval driver
applies exactly its proof kernel to existing endpoint/midpoint evidence;
the [current reproduction guide](../docs/GATE7_CURRENT_REPRODUCTION.md)
records the frozen extension and quantitative results. No successful inherited
action calculation or eigenpair solve is repeated.

The fixed `(R18,W18)` obstruction concerns that fixed chart. It does not
imply loss of the eigenline. Link diagnostics distinguish (1) chart
conditioning, (2) insufficient enclosure, (3) missing intermediate chart,
(4) demonstrated loss of the simple eigenline/gap, and (5) demonstrated
action/domain incompatibility. A failed lower gap estimate is category 2,
not proof of category 4. Only established 4 or 5 could support a physical
closure boundary, and neither alone establishes a decay or core outcome.

| Proof layer | Obligation retained |
|---|---|
| A: identity | Same selected branch through certified chart connections |
| B: physical neighborhood | All allowed state, midpoint and output perturbations, including the required dense-output tube |
| C: global remainder | Complete physical derivative remainder and `kappa_L`, `kappa_T` on the required history |
| D: Gate-7 contraction | Full physical self-map and contraction inequalities |

Layer A supplies identity control for subsequent derivative work. It does
not by itself prove B, C or D. Moving chart differentiation in later rate
jets must retain the complete action-owned derivatives and coordinate
transformations; no fixed-chart bound or centerline bound is silently reused
as a full-neighborhood estimate.

## OPEN

The general physical child selector; general core transition/reconstruction;
electroweak decay; QCD collective closure; the EM bonding hierarchy;
gravitational/cosmic realization; and a universal transition-rate law remain
open. Fixed-history covariance nonuniqueness and sector attachment gaps
remain. No interpretation removes them.

The owner-supplied completion order is:

1. Close Gate 7 physical history/background.
2. Establish action-owned child persistence/transition conditions.
3. Complete same-action gauge/fermion/HS attachment.
4. Derive QCD/color collective closure.
5. Derive electroweak parent/daughter transition structure.
6. Derive electromagnetic bound/collective child structure.
7. Complete mass/pole/flavor/neutrino/RG observables.
8. Attach cosmology as the large-scale consequence layer.
9. Freeze blind predictions without altering existing frozen predictions.
10. Complete reproduction, manuscript and public release.

The cosmology paper remains the observational teaser. A later BHSM paper
can explain the common architecture only with these sector-specific statuses
intact. Computation follows `VERIFY ONCE -> FREEZE -> REUSE` and
`LOCAL CHILD CHART -> CERTIFIED OVERLAP -> NEXT CHILD CHART`. Exhaust existing
endpoint and midpoint evidence before new anchors; no 370-endpoint campaign
is authorized by this atlas.
