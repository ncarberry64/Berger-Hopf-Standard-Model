# Same-center Gate-7 action-block screen

The accepted replay center, branch, causal frame, and frozen preconditioner are
unchanged.  The scalar 74-dimensional radii polynomial is already obstructed,
but that result alone does not decide whether a vector radius can retain useful
anisotropy.

The first action-owned split is the existing causal decomposition into 73
constraint-tangent field coordinates and one independently carried descriptor
coordinate.  Before evaluating a dense block curvature tensor, the necessary
field self-map inequality can be tested from two existing objects:

1. the field share of the accepted-center defect, recomputed by 384-bit Arb
   value enclosures at all 371 endpoints and 370 retained midpoints; and
2. the already-certified coordinate-61 curvature witness, whose input lies
   entirely in the field block.

For any nonnegative block majorant, dropping its linear, cross, and descriptor
terms only weakens the field inequality.  A necessary condition is therefore

```text
Y_field - r_field + Z_field<-field r_field^2 < 0.
```

The outward values are

```text
Y_field > 6.157752658017768e-7,
Z_field<-field > 3376436.18656192,
1 - 4 Y_field Z_field<-field < -7.316503560971616.
```

Hence no positive field radius satisfies even this weakened polynomial.  The
coarse 73+1 field/descriptor block route is obstructed without a dense new
curvature campaign.  This is a proof-coordinate route obstruction, not root
nonexistence or physical instability.  It does not adjudicate componentwise
radii or a finer partition inherited from the action blocks.

The surviving calculation is a same-center componentwise or finer
action-owned block screen.  Only direction-by-output-block curvature envelopes
needed by that declared partition should be persisted; the center, trajectory,
action, branch, and scaling remain frozen.
