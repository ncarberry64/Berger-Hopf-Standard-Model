# N=12 Gate-7 quantitative capture-bridge recombination

Status: `LEADING_BORDERED_INVERSE_RECOMBINED_FULL_TRAPPING_AND_RESET_COVER_OPEN`.

The captured-NHIM rank-72 relative-tail theorem reduces the remaining
geometric Gate-7 obligation to a nonempty event-generated family entering the
existing finite-N12 capture basin, or reaching a retained later stop.  The
older quantitative artifacts already close more of the capture side than
their historical routing suggests, but they do not close that connection.

Let `B_-2=A7+2 H0 E7` be the 74-dimensional physical bordered recurrence
pencil in the common `H6_q x H5_v x H6_m` product chart.  The determinant
graph-equivalence and uniform nonlinear-defect certificates give

`theta_7(rho) <= M3 rho + (M4/2) rho^2`

without forming either the combined Euler--Dirac inverse or separate
constraint and kinetic inverses.  At half the previously certified nonlinear
radius,

`rho_bridge = rho_nl/2`,

the directed bound is at most `1/16`, so the leading-weight bordered map has
a strict Neumann margin of at least `15/16` throughout this positive product
ball.  Thus separate leading normalized constraint-block and reduced-kinetic
inverse obligations are superseded there by the already certified combined
bordered theorem.

This does not yet control the epsilon-dependent operator perturbation from
`L5,L3,L1,L-1` and inverse inertia, nor the inhomogeneous full-action
remainder.  A complete Krawczyk/Neumann enclosure may reserve at most `7/16`
for that lower-weight relative operator defect, leaving total defect at most
`1/2`, and must enclose the remaining inhomogeneous correction inside the
unused product-ball radius.  Certified repeated bordered solves are the
inverse-free proof object.

The directed first lift has certified product norm `C_X5`.  Reserving half of
`rho_bridge` for it gives the necessary first-lift feasibility condition

`epsilon C_X5 <= rho_bridge/2`,

or

`epsilon <= 1.1441832196253499e-617`,

`R4 >= 2.9563252513094167e308`.

This extreme scale is a conservative determinant/Frobenius existence scale,
not a new physical threshold and not evidence that connection is impossible.
The certified reset has `epsilon approximately 0.99535`; no current validated
cover spans the resulting gap.

After the full lower-weight Krawczyk bound, the remaining dynamical proof is a
single action-owned stable-cone/trapping certificate on the physical quotient.
Writing `eta` for stable-normal displacement, `a` for the 24 center-shape
coordinates, and retaining `epsilon'= -2 H4 epsilon`, sufficient data are
positive constants and margins satisfying, on one declared tube,

`H4 >= h_star > 0`,

`D^+ ||eta|| <= -gamma_star ||eta|| + C_epsilon epsilon`,

`||a'|| <= C_a (||eta||+epsilon)`,

together with inward-pointing stable-boundary and integrated center-drift
budgets and all selected-line, constraint, metric, lapse, shift, and AE2
domain margins.  The existential NHIM theorem guarantees some local version;
Gate 7 needs an interval-certified quantitative version that a nonempty reset
stratum is shown to enter.  A retained later event or canonical stop remains
the exact alternative.

No selector, recurrence, chord, fitted threshold, new scale, action term,
physical time direction, or frozen-prediction change is introduced.  The
captured-family rank-72 tail theorem remains certified, while Gate 7 and full
BHSM completion remain open.
