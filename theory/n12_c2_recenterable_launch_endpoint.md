# N12 C2 recenterable launch endpoint

The pole-free Jacobi estimate permits the launch length to be set by the
certified positivity tube for `Delta`, rather than by the superseded crude
Gronwall bound.  At that length the new Jacobi exponent is below `0.001`.

A numerical predictor is formed from the refined proof center and the birth
limit `psi/c_psi`.  It is not asserted to be a physical endpoint.  The exact
endpoint set is enclosed by the propagated refined-root radius, a conservative
predictor mismatch, and the nonlinear flow remainder.  Twice that tube radius
defines the next translated proof ball, which remains strictly inside the
outer action ball where the retained action and selected line are certified.

This closes the first recenterable continuation handoff.  Neither completed
encapsulation nor a canonical stop has yet been reached.
