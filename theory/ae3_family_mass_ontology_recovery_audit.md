# AE3 recovery of the harmonic/scale/relative-energy mass ontology

The recovery audit confirms the central historical point: BHSM did not define
physical particle mass as a raw internal eigenvalue. The v14.54 contract is

\[
 \Delta H_\xi
 =Q_\xi[\Phi_{P+C}]-Q_\xi[\Phi_P^{\rm matched}],
 \qquad E_{\rm rel}=mc^2
\]

in a stable rest frame, with agreement to the pole or Floquet quasi-energy on
the appropriate branch. That ontology remains valid and was never disproved.

## What the hardening steps actually proved

The v15.54 scalar calculation preserved the family labels and their Berger
eigenvalues but correctly refused to call them fermion masses. The v15.56
calculation proved that a fiber-invariant local Higgs overlap is `I3` and that
raw vertical Dirac levels are not chiral mass entries. Neither result evaluates
the full composite-minus-parent energy of three distinct field configurations.

Current AE3 preserves all family/mode fibers through the enclosure, while its
attached local product-Dirac block remains family-central. This is compatible
with different total relative energies; it merely leaves them unevaluated.

## The recovered semigroup branch

The later Hopf-base packet constructed a different object from positive local
gradient energy:

\[
 \mathcal T_\ell=\exp[-\mathcal L_{a,\ell}/(4\pi)].
\]

For the frozen charged-lepton labels and historical squashing it gives the
weights

\[
 1,quad 0.0600744709\ldots,\quad 0.0002972911\ldots.
\]

This decreasing contraction explains why the zero-mode slot is the heaviest.
The associated dimensionful triplet used a trace-normalized charged source and
a Planck-to-electroweak Hopf lift. It used no charged-lepton mass input, but its
own packet classified the Hopf-base convention, squashing, scale candidate,
profile package, and lift interpretation as conditional. A later manual v11.3
research packet declared an intrinsic action insertion; it was not transported
into or proven equivalent to the current `BHSM-AE-3.0.0` C2 action.

Therefore the semigroup candidate is recovered as a serious historical
candidate, not promoted as a current prediction and not conflated with the
v14.54 relative charge.

## Exact missing bridge

A numerical radius alone is insufficient. The relative-energy route still
requires a normalized current-C2 realization of each mode, a matched parent,
complete covariant `Theta`/`Q_xi`/boundary-ensemble data, a mode-resolved
relative charge or stable cycle, and the pole/rest-frame/absolute-unit map.

The semigroup route requires current-C2 action ownership of its generator and
unit response time, profile radius, trace-normalized source, dimensionful Hopf
lift, broken-sector insertion, fermion poles, and equivalence to the v14.54
mass contract.

Their shared owner is:

```text
CURRENT_C2_MODE_RESOLVED_ACTION_ENERGY_MAP_WITH_ITS_OWNED_DIMENSIONFUL_SCALE_
AND_PHYSICAL_POLE_OR_RELATIVE_CHARGE_READOUT
```

The next finite audit should type the historical semigroup source, radius, and
lift against the AE3 C2 action inventory. It must stop at the first ownership
failure; only if all survive should it derive poles and compare them with
`Delta H_xi`.
