# N12 Gate-7 projected exact-affine dense-center flow defect

The corrected dense candidate adds two piecewise-affine action-coordinate
profiles to the retained DOP853 dense polynomial: the certified Taylor26
signed-source response and the minimum-action-norm constraint projection.  Its
descriptor uses the corresponding coupled direct-plus-state-induced response.
The final native dense cell is evaluated only through the retained stop
fraction.

At every cell midpoint, compare the analytic candidate derivative with the
normalized direct 96-point action field and independently re-evaluate all 25
scaled action constraints.  This is a numerical routing audit.  It does not
turn midpoint sampling into a residual enclosure, a continuous orbit, a
descriptor-fiber theorem, or a first-hit theorem.

A nonzero defect must be solved by a constraint- and descriptor-fiber-augmented
collocation correction before the continuous variational carrier can be
rebuilt.

`FULL_BHSM_COMPLETE = FALSE`.
