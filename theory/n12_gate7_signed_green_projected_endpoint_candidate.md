# Gate-7 signed-Green projected endpoint candidate

Replay the 371 endpoints produced by the signed Green Newton correction with
the direct retained action.  Evaluate the 25 scaled constraints first and
apply one minimum-action-norm projection only when their norm exceeds the
declared numerical trigger.  Re-evaluate the selected branch after projection,
set each complete preterminal descriptor to its selected eigenvalue, and
rebuild the terminal sign bracket on the constraint-projected final segment.
If the signed Green correction leaves the old endpoint strictly preterminal,
continue once along its retained exact normalized field inside the unused
remainder of the original quarter cell and bracket there.  This continuation
is numerical center construction, not an outward first-hit theorem.

This closes the endpoint constraints and descriptor fiber numerically.  It
does not establish the continuous path: the new endpoints must be joined by
endpoint-field-matched cubics and all 1,110 retained Gauss-3 residual samples
must be replayed before an interval shadowing theorem is attempted.

`FULL_BHSM_COMPLETE = FALSE`.
