# Verified point variation inside the existing endpoint tube

This diagnostic samples the complete retained physical derivative on the same
frozen trial column at longitudinal parameters -3/4 and +3/4 of the existing
endpoint tube radius, with every transverse parameter zero. The raw state and
descriptor construction retains outward rounding and must lie inside the paired
physical domain. The field graph, normalization, weights, reference, and selected
eigenline index 24 remain unchanged. No empirical data or fitting enters.

At each sample, independently verify the normalized eigenpair and all three
physical linear solves. Require the eigenpair and primal response to belong to
the exact paired family used by the uniform derivative, and require the paired
uniform directional action to contain the complete verified point action.

Subtract the already paired anchor derivative with its full uncertainty. The
distance of that difference ball from zero is a certified lower bound on the
observed coordinate variation. Any single interval enclosing both actual values
needs at least half that distance as its radius. This is a necessary bound only:
two samples do not establish a uniform upper bound, exclude larger variation
elsewhere, or prove a local/global contraction. Maxima in different rows must not
be treated as one coordinatewise error budget.

Each sample must reproduce in two fresh processes before its numerical evidence
is used. Preserve any branch, response, or uniform-containment failure. The point
probe is diagnostic evidence about interval slack and actual variation, not a
replacement for the complete uniform derivative or a physical prediction.
