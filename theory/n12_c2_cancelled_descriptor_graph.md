# N12 C2 cancelled signed-descriptor graph

Extend the cancelled field by the signed descriptor equation

`dY/dtheta=G_theta`, `ds/dtheta=Delta`.

Because the retained selected-line incidence gives

`Dlambda[G_theta]=Delta`,

the graph defect

`E=s-lambda(Y)`

satisfies `dE/dtheta=0` exactly.  Hence a history launched on the certified
descriptor fiber stays on it.  The apparent 99th interval coordinate is not a
new physical degree of freedom and must not be propagated as an independent
absolute-`Delta` scalar tube.

This identity explains the descriptor wrapping seen in the expanded finite
cover and supplies the correct shearing for the next Lohner/multiple-shooting
enclosure.  It does not itself recenter the 98-dimensional state tube or prove
capture/a retained stop.

