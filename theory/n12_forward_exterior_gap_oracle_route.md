# N12 forward exterior gap-oracle route adjudication

Status: `GAP_ONLY_EXTERIOR_ORACLE_ROUTE_RIGOROUSLY_INSUFFICIENT`.

The positive nonzero sector gap and the Friedrichs endpoint rule solve an
existence problem: for real (z<0), they make the retained Dirichlet pencil
(P_C^D-zI) coercive and hence define its resolvent and birth Weyl map. They
do not solve the distinct evaluation problem.

An exact counterfamily is

\[
 P_Q=-\frac{d^2}{d\tau^2}+Q,
 \qquad \tau\in[0,\infty),\qquad Q\geq1.
\]

Every member has the same temporal principal part, birth trace space,
Friedrichs decay class, and common form lower bound (P_Q\geq1). At (z=-1),
the unit-trace Friedrichs solution and its birth conormal are

\[
 u_Q(\tau)=e^{-\sqrt{Q+1}\tau},\qquad
 M_Q(-1)=-u'_Q(0)=\sqrt{Q+1}.
\]

Thus (sup_{Q\geq1}M_Q(-1)=\infty). A lower spectral gap plus a Friedrichs
endpoint class proves that each response exists, but neither identifies it nor
gives a finite uniform upper enclosure. This is a no-go for that proof route,
not for the retained action: a global action-owned upper form/coefficient
comparison or a direct Weyl calculation could still determine the response.

The same family makes the geometry-variation distinction explicit:

\[
 \frac{dM_Q(z)}{dQ}=\frac1{2\sqrt{Q-z}},\qquad
 \frac{d^2M_Q(z)}{dQ^2}=-\frac1{4(Q-z)^{3/2}}.
\]

These identities are exact after a coefficient family is realized. The BHSM
ledger presently has no maximal-exterior first- and second-coefficient
variation tube, so they cannot be promoted to bounds for
(D_\Phi M_C) and (D_\Phi^2M_C).

The two certified chords add a valid finite core. The existing scalar
constant-gap endpoint comparison nevertheless leaves a best-case uncertainty
above (4.4\times10^7), more than (5.7\times10^7) times the magnitude of
the downstream finite comparison value. More importantly, that scalar
comparison is not the operator-valued Weyl map on the physical BRST source
space and supplies no first- or second-geometry-variation enclosure. It also
does not realize the domain-parametric pair-plus-contact incidence.

Ward/BRST symmetry does not remove the oracle either. The retained graded
ledger cancels the longitudinal gauge/complex-ghost pair mode by mode, but it
does not cancel the transverse gauge, Weyl, and Hubbard--Stratonovich geometry
responses. The historical common-operator seed has the explicit nonzero scale
force `d Gamma_1/d log R4 = 8.370660134847`. Because that witness used the
retired periodic domain, it is not an N12 forward-force value; it is only an
exact counterexample to any claimed universal symmetry identity forcing the
zero-source geometry response to vanish.

Therefore another unpromoted finite chord has no established proof value for
this dependency. Chord 3 remains unauthorized, terminal return remains
optional, and Gate 7 is unchanged. The exact next object is either:

1. an action-owned common maximal-forward form/coefficient comparison through
   second geometry variation for (P_C(\Phi,A)); or
2. a direct enclosure of
   ((M_C,D_\Phi M_C,D_\Phi^2M_C,\Pi_C^{\rm pair+contact}))
   on one nonempty native resolvent region.

Only after that object exists can the zero-source force, same-action saddle,
and physical pair-plus-contact Hessian be evaluated. No endpoint, periodicity,
(p^2) label, selector, fitted tolerance, or new physics is introduced.
