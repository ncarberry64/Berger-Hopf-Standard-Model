# Gate-7 open-family stop transversality reduction

The certified core endpoint map `E` carries a nonempty open 72-dimensional AE2 reset-quotient subball into the regular constrained-child manifold.  The retained forward field adds the missing flow coordinate, so

`F(xi,a)=Phi_a(E(xi))`

has 73 input coordinates.  At launch, the reset seed tangent plus the forward field has rank 73.  Flow equivariance gives

`[D_xi F,V(F)] = D Phi_a(E(xi)) [D E,V(E(xi))]`,

and regular-flow invertibility preserves that rank until a canonical stop.

The existing Euler–Dirac stop face is `s=0` with `Delta<0` and all other retained margins strict.  Its time derivative is action-owned:

`partial_a s(F(xi,a)) = D s[V] = Delta/||G||`.

Thus one exact center history reaching this face transversely is enough.  The implicit-function theorem supplies a unique nearby hit time `a(xi)` for every seed in a nonempty open neighborhood of the center.  Gate 7 does not require a 72-parameter multiple-shooting solve or universal stop reachability.  Openness follows after one witness is certified; proof coordinates used to isolate that witness do not select a physical member or modify the action.

The stored quarter-step center has an exact rational first hit only as a polynomial center.  Its transverse stop target and the sampled lapse, radius, and selected-line margins are strong, while its correlated correction and tangent transfer remain diagnostics rather than interval authority.  The sole remaining geometric certificate is therefore the outward-rounded correlated `Y,Z1,Z2` shadowing of this one center, strict transfer of all preterminal domain margins, and scalar interval Newton at the first hit.  If that closes, the open reset stratum follows automatically and the NHIM bridge is unnecessary for the existence-only Gate-7 alternative.
