# N12 C2 pole-free outer-margin extension

The initial positivity tube used a crude hard-rate bound.  Substituting the
certified covariant hard rate into `R=D lambda[(q_dot,r_h)]` sharply reduces
the `lambda R` loss.  The resulting `Delta` positivity radius exceeds the
available outer action margin, so the retained outer ball—not a soft-pole
estimate—becomes the active local limit.

A strict eighty-percent interior fraction of that margin supplies a longer signed-eigenvalue
segment.  The pole-free Jacobi bound propagates the refined root enclosure,
and the action-owned `psi/c_psi` predictor gives a translated endpoint tube.
The predictor is only a proof center.  No completed encapsulation endpoint or
canonical stop is asserted.
