# N12 forward gauge Weyl readout family

Status:
`FORWARD_NATIVE_RESOLVENT_WEYL_FAMILY_DERIVED_EXTERIOR_EVALUATION_SOURCE_INCIDENCE_AND_SCALARIZATION_OPEN`.

Let \(\mathcal P_C^{\max}\) be the differential expression underlying the
retained maximal-forward form, with the retained maximal endpoint class but
before imposing the birth trace--conormal graph.  Restricting the same
nonnegative form to \(\Gamma_0^{\rm birth}U=0\) gives the action-owned
Dirichlet reference operator \(P_C^D\). The physical operator remains (K_C)
with the retained birth graph, and the spectral theorem supplies

\[
 R_C(z)=(K_C-zI)^{-1},\qquad
 K_C=\int\lambda\,dE_C(\lambda).
\]

The reference resolvent family used to define the boundary response is

\[
 R_C^D(z)=(P_C^D-zI)^{-1},\qquad z\in\rho(P_C^D).
\]

For real \(z<0\), the pencil \(P_C^D-zI\) is coercive. It is a spectral probe
of the unchanged operator, not a new action term. For admissible
birth-boundary trace data \(a\), let \(\gamma_C(z)a\) solve

\[
 (\mathcal P_C^{\max}-zI)U_a=0,
 \qquad \Gamma_0^{\rm birth}U_a=a,
\]

with the action-owned maximal endpoint class: the retained reset graph when
actually hit and the Friedrichs closure at an infinite or excluded end.  The
birth-boundary Weyl--Calderón map is

\[
 M_C(z)a=\Gamma_1^{\rm birth}\gamma_C(z)a.
\]

The form Green identity gives the exact derivative theorem

\[
 \langle a,M_C'(z)b\rangle_{\partial}
 =-\langle\gamma_C(z)a,\gamma_C(z)b\rangle_{\rm bulk}.
\]

Hence \(M_C'(z)\) is negative semidefinite on the real resolvent interval
below the spectrum. This constructs the native nonperiodic forward resolvent
and Weyl family without restoring a temporal Fourier mode or selecting a
temporal source profile. The spectral theorem simultaneously supplies

\[
 P_C^D=\int\lambda\,dE_C^D(\lambda).
\]

The parameter \(z\) is not momentum squared. The inherited
\(\partial_{p^2}\) contract remains retired unless a separate action-owned
translation, asymptotic, or measurement theorem derives such a map.

The same Green identity also compresses the exterior contribution to the
weak geometry force. For a retained geometry variation \(\delta\Phi\),

\[
 \langle a,D_\Phi M_C(z)[\delta\Phi]b\rangle_\partial
 =\langle\gamma_C(z)a,
 D_\Phi P_C[\delta\Phi]\gamma_C(z)b\rangle_{\rm bulk}
 +\text{retained explicit boundary-contact variation}.
\]

An exact two-block witness evaluates both sides as (1707/6160). Thus the
exterior data required by the zero-source force are compressed into
(D_\Phi M_C(z)). The same-action saddle certificate additionally requires
the corresponding second geometry variation, obtained from the same
resolvent pair-plus-contact identity. A complete coefficient history or an
actual finite reset endpoint is sufficient to compute this oracle bundle, but
neither representation is logically necessary if
((M_C,D_\Phi M_C,D_\Phi^2M_C)) can be enclosed directly.

The physical birth graph is then imposed exactly once.  At operator level,
the retained joint gauge kernel is

\[
 H_{A,i}^{\rm joint}(z)
 =W_{{\rm gauge},i}+M_{C,i}(z)
  +\Pi_{i,C}^{\rm pair+contact}(z),
\]

where the existing \(W_{{\rm gauge},i}\) already carries the retained
\(K_F^{(5)}(5/3,1,1)\sqrt{\Delta_1^{\rm coexact}}\) birth contribution.

The full pair and seagull contact response remains one Fréchet Hessian. This
operator-valued holomorphic response is now defined abstractly on the common
resolvent domain. A single physical coupling is not yet defined: the action
must either prove that this operator has the required scalar action on the
physical BRST-transverse sector or supply an action-owned normalized source
and observable map. Choosing one favorable polarization by hand, or naming
\(z\) as \(p^2\), would not establish a universal gauge coupling.

The next retained-action dependency is to realize or rigorously enclose the
exterior oracle bundle
\((M_C(z),D_\Phi M_C(z),D_\Phi^2M_C(z))\) and the common pair-plus-contact
response on a nonempty native resolvent region. The nonzero admissible BRST
source incidence is already assembled parametrically. The weak geometry force
then precedes the same-action saddle and any physical Gate-7 scalar readout.

No endpoint, action term, periodicity, source profile, scalar selector, scale,
fit, prediction, or new physics is introduced.  Gate 7 remains active, Gate 8
remains locked, and chord 3 remains unauthorized.
