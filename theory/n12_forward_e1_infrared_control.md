# N12 forward E1 infrared-control audit

Status: `CURRENT_TWO_CHORD_RESOLVENT_BOUNDS_DO_NOT_CONTROL_E1_INFRARED_SYNTHESIS`.

The first spectral multiplier of the retained functional is

\[
 f'(\lambda)=\frac12\frac{e^{-\ell_\kappa^2\lambda}}{\lambda},
 \qquad f'(\lambda)\sim\frac1{2\lambda}
 \quad(\lambda\downarrow0).
\]

Consequently, synthesizing the zero-source force requires infrared spectral
control, not merely a resolvent value separated from zero.

For the negative probe \(z=-\kappa^2\), the scalar/de Rham Poisson energy
bound contains \(M_{\max}/\kappa^2\). The factorized product-Dirac first bound
contains \(M_{\max}/\kappa\), and its mixed bound contains
\(M_{\max}/\kappa^2\). The deterministic audit evaluates
\(\kappa^2=1,10^{-2},10^{-4},10^{-6}\) and verifies the resulting growth.
Thus the existing comparison estimates do not remain integrable as the probe
approaches zero.

This is not a no-go for the retained action. The abstract maximal-forward
source domain is action owned, but the actual maximal outcome, complete
coefficient oracle, uniform global radius bound, and uniform positive source
gap are not certified. Two native routes remain:

1. If the actual maximal history has a finite retained terminal or domain-exit
   graph, evaluate its finite-interval operator and certify its spectrum.
2. If the history is infinite and regular, derive low-energy spectral-measure
   control, an action-owned relative heat-trace cancellation/reference, or
   temporal confinement sufficient for the `E1` functional calculus.

Terminal-event reachability is not imposed in advance. The `z=-1` channel
enclosures remain valid and unretracted, but they do not close the infrared
synthesis or zero-source force. Gate 7 remains active.
