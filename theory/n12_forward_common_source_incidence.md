# N12 forward common-source incidence

Status: `LOCAL_NONZERO_INCIDENCE_DERIVED_MAXIMAL_HISTORY_REALIZATION_OPEN`.

The retained rank-16 Weyl, unit Einstein--Cartan HS, complex HS-doublet,
non-Abelian one-form, and complex-ghost source/contact vertices have been
separated from the retired periodic temporal realization. The resulting
builders take the realized temporal first derivative and Laplacian, the
maximal-history radius coefficients, and an admissible source section as
arguments. They do not select any of them.

This is precisely the weakest action-native source family now required:
supplied admissible sections in the BRST physical source space. No plane-wave,
(p)-indexed, or (p^2)-indexed source family is required. The native
operator parameter is the neutral resolvent coordinate (z), which is not
momentum squared.

Substituting the historical periodic derivative and Laplacian reproduces every
v16.01 and v16.04 operator, pair vertex, and contact vertex to machine zero.
Thus no old response formula or carrier weight was changed. The fixed
three-family hypercharge trace, HS weight, 24 Einstein--Cartan pairings,
one-form/ghost BRST subtraction, and global gauge-zero-mode quotient remain
unchanged.

This closes local nonzero incidence assembly only. It does not supply the
action-selected maximal history, choose a source profile, evaluate the weak
geometry force, transfer the current local-action N12 root to the replacement
action, or evaluate the gauge Hessian. Gate 7 therefore remains active.

The next object is the exterior Weyl--Calderón response (M_C(z)), or a
rigorous enclosure of it on a nonempty native resolvent region. A complete
pointwise history or an actual finite reset endpoint is sufficient but not
logically necessary if this response oracle is obtained directly. Its
geometry variation is then combined with these already-assembled incidence
matrices to evaluate the zero-source weak geometry force before the
same-action saddle and final pair-plus-contact Hessian.
