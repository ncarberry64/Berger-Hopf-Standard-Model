# N=12 Gate-7 reset-to-capture diagram matching

Status: `RESET_TO_CAPTURE_DIAGRAM_ASSEMBLED_TWO_CERTIFICATE_BLOCKS_OPEN`.

The forward diagram is

`AE2 reset chart -> fixed-s birth collar -> regular proper-time flow ->`
`compactified capture chart -> certified stable tube`.

Every leg except the last two certificate blocks already has a BHSM-native
owner.  The reset-generated launch chart supplies the 72-dimensional event
image and the exact fixed-`s` field supplies its transverse descriptor
direction.  The compact 1,222-segment cover proves a nonempty local
73-parameter family through every finite prefix without choosing a reset
member.

The apparent absence of a regular post-collar vector field is not a missing
physical object.  On every regular point with `s>0`, positive boundary lapse
`N_boundary`, and positive desingularizing denominator `Delta`, the retained
proper-time incidence is

`d tau/ds = q_tau = N_boundary*s/Delta > 0`.

Consequently the same action trajectory has the exact proper-time generator

`V_tau = dY/dtau = (Delta/(N_boundary*s))*F_s`,

and `D lambda[V_tau]=Delta/(N_boundary*s)>0`.  This is an orientation-preserving
change of independent variable, not a second flow, a selector, or a new time
direction.  The `s=0` endpoint remains owned by the already-certified
desingularized birth collar; the proper-time chart is used only after any
positive collar section.

The generic inverse-free finite-connection residual can therefore consume
this exact callback.  Its reset rows, positive-duration chart, domain-margin
monitors, and retained event/canonical-stop alternatives are already typed.
The remaining obstruction is not another Euler--Dirac vector-field formula.

Two certificate blocks remain genuinely open:

1. an executable nonlinear transition from an admissible 98-state child
   point to the 74-component compactified physical coordinates
   `(epsilon,a,eta,m)`, including the intrinsic gauge/time quotient,
   common-scale recentering, and first/mixed-second jets; and
2. a validated set-valued forward cover (or a nonzero-degree/intersection
   proof) carrying a nonempty reset-quotient stratum from the certified core
   to strict inclusion in the quantitative capture tube, while monitoring
   every retained canonical stop.

The existing compactified-chart record declares the target ordering and norm
but does not supply the nonlinear transition map.  The stable-tube theorem
declares the terminal inequalities but does not supply reset entry.  A stored
proof center, a single nominal trajectory, dimension counting, or repeated
microscopic continuation cannot replace either certificate.

Thus the next construction is the nonlinear terminal chart projection and
its jets, followed by a no-selector set-propagation or degree certificate.
Gate 7 remains open; chord 3 remains unauthorized; frozen predictions are
unchanged.
