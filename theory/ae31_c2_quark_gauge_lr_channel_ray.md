# AE3.1 current-C2 quark gauge LR channel ray

The historical single-carrier gauge-current calculation contains a reusable
relative result. With

```text
G_Y:G_2:G_3=3/5:1:1
```

and the already-derived quark representations, color-singlet LR exchange has

```text
C_u=4/3+(3/5)(1/6)(2/3)=7/5,
C_d=4/3+(3/5)(1/6)(-1/3)=13/10.
```

Therefore

```text
C_u-C_d=1/10,
C_u/C_d=14/13.
```

This is group and representation data, not a mass fit. On current C2 the
common static nonlocal geometric factor multiplies both channels and cancels
from their ratio:

```text
K_LR=2 G_C2 diag(7/5,13/10).
```

The result does not require or repair the failed local Lorentzian Maxwell
residue. No nonlocal DtN kernel is relabelled as a local photon exchange.

The unequal diagonal entries break the `O(2)` degeneracy found in the isolated
equal-multiplicity quark trace and order the gauge attraction with the up
channel first. They do not select a mixed single-Higgs direction: a diagonal
two-channel kernel has the pure up/down axes as eigenvectors. Nor is
`14/13` a Yukawa-residue ratio.

The complete selector must add, from the same current-C2 action, the regulated
LR susceptibility, the odd scalar/composite two-point kernel, intrinsic-Higgs
mixing, and any off-diagonal up/down terms. If those off-diagonal terms vanish,
the first gauge-driven instability is pure up rather than the shared physical
Higgs direction.

Promoted:

- the exact relative current-C2 gauge LR ray `diag(7/5,13/10)`;
- breaking of the isolated quark-plane `O(2)` degeneracy;
- the up-first diagonal attraction ordering.

Not promoted:

- an absolute local gauge kernel;
- a mixed physical Higgs direction or `c_u/c_d`;
- quark masses, poles, or CKM mixing.

`FULL_BHSM_COMPLETE = FALSE`.
