# N12 C2 cancelled-field Lohner step

The fixed-descriptor field is kept in its exact cancellation form

`F_s=Psi/c+s(c V-R Psi)/(c Delta)`.

The complete bordered hard response and `b_psi` are first enclosed through
second variation.  The selected eigenvalue third variation follows by
differentiating the simple-eigenline identity and using the retained action
`D3--D5` majorants.  Only then are the numerator and denominator quotient
bounds formed.  This preserves the exact birth cancellation before norms.

The terminal-parent action majorants are regenerated at radius `1.5e-10`
from the retained terminal reset-stratum checkpoint.  This radius is derived
to contain the final center distance plus the new flow domain; the
materialization driver fixes the raw-coordinate and import-path conventions.

The center tangent matrix supplies the logarithmic norm.  Its variation on
the fixed-descriptor domain is bounded by the complete second derivative of
`F_s`.  A Lohner-style endpoint inequality includes:

- matrix growth of the incoming tube;
- first-order center transport;
- the Euler center truncation remainder;
- the intersection of the fresh eigenline chart and the terminal parent
  action ball.

The step length is the largest tested fraction obtained from these derived
inequalities.  It is proof machinery, not a validation cutoff or physical
parameter.  The predictor is not declared a physical endpoint.  A positive
stored displacement, positive `u` increment, positive proper duration, branch
24, and all inherited domain margins are checked explicitly.

This segment removes the prior scalar-wrapping/binary64 zero-step artifact.
It reaches neither a later event nor a canonical stop, so Gate 7 remains open.
