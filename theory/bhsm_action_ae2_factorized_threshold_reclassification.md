# AE2 factorized threshold reclassification

Status: `STRICT_ZERO_WRONSKIAN_RECLASSIFIED_AS_SUFFICIENT_NOT_NECESSARY`.

The retained product-Dirac source form is factorized. To test whether Gate 7
must first prove a strict zero-energy Wronskian, take

\[
 A_s=\partial_x+s\mathbf 1_{[0,T]},\qquad K_s=A_s^*A_s,
\]

with the natural graph `A_s u(0)=0` and a free exterior after `T`. At zero
energy, `u=e^{-sx}` on the core and continues as a nonzero constant. Thus the
model has a genuine threshold resonance and zero Wronskian margin.

For positive spectral momentum `k`, propagate `(u,w=A_su)` exactly. In the
log-radius direction `D_h A_s=-s` on the core, exterior delta normalization
gives

\[
 2\operatorname{Re}\langle A_su,D_hA_su\rangle
 = C(s,T)k^2+o(k^2),
\]

where

\[
 C(s,T)=\frac{2e^{2sT}}{\pi}
 \left[T-\frac{1-e^{-2sT}}{2s}\right]>0.
\]

Therefore the cumulative first-geometry source weight is
`(C/3) Lambda^(3/2)+o(Lambda^(3/2))`, which satisfies the retained E1
source-measure exponent even though the Wronskian margin vanishes.

This does not prove that the unknown maximal N12 exterior is free or in this
constant-core class. It proves that strict Wronskian positivity was an
overstrong intermediate requirement for factorized channels. The weakest live
lemma is now a resonance-compatible source-weighted limiting-absorption/Weyl
bound on the realized AE2 exterior. The nonfermion margin remains certified;
the actual factorized N12 bound, angular tail, force, and Gate 7 remain open.
