# BHSM Completion Campaign Addendum

The BHSM completion campaign packages four development tracks without retuning
the frozen model:

1. v1.3 corrected formal-kernel `(H_T)` scaffold;
2. v1.4 QCD/RG comparison scheme scaffold;
3. v1.5 scalar/topographic decoupling scaffold;
4. v2.0 integrated dependency graph and theorem ledger.

The corrected `(H_T)` reference is `DIRAC_PROXY_LEVEL_2_FORMAL_KERNEL`, with
formal kernel

```text
K_formal = span{|ell,0,0,q=0,chi=-1>, |u,0,0,q=0,chi=-1>, |d,0,0,q=0,chi=-1>}.
```

The old coordinate-first block `(0,1,2)` is superseded wherever earlier
Level 2 conclusions depended on it.

QCD/RG matching remains scaffold-level. The precision-QCD reference set is a
placeholder, not a source of final residual verdicts.

Scalar/topographic decoupling remains scaffold-level. The current proxy
inventory has exactly one light Higgs projection and no current forbidden/open
scalar modes, but full action-level decoupling is not proven.

The dependency graph reports no hidden circularity or empirical-residual
dependency. The full twisted Dirac / `(H_T)` theorem, topological index theorem,
mirror exclusion, scalar action proof, and precision QCD/RG matching remain
open.
